package com.teamyg.parfait.core.designsystem.component.ygtutorial

/**
 * 설명 카드가 화면 위·아래 중 어디에 붙는가.
 *
 * 고르는 기준은 취향이 아니라 **그 장이 강조하는 UI 가 어디 있는가**다 — 카드는 강조 대상의
 * 반대편에 붙어야 정작 보여 줘야 할 자리를 자기가 덮지 않는다.
 */
enum class YGTutorialBoxPlacement {
    Top,
    Bottom,
}
