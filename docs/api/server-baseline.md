# 서버 API 문서 검증 기준선 (Server Baseline)

> `parfait/api/` 계약 문서를 **어느 서버 커밋 기준으로 마지막 검증했는지** 기록하는 단일 출처(SoT).
> "서버 API 문서 점검"을 요청받으면 아래 기준선부터 현재 `origin/main`까지의 **delta만** 감사하고,
> 끝나면 기준선을 갱신한다.

## 현재 기준선
- **repo**: `TEAMYG-SERVER` (`mash-up-kr/TEAMYG-SERVER`) **`main`**
- **커밋**: `82e6edc`
- **요약**: `Merge pull request #139 from mash-up-kr/fix/#138` (그룹 목록 최신 사진 테두리 정보)
- **검증일**: 2026-09-11 (21회차)

### 왜 `main`인가
서버 저장소의 기본 브랜치가 `main`이고(`origin/HEAD -> origin/main`) 기능 PR이 main으로 머지된다.
`develop`은 main을 주기적으로 끌어오는 쪽이라 **뒤처진다** — 체계 신설 시점에 develop은
signup·파르페 연도 조회 두 API를 갖고 있지 않았다. 앱이 바라볼 서버는 main에서 나온다.

⚠️ TJYG-Android는 `develop`을 추적한다([doc-baseline.md](../doc-baseline.md)).
**두 저장소의 통합 브랜치 이름이 다르다** — 혼동하지 말 것.

## 점검 절차 (다음 요청 시)
로컬 경로는 개인정보라 `wiki/personal-private/project-paths.md` 참고(아래 `<S>`).

1. **최신화**: `git -C <S> fetch origin main`
2. **신규 커밋 나열**: `git -C <S> log --oneline <기준선>..origin/main`
   - 기능 PR이 squash로 들어와 merge 커밋이 아닐 수 있다 → **`--merges` 필터를 쓰지 않는다.**
   - 변경 파일: `git -C <S> show --stat <hash>`
3. **계약 대조**: 컨트롤러·`*Request`/`*Response` DTO·`*ErrorCode` enum·`SecurityConfig`·
   `ApiResponse`·`GlobalExceptionHandler` 변경이 `parfait/api/*.md`와 어긋나는지 검사.
   - 파일 조회는 항상 `git -C <S> show origin/main:<path>` — **워킹트리를 믿지 않는다**(로컬은 `develop`).
   - 신규 도메인이면 [template.md](template.md)로 문서 신설 + [README.md](README.md) 인덱스 등록.
   - **엔드포인트·DTO·에러 코드가 하나도 안 바뀐 delta도 계약을 뒤집는다.** 2026-09-01 라운드가 그
     증거다 — `persistence`의 네이티브 쿼리 한 곳만 바뀌었는데 응답 필드 **두 개의 뜻**이 달라졌고,
     타입·JSON 키가 그대로라 앱 테스트도 와이어 계약 테스트도 못 잡는다. **`persistence`·`application`
     변경도 "이 필드가 무엇을 뜻하는가"를 바꾸는지 보고 넘어간다.**
   - **delta에 `resources/db/migration/` 변경이 있으면 그것이 계약 서술을 뒤집는지 본다.**
     코드만 대조하면 놓친다 — 2026-08-24 라운드가 그 증거다. 코드에서 검사가 사라진 뒤에도 운영 DB에
     제약이 남아, "허용된다"고 정확히 적은 서술이 실제로는 500이었다(conventions.md
     "스키마 소유권" 절). 스키마 전체를 미러하지는 않는다 — **제약·기본값·컬럼 삭제가 이미 적어 둔
     계약 서술과 어긋나는지만** 확인한다.
   - **계약이 HTTP 왕복에만 있다고 전제하지 않는다.** 2026-09-04 라운드가 그 증거다 — 컨트롤러·DTO·
     에러 코드·`SecurityConfig`가 전부 불변이라 **엔드포인트를 세는 방식으로는 변화가 0으로 보이는데**,
     서버가 앱으로 미는 푸시 축이 새로 생겨 문구·`data` 키·채널 id·재시도·중복 수신이 통째로 앱의
     의무가 됐다. **`external`·`core/notification` 같은 비-`http` 모듈의 신규 디렉토리를 발견하면
     "앱이 맞춰야 할 것이 있는가"를 따로 묻는다.**
4. **기준선 갱신**: 위 "현재 기준선"을 새 `origin/main` HEAD로 교체하고 아래 이력에 한 줄 추가.

## 기준선 이력
| 검증일 | main 커밋 | 요약 | 비고 |
|--------|-----------|------|------|
| 2026-08-01 | `6b05b8c` | `[Feat/#61] 그룹별 캘린더 연도 리스트 조회 API (#62)` | 체계 신설. 도메인 3건(auth 2·parfait-group 8·parfait 1) 전량 초기 작성. Android 대응 심볼 0건 → 전 엔드포인트 `미구현`. 불일치 3건·URL 규약 혼재 open-questions 등록. 체계 신설 도중 서버가 전진해 같은 라운드에서 `6f5bffc`로 올림 |
| 2026-08-02 | `6f5bffc` | `[Feat/#45] 토큰 재발급(refresh) / 로그아웃 API 구현 (#63)` | 패키지 전면 재편(`http/api/auth`→`http/auth`, `http/api/parfait`→`http/parfait`, `http/api/health`→`http/global/health`, `http/parfaitgroup/*.kt`→`{controller,dto,exception}/`) · auth 신규 엔드포인트 2(`reissue`·`logout`) · `AuthErrorCode` 12종(`FORBIDDEN_REFRESH_TOKEN` 신설) · 화이트리스트 `/api/v1/auth/**`→개별 3경로(`kakao`·`signup`·`reissue`) 축소, `logout` 제외 · 그룹(`parfait-group`) 계약 불변 |
| 2026-08-03 | `69654bc` | `[Feat/#64] 약관 목록 조회 API 구현 (#65)` | delta 1커밋. 신규 도메인 `policy`(`GET /api/v1/policies`, 문서 [policy.md](policy.md) 신설·인덱스 등록) — 서버 위치는 `http/auth`인데 URL 세그먼트가 최상위라 auth와 분리. 화이트리스트에 `/api/v1/policies` 추가 · 도메인 전용 에러코드 0종(빈 배열도 200) · `url`은 `Tos.content` 컬럼 재사용 · 정렬은 `PolicyQueryService`가 `TERMS_OF_SERVICE`→`PRIVACY_POLICY`로 고정. auth·parfait-group·parfait 계약 불변. **미결 1건 해소**(약관 목록 API 부재) + 신규 2건 등록(`url` 의미·앱 연동 미착수) |
| 2026-08-10 | `5bb2a3a` | `Merge pull request #73 from mash-up-kr/feat/#72` (이미지 업로드 확인 API) | delta 6커밋(기능 PR 2건 #71·#73 + 각 테스트). 신규 도메인 `image`(`http/image`, 엔드포인트 2 — `POST /api/v1/images`·`POST /api/v1/images/{imageId}/confirm`, 문서 [image.md](image.md) 신설·인덱스 등록). S3 presigned PUT 2단계 업로드로 **바이트가 서버를 지나지 않는다** · `ImageErrorCode` 4종 신설(`MEMBER_NOT_FOUND`가 세 번째 enum에 중복) · 화이트리스트 불변이라 **둘 다 인증 대상** · 리소스 생성인데 `ApiResponse.ok`라 200/`"OK"`. auth·policy·parfait-group·parfait 계약 불변, `SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler` 불변. Android 대응 심볼 0건(`android_status: none`). **신규 미결 3건**(`fileName` 미사용·confirm 소유자 미검증·`PENDING` 고아 정리 부재) + **기존 2건 갱신**(업로드 타임아웃 전제 도래·`http/` 요청 모음 2건 공백). 같은 라운드에서 **OpenAPI 실물 재대조**(스키마 `required`가 Bean Validation 애노테이션만 반영 — `imageType`·`agreements`·그룹 생성 3필드가 비널인데 목록에 없음, conventions에 규칙 등재)와 **TJYG-Android 브랜치 전수 확인**(image 심볼 0건, 원격 표면 4 Service = 14 엔드포인트 그대로)을 수행해 전 도메인 문서 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-11 | `2c5499a` | `[Feat/#67] 내 계정 정보 조회 API (#84)` | delta 10커밋(기능 PR 4건 #76·#77·#81·#84 + 테스트/배포 chore). **신규 도메인 2건** — `member`(`http/member`, `GET /api/v1/users/me`·`PATCH /api/v1/users/me/nickname`, 문서 [member.md](member.md) 신설) · `parfait-image`(`http/parfaitimage`, `POST`·`PATCH .../groups/{groupId}/parfaits/{parfaitId}/images`, 문서 [parfait-image.md](parfait-image.md) 신설). **기존 도메인 증가** — auth에 애플 로그인(`POST /api/v1/auth/apple`) 1건 추가로 4→5, 화이트리스트에도 개별 등재. 총 16→**21 엔드포인트**, 도메인 5→7. 신규 enum `MemberErrorCode` 2종·`ParfaitImageErrorCode` 5종, `AuthErrorCode` 12→14(`APPLE_SERVER_ERROR`·`APPLE_SERVER_UNAVAILABLE`). `MEMBER_NOT_FOUND` 문자열을 가진 enum이 넷이 됐고 `GET /users/me`는 **한 엔드포인트에서 401·404 둘 다**로 낸다. signup의 애플 분기가 TODO에서 실구현으로 바뀌어 `INVALID_TOKEN` 경로가 하나 늘었다. `ApiResponse`·`GlobalExceptionHandler`·parfait-group·parfait·policy·image 계약 불변. **⚠️ 중대 정정 1건** — 로그인 판별자 JSON 키가 `newUser`가 아니라 **`isNewUser`**다(서버가 `jackson-module-kotlin` 사용, 컨트롤러 테스트가 응답 본문을 단언, 팀 명세도 일치 / OpenAPI 스키마만 반대 — springdoc 자체 ObjectMapper 산물). 2026-08-02 판본의 기술이 틀렸고 Android가 그대로 `@SerialName("newUser")`를 구현해 **카카오 로그인이 `⚠️불일치`**가 됐다. **미결 신규 6건**(OQ-P-116~121: 판별자 정정·서버 7건 선행·POST 소유권·배치 계약 공백·닉네임 전파·GOOGLE provider 500) + **해소 1건**(OQ-P-062 전역/그룹 닉네임 규칙 동일 확인) + **기존 3건 갱신**(OQ-P-060 URL 형태 5종·OQ-P-075 전제 붕괴·OQ-P-108 `http/` 공백 7건). 전 도메인 문서와 `spec/` 4건의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-15 | `36ecd1c` | `[Feat/#85] 캔버스 상태 자동 전환 배치(새벽 3시) 구현 (#97)` | delta 17커밋(기능 PR 6건 #83·#88·#89·#90·#92·#94 + 배치 #97 + 테스트). **엔드포인트 21→26 + 테스트 전용 1** — parfait 1→3(`GET .../parfaits/today` 오늘 캔버스 전량·`GET .../parfaits` 과거 목록), parfait-image 2→4(`PATCH .../{parfaitImageId}/border` 테두리 수정·`DELETE .../{parfaitImageId}` 삭제), member 2→3(`DELETE /api/v1/users/me` 탈퇴). 신규 도메인 0건 — 기존 문서 3건만 커진다. **신설 enum `ParfaitErrorCode` 2종**(`INVALID_DATE_RANGE`·`PARFAIT_ALREADY_CLOSED` — 후자는 공개 경로 도달 불가), `parfait` 테이블에 `status`(`ACTIVE`·`CLOSED`·`EMPTY`)·`background_type`·`background_value` 신설(V11), Spring Batch 스키마(V12). **캔버스 회전 배치** 03:00 Asia/Seoul + **테스트 전용 회전 엔드포인트가 화이트리스트에 등재**(인증 없이 전 그룹 마감, 서버 TODO가 프로덕션 전 제거 예고). **⚠️ 계약 축소 1건** — 애플 로그인 요청에서 `authorizationCode`가 **제거**됐고(#89) 애플 refresh token 저장·교환·client secret 생성·`signup`의 provider 분기가 전부 사라졌다(V10이 컬럼 삭제) → `INVALID_ID_TOKEN`·`APPLE_SERVER_*`의 원인이 JWKS 한 갈래로 좁혀졌다. **envelope 예외가 2건**이 됐다(`logout`·탈퇴 — 둘 다 204 본문 없음, 반면 토핑 삭제는 200+`data: null`). `image_meta.reference_count`에 증감 경로가 생겼다(배치 +1·삭제 -1, 0이면 S3 객체 삭제). 탈퇴는 회원 행 하드 삭제 + 그룹 멤버십 `leave()`(닉네임 `(알수없음)`)이고 `GroupNickname.of`에 그 값 특례가 붙었다. **Android 표면 20/25로 공백 5 재발**(세 번째). **미결 신규 7건**(OQ-P-158~164) + **부분 해소 2건**(OQ-P-119 ①②③ — 목록 조회는 `today`가 대신 닫음 / OQ-P-107 ③ referenceCount 주체 확정) + **재개 1건**(OQ-P-108 `http/` 공백 5건). 전 도메인 문서와 `spec/` 4건의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-15 | `e4ff23f` | `[Fix] 닉네임 자모 허용 및 그룹 내 중복 검사 제거 (#101)` | delta 3커밋(#99·#100·#101, 전부 squash — `--merges` 필터였다면 0건으로 보였을 것). **엔드포인트 증감 0**(26 + 테스트 전용 1 유지), 신규 도메인 0, `SecurityConfig`·`ApiResponse` 불변. 바뀐 것은 **값 규칙 3건**이고 전부 `parfait-group` 계열이다. ① **초대코드 자릿수 8 → 6**(`InviteCode.LENGTH`·JPA 컬럼·V13) — 앱 `InviteCode.LENGTH`와 A-004 입력 칸은 처음부터 6이라 **드러나지 않던 불일치가 서버 쪽에서 닫혔다**(실서버 요청 0건이라 표면화되지 않았고, 계약 문서에 서버 자릿수를 안 적어 대조로도 못 잡았다 → OQ-P-180). ② **닉네임 정규식에 자모 추가**(`GroupNickname`·`GlobalNickname` 동시, 사유는 iOS가 통과시키는 `ㅋㅋ`류가 서버에서만 400) — 이번엔 앱이 더 좁아졌다(`CheckNameValidUseCase`는 완성형만) → OQ-P-171 전제 반전. ③ **그룹 내 닉네임 중복 검사 제거** — 참여·닉네임 변경 양쪽의 `existsByGroupIdAndNickname`과 `GROUP_NICKNAME_ALREADY_USED`가 포트·어댑터·리포지토리·에러 코드까지 삭제돼 `ParfaitGroupApiErrorCode` **11 → 10**, join·join-preview 에러 6 → 5종, 닉네임 변경 4 → 3종. 앱 `GroupNickNameViewModel`의 `ALREADY_USED` 분기·유닛 테스트·`ServerErrorCode` 상수가 死코드가 됐다 → OQ-P-179. `GlobalExceptionHandler`도 바뀌었으나 `log.info` 인자에서 예외 객체를 뺀 것뿐이라 **계약 영향 없음**(#100, 나머지는 배포 워크플로). Android 열 값 변동 0(요청·응답 형태 불변). **미결 신규 2건**(OQ-P-179·180) + **기존 1건 전제 반전**(OQ-P-171). 전 도메인 문서와 `spec/` 4건의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-16 | `22717fe` | `Merge pull request #103 from mash-up-kr/feat/#102` (파르페 배경 변경 API) | delta 7커밋(기능 PR 2건 #96·#103 + 각 테스트 + 머지). **엔드포인트 26→28 + 테스트 전용 1 유지** — 전부 `parfait` 도메인이라 신규 도메인 0건, 문서도 [parfait.md](parfait.md) 하나만 커진다. ① **상세 조회** `GET /api/v1/groups/{groupId}/parfaits/{parfaitId}`(#96) — 응답 타입이 `today`와 **같은 클래스**(`GetTodayParfaitResult`·`GetTodayParfaitResponse`)라 앱 DTO·VO·매퍼가 이미 다 있다. `today`와 달리 **부작용이 없고**(`readOnly`, `EnsureActiveCanvasUseCase` 미호출) 날짜가 아니라 id로 찾으며(`findByIdAndParfaitGroupId`) 그룹이 다르면 404 `PARFAIT_NOT_FOUND`(403이 아니다). **상태로 거르지 않아 오늘의 `ACTIVE` 캔버스도 조회된다** → 오늘 캔버스를 얻는 경로가 둘이 됐고 한쪽만 부작용이 있다(OQ-P-192). ② **배경 변경** `PATCH .../parfaits/{parfaitId}/background`(#103, 별도 컨트롤러 `ChangeParfaitBackgroundController`) — **`background_type`·`background_value`가 V11에서 생긴 뒤 처음으로 쓰기 경로를 얻었다.** `type`(`COLOR`·`IMAGE`)에 따라 `value`(HEX `#`+6자리, 도메인 `HEX_COLOR_PATTERN` 검증) 또는 `imageId`(업로드 `COMPLETED` 확인)가 필수인 **조건부 필수의 첫 사례**인데 Bean Validation 애노테이션이 0개라 OpenAPI `required`에 `type`조차 안 나온다(conventions에 규칙 등재). 저장·응답되는 `value`는 `imageId`가 아니라 **`ImageMeta.url`**이다. 한 엔드포인트가 세 enum(`ParfaitErrorCode`·`ImageErrorCode`·`ParfaitGroupApiErrorCode`)의 코드를 섞어 낸다. **`ParfaitErrorCode` 2 → 5**(`PARFAIT_NOT_FOUND`·`INVALID_BACKGROUND`·`BACKGROUND_IMAGE_NOT_CONFIRMED`). `SecurityConfig`(화이트리스트)·`ApiResponse`·`GlobalExceptionHandler`·auth·policy·parfait-group·image·member·parfait-image 계약 불변. **Android 표면 25/27로 공백 2**(다섯 번째 왕복 — `http/` 요청 모음도 같은 2건 공백, OQ-P-108). **미결 신규 4건**(OQ-P-189 마감 캔버스 배경 변경 무가드 / OQ-P-190 배경 이미지가 `reference_count`를 안 올려 토핑 삭제 시 S3 객체와 함께 배경이 깨짐 / OQ-P-191 요청 id·응답 URL 비대칭 + 조건부 필수 표현 / OQ-P-192 오늘 캔버스 조회 경로 이중화) + **전제 반전 1건**(OQ-P-173 — "서버에 배경 쓰기 경로가 없다"가 사실이 아니게 됐다. ①이 "서버에 요청한다"에서 "앱이 연동한다"로 바뀌고 ②는 유일 선택지에서 임시 수단으로 되돌아갔다) + **기존 2건 갱신**(OQ-P-181 ③ 미지 배경 type 폴백 전제 변경 · OQ-P-108 재개). 전 도메인 문서와 `spec/` 4건의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-18 | `08df1bf` | `[Fix] 그룹/캔버스 API에 Nametag-Chip 노출&배치 작업 시간 수정 (#104)` | delta 1커밋(squash, 기능 PR #104). **엔드포인트 증감 0**(28 + 테스트 전용 1 유지), 신규 도메인 0, `SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler`·에러 코드 enum 전부 불변. 바뀐 것은 **응답 필드 넷과 "오늘"의 정의**다. ① **Nametag-Chip 부여 주체가 서버가 됐다** — 신설 `NameTagChipType`(`TYPE1`~`TYPE12` + `RELEASED`), 참여·생성 시 `assignRandom`이 **그 그룹의 활동 멤버가 안 쓰는 값 중 무작위**로 배정하고 `leave()`가 `RELEASED`로 반납한다(그룹 탈퇴·회원 탈퇴 공용). 유일성은 **그룹 안에서만** 성립해 계정 공통이 아니고, 재배정 경로는 없다. 마이그레이션 V14가 기존 행을 채웠다. 응답 필드 셋이 늘었다 — 그룹 상세 `members[].nametagChip` · 그룹 목록 `lastPlacedByNametagChip`(같은 네이티브 쿼리의 별개 서브쿼리, 마지막 토퍼가 탈퇴했으면 `RELEASED`) · 캔버스 `placedBy.nametagChip`. ⚠️ **`groupMembers`에는 안 붙어** C-001 상단 멤버 칩은 여전히 계약 밖이다(OQ-P-224). ② **그룹 상세가 `groupName`·`memberLimit`을 싣는다** — 앱이 목록을 한 번 더 읽어 이름을 붙이던 조합(OQ-P-216 ①)과 "N명 남음" mock 1(OQ-P-139 ②)의 **계약 공백이 둘 다 서버에서 닫혔다**. ③ ⚠️ **하루 경계가 03시가 됐다** — 신설 `ParfaitDay.current()`를 오늘 조회·그룹 생성·회전 가드가 쓴다(자정~03시는 전날이 진행 중). 회전 가드가 `isAfter`에서 `!isBefore`로 바뀌어 **오늘 날짜 캔버스도 마감 대상에서 빠졌고**, 그 때문에 막힌 강제 마감을 위해 **테스트 전용 회전만 가드를 건너뛰는 `ForceRotateParfaitCanvasesUseCase`로 갈라졌다**. 과거 목록의 `to` 기본값만 `LocalDate.now()`(자정)로 남아 **서버 안에서도 두 기준이 공존**한다. **⚠️ Android 불일치 2건째** — 앱 `parfaitToday()`는 KST 자정 기준이라 00:00~03:00에 `GetTodayParfaitUseCase`가 정상 응답을 어긋난 것으로 보고 오늘 조회를 두 번 부르고(부작용 있는 GET 두 배) 화면이 D−1 캔버스를 D 아래 그린다 → `parfait.md` 표의 `today` 행이 `⚠️불일치`가 됐다. 신설 필드 넷은 앱이 안 읽지만 `ignoreUnknownKeys = true`라 역직렬화는 안 깨진다(불일치 아님). Android 표면 셈 27/27 유지, `http/` 커버 25/27 유지(엔드포인트 미증가). **미결 신규 3건**(OQ-P-222 하루 경계 / OQ-P-223 칩 배정 규칙이 정책 문서에 없음 + `RELEASED` 표시 미정 / OQ-P-224 `groupMembers` 누락 + 앱이 신설 필드 넷 미소비) + **해소 1건**(OQ-P-139) + **기존 3건 전제 반전**(OQ-P-140 ①②③ 서버가 답함 · OQ-P-210 ① 이 화면만 비껴감 · OQ-P-216 ① 전제 소멸). 전 도메인 문서와 `spec/` 4건의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-19 | `57529ec` | `fix: placedBy 스키마 이름 충돌 해소 및 nameTagChip 필드명을 스펙에 맞게 통일 (#107)` | delta 2커밋(#106·#107, 둘 다 squash). **엔드포인트 증감 0**(28 + 테스트 전용 1 유지, 두 라운드 연속), 신규 도메인 0, `SecurityConfig`·`ApiResponse` 불변. 직전 라운드가 벌여 놓은 것을 정리하다 **응답 JSON 키를 바꾼 라운드**다. ① ⚠️ **키 리네임** — `nametagChip` → `nameTagChip`, `lastPlacedByNametagChip` → `lastPlacedByNameTagChip`. 코어·도메인·영속성의 내부 프로퍼티명은 그대로이고 **HTTP 응답 DTO 경계에서만** 바뀌었다(계약 문서가 보는 것은 후자). develop은 이 필드를 안 읽어 무해하지만 **미머지 브랜치 `feature/#294-group-ssot`이 옛 키 + `String? = null`로 읽어 값이 조용히 `null`이 된다** — `MissingFieldException`도 안 나서 OQ-P-227이 걱정한 "큰 소리로 깨짐"의 반대 극단이다(OQ-P-234). ② **칩 필드가 두 자리 늘었다** — 캔버스 조회 `groupMembers[].nameTagChip`(C-001 상단 멤버 칩이 드디어 계약 안으로 → **OQ-P-224 ① 해소**)와 토핑 배치 응답 `placedBy.nameTagChip`. 후자는 springdoc이 `PlacedByResult`/`PlacedByResponse` 동명 클래스를 한 스키마로 취급해 캔버스 쪽 필드가 스웨거에 안 보이던 문제 때문에 **토핑 배치 쪽을 `PlaceParfaitImagePlacedByResult`/`Response`로 개명**하며 함께 들어왔다 — 앱이 두 패키지에 같은 이름을 두던 근거("서버가 그렇다")가 사라졌다(OQ-P-236). ③ **널이 사라졌다** — 목록·생성 응답의 `recentImageUploadedAt`·`lastPlacedByNameTagChip`이 `COALESCE`(토핑 0건이면 그룹 생성 시각 / 생성자 칩)로 비널이 됐고, `NameTagChipType`도 도메인·JPA·응답에서 널이 빠졌다. ⚠️ **그 탓에 기존 Android 불일치의 사정거리가 넓어졌다** — 앱 매퍼가 `recentImageUploadedAt?.let(Instant::parse)`라 널일 때만 파싱을 건너뛰었는데 그 우회로가 없어져 **그룹이 하나라도 있으면 G-001 목록이 실패**한다. ④ **반납 값 `RELEASED` → `DEFAULT`**(V15가 기존 행 갱신 + 컬럼 `NOT NULL DEFAULT 'DEFAULT'`). `TYPE1`~`TYPE12`와 달리 **유일성 제약이 없어 한 그룹에 여러 명이 가질 수 있다**. ⑤ **그룹 생성 응답 +3 필드**(`recentImageUrl` 항상 `null`·`recentImageUploadedAt`·`lastPlacedByNameTagChip`) — 같은 필드가 생성은 `updatedAt`, 목록은 `created_at`에서 나온다(OQ-P-235). ⑥ **하루 경계가 서버 안에서 통일**됐다 — 과거 목록 `to` 기본값도 `ParfaitDay.current()`가 돼 **OQ-P-222 ③ 해소**(앱과의 불일치는 그대로). ⑦ **전역 405 신설** — `CommonErrorCode.METHOD_NOT_ALLOWED` + `HttpRequestMethodNotSupportedException` 전용 핸들러(그전엔 catch-all로 떨어져 500), `CommonErrorCode` 2 → 3, `GlobalExceptionHandler` 핸들러 4 → 5. ⑧ **탈퇴 후 재가입 500 수정** — tombstone rename UPDATE가 같은 트랜잭션의 delete 때문에 dirty-check에서 빠지던 것을 `flush()`로 강제(계약 불변, 바뀐 것은 재가입 성공 여부). ⑨ Discord 알림 큐 로그백 필터는 계약 영향 없음. Android 표면 셈 27/27 유지, `http/` 커버 25/27 유지. **미결 신규 3건**(OQ-P-234 키 리네임 미반영 브랜치 / OQ-P-235 `recentImageUploadedAt`이 두 뜻 + 생성·목록 출처 불일치 / OQ-P-236 앱 DTO 이름이 서버 거울 아님) + **해소 2건**(OQ-P-222 ③ · OQ-P-224 ①) + **기존 2건 갱신**(OQ-P-223 `RELEASED`→`DEFAULT` · 시각 파싱 불일치 상시화). 전 도메인 문서와 `spec/` 4건의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-20 | `efbf98f` | `Merge pull request #109 from mash-up-kr/fix/#108` (마감된 파르페 편집 요청 거부) | delta 4커밋(기능 squash #108 + 테스트 + 미사용 포트 제거 + 머지). **엔드포인트 증감 0**(28 + 테스트 전용 1 유지, **세 라운드 연속**), 신규 도메인 0, **신설 에러 코드 0**, `SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler`·요청/응답 DTO 전부 불변. 바뀐 것은 **실패 경로뿐이고 성공 형태는 한 글자도 안 바뀌었다** — 그런 라운드는 처음이다. ① **쓰기 다섯 경로가 캔버스 마감 상태를 본다** — 배경 변경(`ChangeParfaitBackgroundService`)과 토핑 배치·수정·테두리·삭제 넷이 대상 파르페의 `status != ACTIVE`면 **409 `PARFAIT_ALREADY_CLOSED`**로 거부한다. 증상은 03시 회전 직후였다: 쓰기가 200으로 성공하고도 뒤이은 `today` 조회가 새 캔버스를 줘서 사용자에게는 편집이 사라진 것처럼 보였다. ② **`PARFAIT_ALREADY_CLOSED`가 공개 경로를 처음 얻었다** — 2026-08-15 신설 이래 회전 로직 안에서만 던져져 "공개 경로 도달 불가"로 적혀 있던 코드다. 토핑 네 엔드포인트가 **자기 enum이 아니라 `ParfaitErrorCode`의 값을 내므로** 도메인 경계를 넘는 코드가 하나 더 늘었다. ③ **토핑 수정·테두리·삭제 셋이 파르페 존재 검사를 처음 얻었다**(404 `PARFAIT_NOT_FOUND`, `ParfaitImageErrorCode`) — 그전에는 경로의 `parfaitId`를 배치 행과 대조만 하고 `groupId`는 멤버십 확인에만 썼다. 새 검사가 **소유권 뒤**라 남의 토핑을 마감된 캔버스에서 고치려 하면 409가 아니라 403이다. 삭제는 가드가 부수효과보다 앞이라 마감 캔버스에서 참조 카운트 감소·S3 삭제가 시작되지 않는다. ④ 토핑 배치의 존재 확인이 `existsByIdAndGroupId`(불리언) → `findByIdAndGroupId`(엔티티)로 바뀌며 **같은 조회로 상태까지 읽게 됐고**, 쓰임이 사라진 전자는 포트·어댑터·리포지토리에서 걷혔다(계약 영향 없음). ⑤ 신설 가드는 컨트롤러 테스트가 아니라 **서비스 테스트 5종**이 잠근다(`*ServiceTest`의 "이미 마감된 파르페면 PARFAIT_ALREADY_CLOSED를 던진다"). Android 표면 셈 27/27 유지, `http/` 커버 25/27 유지, **Android 열 값 변동 0**(요청·응답 형태가 그대로라 `⚠️불일치` 아님). **해소 2건**(OQ-P-189 배경 변경 무가드 · OQ-P-160 ② 토핑 네 경로 무가드) + **신규 1건**(OQ-P-244 — "서버가 마감 캔버스를 막지 않는다"고 단정한 앱 주석 7곳이 하루 만에 거짓이 됐고 `ServerErrorCode`에 이 코드 상수가 없다). 전 도메인 문서와 `spec/` 4건의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-24 | `a404ac2` | `Merge pull request #111 from mash-up-kr/chore/#110` (Flyway 재정비 — V5~V15 미적용 해소 및 ddl-auto 의존 제거) | delta 5커밋(마이그레이션 · 설정 · 테스트 · 런북 · 머지). **엔드포인트 증감 0**(28 + 테스트 전용 1 유지, **네 라운드 연속**), 신규 도메인 0, 신설 에러 코드 0. **컨트롤러·요청/응답 DTO·에러 코드 enum·`SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler`가 한 파일도 안 바뀌었다** — 계약 파일 변경 0건인 첫 라운드다. 대신 **이 체계가 몰랐던 근거 축 하나가 드러났다: 운영 DB 스키마**. ① Flyway 히스토리가 운영에 V4까지만 기록돼 있었고 그 뒤로는 `ddl-auto: update`가 스키마를 대신 관리했다. `update`는 컬럼·인덱스를 **추가만 하고 삭제하지 않으므로** DROP을 포함한 V5·V6·V8·V10, 제약·기본값을 바꾸는 V7·V11·V15, 데이터 이관을 담은 V14가 **운영에 반영되지 않았다.** ② ⚠️ **그 탓에 이 문서들이 코드 근거로 정확히 적은 서술 최소 둘이 운영에서는 거짓이었다** — (가) 2026-08-15에 기록한 "그룹 내 닉네임 중복 허용"은 유니크 인덱스 `uk_parfait_group_member_group_nickname`이 남아 있어 실제로는 **처리되지 않은 500**이었고(V6 미적용), (나) **토핑 배치 POST가 운영에서 500으로 실패**했다(`parfait_image` INSERT). 코드 대조만 아홉 라운드를 돌면서 둘 다 못 잡았다 → OQ-P-285. ③ `V16__reconcile_ddl_auto_schema_drift.sql`이 그 차이만 메운다 — 고아 컬럼 제거(`member.email`·`member.apple_refresh_token`) · 그룹 닉네임 유니크 인덱스 제거 · `parfait_image.placed_by_group_member_id` FK 복원 · `image_meta.image_type`(`BACKGROUND`)·`status`(`PENDING`)와 `parfait.status`(`ACTIVE`) 기본값 복원 · `nametag_chip` 백필(활성 멤버에게 그룹 내 미사용 `TYPEn`, 탈퇴 멤버는 `DEFAULT`)과 `NOT NULL`. `information_schema` 조건부 SQL이라 신규 DB에서도 돌고 `FlywayMigrationTest`가 두 경로의 수렴을 잠근다. ④ **`ddl-auto: validate`로 내려 조용한 드리프트가 끝났다** — 어긋나면 기동 시점에 실패한다. 배치 메타테이블 `initialize-schema`도 `always` → `never`로 되돌아가 소유권이 V12로 갔고, `JpaConfigurationTest`가 세 값을 고정한다. ⑤ ⚠️ **전환은 배포만으로 끝나지 않는다** — 운영 히스토리가 V1~V4뿐이라 Flyway가 V5부터 재실행하다 죽는다. V5~V15를 적용된 것으로 기록하는 **baseline SQL을 사람이 1회성으로** 실행해야 한다(서버 `docs/operations/flyway-cutover.md`). 그 시점을 앱이 알 수 없어 "이 문서의 서술이 운영에서 언제부터 참인가"가 확정되지 않는다 → OQ-P-284. Android 표면 셈 27/27 유지, `http/` 커버 25/27 유지, **Android 열 값 변동 0**. **미결 신규 2건**(OQ-P-284 baseline 실행 시점 미상 / OQ-P-285 감사에 운영 스키마 축이 없다) + **conventions에 절 신설**(스키마 소유권) + **점검 절차에 마이그레이션 대조 축 추가**. 전 도메인 문서와 `spec/` 4건의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-25 | `bd18af4` | `Merge pull request #113 from mash-up-kr/chore/#112` (도메인 HTTPS 적용 — Caddy 리버스 프록시) | delta 3커밋(프록시 설정 · `forward-headers` · 런북) + 머지. **엔드포인트 증감 0**(28 + 테스트 전용 1 유지, **다섯 라운드 연속**), 신규 도메인 0, 신설 에러 코드 0. **컨트롤러·요청/응답 DTO·에러 코드 enum·`SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler`가 한 파일도 안 바뀌었다** — 계약 파일 변경 0건인 **두 번째** 라운드다. 바뀐 것은 계약이 아니라 **계약이 실려 오는 길**이다. ① **TLS 종단이 앞단 리버스 프록시(Caddy)로 갔다** — 도메인에 Let's Encrypt 인증서가 붙고 갱신은 자동이며, 프록시가 애플리케이션 컨테이너의 평문 포트로 넘긴다. **애플리케이션 자체는 여전히 평문으로 듣는다**(HTTPS는 프록시가 만든다). ② **`server.forward-headers-strategy: framework` 신설**(`application.yaml`) — 없으면 애플리케이션이 스킴을 `http`로 인식해 OpenAPI 문서의 `servers[0].url` 같은 **절대 URL이 `http://`로 나간다**. 이 체계가 보조 근거로 쓰는 스키마의 정확성에도 걸리는 설정이다. ③ ⚠️ **대가로 `X-Forwarded-*`를 무조건 신뢰한다** — 프록시를 우회하는 평문 포트가 열려 있는 동안 스킴·클라이언트 IP를 위조할 수 있어, 런북이 검증 뒤 **평문 포트 차단**을 절차에 두었다. ④ ⚠️ **그 차단이 앱을 끊는다** — 지금 앱이 붙는 주소가 그 평문 포트고, 닫히는 순간 기존 `YG_BASE_URL`로 빌드된 앱은 전부 **연결 실패**한다. 차단은 배포가 아니라 사람이 하는 1회성 인프라 조작이라 **서버 커밋에 남지 않는다** — OQ-P-284에 이어 "코드로 읽을 수 없는 전환 시점"이 **둘째**가 됐다. ⑤ **conventions의 Android 서술 1건 정정** — "앱에 `usesCleartextTraffic`도 `networkSecurityConfig`도 없다"고 적어 왔으나 `android:usesCleartextTraffic="true"`가 **2026-08-15(앱 PR #241)부터 main 매니페스트에 있었다**. 열흘 묵은 오기이고, 서버가 HTTPS를 갖춘 지금은 그 플래그에 남는 효용이 없다(제거는 앱 결정). Android 표면 셈 27/27 유지, `http/` 커버 25/27 유지, **Android 열 값 변동 0**(요청·응답 형태 불변). **미결 신규 1건**(OQ-P-302 평문 포트 차단 시점 미상) + **부분 해소 1건**(OQ-P-076 — ① 서버 HTTPS 전환이 채택됐고 앱 base URL 교체·플래그 제거 둘이 남았다). conventions의 전송 절을 전면 재작성하고 전 도메인 문서와 `spec/` 4건의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-26 | `e7092a3` | `[Feat/#114] 오늘/상세 캔버스 응답 placedBy에 ownerType(ME/OTHER) 필드 추가 (#115)` | delta 1커밋(squash, 기능 PR #115). **엔드포인트 증감 0**(28 + 테스트 전용 1 유지, **여섯 라운드 연속**), 신규 도메인 0, 신설 에러 코드 0, `SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler`·마이그레이션 전부 불변. 바뀐 것은 **응답 필드 하나**이고, 그 하나가 이 체계에서 처음 보는 성질을 갖는다. ① **`OwnerType`(`ME`·`OTHER`) 신설**(`core/parfait/domain`)과 함께 오늘 조회·상세 조회 두 응답의 `images[].placedBy`에 `ownerType`이 붙었다(`PlacedByResult`·`PlacedByResponse`). **토핑 배치 응답(`PlaceParfaitImagePlacedByResponse`)에는 안 붙었다** — 2026-08-19에 갈라 놓은 두 클래스가 여기서 실제로 다른 모양이 됐다. ② ⚠️ **응답이 요청자에 따라 달라지는 첫 사례다** — `GetTodayParfaitService`·`GetParfaitDetailService`의 `buildImages`가 인자로 `requesterMemberId`를 받아 배치자의 계정 id와 견준다. 같은 캔버스라도 보는 사람마다 다른 JSON이므로 **사용자 사이에 공유·캐시할 수 없는 응답**이 됐다(그전까지 이 도메인의 모든 응답은 요청자 비종속이었다). ③ **판정 축이 계정 id다** — 응답에 함께 실리는 `placedBy.groupMemberId`는 그룹 멤버십 행 id라 축이 다르다. 서버가 판정을 끝내 주므로 소비 측은 두 축을 맞출 필요가 없다. ④ **Android 미결 하나가 서버에서 닫혔다(OQ-P-250 ①)** — `CanvasMainViewModel`의 `CanvasToppingVO.isMine()`이 **상수 `false`**라 C-202의 본인 갈래가 통째로 비어 있었고, C-301 편집 탭은 계정 id와 멤버십 행 id를 견주는 **축이 다른 비교**로 선택·이동·삭제 게이트를 열고 닫았다. 이 필드가 둘 다 없앤다. 남은 것은 ②(앱이 읽는 일)·③(C-305 목적지 부재)다. ⑤ 서버 테스트 셋(`GetTodayParfaitServiceTest`·`GetParfaitDetailServiceTest`·`ParfaitControllerTest`)이 본인·타인 두 갈래와 JSON 노출을 잠근다. Android 표면 셈 27/27 유지, `http/` 커버 25/27 유지, **Android 열 값 변동 0**(develop이 이 필드를 안 읽을 뿐 요청·응답 형태와 어긋나지 않는다 — `ignoreUnknownKeys = true`). **미결 신규 0건** + **부분 해소 1건**(OQ-P-250 ①). 전 도메인 문서와 `spec/` 4건의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-08-31 | `de3a99a` | `Merge pull request #123 from mash-up-kr/feat/#122` (캔버스 리스트 조회 응답에 status 추가) | delta 5커밋(기능 2 + 테스트 1 + 머지 2). **엔드포인트가 여섯 라운드 만에 늘었다 — 28 → 29 + 테스트 전용 1**, 신규 도메인 0, 신설 에러 코드 0, `SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler`·마이그레이션 전부 불변. ① **토핑 일괄 수정 PATCH 신설**(`79a6d35`, PR #119) — `UpdateParfaitImagesController`가 컬렉션 경로 `/api/v1/groups/{groupId}/parfaits/{parfaitId}/images`에 **경로 인자 없는 `@PatchMapping`**으로 붙어, 같은 URL을 POST(배치 확정)와 메서드로 나눠 갖는다. 요청은 `items` 배열이고 항목은 단건 PATCH의 다섯 nullable 필드 + `parfaitImageId`, 응답은 **단건 응답 DTO를 재사용**한 `images` 배열이다. 병합 규칙(`null`이면 유지)도 단건과 같다. ② ⚠️ **검사 순서가 단건과 반대다** — 일괄은 그룹 소속·파르페 존재·마감을 요청당 한 번씩 **앞에서** 보고 항목별 소유권을 뒤에 보므로, 마감된 캔버스의 남의 토핑에 **단건은 403 `PARFAIT_IMAGE_NOT_OWNED`, 일괄은 409 `PARFAIT_ALREADY_CLOSED`**가 온다. 같은 일을 하는 두 엔드포인트가 같은 상황에 다른 코드를 낸다. ③ ⚠️ **부분 성공이 없다** — `@Transactional` 하나라 항목 하나가 걸리면 전부 롤백이고, **어느 항목이 걸렸는지 응답에 없다**(코드만 오고 `parfaitImageId`는 안 온다). `items` 개수 상한도 없고 `@Valid`도 없다. ④ ⚠️ **빈 `items`는 검증을 하나도 안 돌고 200 + `images: []`다** — 그룹 밖 사람이 빈 요청을 보내도 403이 아니라 200이다(`items.isEmpty()`가 모든 검사보다 앞이다). ⑤ **과거 캔버스 목록 응답에 `status` 추가**(`00622cb`, PR #123) — 경량 프로젝션 `ParfaitSummary`부터 `PastParfaitResult`·`PastParfaitResponse`까지 관통시켰다. 값 집합은 `today`·상세와 같은 `ACTIVE`·`CLOSED`·`EMPTY`, 널 아님. 엔드포인트·요청·에러 불변. ⑥ **커밋 본문이 앱의 지금 모양을 정확히 겨냥했다** — "클라이언트가 단건 수정 API를 개수만큼 반복 호출해야 했다"가 동기이고, 앱 `CanvasBGEditViewModel`이 실제로 `async` + `awaitAll`로 그렇게 부른다(PR #336). Android 표면 셈 **27/27 → 27/28**(공백 1 = 일괄 PATCH), `http/` 커버 **25/27 → 25/28**(`parfait-image.http`에 일괄 요청 없음 — **여섯 번째 왕복**이고 이번에는 `:data` 표면도 함께 비었다), **Android 열 값 변동은 `parfait-image.md`에 행 1개 추가뿐**(`⚠️불일치` 0건 — 과거 목록의 `status`도 앱이 안 읽을 뿐 어긋나지 않는다, `ignoreUnknownKeys = true`). **미결 신규 2건**(OQ-P-333 과거 목록 `status` 미수용 + `isEmpty`를 개수로 따로 추론 / OQ-P-334 일괄 API 미사용 · 부분 성공 소멸 · 실패 항목 식별 불가 · 코드 갈림). 전 도메인 문서의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-09-01 | `02e11be` | `[Fix/#117] 오늘 토핑 없는 그룹의 "N시간 전"이 그룹 생성 시각 기준으로 표시됨 (#124)` | delta 1커밋(squash 머지 1). **엔드포인트·DTO 필드·에러 코드·`SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler`·마이그레이션 전부 불변** — 바뀐 것은 `persistence` 한 곳(`ParfaitGroupMemberRepository.findMyGroupSummaries` 네이티브 쿼리 + `ParfaitGroupAdapter.findAllByMemberId`)뿐인데 **계약 서술은 세 군데가 뒤집혔다.** ① **네 서브쿼리가 `parfait_image`를 `LEFT JOIN` → `INNER JOIN`으로 바꾸고 `ORDER BY`에서 `parfait_date`를 뺐다** — 새벽 3시 회전 배치가 만든 빈 오늘 캔버스가 NULL 행으로 맨 앞에 와 `COALESCE`가 **그룹 생성 시각**까지 새게 하던 자리가 막혔다. `recentImageUploadedAt`은 이제 날짜 무관 **정말 마지막 토핑 시각**이고, 대체값(그룹 생성 시각)은 **토핑이 한 건도 없는 그룹**에만 남는다. ② ⚠️ **`recentImageUrl` 서브쿼리에만 `parfait_date = :today`가 붙었다**(어댑터가 `ParfaitDay.current()`를 넘긴다) — 이 필드는 이제 **오늘 캔버스의 토핑**만 가리키고, 어제 토핑이 있어도 오늘이 비면 `null`이다. ③ ⚠️ **그래서 "토핑 0건"을 가르던 수단이 사라졌다** — OQ-P-235가 ①번 항목으로 적어 둔 "앱이 `recentImageUrl == null`을 활동 없음으로 읽는다"가 **선택지째 무효**가 됐다. ④ `lastPlacedByNameTagChip`은 `recentImageUploadedAt`과는 여전히 같은 토핑을 가리키지만 **`recentImageUrl`과는 갈린다**(그쪽만 오늘 한정). 탈퇴자 `DEFAULT` 동작은 그대로이나 **그 이유를 적어 두었던 쿼리 주석이 삭제됐다** — 계약 문서의 근거를 주석에서 쿼리 구조(`placer`만 `LEFT JOIN`)로 옮겼다. ⑤ **목록 정렬 규칙을 계약 문서에 처음 적었다**(마지막 토핑 `created_at` 내림차순, 없으면 그룹 생성 시각, 동률은 `parfait_group.id` 내림차순). ⑥ **`⚠️불일치`가 0건 → 1건**(`parfait-group.md` GET 목록) — 앱 `MyParfaitGroupVO` KDoc과 `ToppingImage.kt#toToppingImage`가 아직 옛 뜻으로 읽어, 어제까지 활동한 그룹이 G-001에서 템플릿 그래픽으로 그려진다. Android 표면 셈 **27/28 유지**, `http/` 커버 **25/28 유지**. **미결 신규 1건**(OQ-P-336) + OQ-P-235 갱신. ⚠️ **타입·키 대조로는 못 잡는 첫 부류다** — 앱 코드도 와이어 문자열도 하나 안 바뀌었고 서버가 같은 필드에 담는 **뜻**만 바뀌었다. 전 도메인 문서의 `server_commit`·`verified`를 이 기준선으로 올림 |
| 2026-09-04 | `aa9cc9b` | `[Feat/#127] FCM 발송 인프라 + 3종 알림 트리거 연결 (#129)` | delta 1커밋(squash 머지 1). **HTTP 표면 변화 0** — 컨트롤러·요청 DTO·에러 코드·`SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler` 전부 불변이고 총계도 **30 + 테스트 전용 1** 그대로다. **그런데 이 체계가 다뤄 온 적 없는 축이 생겼다** — 서버가 앱으로 미는 **FCM 푸시**다. ① **트리거는 토핑 등록 1종**: `PlaceParfaitImageService.place()`의 신규 배치(`existing == null`)만이고 **재배치는 안 보낸다**. 수신자는 `ToppingPlacedNotifier`가 그룹 구성원에서 **나간 사람과 작성자 본인을 빼고** 정한다. ② **페이로드가 계약이다**(`NotificationMessageFactory`·`FcmNotificationSender`) — 제목 `{그룹명} 파르페에 체리 얹을 타이밍!`, 본문 `{닉네임}님이 새 토핑을 쌓았어요`(작성자 탈퇴 시 `누군가 …`), `data` 키 **`type`·`route`·`groupId`·`date`**(값 전부 문자열), TTL 6시간, **Android 채널 id `parfait_default`**·priority HIGH, APNs `apns-priority`·`apns-expiration`·sound. ③ **at-least-once** — `dedup_key`로 생산자는 멱등이지만 **중복 수신이 가능**하다. 지연은 폴링 기본 2초 + `AFTER_COMMIT` 즉시 깨우기, 실패는 1분→5분→15분→1시간→6시간 최대 5회 뒤 **조용히 버려진다**(응답도 통지도 없다). ④ **발송 직전 재검증** — 그룹 삭제·수신자 탈퇴·수신자 토큰 0건이면 취소하고, **작성자가 나갔으면 익명 문구로 치환**한다. 즉 알림은 올린 순간이 아니라 **보내는 순간의 상태**를 담는다. ⑤ **기기 토큰 삭제 경로가 둘 → 셋**: 죽은 토큰(`UNREGISTERED`·`INVALID_ARGUMENT`·`SENDER_ID_MISMATCH`)을 발송이 발견해 `deleteByToken`으로 회수한다 — **세션을 안 보고 토큰만 보므로 널 세션 행도 걷힌다**(OQ-P-342 사각 축소). ⑥ 마이그레이션 `V18__create_notification_outbox.sql` — FK 없는 큐, `dedup_key` 유니크, `(status, scheduled_at)` 인덱스, 종료 행은 매일 04:00 KST에 7일 경과분 삭제. ⚠️ **커밋 제목의 "3종 트리거"는 코드와 어긋난다** — 문구 조립 함수도 `ToppingPlacedNotifier` 호출부도 하나뿐이다. ⚠️ **Android 수신부는 0건**(develop 기준, 2026-08-22 PR #325가 FCM 축을 걷어냈다) — 등록 토큰이 0건이라 발송이 전부 `NO_DEVICE_TOKEN`으로 취소돼 **동작 영향 0**이지만, 앱이 등록을 시작하면 ②③이 곧바로 구속력을 갖는다. 조치: [notification.md](notification.md) 전면 개편(푸시 절·앱 요구사항 표·삭제 세 경로·`V18` 신설), [conventions.md](conventions.md) **"HTTP 밖 계약" 절 신설**, [README.md](README.md) 도메인 표·총계·회차 문단, [index.md](../index.md) 지금 상태·ADR-0013 행, open-questions 3건 신설(OQ-P-351 정책 근거 부재·OQ-P-352 채널 id 합의·OQ-P-353 TTL×백오프 경합)·3건 갱신(341·342·343 부분 해소) |
| 2026-09-02 | `0c59af9` | `[Feat/#125] 기기(FCM) 토큰 저장/삭제 API + device_token 스키마 (#126)` | delta 1커밋(squash 머지 1). **여덟 번째 도메인 `notification`이 신설돼 엔드포인트가 29 → 30 + 테스트 전용 1**이 됐다(도메인 증가는 2026-08-11 이후 처음). 문서 [notification.md](notification.md) 신설·[README.md](README.md) 인덱스 등록. ① **`POST /api/v1/notifications/devices`** — 인증 필요(화이트리스트 불변), 요청 `RegisterDeviceTokenRequest`(`token` `@NotBlank` · `platform` `IOS`/`ANDROID`), 성공 **204 + 본문 없음**이라 **envelope 예외가 2건 → 3건**이 됐다. ② **등록은 `token` 유일 키 upsert** — 기존 행이 있으면 `DeviceToken.reassign`이 소유자·세션·플랫폼·갱신 시각을 덮어써 기기 양도·계정 전환 시 이전 매핑이 자동 정리된다. **삭제 엔드포인트는 없다** — 삭제는 로그아웃(세션 단위, `LogoutService`)과 탈퇴(회원 단위, `MemberService.withdraw`의 `afterCommit`, refresh token 정리와 별개 `runCatching`)의 부수 효과뿐이다. ③ **access token이 `sessionId` 클레임을 싣기 시작했다**(`createAccessToken(memberId, sessionId)`, `validateAccessToken` 반환이 `Long` → `AccessTokenClaims`, `JwtAuthFilter`가 credentials 슬롯에 적재) — **와이어 계약은 불변**이고 구 토큰은 널로 채워 정상 인증된다. ④ **신설 에러 코드 0종**(400은 전역 `INVALID_REQUEST`, 401은 `AuthErrorCode` 4종), `ApiResponse`·`GlobalExceptionHandler`·`SecurityConfig` 전부 불변. ⑤ 마이그레이션 `V17__create_device_token.sql` — `token` 유니크·`member_id` FK·**`session_id` 널 허용**·`(member_id, session_id)` 인덱스. ⚠️ **Android 대응 심볼 0건**(develop `0173e454`에 `FirebaseMessaging`·FCM 취득이 없다) → 표면 27/28 → **27/29**, `http/` 요청 모음 25/28 → **25/29**(일곱 번째 왕복). 미결 3건 신규 등록(OQ-P-341 **FCM 철회 근거 소멸** — 2026-08-22 PR #325가 "보낼 서버가 없다"를 이유로 걷어낸 축의 전제가 이 delta로 사라졌다 · OQ-P-342 세션 널 행의 로그아웃 사각 · OQ-P-343 발송 트리거 계약 부재) |
| 2026-09-08 | `09e7d92` | `Merge pull request #132` (캔버스 응답 `groupName`) + `[Feat/#128] P-02·P-03 데일리 리마인드 배치 스케줄러 (#130)` | delta 4커밋(실질 3 — 머지 1·기능 2·`AGENTS.md` 1). **엔드포인트 증감 0**(30 + 테스트 전용 1 유지)인데 **두 축이 함께 움직였다.** ① **응답 필드 신설** — `GetTodayParfaitResult`·`GetTodayParfaitResponse`에 **`groupName`(String, 비널)**이 붙어 **오늘·상세 두 조회 모두** 그룹명을 함께 준다. 커밋 메시지가 근거를 적는다: **그룹 id만 쥔 채(푸시) 캔버스로 들어오면 상단 그룹명을 얻을 길이 없었다.** 두 서비스가 `ParfaitGroupQueryPort.findById`를 새로 부르므로 **404 `GROUP_NOT_FOUND` 경로가 두 조회에 생겼다**(멤버십 검사가 앞이라 실제로는 거의 안 닿는다). **앱은 이 필드를 안 읽는다**(`ignoreUnknownKeys`로 조용히 버려짐) → OQ-P-383. ② **푸시가 1종 → 3종** — `REMIND_AM`(10:00 KST)·`REMIND_PM`(20:00 KST) 데일리 리마인드가 붙었다. 대상은 `left_at IS NULL` 멤버십 1개 이상 + 토큰 보유자 전원(`device_token` 기준 `EXISTS` 세미조인이라 **사용자당 1건**), `data`는 **`type`·`route`(=`group`) 둘뿐**(토핑의 `groupId`·`date`가 없다), TTL **1시간**. **Outbox를 안 쓴다** — 500개씩 멀티캐스트(`sendEachForMulticast`, 청크 사이 200ms), **재시도 없음·중복 없음·실패는 로그뿐**. P-03은 **배치 시작 시각이 21:00 이상이면 통째 스킵**(E-09)인데 **뒤쪽 청크의 도착 시각은 못 막는다** → OQ-P-385. ③ **토핑 알림 문구 변경** — 제목 `체리 얹을 타이밍!` → **`체리 하나 톡!`**, 본문 닉네임은 **8자 초과 시 앞 8자 + `...`**(닉네임 상한 15자라 잘리는 값이 실제로 있다). **문구는 발송 시점 조립이라 앱 배포와 무관하게 즉시 바뀐다.** ④ **`NotificationSenderPort.sendMulticast` 신설** — "멀티캐스트는 후속 스펙" 서술 폐기. ⑤ **토큰 삭제 경로 셋 → 넷** — 리마인드가 회차의 죽은 토큰을 모아 `deleteByTokenIn`으로 일괄 회수한다(판정은 셋째와 같은 `FcmErrorCodes.DEAD_TOKEN`, 범위만 다르다). ⑥ **`FcmErrorCodes` 추출** — 죽은 토큰·재시도 코드 집합이 `core/notification/domain`으로 모였고 **값은 불변**이다. ⑦ **`ReminderTargetQueryPort` KDoc의 "권한 거부 이용자는 토큰이 없다"는 전제가 앱과 어긋난다** — PR #450이 등록을 권한과 무관한 세션 축으로 옮겼으므로 **거부한 기기도 대상에 든다** → OQ-P-384. ⑧ **HTTP 표면 불변** — 컨트롤러 경로·요청 DTO·`SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler`·마이그레이션 전부 그대로. 디스패처 변경은 로그 요약(`cancelledByReason`)뿐이라 계약 영향 없다. 신규 OQ 3건(383·384·385), 기존 2건 갱신(343·361 각각 한 갈래 해소) |
| 2026-09-10 | `d76b27a` | `fix: 그룹 탈퇴 후 재참여가 불가능하던 문제 해결 (#134)` | delta 1커밋. **엔드포인트·요청/응답 DTO·에러 코드 enum·`SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler`·마이그레이션이 전부 그대로인데 권한 경계가 두 군데 움직였다** — 2026-09-01 회차와 같은 종류의 delta다(계약은 HTTP 표면에만 있지 않다). ① **멤버십 판정이 `leftAt IS NULL`로 좁아졌다** — `existsByGroupIdAndMemberId`의 구현이 `existsByParfaitGroupIdAndMemberId` → `existsByParfaitGroupIdAndMemberIdAndLeftAtIsNull`. 이 검사를 쓰는 자리는 **참여 미리보기·참여의 `alreadyJoined` 판정**과 **파르페 다섯 경로**(연도·오늘·과거·상세 조회와 배경 변경)다. 앞에서는 **탈퇴 이력만 있는 회원의 재참여가 열렸고**(직전까지 409 `GROUP_ALREADY_JOINED`), 뒤에서는 **탈퇴한 회원이 403 `GROUP_NOT_JOINED`로 막힌다**(직전까지는 탈퇴 행이 남아 있는 것만으로 나간 그룹의 캔버스를 읽고 배경까지 바꿀 수 있었다). ② **재참여는 새 행을 만들지 않는다** — `(parfait_group_id, member_id)` 유니크 제약 때문에 INSERT가 실패하므로, `findAnyByGroupIdAndMemberId`로 탈퇴 행을 찾아 **`ParfaitGroupMember.rejoin`으로 재활성화**한다. 그 전이는 `leftAt`을 널로 되돌리고 `joinedAt`을 재참여 시각으로 갱신하며 `groupNickname`을 **전역 닉네임으로 초기화**하고 `nametagChip`을 **다시 뽑는다**(탈퇴 전 닉네임·칩은 복원되지 않는다). **칩의 "내부 재추첨 경로가 없다"는 서술이 이 커밋으로 깨졌다.** ③ **멤버십 id가 유지되는 것이 계약 영향의 본체다** — 소유권 판정이 `groupMember.id == parfait_image.placed_by_group_member_id`라, 재참여한 회원은 **탈퇴 전 자기 토핑의 소유권을 되찾고**(마감 검사가 뒤라 실제 편집은 `ACTIVE` 캔버스 한정), 캔버스 응답 `placedBy`의 표시도 `(알수없음)`·`DEFAULT`에서 **새 닉네임·새 칩으로 되살아난다**. ④ 정원 계산(`countByGroupId`)은 이미 `leftAt IS NULL` 기준이어서 바뀐 것이 없다. 근거 테스트 넷이 함께 들어왔다 — `ParfaitGroupServiceTest`(재활성화 단언)·`ParfaitGroupAdapterTest`·`ParfaitGroupMemberRepositoryQueryTest`·`RejoinAfterLeaveIntegrationTest`(MySQL 컨테이너로 참여→탈퇴→재참여 왕복). 문서 조치: `parfait-group.md`·`parfait.md`·`parfait-image.md` 계약 절 갱신 + 세 문서 frontmatter `server_commit`·`verified` 갱신, `README.md` 회차 주석. 신규 OQ 1건(OQ-P-398 — 재참여 규정이 정책 문서에 없다) |
| 2026-09-11 | `82e6edc` | `Merge pull request #139 from mash-up-kr/fix/#138` (그룹 목록 최신 사진 테두리) + `Merge pull request #137 from mash-up-kr/fix/#136` (그룹명 자모 허용) | delta 6커밋(기능 2·테스트 2·머지 2). **엔드포인트 증감 0**(30 + 테스트 전용 1 유지), 신규 도메인 0, 신설 에러 코드 0, `SecurityConfig`·`ApiResponse`·`GlobalExceptionHandler`·마이그레이션 전부 불변. 바뀐 것은 둘이고 전부 `parfait-group` 계열이다. ① **`GroupName` 정규식에 자모 범위(`ㄱ-ㅎ`·`ㅏ-ㅣ`)가 들어왔다**(`21d8bd1`, PR #137). 닉네임 값 객체 둘(`GroupNickname`·`GlobalNickname`)은 2026-08-15 `e4ff23f`에 이미 받았는데 그룹명만 빠져 있었다. ⚠️ **이 체계는 그 사이 그룹명도 자모를 받는다고 적어 왔다** — `parfait-group.md` 정책 대조 메모가 두 값 객체의 규칙을 하나로 적으며 `GroupName`까지 묶었고, `git log -S'ㄱ-ㅎ'`로 확인하니 08-15 커밋이 건드린 파일은 닉네임 둘뿐이었다. 그 기간에 앱 `CheckNameValidUseCase`(A-005 그룹명·닉네임 공용, PR #250)는 자모를 통과시켜 **자모가 든 그룹명이 서버에서만 400 `INVALID_GROUP_NAME`**이었다. 서버가 따라와 앱 코드 변경 없이 닫혔다. ② **그룹 목록 응답에 테두리 세 필드가 붙었다**(`2510998`, PR #139) — `recentImageBorderType`(`NONE`·`SOLID`)·`recentImageBorderColor`·`recentImageBorderWidth`, 모두 널 허용. `findMyGroupSummaries`의 새 서브쿼리 셋이 `recentImageUrl`과 조건(`parfait_date = :today`)·정렬이 같아 **같은 오늘 캔버스 토핑**을 가리키고, 오늘 캔버스가 비면 넷이 함께 `null`이다. `border_type` 컬럼이 `NOT NULL`이라 이미지가 있으면 종류도 있고, 색·두께는 `SOLID`일 때만 보장된다(`NONE`은 검사 없이 보낸 값을 저장). Android `MyParfaitGroupResponse`는 이 필드를 안 읽는다 — `ignoreUnknownKeys = true`라 **`⚠️불일치` 아님**(목록 행은 OQ-P-336 때문에 이미 `⚠️불일치`로 남아 있다). Android 표면 셈·`http/` 커버 변동 0. 문서 조치: `parfait-group.md` 응답 표 +3행·계약 절 신설·정책 대조 메모 정정·Android 매핑 메모·미결 절, frontmatter `server_commit`·`verified` 갱신(다른 도메인 문서는 계약 불변이라 그대로), `README.md` 총계·회차 문단, `index.md` 서버 커밋. **신규 OQ 0건** + **기존 2건 갱신**(OQ-P-316 ① 서버 쪽 해소 · OQ-P-171 그룹명 기간 정정) |
