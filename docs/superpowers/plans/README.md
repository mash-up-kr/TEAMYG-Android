# Plans

Parfait 프로젝트의 작업 계획 문서를 모읍니다.

> **참고** — 파일명 컨벤션: `YYYY-MM-DD-kebab-case-topic.md`. 완료/폐기된 계획은 `archive/` 하위로 이동합니다.

| 계획 | 내용 |
|------|------|
| [2026-08-28-login-debug-mode.md](2026-08-28-login-debug-mode.md) | **로그인 화면 디버그 모드**(4 Task, 신규 6파일·수정 7파일). 로그인 화면 빈 영역 더블탭 7회 + 롱프레스로 디버그 모드를 켜고 우측 상단 배지 탭으로 끈다. 켜지면 카카오 로그인이 카카오톡을 건너뛰고 웹 로그인으로 간다. **Task 1** `DebugModeLocalDataSource` → `DebugModeRepository` 얇은 슬라이스(기존 `parfait_preferences` DataStore의 `debug_mode` 불린 하나, UseCase 없음, `@Binds` 둘). **Task 2** `KakaoLoginHelper.login(forceAccountLogin)` — 참이면 `isKakaoTalkLoginAvailable` 검사 자체를 건너뛴다. **Task 3** `LoginViewModel`이 저장소를 구독해 `LoginState.isDebugMode`로 투영하고 제스처 판정(**정확히 7**, 롱프레스가 성공 여부와 무관하게 카운터 리셋)을 든다. ⚠️ `RequestLoginWithKakao`가 `data object`에서 `data class`로 바뀌므로 **기존 테스트 5곳의 단언을 함께 고쳐야** 컴파일된다. **Task 4** `LoginRoute`가 `Box`로 감싸 `pointerInput`을 **부모에** 달고 배지를 `TopEnd`에 그린다 — 콘텐츠 위 오버레이로 덮으면 카카오 버튼 탭까지 먹는다. 커밋하지 않는다(사용자 미요청). 계획 대응 스펙 [login-debug-mode](../specs/2026-08-28-login-debug-mode.md) 📌 **구현이 release 계보에만 있다(2026-08-30)** — `origin/feature/debug-mode`(커밋 6개)가 `release/version-0.1.0-4`에 머지됐고 develop 에는 `DebugMode*` 심볼이 0건이다. 아카이브 판정을 develop 머지로 두는 한 이 계획은 구현이 끝난 채로 `draft`에 남는다(OQ-P-311 ③) |
| [2026-08-23-segmentation-preprocessing.md](2026-08-23-segmentation-preprocessing.md) | **세그멘테이션 입력 전처리**(14 Task, **4단계 스파이크 게이트 구조**, 베이스는 **`develop`** — 초판이 지정한 `feature/c103-multi-subject-ui` 팁 위 스택은 그 브랜치가 PR #342로 머지되면서 근거가 사라져 2026-08-24에 정정했다). 스펙이 근거 등급으로 항목을 갈라 놓았으므로 계획도 그대로 갈린다 — **1단계는 근거가 확정된 것만**(Task 1~4), **2단계는 사람이 실기기에서 하는 측정**(Task 5), **3단계는 사람이 하는 측정과 판정**(Task 6~8, Task 8은 회전 확장의 독립 조건부), **4단계는 판정을 통과한 것만**(Task 9~13), 마무리가 Task 14다. 조건을 안 만족하면 3단계를 통째로 버리고 그 사실을 스펙에 적는 것이 정답이다. Task 1은 `ImageCapture.Builder()`에 `CAPTURE_MODE_MAXIMIZE_QUALITY` + `setJpegQuality(100)`를 준다 — **손실은 저장이 아니라 여기서 처음 생긴다**(기본 빌더가 JPEG로 촬영하고 `ImageProxy.toBitmap()`이 그것을 디코드한다). 유닛 테스트가 없는 유일한 무조건 항목이다. Task 2·3이 회전 정합이다 — `exifOrientationToDegrees`를 순수 함수로 빼 JVM으로 덮고(미러링 4종이 0인 것을 테스트가 지킨다), `decodeUriToBitmap`의 **API 28 미만 갈래에만** 결선한다. ⚠️ **API 28 이상은 손대지 않는 것이 기본값이다**(OQ-P-280 — 확인 못 한 것을 근거로 이중 회전 쪽으로 기울지 않는다). EXIF는 `ImageDecoder.createSource`가 스트림을 소비하므로 **uri를 한 번 더 열어** 읽고, 못 읽으면 0으로 진행하되 **로그를 남긴다**(재개방 실패가 상시 참이 되면 보정이 조용히 무효가 되는데 사진으로도 안 드러난다). 회전 판은 전체 해상도라 원본을 회수한다. **Task 4는 스펙이 "PNG 채택 시"로 둔 것을 1단계로 올렸다** — `RecentImageRepositoryImpl#fileExtension`의 `SOURCE -> "jpg"` 하드코딩은 **지금도 참인 결함**이다(갤러리에서 고른 PNG가 `.jpg`로 앉고, C-301 배경 편집이 최근 목록에서 그것을 고르면 `formatOf`가 확장자 MIME을 스니핑보다 먼저 믿어 `image/jpeg`로 올라간다). `UploadImageFormat.ofBytes`가 이미 있어 한 줄이다. Task 5가 게이트다 — 사진 6장(EXIF 회전 / 512 미만 / 머리카락·털 / 뷰파인더 변에 걸친 / 저대비 / 다중 피사체), **회전 사진은 태그가 실제로 6·8인지 먼저 확인**(픽셀에 굽고 태그는 정상으로 쓰는 앱이 많아 그냥 하면 아무것도 못 배운다), API 27 에뮬레이터로 하위 갈래 판정, 손실은 **3벌**(현행 / 품질 상향+JPEG / 품질 상향+PNG)로 갈라 기여를 분리, 512 사진은 **배치까지 끝내** 토핑 크기 회귀를 본다(OQ-P-282). Task 6·7이 확대다 — `computeUpscaleTarget`에 **총 픽셀 상한 4M**을 넣는다(짧은 변만 보고 키우면 극단 종횡비에서 긴 변이 폭증하고 `largeHeap` 없이 OQ-P-228의 피크 위에 얹힌다). 결선은 `segmentImage`가 아니라 `decodeImage`이고 근거는 **일관성**이다(좌표계가 아니다 — `buildCutoutBitmap`이 늘려 그린다). Task 8·9·10이 PNG다 — `CameraCacheFormat` 기본값 JPEG라 **시스템 카메라 경로는 한 글자도 안 바뀌고**, 포맷은 `CustomCameraRoute`가 `returnResultOnly`로 가른다(**배경 촬영은 누끼를 안 타므로 JPEG 유지**). PNG와 함께 반드시 들어가는 둘이 **촬영 중 상태·셔터 잠금**(이동이 저장 뒤라 인코딩 공백에 연타가 들어오고 파일명이 초 단위라 서로를 덮는다)과 **실패 시 파일 삭제**(그 디렉토리에는 지우는 코드가 없다, OQ-P-279)다. Task 14는 실기기 회귀와 문서 갱신이고 **버린 항목의 이유를 남기는 것이 산출물**이다. 스펙: [specs](../specs/2026-08-23-segmentation-preprocessing.md). **계획 검수 2회가 초판의 실행 결함 다섯을 잡아 구조를 다시 짰다.** ① **게이트가 순환이었다** — 측정하려면 3단계를 만들어야 하는데 3단계를 할지는 측정이 정했다. **버리는 스파이크 브랜치**(Task 5, 커밋·테스트·ktlint 없음)로 끊고, 판정 4조합의 실행 범위를 표로 못 박고(Task 7), 스파이크는 어느 경우에도 머지하지 않는다. ② **Task 3이 `ContentResolver.kt` 전문을 제시해 `readBytes`를 지웠다**(소비자 둘: `FileRecentImageLocalDataSourceImpl`·`ImageFileLocalDataSourceImpl`) — 전문 교체를 추가·치환 지시로 바꾸고 Global Constraints에 **"기존 파일을 전문으로 덮어쓰지 않는다"**를 넣었다. ③ **`@OptIn(FormatStringsInDatetimeFormats::class)` 유실**로 `:data` 컴파일이 깨질 자리. ④ **"동작 무변경"이라던 Task가 JPEG 품질을 95에서 100으로 올려** 3벌 비교의 측정 축을 섞었다 — 95를 유지한다. ⑤ **셔터 잠금이 `core/designsystem`의 `YGCameraShutter`를 고쳐야 하는데** 그 파일이 목록에 없었다(`enabled` 파라미터 자체가 없다, 3홉 중계 필요). 그 밖에 **API 28 이상 회전 확장이 무관한 확대 태스크를 가리켜** 확대가 기각되면 갈 곳이 없던 것을 **독립 조건부 Task 8**로 떼고, `feature/camera/impl`에 **테스트 소스셋이 없다**는 스펙의 사실이 계획으로 안 옮겨져 연타 가드가 무테스트로 남던 것을 `parfait.test.unit` 추가 + `CustomCameraViewModelTest` 3건으로 닫고, **죽은 `recycle()` 가드의 거짓 주석**(`createScaledBitmap`의 identity 반환은 `!isMutable` 조건이 붙는데 디코더가 `isMutableRequired = true`를 준다)을 걷고, 계획 예시 코드의 주석이 스스로 `parfait/CLAUDE.md` 규약을 어기던 것을 스펙 포인터로 접었다. 미결 신규: OQ-P-283(확장자 판정을 고쳐도 이미 `.jpg`로 앉은 PNG는 남아 최근 목록에서 두 칸을 먹는다 — 마이그레이션 안 하기로 감수). ✅ **1단계(Task 1~4)가 develop에 들어갔다**(2026-08-25 확인, PR #349 `a5e8a760`, 커밋 다섯·9파일 177/6). **2단계 이후는 미착수**라 계획은 active에 남는다. as-built 이탈 둘은 Task 3에 몰려 있고 둘 다 계약을 안 바꾼다 — `readExifDegrees` KDoc의 근거가 실제 호출 경로(`MediaStore.Images.Media.getBitmap`)로 정정됐고, 입력 스트림이 `null`인 갈래에 경고 로그가 붙었다. |
| [2026-08-05-orchestration-session-pipeline.md](2026-08-05-orchestration-session-pipeline.md) | 오케스트레이션 파이프라인 스킬 구현(6 Task, **TJYG-Android 코드 변경 0**): `start-session` → `start-default-session` 개명 → `start-orchestration-session` 골격(전제 확인·요구사항 수집·Run 생성) → 문서 단계 W1~W4 + 게이트 G1·G2 → 구현 단계(모듈 worktree·모델 지정 2단계 경로·RED/GREEN 증거 계약) → 통합·코드리뷰·최종 산출·에스컬레이션 → CLAUDE.md 라우팅. 산출물은 마크다운 스킬 문서 2개뿐이라 자동 테스트가 없고, 검증은 **frontmatter `name`↔디렉토리명 일치 · placeholder grep · 문서에 적은 orca 명령·플래그가 `--help`에 실재하는지 대조**로 한다. 스펙: [specs](../specs/2026-08-05-orchestration-session-pipeline.md) |

## 아카이브

> ✅ **C-106 결선 스택(PR3~PR6)이 develop에 들어왔다**(2026-08-22, PR #334 `19cb5299`). 넷을
> `ef55a58c` 위로 다시 쌓은 뒤 최상단 하나만 머지했고, **머지 트리가 PR6 브랜치 팁 `656cbf2e`와 같다** —
> 아래 네 행의 as-built 수치는 재측정 없이 그대로 develop 사실이다. 리베이스 라운드 기록은
> [PR3 계획서](archive/2026-08-20-c106-pr3-topping-draft-ssot.md)에 있다.

| 계획 | 내용 |
|------|------|
| [2026-09-14-build-cache-measurement-harness.md](archive/2026-09-14-build-cache-measurement-harness.md) | **✅ 완료·develop 머지(2026-09-16, PR #499 `f37a76540` — 트리 = 브랜치 팁 `6600f9efa`, 충돌 해소 편집 0건, Task 1~7 전량)** — 머지본이 **7 Task 계획보다 한 자리 넓다**: 계획에 없던 `report.py`가 CSV를 단일 파일 `report.html`로 조립한다(커밋 `6600f9efa`). 6파일 · 삽입 1200줄 · 커밋 19개 — 계획의 "신규 4파일"은 `README.md`를 빼고 센 수이고 실제 신규는 5파일이다. `tools/` 밖을 건드리지 않아 테스트 수는 그대로다. **로컬 빌드 캐시 측정 하니스**(7 Task, 신규 4파일·수정 1파일, 브랜치 `build/build-cache-measurement`). 리모트 빌드 캐시를 도입할 값어치가 있는지 재는 도구를 만든다. 스펙 [2026-09-14-build-cache-measurement-harness](../specs/archive/2026-09-14-build-cache-measurement-harness.md). **Task 1~2 init script** — `BuildEventsListenerRegistry` + `OperationCompletionListener` 로 태스크 outcome과 `getExecutionReasons()` 를 CSV로 받고(`TaskSkippedResult` 는 `TaskExecutionResult` 를 상속하지 않아 `as?` 로 받는다), `settingsEvaluated` 에서 `buildCache.local.directory` 를 전용 경로로 돌려 개발자의 `~/.gradle` 캐시를 안 건드린다. init script 스코프에 `objects` 가 없어 `serviceOf` 를 써야 한다. **Task 3이 게이트** — 다른 절대경로가 만든 캐시 항목이 적중하지 않으면 거기서 멈추고 보고한다. seed·probe **둘 다 worktree**이고 각각 `local.properties` 를 복사받으며 probe 는 `clean` 뒤에 잰다(계획 검수: 셋 중 하나만 빠져도 게이트가 거짓 실패한다). **Task 4~6 러너** — 선행 조건 검사, 시나리오 순서 고정(`--scenarios` 순서를 안 믿는다), S0~S2, 커밋 쌍 기반 S3(`T_local`)·S4(`T_remote`). ⚠️ **캐시를 채우는 빌드 앞에는 반드시 `clean` 이 온다** — Gradle 은 태스크가 실제로 실행될 때만 캐시에 저장하므로, 출력이 남아 있으면 시딩이 up-to-date 로 끝나 빈 캐시로 측정되고 핵심 값의 부호가 뒤집힌다(계획 검수가 실측 −3196ms 로 잡았다). S3·S4는 **측정 직전 출력 상태를 둘 다 커밋 `A` 기준으로** 맞춘다 — 한쪽만 지우면 캐시가 아니라 출력 유무가 차이를 만든다. 트리는 `git worktree` 로 만들고 `--tree` 로 빌리면 HEAD를 되돌린다(`ensure_tree` 를 명령 치환으로 부르면 서브셸이라 `trap` 이 무력해진다). **Task 7** — 핵심 값 `S3 − S4` 를 맨 앞에 둔 `summary.md`, actionable 기준 전 회차 적중률, README. macOS 전제: BSD `date` 는 `%3N` 을 오류 없이 쓰레기값으로 뱉으므로 `python3` 로 시각을 찍고, BSD `awk` 에 `asort` 가 없어 중앙값은 `sort` 로 낸다. 새 의존성 없이 실제 Gradle 실행으로 검증한다. ⚠️ 측정 전에 도입 문턱값을 정해야 한다 |
| [2026-09-11-g001-group-list-topping-border.md](archive/2026-09-11-g001-group-list-topping-border.md) | **✅ 완료·develop 머지(2026-09-16, PR #497 `a1fc2377f` — 트리 = 브랜치 팁 `108b4fa7d`, 충돌 해소 편집 0건, Task 1~5 전량)** — 10파일 · 삽입 449줄 · 삭제 29줄 · 커밋 5개. 유닛 1292 → **1298건**, 계측 39 → **46건**으로 계획이 센 케이스 수(6 + 7)와 정확히 같다. **계획과 어긋난 자리가 없다.** **G-001 그룹 목록 최신 토핑 테두리**(#494, 5 Task, 신규 2파일·수정 8파일, 브랜치 `feature/#494-group-list-topping-border`). **Task 1** `ToppingBorderPlateCache`가 열쇠당 판 한 장 대신 최근 순 선반(`PLATES_PER_KEY = 3`)을 둔다 — `put`은 1.25배 안에 드는 판을 대체하고, `get`에 `subjectLongSide: Int? = null`이 붙는다. 계측 테스트 7건(기존 designsystem androidTest, 기기 필요). **Task 2** `YGToppingCutoutImage`의 그리기 단계가 상태 판이 크기에 안 맞으면 캐시에서 맞는 판을 고른다(판을 만드는 `LaunchedEffect`는 불변). **Task 3** `YGToppingBorder`(색·dp·거리판) 신설, `YGToppingImage.Remote(url, border = null)`, `Remote` 분기가 **`size(Size96 px)`를 명시한** Coil 요청 + `rememberAsyncImagePainter` + `YGToppingCutoutImage`로 그리고 `padding(borderWidth)`을 `clip` 아래에 붙인다(로드 실패 그래픽은 96dp 그대로). **Task 4** `toToppingImage(outlines)`·`borderedImageUrls()`(색을 못 읽으면 테두리를 버린다) + JVM 테스트 6건, `GroupListContent`가 `rememberToppingOutlines(retryKey = 0)`로 거리판을 불러 넘긴다. **Task 5** app-preview "Remote + 테두리" 샘플(4dp·30dp·비정사각 8dp), 전체 빌드·테스트, 실기기 확인 6항목(목록↔캔버스 왕복 깜빡임, 캔버스 화면 넷 회귀 포함)은 사용자가 판정한다. 기본 미커밋 |
| [2026-09-10-segmentation-retry-recovery.md](archive/2026-09-10-segmentation-retry-recovery.md) | **✅ 완료·develop 머지(2026-09-10, PR #487 `95b7fc4d5`, Task 1~7 전량 + Task 8 자동 검증)** — **세그멘테이션 재시도 회복**(8 Task, 신규 7파일·수정 6파일, Task마다 커밋 — 사용자 요청). Task 1~3은 순수 추가(좌표·해상도·가드 / 대비 LUT / 마스크 분해를 위임 껍데기로), Task 4는 수확 코드 순수 이동, Task 5가 동작 변경(`PlateSource` 출처 분리 · 공통 subject 루프 · 캔버스 밖 후보 버림 · 폴백의 `ModuleNotReady`만 승격)이다 — 이동과 변경을 갈라 리뷰가 따로 기각할 수 있게 했다. Task 6은 정규화 실행기 + 사다리 + UseCase, Task 7은 ViewModel 끈적한 플래그, Task 8은 검증·문서다. 머지본이 계획과 **네 자리에서 다르다**: 신규 유닛 46 → **52건**(`isLongSideCapped` 4 · `projectAlpha` 2), 최상위 상수와 좌표·단계 타입이 `data/model/image/`와 `Segmentation*Spec` object로 이동, 최종 리뷰 수정 3커밋, 계획에 없던 `3217e62f7`(「직접 편집」 전환, 유닛 +1)이 Task 8 Step 2의 4번 확인 항목을 대상 없이 만들었다. ⚠️ **체크박스는 실행 세션이 남기지 않아 전부 미체크**(43개) — 진행의 정본은 `git log` 다. ⚠️ **Task 8 Step 2(1차 경로 실기기 회귀)는 수행 기록이 없다**(OQ-P-400). 대응 스펙 [segmentation-retry-recovery](../specs/archive/2026-09-10-segmentation-retry-recovery.md) |
| [2026-09-10-canvas-adaptive-polling.md](archive/2026-09-10-canvas-adaptive-polling.md) | **✅ 완료·develop 머지(2026-09-10, PR #482 `efa771503`)** — **캔버스 폴링 적응형 주기**(6 Task, 신규 2파일·수정 5파일 + 문서 2건). 고정 5초를 10/15/20 적응형으로 바꾸고 푸시가 되돌린다. 머지본이 계획과 **세 자리에서 다르다**: `acquire` 의 락이 하나로 합쳐졌고(`refreshNow` 와 같은 이유 — 갈라 두면 그 사이 마지막 `release` 가 끼어들어 구독자 없이 폴 잡이 산다), 폴러가 `CanvasPollInterval` 을 기본 인자로 받으며(`clock` 과 같은 모양), 토핑 판정이 `toppingGroupIdOrNull()` 로 서비스 밖에 나와 순수 `Map` 테스트로 잠겼다(`RemoteMessage` 를 계측 없이 못 만든다). ⚠️ 같은 PR 이 폴링과 무관한 화면 수정 다섯을 함께 실었다 — 그쪽 정본은 [canvas-feedback-fixes](../specs/archive/2026-09-10-canvas-feedback-fixes.md). ⚠️ **체크박스는 실행 세션이 남기지 않아 전부 미체크**(41개) — 진행의 정본은 `git log` 다. 대응 스펙 [canvas-adaptive-polling](../specs/archive/2026-09-10-canvas-adaptive-polling.md) |
| [2026-09-09-topping-upload-source-scaled.md](archive/2026-09-09-topping-upload-source-scaled.md) | **✅ 완료·develop 머지(2026-09-09, PR #480 `93cb002b5`)** — **토핑 업로드 원본 기준 축소**(46 체크박스). 축소 기준을 잘린 판에서 원본 사진 긴 변으로 옮겨 `SourceLongSide` 로 세그멘테이션부터 업로드 경계까지 나른다. 계획이 예고한 "#479 가 먼저 머지되면 인자 추가 지점이 한 겹 늘어난다"가 **실제로 그렇게 됐고** 인자는 UseCase 쪽에 얹혔다. as-built 넷(로그 함수 `ruleScaleOf` 신설·`Passthrough` 로그·`domain` 의존이 잠시 들어왔다 걷힘·배경 JPEG 품질 70 동반 인하)은 스펙에 있다. ⚠️ **체크박스 전부 미체크** — 진행의 정본은 `git log`. ⚠️ 이 계획은 active 표에 등록된 적이 없다(등록 누락). 대응 스펙 [topping-upload-source-scaled](../specs/archive/2026-09-09-topping-upload-source-scaled.md) |
| [2026-09-09-topping-draft-usecase-extraction.md](archive/2026-09-09-topping-draft-usecase-extraction.md) | **✅ 완료·develop 머지(2026-09-09, PR #479 `5f3aee286`)** — **토핑 초안 접근 UseCase 분리**(32 체크박스, 동작 불변 리팩터). `:domain` 에 UseCase 5종을 만들고 ViewModel 넷의 생성자 의존성·호출부만 바꾼다. `ToppingDraftRepository` 인터페이스와 `:data` 구현은 한 줄도 안 고친다. 설계대로 들어왔고 `feature/` 아래 Repository 참조는 0건이다. as-built 는 테스트 둘(판정 실패 테스트의 초안값 정정·`record` 미호출 단언 되살림). ⚠️ **체크박스 전부 미체크** — 진행의 정본은 `git log`. 대응 스펙 [topping-draft-usecase-extraction](../specs/archive/2026-09-09-topping-draft-usecase-extraction.md) |
| [2026-09-09-release-analytics-screen-tracking.md](archive/2026-09-09-release-analytics-screen-tracking.md) | **✅ 완료·develop 머지(2026-09-09, PR #478 `c56ed15eb`)** — **화면 진입 계측과 기기·앱 사용자 속성**(6 Task, 신규 7파일·테스트 3파일·수정 5파일). 머지본이 스펙과 어긋나는 자리는 없으나 **계획 문서 자체가 두 자리에서 낡았다** — Architecture 절이 트래커를 `@Singleton` 이라 적었는데 실제는 `@ActivityRetainedScoped` 이고(`@Singleton` 이면 `Navigator` 보다 오래 살아 그 실행의 A-001 이 통째로 빠진다), 판정 기준도 "백스택 최상단"이 아니라 **크기와 최상단의 짝**이다. ⚠️ **체크박스 41개 중 3개만 체크** — 진행의 정본은 `git log`. 대응 스펙 [release-analytics-screen-tracking](../specs/archive/2026-09-09-release-analytics-screen-tracking.md) |
| [2026-09-08-topping-edit-empty-subject-guard.md](archive/2026-09-08-topping-edit-empty-subject-guard.md) | **✅ 완료·develop 머지(2026-09-09, PR #472 `753675abe`, Task 1~4 전량 수행)** — **토핑 편집 빈 알맹이 차단**(5 Task, 신규 3파일·수정 6파일). 편집에서 영역을 전부 지워도 완전 투명 이미지가 서버까지 올라가는 구멍을 막는다. **새 하한을 만들지 않는다** — 자동 누끼가 이미 쓰는 커버리지 하한(5/10000·2,500px·알파 합)을 `data` 의 `internal` 에서 `domain` 의 `SubjectCoverage` 로 올려 자동 경로 두 호출부(`SegmentationCandidateFilter`·`ImageSegmentationRepositoryImpl#toCandidatePairs`)와 수동 편집이 같은 함수를 본다. **Task 2 는 TDD 의 red 를 만들지 않는다** — 판정 결과가 바뀌면 안 되는 이관이라 기존 `SegmentationCandidateFilterTest` **13건이 그대로 초록인 것이 곧 검증**이고 새 단언을 더하지 않는다. Task 3 이 `trimTransparentBounds` 를 `measureSubject`(순수 함수, `IntArray` 만 받아 JVM 유닛으로 덮인다) + `trimTo` 로 가르고 **한 번의 스캔**에서 경계와 알파 합을 함께 낸다(알파 합은 `Long` — 12MP 불투명이 `Int` 를 넘는다). Task 4 가 `completeEdit` 에서 **파일을 쓰기 전에** 판정해 비트맵 회수·`isSaving` 복구·`SubjectTooSmall` effect 로 되돌리고, Route 는 Toast 만 띄우고 **화면을 떠나지 않는다**(`LoadFailed` 와 달리 `onBack()` 을 안 부른다 — 되돌리기로 방금 지운 획을 되살려야 한다). ⚠️ **Task 3 끝과 Task 4 시작 사이에 `:feature:segmentation:impl` 컴파일이 일시적으로 깨진다** (호출부가 사라진 함수를 가리킨다) — 두 Task 는 한 덩이다. ViewModel 글루는 `android.graphics.Bitmap` 에 묶여 Robolectric 없이는 못 덮으므로 Task 5 실기기 5갈래로 확인한다. 브랜치는 **`develop` 에서 새로 판다**(다운스케일 브랜치 위에 얹지 않는다), **커밋하지 않는다**. 스펙: [specs](../specs/archive/2026-09-08-topping-edit-empty-subject-guard.md) Task 5(실기기 수동 검증 5갈래)는 저장소에서 확인할 수 없어 미체크로 남겼다. 신규 유닛 13건(`SubjectCoverageTest` 8 · `ToppingEditMaskTest` 5). |
| [2026-09-08-upload-image-downscale.md](archive/2026-09-08-upload-image-downscale.md) | **✅ 완료·develop 머지(2026-09-09, PR #473 `acbc4b457`, Task 1~3 전량 수행)** — **업로드 이미지 다운스케일·배경 JPEG 고정**(4 Task, 신규 4파일·수정 3파일). 서버로 나가는 이미지를 업로드 직전에 긴 변 상한까지 줄이고 배경은 JPEG 로 고정한다. 축소를 소스가 아니라 **`ImageUploadRepositoryImpl.upload` 한 자리**에서 하는 것이 이 계획의 골자다 — 누끼 파일은 `ToppingEditViewModel` 수동 편집의 입력이기도 해서 소스에서 줄이면 편집 품질이 함께 내려간다. **Task 1** 순수 판정 `planUploadImage`(`UploadImageScale.kt`) + JVM 유닛 8건 — 상한은 imageType 마다 다르고(`NUKKI` 1500 / `BACKGROUND` 2048, iOS `ToppingImageEncoder.maximumLongEdge`·`BackgroundImageLoader.maximumLongEdge` 와 맞춘 값) 치수·포맷 **둘 다 그대로일 때만** 통과시킨다. `inSampleSize` 는 목표보다 작아지지 않는 선까지만 2 의 거듭제곱으로 내린다(넘겨 줄이면 뒤에서 확대하게 된다). **Task 2** `UploadImagePreprocessor` 인터페이스 + 저장소 배선 — ⚠️ **생성자 인자가 늘어 기존 테스트 12개가 전부 컴파일 실패한다**, 원본을 통과시키는 대역을 `givenAllStepsSucceed()` 에 넣으면 기존 단언이 그대로 성립하고 확장자 판정이 전처리기로 옮겨간 `upload_unsupportedExtension_...` 만 다시 쓴다. 신규 5건은 임시 파일 정리(성공·PUT 실패 양쪽)와 통과 파일은 **안 지운다**는 것을 덮는다. **Task 3** `UploadImagePreprocessorImpl` — `inJustDecodeBounds` → `inSampleSize` → `createScaledBitmap` 순으로 원본을 통째로 안 올리고, JPEG 이면서 알파가 있으면 흰색 합성(안 하면 투명한 자리가 검게 앉는다), `Bitmap.CompressFormat` 매핑은 **`UploadImageFormat` 안에 두지 않는다**(그 열거형을 JVM 유닛이 읽는데 단위 테스트용 android.jar 가 실제 값을 보장하지 않는다). ⚠️ **이 Task 에는 자동 테스트가 없다** — Robolectric·계측 소스셋을 안 들이기로 확정했다. **Task 4** 실기기 수동 7갈래(큰 사진 누끼 / 상한 이하 누끼 **로그 없음** / JPEG 배경 / PNG 스크린샷 배경 / 투명 PNG 흰색 합성 / 재업로드 **2회차 무동작** / 토핑 최대 확대 화질) — 축소 전후 바이트가 이 변경의 유일한 효과 지표다. 커밋하지 않는다(사용자 미요청). 계획 대응 스펙 [upload-image-downscale](../specs/archive/2026-09-08-upload-image-downscale.md) 📌 **구현 뒤 재배치(2026-09-09)** — 판정이 `UploadImageScale.kt` 의 top-level 함수에서 `UploadImagePlan.of` 로 옮겨졌고, 전처리기는 데이터 접근이 아니라 비트맵 도구라 `utils/image/` 로 가면서 Hilt 바인딩도 신설 `UtilsModule` 로 갔다(계획 본문 경로는 작성 시점 기준) Task 4(실기기 수동 검증 7갈래)는 저장소에서 확인할 수 없어 미체크로 남겼다. 신규 유닛 12건(`UploadImagePlanTest` 8 · `ImageUploadRepositoryImplTest` 순증 4). 계획 이후 재배치 둘 — 각도 판독이 `File#readExifDegrees` 로 서고 전처리 모델 셋이 `data/model/image` 로 내려갔다. |
| [2026-09-07-topping-border-distance-field.md](archive/2026-09-07-topping-border-distance-field.md) | **✅ 완료·develop 머지(2026-09-08, PR #464 `23675cc1f`, 8 Task 전량 수행)** — **토핑 테두리 거리장 렌더링 통일**(8 Task, 신규 4파일·삭제 3파일·수정 8파일, 브랜치 `refactor/#337-topping-border-optimization`). 테두리를 그리는 코드 둘(편집 화면 거리장 / 나머지 넷의 여덟 방향 스탬프)을 거리장 하나로 합치고 **같은 거리장이 터치 판정까지 먹인다.** 스탬프는 **원판과의 민코프스키 합이 아니라 여덟 평행이동의 합집합**이라 인접 사본 사이가 굵기의 약 0.765배 벌어지고, 그보다 가는 부위(빨대)가 갈래로 갈라진다. **Task 1** `core:util:jvm` 에 `ToppingOutline`·`ToppingBorderBand`·`ToppingBorderTarget` 신설 + 유닛 9건. **Task 2** `core:util:android` 비트맵 변환. **Task 3** 편집 화면 이관 + 옛 거리장 삭제 + `MAX_BORDER_PADDING_DP` 를 `MAX_BORDER_WIDTH_DP` 에서 파생(OQ-P-337 ①). **Task 4** `YGToppingCutoutImage` 를 띠 1장 + 원본 1장으로. `outline` 에 기본값 `null` 을 줘 호출부를 안 건드리고 designsystem 안에서 닫는다. **Step 4가 실기기 게이트다** — `ALPHA_8` 의 `copyPixelsFromBuffer` 예외 여부와 `ColorFilter.tint` 동작을 본다(스튜디오 프리뷰 렌더러는 가속 캔버스가 아니다). 실패 시 `toBorderArgbBitmap` + 키에 색 추가 폴백. **Task 5** `core:ui` 에 거리판 캐시 **추가만** (옛 캐시 유지 — 깨진 Task 경계를 안 만든다). 키가 `"$retryKey|$model"` 이라 **OQ-P-356 이 함께 닫힌다.** **Task 6** canvas impl 전부를 한 덩이로 — 판정 교체 + `ToppingHitTestTest` 갱신 + **`ToppingAlphaMaskTest.kt` 삭제** + 두 화면 결선 + 옛 마스크·캐시·스탬프 상수 삭제. **Task 7** 토핑 하나짜리 화면 둘 결선. **Task 8** 실기기 육안 6항목 + 문서 갱신. **커밋하지 않는다**(사용자 미요청). ⚠️ **서브에이전트 검수 3회가 초판의 치명 결함 일곱을 잡아 구조를 다시 짰다.** ① **판 좌표 매핑이 실루엣을 여백까지 늘였다** — `buildBorder*` 가 판 전체를 목표 전체에 대응시켜 200px 토핑에 150px 테두리면 실루엣이 2.5배로 부풀었다. `ToppingBorderTarget` 이 알맹이 자리를 따로 받아 고친다. ② **판 밖 좌표 클램프**로 실루엣이 판 변에 닿은 토핑에서 여백이 통째로 거리 0 이 돼 칠해지고 판정도 모서리마다 부풀었다 — 벗어난 만큼을 거리에 더한다. ③ **양자화한 상자를 기하로 썼다**(스펙은 캐시 키라고 적었다) — 띠가 최대 31px 크게, 중심이 15.5px 어긋났다. 양자화를 버리고 실측 크기 + 짧은 지연으로 간다. ④ **컴파일이 깨진 모듈에서 유닛을 돌리라고 했다** — `--tests` 는 실행 필터일 뿐 `testDebugUnitTest` 가 main 컴파일을 선행으로 잡는다. Task 5·6·7 을 재배치해 **깨지는 Task 경계를 없앴다.** ⑤ **`ToppingAlphaMaskTest.kt`(@Test 8건) 삭제 누락** → test 컴파일 영구 실패. ⑥ **`kotlin.math.min` import 누락** → Task 2 부터 연쇄 차단. ⑦ **얇은 막대 회귀 테스트가 스탬프에서도 통과했다**(판 끝까지 이어진 막대는 180° 스탬프가 열 전체를 덮는다) → **씨앗 한 칸 + 원판 채움** 검사로 교체. 그 밖에 **배경 편집 인셋 우회 제거를 철회**(띠 판은 정의상 상자 밖으로 나가 `alpha < 1` 오프스크린 버퍼가 여전히 자른다), 겹 색 테스트 기대값을 `EDGE_FEATHER_PX` 반영해 정정, `@PublishedApi` 불필요 판정(`private inline` 은 private 멤버를 부를 수 있다), 낡는 KDoc 넷과 BGEdit `isMine` 필터 소실을 지시에 명시. 대응 스펙 [topping-border-distance-field](../specs/archive/2026-09-07-topping-border-distance-field.md) · ADR-0030 | **Task 1~7 완료(2026-09-07)** — SDD, 브랜치 `refactor/#337-topping-border-optimization`, 커밋 9개 `2285d09da..0fdb1a74`(`28ecd9e3c`·`040cfa7f4`·`35240b2ba`·`1d0a0ba99`·`c29875d18`·`5c949614f`·`e6b9cae09`·`9c4a45ab9`·`0fdb1a740`), `:app:assembleDebug` 통과. **Task 8까지 완료(2026-09-07)** — 실기기 육안 6항목이 통과했고 `ALPHA_8` + `ColorFilter.tint` 게이트도 통과해 폴백을 안 쓴다. `OUTLINE_LONG_SIDE` 는 256을 유지했다. ADR-0030 `status`를 `accepted`, 스펙 `status`를 `implemented` 로 올렸다. **develop 머지(2026-09-08, PR #464 `23675cc1f`) 후 스펙·계획을 아카이브로 옮겼다.** **계획과 갈린 자리 둘.** ① **`ALPHA_8` 실기기 게이트를 Task 4에서 Task 8로 옮겼다** — 게이트 결과가 `YGToppingCutoutImage` 내부 비트맵 포맷 함수 하나만 바꾸고 공개 인터페이스를 안 건드리므로, Task 6·7이 그 판정을 기다릴 이유가 없었다. ② **Task 1·Task 4에서 수정 라운드가 각 1회 있었다** — Task 1(`040cfa7f4`)은 배율 ≠ 1 테스트 누락과 `buildBorderPixels`의 굵기 0 가드를 더했고, Task 4(`5c949614f`)는 `offset`을 판에 굳혀 크기 변화 중 띠가 알맹이에서 떨어져 나가던 것을 자리를 다시 재는 방식으로 고쳤다. **뒤의 것은 계획이 틀렸던 자리다** — 계획은 판을 만들 때의 자리를 그리는 시점까지 그대로 쓸 수 있다고 가정했다. ③ **Task 8 육안 확인이 깜빡임을 새로 드러내 수정 라운드가 한 번 더 붙었다** — 띠 판이 컴포저블 `remember` 수명이라 화면 전환·Spotlight 전환마다 사라지고 다시 만드는 동안 테두리가 없었다. `ToppingBorderPlateCache.kt`(`core:designsystem`, LRU 32칸)를 신설해 판을 컴포지션 밖에 남기고, `peekToppingOutline` 으로 거리판 맵을 첫 프레임부터 채운다. **여기도 스펙이 틀렸던 자리다** — 스펙은 띠를 컴포지션 수명으로 둬도 된다고 적었다. 그 수정을 서브에이전트가 리뷰해 결함 넷을 더 잡았고 같은 라운드에서 고쳤다 — ⓐ 크기가 다른 판을 재사용하면 굵기가 그 배율만큼 틀어져(굵기가 판에 구워져 있다) 1.25배 초과는 안 그리도록 `fitsSubject` 를 뒀고, ⓑ `remember` 키에서 굵기·비율을 빼 그 둘이 바뀔 때 화면이 비지 않게 했고, ⓒ 거리판 맵의 `remember` 키를 도로 없애 `retryKey` 재시도 중 이미 받아 둔 판이 사라지던 것을 막았고, ⓓ 배치 화면은 초안이 비동기라 `produceState` 의 `initialValue` 가 언제나 `null` 로 굳어 동기 조회가 죽은 코드였던 것을 `remember` + `LaunchedEffect` 로 바꿨다. 거리판 인스턴스 강참조·초기화 함수 도달 불가·모델 문자열 불일치(OQ-P-380)는 문서에만 남겼다. **as-built: 신규 8파일·삭제 3파일·수정 16파일**(계획은 신규 4 — 값 타입 셋이 `core:util:jvm` 의 `model/` 로 갈라졌고 `ToppingBorderPlateCache` 가 새로 생겼다). 지연 재생성(`BORDER_REBUILD_DELAY_MS`)과 그것을 조정하는 Task 8 Step 3은 폐기됐다. |
| [2026-09-07-canvas-save-preview-capture-holder.md](archive/2026-09-07-canvas-save-preview-capture-holder.md) | **✅ 완료·develop 머지(2026-09-07, PR #463 `2285d09da`)** — **캔버스 저장 미리보기 캡처 전달**(5 Task, as-built **신규 3파일·수정 4파일** + 문서 2건 — 계획은 신규 2였고 압축 실패 유닛이 하나 더 붙었다). 저장 미리보기에 들어가도 이미지가 곧바로 안 뜨는 것을 고친다. **Task 1** `CanvasCaptureHolder`(`put`/`peek`/`clear`, 필드 `@Volatile`) + JVM 유닛 4건. ⚠️ **읽으면서 비우지 않는 것이 핵심이다** — 미리보기는 화면에서 사라지지 않고도 다시 컴포즈되고(Activity 재생성·백스택 하강 후 복귀), 비우는 홀더였다면 그때마다 그림을 잃는다. 🔁 **as-built 정정: `clear`가 테스트 전용이 아니다** — 코드리뷰가 마지막 캡처 한 장이 다음 캡처까지 사는 것을 지적해, 미리보기 `onDispose`에서 자기 `NavKey`가 `Navigator.backStack`에 **없을 때만** 비우는 조건이 들어왔다(비파괴 성질은 그대로). **Task 2** `writeToCanvasCaptureCache`가 `compress`의 `Boolean`을 `check`로 본다 — 지금은 버려서 잘린 파일이 성공으로 나가고, 홀더가 들어가면 그 실패가 확정 이후로 밀린다. 🔁 **as-built 정정: 유닛 2건이 붙었다** — `Context`를 `mockk`로 세우고 `cacheDir`에 `TemporaryFolder`를 물리면 실제 파일 IO가 도는 채로 `compress`만 스텁할 수 있다(계획의 "감싸기 어렵다"가 틀렸다). **Task 3** `CanvasImageSaveScreen`이 `bitmap: ImageBitmap?` + `fallbackImagePath` 둘로 갈리고 프레임 안쪽만 분기한다(폭·종횡비·테두리·`ContentScale`·`contentDescription` 공유). `CanvasImageSaveRoute`가 `remember { peek()?.asImageBitmap() }`. ⚠️ **이 시점의 앱은 동작이 지금과 같고 `bitmap != null` 분기는 죽은 코드다** — Task 4의 수동 확인이 그걸 처음 그린다. **Task 4** `CanvasMainRoute`가 `goTo` 직전에 `put(bitmap)`. ⚠️ Task 3 없이 이것만 채택하면 아무도 읽지 않는 홀더가 비트맵을 붙든다. 기기 수동 5갈래(오늘·지난 캔버스 즉시 표시 / 날짜 라벨 / 확정 후 갤러리 / 취소 후 재진입 / **다크모드 토글 시 그림 유지**). **Task 5**(문서 저장소 작업) `navigation-flow.md` 왕복 절 갱신 + **백스택이 저장되지 않는다**는 사실 신설, OQ-P-364 ① 전제 정정, OQ-P-365 ②에 홀더 추가, 저장 아이콘 연타 신규 미결. **`feature/groups/canvas/api` 무변경** · **커밋하지 않는다**(사용자 미요청). ⚠️ 자동 검증은 홀더 계약만 본다 — `put`/`peek` 배선을 지워도 유닛 4건은 통과하고 앱은 조용히 느린 경로로 돌아간다. 대응 스펙 [canvas-save-preview-capture-holder](../specs/archive/2026-09-07-canvas-save-preview-capture-holder.md) |
| [2026-09-05-c103-error-use-original.md](archive/2026-09-05-c103-error-use-original.md) | **✅ 완료·develop 머지(2026-09-06, PR #457 `5f860cb9c`)** — **C-103-Error 실패 화면 통합과 「편집 없이 사용」**(4 Task, 신규 0파일·수정 8파일). 디자인 확정본에 맞춰 실패 문구를 한 벌로 합치고 원본을 그대로 토핑 재료로 쓰는 버튼을 더한다. **Task 1** `SegmentationErrorKind` 삭제 · `errorKind` → `isError` · 모듈 실패 문구 2건 삭제 · 화면이 문구를 스스로 읽게 해 제목·설명 파라미터 제거(기존 테스트 단언 5곳 전환, `init_moduleNotReady_marksErrorLikeAnyOtherFailure`는 이름을 바꿔 **남긴다** — 통합 결정을 잠그는 자리다). **Task 2** 디코드 실패를 `SegmentationEffect.GoBack` → `navigator.onBack()` 으로 보낸다(토스트 없음) — 그 결과 실패 화면은 원본이 반드시 사는 상태에서만 떠서 다음 Task의 버튼에 비활성 분기가 필요 없다. ⚠️ **뒤로 가면 사진 확인 화면이 아니라 카메라·갤러리 피커가 나온다** — `PictureConfirmRoute` 가 `goToAndPopCurrent` 로 자기 자리를 치환하기 때문이다(서브에이전트 검수가 잡은 치명 오류, 스펙·계획 서술 정정). **Task 3** `UseOriginal` 인텐트와 `useOriginal()` — `saveBitmap`(실행 뒤 리뷰 반영으로 `saveEditedImage` 에서 개명) **1회**로 떨군 경로를 `record` 와 `GoToConfirm` 두 자리에 싣는다. ⚠️ `AndroidBitmap` 생성자가 모듈 내부라 `state.originBitmap` 을 다시 감쌀 수 없어 **디코드가 준 `BitmapWrapper` 를 필드로 붙든다**. `persistSubject` 로 보내지 않는 이유는 원본 크기 비트맵을 한 벌 더 만들어 메모리가 두 배로 뛰기 때문이다. 신규 테스트 3건. **Task 4** `YGButtonType.Medium.Secondary` 「편집 없이 사용」 버튼 배치와 Route 결선 — 폭은 디자인 실측값 `161.5.dp` 를 감싸는 Column 에 한 번만 주고 버튼은 `fillMaxWidth` 로 채운다. 세로 간격은 아이콘→문구 `gap3`(8dp) · 제목→설명 `gap1`(2dp) · 설명→버튼 `gap7`(24dp) · 버튼→버튼 `gap3` 이고, ⚠️ **바깥 Column 의 균일 배치를 걷고 명시적 Spacer 로 둔다**(남기면 24dp 스페이서 위아래로 8dp 가 더 붙어 32dp 가 된다). 커밋하지 않는다(사용자 미요청). 계획 대응 스펙 [c103-error-use-original](../specs/archive/2026-09-05-c103-error-use-original.md) · 이슈 #348. 4 Task 전부 fix round 없이 리뷰 통과, 최종 브랜치 리뷰의 Important 1건(단계 간 주석 드리프트)만 고쳤다. Minor 5건 park |
| [2026-09-02-segmentation-module-install.md](archive/2026-09-02-segmentation-module-install.md) | **✅ 구현 완료·develop 머지(2026-09-03, PR #438 `24679f8c`, 7 Task 전량 수행·신규 테스트 10건)** — **세그멘테이션 optional module 설치 대기·실패 처리**(7 Task, 신규 5파일·수정 8파일, 브랜치 `feature/ml-kit-model-load-exception`). Galaxy Z Flip 3에서 사진 편집이 매번 실패한 건의 후속이다 — `installModules` 의 Task 성공은 **요청 접수**일 뿐이라 모듈 없는 기기의 첫 사용자가 예외 없이 실패한다. **Task 1** `SegmentationModuleInstaller` + `ModuleInstallGateway` 이음매(TDD 7건, 가짜 게이트웨이가 신호 순서를 정한다) — `Mutex` + `CompletableDeferred` 로 진행 중 설치를 공유하고, ⚠️ **끝난 대기는 재사용하지 않는다**(안 걷으면 한 번 실패한 뒤 재시도가 영영 옛 실패만 돌려준다). 콜백이 정지 함수가 아니라 락을 못 잡으므로 걷는 일도 판정도 `ensureInstalled` 가 한다. **Task 2** GMS 구현 + `@Binds` — `InstallStatusListener` 로 종료를 받고 종료 신호에서 리스너·세그멘터를 닫는다. 모듈 식별자는 세그멘터 옵션과 무관해 판정용 세그멘터를 따로 연다. **단위 테스트 없음**(JVM 재현 불가). **Task 3** 리포지토리가 설치기를 쓰고 조사용 30초 폴링을 걷는다. ⚠️ 새 타입은 **public 이다** — public `RepositoryModule` 이 `internal` 타입을 `@Binds` 로 받으면 `EXPOSED_PARAMETER_TYPE` 이 나고, `:data` 에 `internal` 모듈 선례가 없다(프리플라이트 ruling). **Task 4** `PrepareSegmentationModuleUseCase`. **Task 5** `isError` → `errorKind`(`SubjectNotFound`/`ModuleNotReady`) + `Retry` 인텐트 — ⚠️ 추출 함수가 진입부에서 실패 표시를 되돌려야 하고(안 하면 재시도가 성공해도 에러 화면이 남는다) 진입과 재시도가 **같은 키**를 쓴다. 기존 테스트 3건의 `isError` 단언이 함께 바뀐다. **Task 6** 원인별 문구 + `YGButton` 재시도(디자인 검토 대기 시안, 새 컴포넌트 없음). **Task 7** 사전 설치를 **사진 확인 화면**에 건다 — 촬영·갤러리 두 경로의 유일한 합류점이라 카메라에만 걸면 갤러리 사용자가 안 탄다(검수가 잡았다). `feature/camera/impl` 에 테스트 소스셋이 없어 **무테스트**이고 검증은 실기기 두 경로 확인이다. 스펙: [specs](../specs/archive/2026-09-02-segmentation-module-install.md). ⚠️ 이 계획은 **원인 ②(그 기기의 GMS 가 `INTERNAL_ERROR` 로 설치를 못 잇는 것)를 고치지 않는다** — 앱 범위 밖이고 OQ-P-344 가 추적한다 |
| [2026-08-31-topping-batch-update-and-past-canvas-status.md](archive/2026-08-31-topping-batch-update-and-past-canvas-status.md) | **✅ 완료·develop 머지(2026-09-01, PR #428 `e870fb87` — 트리 = 브랜치 팁 `a8d2efd5`)** — **토핑 일괄 수정 전환 및 과거 캔버스 status 수용**(5 Task, 신규 4파일·삭제 2파일·수정 12파일, 베이스는 `develop`, 브랜치 `feature/#427-sync-backend-api-260831`). 서버 `de3a99a` delta 둘을 앱에 반영한다. **Task 1**이 과거 목록 `status`를 DTO·`PastCanvasVO`까지 올린다 — 기존 `toCanvasStatus()` 재사용이고 ⚠️ **생성자 인자가 늘어 기존 테스트 넷**(`CanvasMainViewModelTest`·`GetParfaitHistoriesUseCaseTest`·`GetParfaitYearsUseCaseTest`·`GetTodayParfaitFlowUseCaseTest`)**을 함께 고쳐야 컴파일된다**. **달력 점 기준은 안 옮긴다** — 위키 [[C-201-캘린더-정책-v0.1]]이 토핑 개수 기준이라 서버 `EMPTY`로 옮기면 오늘의 빈 캔버스에 점이 찍힌다. **Task 2~4는 한 덩이다** — Task 2가 단건 경로를 걷는 순간 `:domain`·`:feature`가 깨지고 Task 4 끝에서 다시 초록이 된다. Task 2가 wire 계약(`UpdateParfaitImagesRequest`·`UpdateParfaitImageItemRequest`·`UpdateParfaitImagesResponse`, 원소는 **단건 응답 DTO 재사용** — 서버가 그렇게 한다)과 `ToppingTransformUpdate`(비널 id + 널 허용 5축)를 만들고 `updateToppings`로 갈아탄다. Task 3이 `ToppingRepository.updateAll`·`UpdateToppingsUseCase`로 교체하고 **빈 목록은 요청을 안 보낸다**. Task 4가 확인 버튼을 **축 단위**로 다시 짠다 — 변형 일괄 1회 뒤 테두리 병렬 N회이고, ⚠️ **일괄 실패 시 변형을 보낸 토핑 전부가 dirty로 남는다**(서버가 전부 롤백하고 실패 항목을 응답이 안 알려준다). `positionZ`는 여전히 안 보낸다. 에러 문구는 안 바뀐다 — `toCanvasBGEditError`가 403·409를 다 `unknown`으로 접는다. **Task 5**가 계약 문서 Android 열을 뒤집고(단건 → 표면 없음, 일괄 → 구현됨) **낡은 서술 둘을 정정한다** — `api/parfait-image.md`의 PR #336·#335 항목이 위치 PATCH·삭제 실패를 "로그 한 줄로 접힌다"고 적는데 현재 develop은 실패 id를 dirty에 남기고 토스트를 낸다. 표면 셈은 **27/28 유지**(공백의 정체만 일괄에서 단건으로 바뀐다), `http/` 커버는 **25/28로 벌어진 채 둔다**(실서버 검증 라운드의 일). 스펙: [specs](../specs/archive/2026-08-31-topping-batch-update-and-past-canvas-status.md). 5 Task 전부 계획대로 develop에 들어갔다. 계획과 갈린 것은 검증 명령 표기 정정(`:domain:testDebugUnitTest`→`:domain:test`, `:domain:compileDebugKotlin`→`:domain:compileKotlin` — 브리프가 잘못 준 태스크 이름)과 ktlint 120자 재포맷뿐, 설계 이탈은 없다. 유닛 996 → **1012건** |
| [2026-08-27-canvas-bgedit-background-tab-toppings.md](archive/2026-08-27-canvas-bgedit-background-tab-toppings.md) | **✅ 완료·develop 머지(2026-08-31, PR #404 `2c7bb31b` — 스택 3단 한 머지, 트리 = 브랜치 팁 `6bd21fb7`)** — **PR1 — 배경 편집 배경 탭 토핑 렌더링**(4 Task, 파일 1개, ViewModel 무변경). `CanvasBGEditScreen`이 토핑 레이어를 `selectedTab == TOPPING` 게이트 하나로 감싸 배경 탭에서 토핑이 아예 안 보이던 것을 고친다. 게이트를 걷고 **그리기와 상호작용을 분리** — 배경 탭은 `graphicsLayer` 의 `alpha` 로 반투명하게 그리기만 하고 딤·`toppingTapInput`/`toppingDragInput`·`ToppingCornerButtons`·`semantics{onClick}` 을 붙이지 않으며, `rememberToppingAlphaMasks` 를 **아예 호출하지 않는다**(마스크 준비는 비트맵 디코딩을 동반한다). **검수가 검증 게이트 둘을 뒤집었다** — ⚠️ `lintDebug` 는 **기준선에서 이미 실패**한다(무관한 `CanvasToppingPlaceRoute.kt` 의 `LocalContextGetResourceValueCall` 4건). 초판의 "BUILD SUCCESSFUL, 새 경고는 고친다"를 따르면 실행자가 **무관한 파일을 건드려** "한 파일만 바뀐다"를 깬다 → 판정을 "`CanvasBGEditScreen.kt` 에 새 지적이 없을 것"으로 바꿨다. 그리고 **CI가 게이트하는 것은 `ktlintCheck`·`test` 이고 `lintDebug` 를 도는 CI는 없다** → 매 Task에 `ktlintMainSourceSetCheck` 를 넣었다. 그 밖에 초판의 검증 단계가 **미기술 편집 둘을 지시**하던 것을 코드 단계로 승격, `ToppingCornerButtons` 가 `painter` 를 읽는다는 **사실 오류** 정정(`entry.topping`·`entry.target` 만 읽는다), ktlint 함수 시그니처 규칙상 1-파라미터 멀티라인 위반 정정, 이 파일에 최상위 상수가 0건이라 지목한 앵커가 없던 것 정정, 기존 Preview 가 `PreviewCanvasBGEditScreen` 하나여서 **개명 + 복제**로 바꿨다(새로 둘을 더하면 중복 셋). Preview 토핑에 **실제 drawable + `borderLayers`** 를 준다(빈 모델이면 painter 가 `Success` 에 못 가 불투명도 판정 불가). 검증 못 한 것: 로딩·실패 상태(`PreviewBox` 가 항상 성공 비트맵을 주입한다). 스펙: [specs](../specs/archive/2026-08-27-canvas-today-ssot-polling.md) 「PR1」 |
| [2026-08-27-canvas-today-ssot.md](archive/2026-08-27-canvas-today-ssot.md) | **✅ 완료·develop 머지(2026-08-31, PR #404 `2c7bb31b` — 스택 3단 한 머지, 트리 = 브랜치 팁 `6bd21fb7`)** — **PR2 — 오늘 캔버스 인메모리 SSoT**(8 Task, PR1 위 스택). 세 화면이 각자 부르던 `GetTodayParfaitUseCase` 를 `:data` 인메모리 저장소 한 벌로 모은다. `CanvasLocalDataSource`(`@Singleton` + `MutableStateFlow<Map<GroupId, CanvasVO>>`) 신설, `ParfaitRepository.getTodayCanvas` 를 **구독·오늘갱신·상세갱신·날짜peek·정리 다섯**으로 분리. `viewedCanvas`/`todayCanvas` 이중화를 `pastCanvas` + `displayedCanvas` 파생으로 정리. **검수가 치명 3건을 잡았다** — ① `ParfaitRepository.getTodayCanvas` 를 지우면 **같은 모듈의 `GetTodayParfaitUseCase.kt` 가 그것을 부르므로 `:domain` 이 통째로 깨진다** → 인터페이스 변경과 UseCase 이관을 **한 커밋(Task 2)**으로 합쳤다. ② `CanvasMainViewModelTest` 는 조회 목킹으로 **상태를 채우는 테스트가 14곳**·`viewedCanvas` 단언 3건이라 "세 건을 더한다"로는 안 되고 **일괄 개조**가 선행돼야 한다. ③ `syncToday()` 의 `todayCanvas = null` 제거는 **하루 경계 안전장치를 잃는다** — 스펙 자신이 "날짜 필터는 재방출이 없으면 평가되지 않는다"고 경고했는데 계획이 그 필터에 맡겼다. 지울 것은 `viewedCanvas` 대입 두 줄뿐. 그 밖에 **`RefreshTodayParfaitUseCase` 가 캐시를 `todayCanvas().first()` 로 읽으면 PR3에서 폴러 계수를 튕겨 부작용 있는 오늘 조회가 덤으로 나가므로** `cachedTodayCanvasDate` peek 표면을 신설(스펙 대비 표면 +1), `:domain` 은 **순수 JVM 이라 `compileDebugKotlin` 이 없고 `testDebugUnitTest` 가 `:domain` 을 조용히 건너뛴다** → `./gradlew test`, `displayedCanvas` 에 `pastCanvas ?: todayCanvas` 폴백(없으면 지난 날 첫 진입에 빈 캔버스가 깜빡인다), `LogoutUseCaseTest` 에 `useCase()` 헬퍼가 없다는 정정, 저장소에 **백틱 테스트명이 0건**이라 카멜케이스로 통일, `GetParfaitDetailUseCase` KDoc 깨진 링크 수정. 스펙: [specs](../specs/archive/2026-08-27-canvas-today-ssot-polling.md) 「PR2」 · ADR: [0029](../../adr/0029-canvas-today-ssot-polling.md) ✅ **PR #407 덮개와의 충돌은 리베이스에서 풀렸고 그대로 develop 에 들어왔다** — 덮개는 계획의 코드블록이 아니라 구독 안에서 파생한다(OQ-P-326 ⑥ 해소) |
| [2026-08-27-canvas-polling.md](archive/2026-08-27-canvas-polling.md) | **✅ 완료·develop 머지(2026-08-31, PR #404 `2c7bb31b` — 스택 3단 한 머지, 트리 = 브랜치 팁 `6bd21fb7`)** — **PR3 — 주기 폴링·병합 규칙·하루 경계**(9 Task, PR2 위 스택, **착수 전 사용자 확인 게이트 있음**). `BaseViewModel.launchWhileSubscribed`(구독 수 기반) 신설 + `CanvasPoller`(`@Singleton`) + 하루 경계 티커 + dirty/툼스톤 병합 + `positionZ` 재계산. **검수 넷이 치명 5건을 잡았고 일부는 실제 컴파일로 확인됐다** — ① **`launchWhileSubscribed` 의 `source` 안에서 `state` 를 수집하면 자기 고착**이 일어나 계수가 0으로 안 내려가고 **폴링이 영영 안 멎는다**(ADR-0029가 「위험·방어」에 적어 둔 실패 그 자체) → 지난 날 게이트를 `state` 가 아닌 별도 `MutableStateFlow` 로 빼고 KDoc 경고 + 회귀 테스트 추가. ② **`Flow.onSubscription` 은 존재하지 않는다**(`SharedFlow`/`StateFlow` 확장) → `onStart`. ③ **`refreshNow` 가 요청을 두 번 보낸다**(`refresh` 뒤 `startPolling` 첫 줄이 또 `refresh`) → 즉시 조회와 타이머 분리. ④ **`async` 를 리시버 없는 `suspend fun` 에서 못 부른다**(1.11.0 오류 수준) → `coroutineScope`. ⑤ **`advanceUntilIdle()` 은 `backgroundScope` 작업을 안 돌린다**(foreground 기준) → 폴러 테스트를 `runCurrent()` 로. 그 밖에 **스펙이 요구한 in-flight 스킵 가드가 통째로 빠져 있던 것**을 넣고, `Mutex` → `synchronized` 로 바꿔 `onCompletion` 취소 경합과 `stopAll` 스레드 문제를 함께 닫고(폴러의 `acquire`/`release` 가 non-suspend가 된다), **세대 카운터**로 정리 뒤 늦게 온 응답이 캐시를 되살리지 못하게 하고, **`TokenAuthenticator` 폴러 중단 Task 가 통째로 없던 것**을 Task 4에 넣고(`:data` 라 `clearTodayCanvas()` 를 안 지난다), **세 화면의 진입 갱신을 전부 제거**(폴러의 구독 시작 즉시 조회가 대신 — 안 지우면 진입마다 부작용 있는 오늘 조회가 나간다), **배경 저장 뒤 강제 갱신 누락** 보완(이펙트 앞에서 기다린다 — 뒤에 두면 되감기며 취소된다), 토핑 추가는 반대로 **기다리면 안 되므로** 폴러 스코프 비동기 표면 신설, `AddToppingUseCase` 5인자·`UpdateToppingUseCase` 8인자 매처 정정, 삭제 스코프 변수명 `selectedId` 정정, `confirmedToppings` 가 UiState 가 아니라 ViewModel `private var` 라는 정정, `@ApplicationScope` 를 관례대로 `data/model/qualifier/` 로, `architecture/state-management.md` 삽입 위치 정정. **as-built 차이 셋**(폴러가 티커 대신 캐시 날짜로 판정 · 저장소 표면 7개 · `confirmedToppings` 위치)은 Task 3·9에서 스펙에 반영. 스펙: [specs](../specs/archive/2026-08-27-canvas-today-ssot-polling.md) 「PR3」 · ADR: [0029](../../adr/0029-canvas-today-ssot-polling.md) ✅ **덮개 충돌의 답이 계획보다 한 겹 두껍게 들어왔다** — 구독 안 파생만으로는 갱신 실패에서 덮개가 안 풀려 폴러에 `refreshFailures` 축을 더하고 `ShowTodayCanvasError` 를 되살렸다(OQ-P-326 ⑥ 해소) |
| [2026-08-27-alpha-kernel-suspend-cancellation.md](archive/2026-08-27-alpha-kernel-suspend-cancellation.md) | **✅ 완료·develop 머지(2026-08-27, PR #363 `4da18230`)** — **알파 커널 취소 확인 전환**(6 Task, **브랜치 3개**, 베이스는 세그멘테이션 스택의 커널 PR) — 취소 확인 수단을 `checkCancelled` 콜백에서 `suspend` + `currentCoroutineContext().job` 호이스팅 + `job.ensureActive()` 로 옮긴다. 프로덕션 동작·확인 지점·빈도 불변. 리뷰 원안 `yield()` 는 벤치로 배제. ⚠️ **`suspend` 가 CPU 루프의 취소를 자동으로 만들지 않는다** — 확인을 넣는 부담은 콜백과 같고 사라지는 건 배관뿐이다. `.job` 을 쓰는 이유는 `get(Job)?.ensureActive()` 계열이 no-Job 을 **조용히 통과**시켜 확인이 통째로 no-op 이 되고도 테스트가 초록으로 남기 때문. 전염 방향이 규칙 — 잎(`ceilDiv`·`bilinear`)은 그대로, **호출 사슬 위쪽은 강제**(`refineWithin`·`maskSubjectAlpha`·`Impl` private 넷). Task 2 를 쪼갤 수 없다(`downscaleMask` 가 `suspend` 가 되는 순간 `postProcessAlpha` 도 그래야 컴파일된다). **계획 검수 2갈래가 치명 넷을 잡았다** — ① **rebase 절차가 근본적으로 틀렸다**(결선은 충돌이 아예 안 나 `rebase --continue` 가 `fatal` 로 죽고, 정련은 중간에 멈춘 뒤 `--continue` 하면 뒤 커밋 둘이 재생되며 `checkCancelled` 를 **되살린다** → **rebase 를 먼저 끝까지 돌리고 그 위에 새 커밋 하나로 얹는 방식**으로 재설계) ② 정련 커밋이 `AlphaPostProcessorTest` 에 `@Test` 셋을 추가하는데 Task 5 파일 목록에 없어 컴파일이 깨진다 ③ 하니스 중단 테스트가 `withContext` 를 써서 **`plus` 의 예외를 잡고 있었다**(완료 단언 미검증, 게다가 `plus` 를 고쳐도 약 10% flaky) → `suspendCoroutine<Unit> { }` 로 교체, `plus` 는 `super.plus(context)` 로 정정(주석의 「CombinedContext 가 internal 이라 흉내 낼 수 없다」는 **거짓**이었다) ④ **위임 전용 함수의 취소 테스트는 개별 회귀를 못 잡는다**(한 단계가 확인을 잃으면 다음 단계가 「첫 확인」이 되어 초록) — 정련 브랜치엔 카운팅 그물이 아예 없어 `refineAlpha` 카운팅 테스트와 뮤테이션 검증 스텝을 신설. 그 밖에 앵커 유일성 위반(`var changed = false` 가 `AlphaPostProcessor.kt` 에 둘), ktlint 게이트 누락, `assertFailsWith` import 누락, `SegmentationMask` 는 위치 인자, `segmentForeground` 는 주석 포함 3줄. **실행 결과**: Task 1~5 를 코드로 실행하고 Task 6(이 문서 작업)으로 마감했다. 브랜치 셋 다 rebase 완료 — 커널 `feature/segmentation-alpha-kernel`(커밋 `a0dc6c9`·`c73a637`·`ac57e4f`) · 결선 `feature/segmentation-postprocess-wiring`(rebase 후 `f72e4c2`) · 정련 `feature/segmentation-alpha-refinement`(rebase 후 `c3bbb56`·`1626a5a`), **force push 는 아직 안 함**(백업 태그 `backup/segmentation-*-pre-suspend` 셋이 rebase 이전을 가리킨다). 검증: `:data:testDebugUnitTest` 396건 통과·`checkCancelled` 잔존 0건·금지 패턴(`job?.ensureActive`·`CoroutineContext.ensureActive`) 0건, 뮤테이션 검증 커널 3지점·정련 5지점 전부 대응 테스트에 잡힘, rebase 무결성(원래 커밋 제목이 백업 태그와 전부 동일 — 결선 6개·정련 8개). **as-built 로 스펙과 갈린 자리 셋** — ① 「자체 확인/전염」 표에서 `applyAreaOpening`·`postProcessAlpha`·`downscaleLuminance`·`downscaleAlpha` 넷은 확인 루프가 없어 「전염」으로 재분류 ② `erodeEdge` 의 `Job` 호이스팅이 **정련 브랜치에서만** rebase 충돌 해소 부작용으로 조기 반환 가드 뒤로 밀려 있었다(커널·결선 두 브랜치는 원래대로 가드 앞이었다) — "실익이 없다"만 근거 삼아 그대로 두면 세 브랜치 중 하나만 달라 정련 PR 의 diff 에 이 라운드와 무관한 한 줄이 섞이므로, 가드 앞으로 되돌려 스택 셋을 바이트 단위로 맞췄다(`TJYG-Android` 커밋 `c95c55b1`) ③ 확인 루프가 없는 함수(`applyAreaOpening`·`postProcessAlpha`·`maskSubjectAlpha`·`refineAlpha`)의 개별 취소 테스트는 서브트리가 확인을 전부 잃었을 때만 실패해 개별 단계 회귀를 못 잡는다는 사실을 스펙 테스트 절에 남겼다 — 그 자리를 잡는 것은 카운팅 테스트 둘(`postProcessAlpha_countingChecks_isCalledPastTheDownscaleStage`·`refineAlpha_countingChecks_visitsEveryStage`)뿐이다. 계획 Task 4 의 `catch (e: CancellationException)` 위치 서술 오류(`buildCandidatePair` 안이 아니라 호출부 `try`)도 계획 본문에서 정정했다. **남은 수동 작업**(계획 밖, 사용자와 함께 처리) — force push 3회(리모트로 나가는 작업이라 승인 후 실행)와 리뷰 스레드 회신. **최종 브랜치 리뷰가 병합 전 결함 둘을 잡았다** — ① 정련 브랜치만 rebase 훙크로 `postProcessAlpha` KDoc 의 취소 문단 두 줄이 빠져 있었다(커널·결선엔 있었다) — 커널 브랜치 형태로 복원해 세 브랜치를 다시 대조했다(`TJYG-Android` 커밋 `3545be7b`) ② 스펙이 열거한 커널별 취소 테스트 열둘 중 `refineAlpha` 하나가 실제로는 없었다(카운팅 테스트만 있었다) — 같은 파일의 다른 취소 테스트와 같은 모양으로 추가해 397건으로 늘었다(같은 커밋). 최종 리뷰가 남긴 선재 사실 하나(`applyAreaOpening`·`downscale`·`guidedCoefficients` 에 첫 확인 전 무확인 구간이 남아 있다는 것)는 스펙 「미결」에 기록만 하고 고치지 않았다 — 확인 지점·빈도 변경은 이 스펙의 범위 밖이다 |
| [2026-08-25-segmentation-alpha-refinement.md](archive/2026-08-25-segmentation-alpha-refinement.md) | **✅ 완료·develop 머지(2026-08-27, PR #363 `4da18230`)** — **세그멘테이션 알파 경계 정련**(7 Task, **1 PR**, **검수 2회 후 재작성**, 베이스는 앞 라운드 3단계 브랜치 `feature/segmentation-postprocess-wiring` 위) — 원본 휘도를 안내자로 삼는 **가이드 필터**로 알파 경계를 실제 물체 경계에 붙이고 하드 매트의 계단을 소프트 매트로 바꾼다. **계수는 축소판, 적용만 원본 해상도**(Fast Guided Filter) — 전 과정을 원본에서 돌면 12MP 후보 판에서 중간 실수 배열 여섯이 수백 MB다. 정련은 `keep 적용`과 `침식` 사이에 들어가고 **침식보다 앞**이어야 한다(경계를 옮긴 뒤에 깎아야 그 한 겹이 실제 경계 바로 바깥이 된다). 측정이 두 번으로 갈리고(정련이 훑을 영역을 1차가 정한다) **bounds 한정은 정련에만** 적용한다(침식은 알파 0을 건너뛰어 여백 비용이 낮다). 커널은 `Bitmap` 을 계속 모르므로 안내자를 **사각형 공급자**로 받아 폴백 경로가 앞 라운드에서 없앤 원본 크기 할당을 되살리지 않게 한다. ⚠️ **적분 영상을 쓰지 않는다** — 12MP에서 누적값이 `Float` 정밀도를 넘어 창 차분이 무너진다. 박스 평균은 행·열 슬라이딩 합 2패스다. 테스트는 변이로 죽는 픽스처를 세운다 — 계수는 상수 안내자에서 `a=0,b=mean(p)`·입력=안내자에서 `a=1,b=0`으로 퇴화하고, 정련은 **같은 마스크를 색 안내자와 균일 안내자로 각각 돌려 비교**해 안내자를 무시하는 구현이면 깨진다. 사진 세트 판정(OQ-P-296·297·298·299·300)은 사람 몫이라 계획 밖. **검수가 초판을 다섯 축에서 뒤집었다** — ① **주 경로 안내자가 원본이 아니라 ML Kit 이 배경을 도려낸 판이었다**(그러면 `I ≡ p` 라 정련이 틀린 경계를 스스로 강화한다. 기능이 조용히 무효화된다) ② **Task 6 이 자기 게이트를 통과 못 했다**(`SegmentationMask.kt` 가 `postProcessAlpha` 를 위치 인자로 불러 파라미터 삽입에 컴파일이 깨진다 — 앞 라운드에 있던 "시그니처 변경은 호출부까지 한 태스크" 제약이 빠진 자리에서 그대로 터졌다) ③ **테스트 넷의 기대값이 수학적으로 틀렸다**(계수를 다시 창 평균하는 단계를 기대값에 안 넣었고, 소프트 매트 픽스처가 `p ≡ I` 라 가이드 필터가 **일부러 보존하는** 상태를 단언했다) ④ **⚠️ 메모가 정반대 진단을 유도했다**(그대로면 구현자가 핵심 단계를 지우고도 전부 초록) ⑤ **원본 해상도 실수 배열 둘이 약 96MB 를 더 얹었다**(휘도를 즉석 계산으로 바꿔 제거, OQ-P-300). 검수가 함께 짚은 변이 생존 자리 여섯(세로 보간·계수 최종 평균·`changed` 항상 참·녹청 계수 교환·축소 세로 인덱싱·`refineEdges` 플래그)에 각각 픽스처를 세웠다 |
| [2026-08-24-segmentation-mask-postprocessing.md](archive/2026-08-24-segmentation-mask-postprocessing.md) | **✅ 완료·develop 머지(2026-08-27, PR #363 `4da18230` — Task 14 사진 세트 판정은 OQ-P-287~295 로 이월)** — **세그멘테이션 마스크 후처리**(14 Task, **3 PR**, 셋 다 베이스 **`develop`**, 1·2단계는 병렬·3단계는 그 뒤). ⚠️ **PR 순서가 계약이다** — 1단계(필터 판정)를 먼저 넣지 않고 커널을 배선하면 현행 `MIN_SUBJECT_AREA_RATIO`가 **사각형 면적**으로 재기 때문에, 구석 잡티 덕에 bounds가 부풀어 있어 지금 통과하던 후보가 tight bounds로 줄면서 탈락한다. 지금 되던 사진이 실패로 바뀌는 회귀다. **1단계(Task 1~4)** 커버리지(`알파 총합 ÷ 255`) 판정·IoU 0.9 병합·전순서 정렬. 알파 총합은 **`Long`이 계약**이다(12MP 전면 불투명 후보의 합이 30억이라 `Int`로 누적하면 음수로 래핑되고 **가장 큰 피사체가 조용히 삭제된다**). **2단계(Task 5~10)** 호출부 없는 순수 커널 — 이진화 후 OR 풀링 → 행 run-length + union-find 8-근방 → area opening → 1픽셀 팽창 → 원본 알파에 적용 → 경계 한 겹 침식 → 측정. 침식 대상은 값 범위가 아니라 **능선 보호**(마주 보는 4-근방 쌍이 둘 다 0이면 건너뛴다)로 가른다 — 폭 1픽셀 보존·하드 매트 침식·램프 단차 없음을 동시에 만족하는 유일한 조건. 판정은 **직전 행 침식 전 알파 한 줄 + 좌측 이전값**으로 한다(비트셋으로는 `min` 침식의 연쇄를 못 막는다). **3단계(Task 11~13)** 램프 사상(⚠️ 버림이라 `알파 > 127 ⇔ 신뢰도 ≥ 0.5006`, 0.5는 배경) + 폴백 중간 판 둘 제거, 주 경로 bbox 사전 절단과 **새 ARGB_8888 판 생성**(자르기는 알파를 안 바꾸고 ML Kit 판은 회수 불가 — OQ-P-266), **후보 단위** 전멸 되돌리기와 0건 3분기(ML Kit 0건 / 후처리 전멸 / 필터 전멸), `OutOfMemoryError` 별도 처리(`Error`라 기존 `catch (e: Exception)`에 안 잡힌다), 관측 세 줄, 탭 승자 역전 회귀. **Task 14**가 사진 세트 판정과 as-built 문서 갱신이다. **계획 검수 3갈래가 차단 결함 넷을 잡았다** — 테스트 헬퍼·`pickCandidateIndex` 시그니처를 계획이 지어냈고(실제는 `candidate(left, top, width, height)` 멤버 함수와 7파라미터 `pick` 헬퍼), `maskSubjectAlpha`가 옵션을 안 받아 8×8 픽스처가 기본 임계 256에 걸려 **항상 `null`**이었으며, 새 커버리지 하한이 캔버스 100×100에서 25%라 **살아남는다고 적은 기존 테스트 둘이 실제로는 깨졌다**. 함께: **ktlint가 파라미터 2개 이상 시그니처를 멀티라인으로 강제**해 한 줄 선언 다섯이 게이트를 깼고, 되돌리기가 리스트 단위라 **부분 전멸 시 후보가 조용히 사라졌으며**, `OutOfMemoryError` try가 픽셀 할당을 안 감쌌고, `related_adr`이 ADR-0017(원격 네트워크)로 오기였다(→ ADR-0011 비트맵 추상화). |
| [2026-08-23-c103-pr1-multi-subject-domain.md](archive/2026-08-23-c103-pr1-multi-subject-domain.md) | **C-103 다중 후보 PR1 — data·domain 다중화**(3 Task). ML Kit 옵션을 `enableMultipleSubjects` + `enableSubjectBitmap`으로 바꿔 `segmentImage`가 후보 **목록**을 주게 하고, 파일 저장을 `persistSubject`로 갈라낸다. Task 1은 후보 모델 + 필터 순수 함수(면적 1% 임계·상한 5·`top`→`left` 타이브레이크·동일 bounds 중복 제거, 유닛 7건 — `BitmapWrapper`가 빈 인터페이스라 mockk 없이 순수하게 짠다). Task 2는 `persistSubject`를 **소비자 0인 채로** 먼저 만들어 리뷰 초점을 좌표 합성 하나로 좁힌다. Task 3이 전환한다 — 옵션·매핑(`right = startX + width`, exclusive라 `-1` 금지)·`getBitmap()` null 후보 제외·폴백(⚠️ `maskSubjectPixels`가 만드는 판은 **원본 전체 크기라 반드시 크롭**해야 하고, `createBitmap`이 자를 게 없으면 원본 인스턴스를 돌려주므로 `!==` 가드가 붙는다)·`SegmentationResult.subjectBounds` 제거·ViewModel 최소 적응(`firstOrNull()`로 **현행 동작 유지**, 저장 시점 이동은 PR2). 화면 동작은 단일 피사체에서만 무변화다 — 다중 사진에서는 박스가 가장 큰 후보 하나로 좁아진다. `persistSubject`의 좌표 합성과 폴백 크롭은 `Bitmap`·`Canvas` 의존이라 JVM 대상이 아니라 **실기기 5항목**으로 넘긴다. 스펙: [specs](../specs/archive/2026-08-23-c103-multi-subject-selection.md) PR 분할 표 1번 행. **머지(PR #342, 2026-08-24 develop `34bf1939`)**: 브랜치 `feature/c103-multi-subject-domain`(develop `d634efd3` + 커밋 4개, `..1d03b772`), 전체 검증 통과, 신규 유닛 필터 7건. 최종 리뷰가 Important 둘을 잡아 fix 웨이브로 닫았다 — 주 경로가 후보 비트맵 불변식을 **가정만** 하고 강제하지 않던 것(`bounds`를 비트맵 실제 치수에서 뽑도록 수정), 그리고 이 PR의 유일한 의도된 동작 변화(`firstOrNull()`)를 지키는 테스트가 없던 것. |
| [2026-08-26-topping-alpha-hit-test.md](archive/2026-08-26-topping-alpha-hit-test.md) | **✅ 완료·develop 머지(2026-08-27, PR #388·#390·#389 `5dc9fbec`)** — **토핑 누끼 모양 터치 판정**(10 Task, **스택 PR 5개**, **검수 2회 후 대폭 수정**) — 캔버스 토핑의 터치 판정을 사각형에서 실제로 보이는 실루엣으로 바꾸고 겹친 토핑의 투명한 자리를 통과시킨다. PR 분할은 ① 배경 편집 테두리 렌더링 ② 판정 순수 함수(전부 JVM 유닛) ③ 마스크 로딩·캐시와 대상별 연타 방어 ④ 캔버스 메인 전환 ⑤ 배경 편집 전환이고, 뒤 브랜치가 앞 브랜치 위에 쌓인다. **검수가 초판을 여섯 축에서 뒤집었다** — ① **탭·드래그를 형제 Box 둘로 나눴다**(전면 크기 포인터 노드는 좌표만 들면 히트라 형제 히트 테스트가 첫 히트에서 끊긴다. 뒤엣것이 앞엣것을 통째로 가려 선택 중에는 탭이 영영 안 돈다. 스펙의 "모디파이어를 둘로 나눈다"는 **같은 노드의 체인**을 나누라는 뜻이었다) ② **`ToppingCornerButtons` 가 삭제 대상인 `rememberToppingPainter`·`rememberToppingSize` 를 쓴다**(스트로크·네 버튼 좌표가 전부 그 크기에서 나와 컴파일이 깨진다) ③ **포인터 입력 핸들러가 참조로 비교된다**(`pointerInputEventHandler === other.…` 확인). 캡처 람다는 리컴포지션마다 새 인스턴스라 제스처가 리셋되고, 캡처한 값은 최초 값으로 굳는다 → 핸들러를 `remember` 로 안정화하고 읽는 값은 `rememberUpdatedState` 로 최신화 ④ **`drag()` 에는 터치 슬롭이 없다**(기존 `dragBy` 는 있다. 그대로 가면 선택된 토핑 위의 탭이 미세 이동으로 이동 처리 → down 좌표로 판정하고 `awaitTouchSlopOrCancellation` 을 거친다) ⑤ **배경 편집 테두리에 painter 성공 가드가 없었다**(디자인시스템 KDoc 이 계약으로 적어 둔 조건. 스펙의 "색 파싱만으로 조건이 맞는다"가 틀렸다) ⑥ **접근성 시맨틱스에 클릭 액션이 빠져** 지금보다 나빠진다. 그 밖에 배치 화면 마스크 로딩 게이트 누락·`isMine` 필터 유실 문장·`entries` 바인딩 스텝 부재·미사용 import 정리 부재(ktlint `no-unused-imports`)·`androidx.collection` 미존재(표준 라이브러리 `LinkedHashMap` + 잠금으로 대체해 **의존성 추가 0**)를 고쳤다. ⚠️ **스택 PR 에는 CI 가 돌지 않는다** — ktlint·테스트 워크플로가 베이스 `develop` 으로 필터돼 있어 PR 2~5 는 검사가 실행되지 않는다. 각 태스크가 로컬로 돌리고, 베이스를 `develop` 으로 바꾼 뒤 다시 확인한다. **스택 PR 코드리뷰 반영 5건**(마스크 칸 번호 내림 · 같은 모델 동시 로드 합류 · 딤 해제 접근성 액션 · 판정 꺼진 화면 시맨틱스 제외 · 배경 편집 painter 타입)은 "실행 뒤 달라진 것" 표에 적었다 |
| [2026-08-23-c103-pr2-multi-subject-ui.md](archive/2026-08-23-c103-pr2-multi-subject-ui.md) | **C-103 다중 후보 PR2 — 후보 선택**(4 Task, 베이스는 **PR1 브랜치 팁**). Task 1이 좌표 변환과 탭 대상 선택을 **Compose 타입 없는 순수 함수**(`ScaledRect`·`scaledRectOrNull`·`pickCandidateIndex`)로 빼 JVM에서 덮는다 — 이 화면에는 UI 테스트가 한 건도 없고 탭 판정이 조용히 깨지면 사용자가 엉뚱한 대상을 고른다(유닛 8건). **탭은 맞는 후보 중 면적 최소를 고른다** — "뒤에서부터 첫 히트"와 결과가 같지만 그러면 컴포넌트의 올바름이 필터의 정렬 기준에 매달린다. Task 2는 하이라이트가 목록을 받아 `Path` + `clipPath(Difference)` 한 번으로 딤을 빼게 한다. Task 3이 핵심이다 — 상태에서 경로 3필드를 걷고 `candidates`를 두며, **선택 시점의 순서를 계약으로 못 박는다**(저장 → 초안 기록 완료 → `isLoading` 해제 → `GoToConfirm`). 확인 화면은 정상 진입에서 초안을 **구독만** 하므로 이 화면이 유일한 writer이고, 순서를 어기면 그 화면이 "다음"을 잠근 채 뜬다. `record`가 `false`(흐름 미개시)면 이동하지 않는다. 중복 탭은 `BaseViewModel.launch(key)` 가드가 막고, `isLoading`은 성공·실패 양쪽에서 내린다(이동이 `goTo`라 화면이 백스택에 남아, 켠 채 나가면 돌아왔을 때 갇힌다). **Task 2가 컴파일이 깨진 채로 끝나고 Task 3이 화면·Route까지 결선하며 닫는다** — 하이라이트만 바꾸면 화면이 옛 파라미터로 부르고, 상태에서 필드를 걷으면 화면이 또 깨진다. 그리기와 상태 재편의 리뷰를 갈라 두되, `testDebugUnitTest`가 main 소스셋 컴파일에 의존하므로 **화면을 Task 3 안에 넣지 않으면 그 Task의 테스트를 아예 못 돌린다**(계획 검수가 잡은 결함). Task 4는 코드 변경 0이고 전체 검증과 문서 갱신만 한다 — 선행 스펙 [c103-segmentation-topping-edit](../specs/archive/2026-08-15-c103-segmentation-topping-edit.md)의 **거짓이 된 네 자리** 표기와, 새 스펙의 `Path` `remember` 서술 정정이다. 실기기 8항목. 스펙: [specs](../specs/archive/2026-08-23-c103-multi-subject-selection.md) PR 분할 표 2번 행. **머지(PR #342, 2026-08-24 develop `34bf1939`)**: 브랜치 `feature/c103-multi-subject-ui`(PR1 팁 위 커밋 4개, `..ab196483`), 전체 검증 통과, `SegmentationViewModelTest` 22건. 계획의 공백 하나를 실행 중 룰링으로 메웠다 — PR1이 넣은 `init_multipleCandidatesDetected_persistsOnlyTheFirstCandidate`가 삭제·수정 목록 어디에도 없는데 이번 변경 둘과 모두 충돌해, 지우는 대신 "걸러지고 남은 후보가 모두 화면까지 온다"로 용도를 바꿨다. 최종 리뷰가 Important 다섯을 잡아 fix 웨이브로 닫았다 — 그중 실제 결함은 `launch(key)`에 `onError`가 없어 **예상 못 한 예외에서 로딩 오버레이가 영영 남던 것**(스펙이 굵게 못 박은 조항이 세 번째 갈래에서 깨졌다), 나머지 넷은 테스트 공백 둘과 KDoc 둘이다. |
| [2026-08-21-c106-pr6-recent-cutout-reuse.md](archive/2026-08-21-c106-pr6-recent-cutout-reuse.md) | **✅ 완료·develop 머지(2026-08-22, PR #334 `19cb5299` — 스택 넷이 한 머지로)** — C-106 결선 스택 PR **6/6** — 누끼 알맹이 재사용(7 Task, 신규 테스트 **24건**, 마지막 하나는 이 저장소 문서 작업). 배치에 성공한 **테두리 없는 트리밍 알맹이**를 갤러리 "최근"에 남기고, 그것을 고르면 카메라·세그멘테이션을 건너뛰어 **누끼 확인 화면으로 직행**한다. OQ-P-255의 선행 결함 넷을 닫았다 — 절대경로 읽기 경로 신설 · 확장자 인자화(알맹이는 `.png`) · **2단 폴백 디코드**(구 `List<String>`을 살려 기존 목록이 통째로 날아가지 않게 한다) · `NavKeySegmentationConfirm` 인자 완화. 저장 대상은 **알맹이 1장뿐**이라 재사용 항목에서는 "사진 편집"이 잠겼다(🔁 2026-08-31 이슈 #424에서 뒤집혀, 잠그는 대신 테두리 편집만 연다). 최근 이미지 계층이 테스트 0건이라 **Task 2가 `android.net.Uri`를 파일 datasource 안으로 삼켜** Repository를 JVM 테스트 대상으로 만든 뒤 종류 축을 얹었다. 베이스는 **PR5 브랜치 `feature/#270-topping-place-wiring` 팁 `67ee0d6a`**(미머지). 스펙: [specs](../specs/archive/2026-08-20-c106-topping-place-api.md) 「누끼 알맹이 재사용 (PR6)」 절 **실행 결과**: subagent-driven-development로 7 태스크, 브랜치 `feature/#270-recent-cutout-reuse` 커밋 10개 `67ee0d6a..656cbf2e`, **30파일 964삽입/145삭제**, **머지·push 안 함**. 태스크 리뷰에서 fix 라운드는 **Task 4·6 두 번**이고 나머지는 지적 0건 통과. **실행 전 서브에이전트 검수 3종이 치명 결함 다섯을 미리 닫았다** — ① 배치 성공 시 저장을 성공 이펙트 뒤에 두면 `popUpTo`가 `viewModelScope`를 걷어 저장과 `clear()`가 끊긴다 ② 재사용 진입 판정에 스펙이 요구한 조건이 빠졌다 ③ Task 6이 기존 테스트 7건이 있는 파일을 전문으로 덮어쓰려 했다 ④ 테스트 픽스처가 저장소를 프로퍼티로 만들어 생성자 초기화식이 빈 흐름을 붙든다 ⑤ 베이스 팁 해시가 낡아 크래시 픽스가 빠질 뻔했다. **태스크 리뷰가 잡은 것 둘이 특히 값졌다** — Task 4·6 모두 "A가 B보다 먼저"라는 순서 불변식이 **테스트로 안 잠긴 채** 초록불이었다(각각 이펙트 채널 상태와 `MutableStateFlow` 스텁으로 잠갔다). **최종 브랜치 리뷰(opus)가 조건부 승인과 함께 Important 둘을 냈다** — ① "배치한 알맹이가 CUTOUT으로 남는다"가 UseCase→Repository 2홉 구간에서 무커버리지였다(SOURCE로 하드코딩해도 전부 통과했다) ② `NavigateToSegmentationConfirm(cutoutFilePath)`의 "cutout"이 바로 다음 홉의 `cutoutImagePath`와 **서로 다른 것**을 가리켜, 뒤바뀌면 컴파일된 채 "다음"이 영영 안 열리는 데드엔드가 됐다. fix 웨이브 한 번으로 여덟 지적을 닫았다. **계획과 갈린 실행 결정 셋** — ① 재사용 판정을 **초안 경로 비교**로(계획은 "비어 있는가"였고 그러면 A를 고른 뒤 B를 골라도 A가 배치된다) ② 갤러리 클릭 인텐트를 **종류별로 둘로** 갈라 uri 되짚기를 없앴다(날짜 그룹 사진이 확인 화면으로 새는 경로가 타입 수준에서 막힌다) ③ `AddRecentImageUseCase`의 `kind` 기본값을 없앴다. 실기기 확인은 아직 안 했다 — 이 라운드 8항목과 PR3·PR4·PR5 이월 22항목이 한 브랜치로 함께 나간다. **마지막 커밋은 주석 정리다**(`656cbf2e`, 비주석 변경 0줄) — 브랜치가 심은 테스트 주석 넷을 걷었다: 이미 삭제된 임시 작업 파일을 가리키던 포인터 · 7줄짜리 `Then` 설명 · 갤러리 화면의 현재 동작을 단정해 곧 낡을 문장 · 3줄짜리 스텁 함정. 미결: 알맹이 셀 디자인 시안 없음(OQ-P-257). ⚠️ 상한 `MAX_SIZE = 9`를 토핑 흐름 하나가 두 칸씩 먹어(원본 + 알맹이) 최근 사진이 두 배 속도로 밀려난다 — 감수 |
| [2026-08-21-c106-pr5-topping-place-wiring.md](archive/2026-08-21-c106-pr5-topping-place-wiring.md) | **✅ 완료·develop 머지(2026-08-22, PR #334 `19cb5299` — 스택 넷이 한 머지로)** — C-106 결선 스택 PR **5/6** — 배치 결선(7 Task, 신규 테스트 **29건** + 삭제 1건, 마지막 하나는 이 저장소 문서 작업). 확인 버튼이 실제로 서버를 부르게 한다: 화면 좌표 → 정규화 서버 좌표 변환 · 테두리 색 `#RRGGBB` 직렬화(계획 당시 8자리였으나 PR5 브랜치 리뷰에서 6자리로 정정) · `AddToppingUseCase` 호출 · 로딩 오버레이 · 실패 토스트 · 영구 실패 판정(다섯 코드, **알린 뒤 화면에 남는다** — 최종 브랜치 리뷰가 되감기를 Critical로 잡아 철회, 아래 참고) · 성공 시 초안 비우기. PR1이 만든 계층에 처음으로 소비자가 붙는 라운드라 잠들어 있던 결함 둘(OQ-P-109·OQ-P-246)을 함께 닫았다. 영구 실패 판정은 `code` 단독으로 하고(OQ-P-247을 ①안으로 닫는다), 취소가 남기는 고아 이미지는 감수한다(OQ-P-248). 베이스는 **PR4 브랜치 `feature/#270-topping-border-contract` 팁 `17a2c09f`**. 스펙: [specs](../specs/archive/2026-08-20-c106-topping-place-api.md) PR 분할 표 5번 행 **실행 결과**: subagent-driven-development로 7 태스크, 태스크 리뷰에서 fix 라운드는 **Task 5 한 번뿐**이고 나머지 여섯은 지적 0건 통과. **최종 브랜치 리뷰(opus)가 지적 8건을 냈다** — Critical 1건이 이 라운드의 되감기 정책을 뒤집었다: `CanvasToppingPlaceRoute`의 `toastPolicy`가 `rememberYGToastPolicy()`로 Route 컴포지션에 매달려 있어 영구 실패에서 `popUpTo`로 캔버스까지 되감으면 안내(토스트)가 **같은 프레임에 함께 폐기**돼 사용자가 실패 사실을 못 듣는 결함이었다(`DraftMissing`은 이미 안 되감는데 바로 아래 영구 실패 갈래가 같은 실수를 반복하고 있었다). 처방은 영구 실패도 `DraftMissing`처럼 **알린 뒤 화면에 남기는 것**(닫기 버튼이 이미 있어 막다른 곳이 아니다) — 캔버스 쪽 토스트 호스트로 안내를 보내는 진짜 처방은 OQ-P-167 소관이라 이 라운드 밖으로 미뤘다. 나머지 7건 중 하나가 **영구 실패 코드를 셋에서 다섯으로** 늘렸다(배치 POST 실패 표의 400 둘 `INVALID_REQUEST`·`INVALID_BORDER` 누락 — 재시도가 4단계를 전부 다시 태워도 결과가 항상 같은 400이라 참조되지 않는 이미지만 쌓인다). 8건을 fix 웨이브 한 번으로 닫은 뒤, **코드 주석·KDoc이 규약을 어기거나 낡은 자리 여덟을 정리하는 커밋**을 별도로 얹었다(그중 하나가 `ServerErrorCode.Parfait.PARFAIT_ALREADY_CLOSED` KDoc의 "화면 이동은 그대로 진행하고" — 되감기 철회로 거짓이 된 문장을 걷었다). 브랜치 `feature/#270-topping-place-wiring`, 베이스 `17a2c09f`(PR4 팁, 미머지), **커밋 10개** `17a2c09f..c443846b`(`6c163d89`·`9f2aed72`·`1d6acb7e`·`245ca5ac`·`af5f5482`·`cdd0cce7`·`56a1c0c4`·`e6a3807c`·`80ba8805`·`c443846b`), **머지·push 안 함**. 신규 테스트는 계획 예상 25건 + 최종 리뷰가 심은 4건 = **29건**(삭제 1건), **23파일 961삽입/64삭제** — 신규 파일 넷(`SelectiveLoggingInterceptorTest`·`ImageServiceTest`·`ToppingPlaceFailureTest`·`ToppingPlacementTest`)과 기존 파일 넷 확장(`StringTest`·`PresignedUploadDataSourceImplTest`·`CanvasToppingPlaceViewModelTest`·`NetworkModuleTest`). 검증은 `:domain:test :data:testDebugUnitTest :core:util:android:testDebugUnitTest :feature:groups:canvas:impl:testDebugUnitTest ktlintCheck :app:assembleDebug` 전부 통과. **계획과 갈린 실행 결정 둘** — ① 계획서 Task 5의 Step 1 테스트(`onClickConfirm_setsLoadingWhileInFlight`, 성공 뒤 `isLoading == false` 단언)와 Step 4 코드(성공 갈래에서 되돌리지 않음)가 서로 모순이었다. `try { … } finally { updateState { copy(isLoading = false) } }`로 합쳐 성공·실패·예외·취소 네 경로를 한 곳에서 덮는 것으로 풀었다 — 진짜 이유는 태스크 리뷰가 찾았다: `Result.onSuccess { action }`이 `action`을 try/catch 없이 불러 `toppingDraftRepository.clear()`가 던지면 `.onFailure`가 아니라 `BaseViewModel.launch`의 `catch` → `onError`로 직행하는데, 그 갈래에 `isLoading`을 내리는 코드가 없어 사용자가 오버레이 밑에 갇혔다. ② `CanvasToppingPlaceEffect.ToppingPlaced`를 지우면 `CanvasToppingPlaceRoute`가 컴파일되지 않는데 계획서 Task 5가 Route를 범위에 넣지 않았다 — Task 5가 자리표시 분기로 컴파일을 살리고 Task 6이 그것을 걷었다(태스크 경계가 한 칸 어긋나 있었다). **계획 실행 전 서브에이전트 검수 3종이 치명 결함 넷을 미리 닫았다** — ① 읽기 쪽 `CanvasToppingLayer`의 `Modifier.size` clamp가 좌표 역함수를 오버플로 구간에서 거짓으로 만들던 것(`requiredSize` 전환) ② 취소 테스트의 `cancelAndJoin`이 RED를 red가 아니게 만들던 것 ③ `CanvasToppingPlaceScreen`의 `@YGPreview` 호출부 누락 ④ `ToppingPlaced`를 단언하던 기존 테스트 삭제 지시 누락. 그중 ③·④가 없었으면 컴파일이 깨졌을 것이다. 실기기 확인은 아직 안 했다 — 이 라운드 9항목과 PR3·PR4 이월 13항목이 한 브랜치로 함께 나간다. **누끼 알맹이의 최근 이미지 재사용은 PR6으로 분리했다**(선행 결함 넷 → OQ-P-255). 문서 반영은 같은 커밋이 담는다 — 스펙(PR 분할 표 6번 행 신설·범위 제외·실패 처리 `code` 단독 판정) · open-questions(OQ-P-109·246·247·248 해소, OQ-P-245 기기 폭 축 추가, OQ-P-255 신설) · api 계약(parfait-image.md·image.md 소비처 셈). **이 행 자체는 별도 드리프트 정정 라운드(2026-08-21, 문서 저장소 단독 커밋)로 최종 브랜치 리뷰·주석 정리 이후 수치(커밋 10개·테스트 29건·23파일 961/64)와 되감기 철회를 반영했다** — 그 라운드는 코드 저장소를 KDoc 한 곳만 고쳤다(`ServerErrorCode.Parfait.PARFAIT_ALREADY_CLOSED`) |
| [2026-08-21-c106-pr4-topping-border-contract.md](archive/2026-08-21-c106-pr4-topping-border-contract.md) | **✅ 완료·develop 머지(2026-08-22, PR #334 `19cb5299` — 스택 넷이 한 머지로)** — C-106 결선 스택 PR **4/5** — 테두리 계약 전환(8 Task, 신규 테스트 **13건**, 마지막 하나는 이 저장소 문서 작업). 편집 화면이 테두리를 픽셀에 굽는 것을 그만두고(`withBorders` 삭제) **테두리 없는 알맹이 + 색·굵기 값**을 초안에 실어, 누끼 확인·배치·캔버스 **세 화면이 같은 8방향 스탬프 컴포저블**(`:core:designsystem`의 `YGToppingCutoutImage`)로 그리게 한다([ADR-0025](../../adr/0025-topping-border-as-server-field.md)). 함께 `SegmentationConfirmRoute`의 `rememberSaveable` 셋을 초안으로 걷고(`SegmentationConfirmViewModel` 신설), `NavKeyCanvasToppingPlace`의 인자를 없애며, 캔버스 종횡비 상수를 하나로 모은다. **초안 쓰기는 화면 진입이 아니라 사건 둘**(세그멘테이션 성공·편집 완료)**에 건다** — 진입에 걸면 프로세스 사망 복원 때 진입 인자가 편집 결과를 덮어써, ADR-0026이 영속을 고른 이유를 스스로 깬다(검수가 잡은 결함). **스펙과 갈린 결정 셋** — ① 종횡비는 스펙의 "`YGCanvas`가 domain 상수를 쓴다"와 **반대로** `domain/model/CanvasConst.kt`를 지우고 디자인시스템 상수로 모은다(새 모듈 간선 0개, 2026-08-15 미결 해소) ② C-301 배경 편집은 **원래 테두리를 그리지 않던 목업**이라 표시를 그 라운드로 미루고 필드 이름만 맞춘다 ③ 확인 버튼 비활성 **표현**은 디자인 근거가 없어 PR5로 미루고 초안이 비었을 때 확정을 막는 방어만 둔다. ⚠️ 비트맵 파이프라인은 Robolectric이 없어 JVM 테스트로 못 잡으므로 **시각 회귀 감지선은 실기기 확인 6항목**이다. 베이스는 **PR3 브랜치 `feature/#270-topping-draft-ssot`(PR 올라감·미머지)**. 스펙: [specs](../specs/archive/2026-08-20-c106-topping-place-api.md) PR 분할 표 4번 행 **실행 결과**: subagent-driven-development 로 8 태스크(마지막은 이 저장소 문서 작업), 브랜치 `feature/#270-topping-border-contract` 커밋 9개 `6c6399c6..d0e5d02e`, **머지·push 안 함**. 신규 테스트는 계획 예상 13건보다 3건 많은 **16건**이고 늘어난 셋은 전부 fix 라운드가 심은 감지선이다. **계획과 갈린 실행 결정 둘** — ① 배치 화면에서 초안이 비었을 때 계획은 "알리고 캔버스로 되감는다"였으나 **되감기가 토스트 호스트를 같은 프레임에 폐기해 알림이 잔상으로 끝나서** 되감기를 걷고 **알린 뒤 화면에 남는** 것으로 바꿨다(닫기 버튼이 이미 있어 막다른 곳이 아니다). 실패를 코드로 갈라 되감는 정책은 PR5가 소유한다. ② 배치 화면은 **초안 흐름이 한 번이라도 방출되기 전에는** 확인 인텐트를 흘려보내지 않는다 — 첫 프레임에는 "아직 못 읽었다"와 "비었다"를 구분할 수 없어 거짓 알림이 뜬다. 계획-시점 판정 ②의 문구는 정정이 필요하다: C-301 배경 편집 화면은 **테두리를 그린 적이 없고**, 그전에는 편집이 돌려주던 파일에 테두리가 구워져 있어 반영된 것처럼 보였을 뿐이다 — 굽기를 멈추자 표시 경로가 없다는 사실이 드러났고 그 자리는 **OQ-P-254**로 열어 뒀다. 실기기 확인은 아직 안 했다 — 이 라운드 몫 8항목(계획은 6항목으로 잡았고 실행이 둘을 더 달았다)과 PR3 이월 5항목이 한 브랜치로 함께 나간다. 문서 반영은 같은 커밋이 담는다 — 스펙(종횡비 통일 방향·스탬프 소유·초안 쓰기 시점) · [module-structure](../../architecture/module-structure.md) 두 항목 · [design-system](../../architecture/design-system.md) 인벤토리 · [ADR-0025](../../adr/0025-topping-border-as-server-field.md) as-built(캐시 장수 트레이드오프 철회, `status`는 develop 머지 전이라 `proposed` 유지) · open-questions(OQ-P-177 ①·OQ-P-245 실현 메모·OQ-P-254 신설) |
| [2026-08-20-c106-pr3-topping-draft-ssot.md](archive/2026-08-20-c106-pr3-topping-draft-ssot.md) | **✅ 완료·develop 머지(2026-08-22, PR #334 `19cb5299` — 스택 넷이 한 머지로)** — C-106 결선 스택 PR **3/5** — 토핑 초안 SSOT + C-001 정비(5 Task, 신규 테스트 **20건**, 커밋 9개). 계층 셋을 쌓는다: 직렬화·영속만 아는 `ToppingDraftLocalDataSource`(기존 `DataStore<Preferences>` 한 키에 JSON), 흐름 규칙을 아는 `ToppingDraftRepository`(**진입 시 통째로 덮어쓰기** + **사라진 캐시 경로 정규화**), 그리고 그것을 부르는 `CanvasMainViewModel`. 함께 C-001을 `YGScaffold`(V1, EntryBuilder 소유)에서 **`YGScaffoldV2`(Route 소유)**로 옮겨 오늘 캔버스 조회 실패를 토스트로 드러내고, 캔버스를 아직 못 받았으면 토핑 추가 버튼을 잠근다. 잠그는 판정 넷 — ① 진입 덮어쓰기가 낡은 초안 문제를 닫는 **유일한** 규칙이라 만료·정리 경로를 두지 않는다 ② 초안이 가리키는 것은 `cacheDir` 파일이라 **경로는 있는데 파일이 없으면 빈 초안으로 읽는다**(세그멘테이션 진입이 그 디렉토리를 비운다) ③ **실패를 매번 알리지 않는다** — 화면이 앞에 설 때마다 재조회하므로 보여 줄 캔버스가 없을 때만 토스트다 ④ `nextPositionZ`는 목록 크기가 아니라 **최대 z + 1**(지워진 토핑이 있으면 겹친다). ⚠️ `TOPPING_EDIT_RESULT_KEY`는 걷지 않는다 — 소비자가 `SegmentationConfirmRoute`와 `CanvasBGEditRoute` 둘이라 걷으면 배경 편집이 컴파일은 통과한 채 조용히 죽는다. 이미지·테두리 채우기(PR4)와 초안 읽어 배치(PR5)는 범위 밖이고, `:core:designsystem`에는 호스트 테스트 소스셋이 없어 Task 3만 Preview로 대신한다. 베이스는 **PR2 브랜치 `feature/#270-topping-place-domain`(미머지)**. 스펙: [specs](../specs/archive/2026-08-20-c106-topping-place-api.md) PR 분할 표 3번 행 **실행 결과**: subagent-driven-development 로 5 태스크, 태스크 리뷰는 Task 2 만 fix 라운드 1회(정규화 테스트가 safe-call 이라 초안 전체 null 회귀를 못 잡던 것)이고 나머지 넷은 지적 0건. **최종 브랜치 리뷰(opus)가 진짜 결함 하나를 잡았다** — 지난 날짜를 보는 동안 하루 경계를 넘기면 `syncToday` 가 `todayCanvas` 를 어제 것인 채로 남기고 `handleClickGoToToday` 가 재조회를 안 해, **어제 `parfaitId` 가 초안에 못 박히는** 경로가 있었다(PR5 에서 편집을 다 마친 뒤 409 를 맞는다). 두 자리를 고치고 테스트 둘로 잠갔다. 함께 닫은 것 셋 — 성공 경로의 내비게이션 이펙트 미단언, DataSource 의 dedupe·decode 순서(`EncryptedPreferences.observe` 와 같은 이유로 원문에서 먼저 dedupe), 같은 결정 설명 3중복. 그 뒤 **주석이 코드의 어려움에 비해 과하다는 지적으로 6파일 23줄을 걷었다** — 그래서 계획 본문의 코드 블록에 적힌 KDoc 은 최종 형태가 아니고 **코드가 정본이다.** 실기기 확인 5항목은 아직 안 했다. 🔁 **스택 셋을 develop `9b112e86` 위로 다시 쌓았고**(2026-08-20 저녁) PR3 만 Spotlight 라운드(PR #298)와 부딪혀 커밋 셋에서 해소했다 — 그 김에 **실패 토스트를 `YGScaffoldV2` 공통 자리가 아니라 캔버스 프레임 상단 호스트로 보내기로** 정했다(작성자 토스트가 이미 그 자리라 큐를 하나로 둔다, OQ-P-167). 리베이스 뒤 커밋 10개 `6938e8a7`..`90eb22a1`. 📌 **베이스가 develop에 들어왔다(2026-08-20, PR #322)** — PR1·PR2가 머지돼 이 브랜치가 업고 있던 커밋 아홉이 develop 것이 됐고, 남은 자기 커밋만 PR로 올리면 된다 |
| [2026-08-20-c106-pr2-topping-place-domain.md](archive/2026-08-20-c106-pr2-topping-place-domain.md) | **✅ 완료·develop 머지(PR #322 `da03c9b0`, 2026-08-20 — PR1과 한 머지)** — C-106 결선 스택 PR **2/5** — 토핑 배치 계층(2 Task, 소비자 0). DataSource `placeTopping` 은 이미 있고 이 PR 이 더하는 것은 그 위 두 층이다: `ToppingRepository`/Impl(**`place` 만** 올린다 — 수정·삭제·테두리 재편집은 소비 화면이 C-301 라운드라 올리면 계약이 바뀌어도 아무도 안 고친다) + 업로드·배치 순서를 도메인 규칙으로 고정하는 `AddToppingUseCase`. **베이스 브랜치는 PR1 의 `feature/#270-image-upload-transport`(미머지)** 이고 그 위에 `feature/#270-topping-place-domain` 을 쌓는다. 잠그는 판정 셋 — ① Repository 층은 에러 변환만 하고 **좌표·테두리를 손대지 않는다**(테두리를 흘리면 서버는 200 을 주고 캔버스에서 테두리만 조용히 사라진다) ② `AppError.Server` 의 `code`·`statusCode` 가 함께 살아 올라온다(PR5 가 둘을 같이 봐야 되감기를 판정한다) ③ **`ImageType` 을 UseCase 가 정한다** — 파라미터로 열면 `BACKGROUND` 로 올라가 객체가 **무증상으로 엉뚱한 S3 접두사에 앉는다**(`api/image.md` 의 키 규칙이 `<imageType 소문자>/…`). 배치는 `imageType` 을 검사하지 않아 아무 실패도 드러나지 않는다. 좌표 변환·되감기 판정·`positionZ` 산출은 전부 범위 밖(PR3·PR5). ⚠️ PR1 과 같은 이유로 **`:app:assembleDebug` 는 DI 안전망이 아니다**(`dagger.fullBindingGraphValidation` 미설정 + 소비자 0) — 그래서 `@Binds` 누락을 잡는 리플렉션 테스트를 Task 1 에 둔다. 신규 테스트 **11건**, 커밋 2개, push·PR 없음. 스펙: [specs](../specs/archive/2026-08-20-c106-topping-place-api.md) PR 분할 표 2번 행 **실행 결과**: subagent-driven-development 로 2 태스크, **태스크 리뷰 두 번 다 지적 0건 통과**(fix 라운드 0회). 브랜치 `feature/#270-topping-place-domain`(PR1 브랜치 위) 커밋 3개(리베이스 후 `11c38113..4b780d17`), **머지·push 안 함** — 다음 PR 은 이 브랜치 위에 쌓는다. 신규 테스트 11건은 계획 예상 그대로이고 `:app:assembleDebug` 까지 전부 통과. 최종 브랜치 리뷰(opus, base 는 develop 머지 베이스가 아니라 PR1 팁 — PR1 은 자체 최종 리뷰를 이미 통과했다)가 **머지 가능·Critical 0·Important 0·Minor 3** 을 냈고 셋을 fix 웨이브 한 번으로 닫아 커밋이 2개가 아니라 3개가 됐다. **계획과 갈린 것 셋**: ① ktlint `chain-method-continuation` 이 `.mapErrorToAppError()` 줄바꿈을 강제해 코드 블록과 다르다 ② 바인딩 테스트가 `ToppingRepository` 만이 아니라 **`ImageUploadRepository` 까지 함께 단언**한다(`AddToppingUseCase` 가 둘 다 주입받는데 감지선이 한쪽에만 있으면 PR1 이 심은 바인딩이 사라져도 PR5 빌드까지 잠든다) ③ 같은 테스트에서 "소비자가 0 이라 이 단언이 유일한 선이다"라는 상태 단정을 걷고 근거를 스펙 포인터로 옮겼다. **이월 1건**: `AddToppingUseCase` 클래스 KDoc 두 문장이 포인터 한 줄 지향보다 길다(최종 리뷰가 "뒷문장은 코드가 말하지 않는 계약"이라 이월 유지로 판정). **계획 defect 0건** — 실행 전 서브에이전트 검수 두 번이 Important 4건을 미리 닫았고 그중 둘이 이 라운드의 감지선(인자 전달 테스트·`@Binds` 리플렉션 테스트)을 만들었다. |
| [2026-08-20-c106-pr1-image-upload-transport.md](archive/2026-08-20-c106-pr1-image-upload-transport.md) | **✅ 완료·develop 머지(PR #322 `da03c9b0`, 2026-08-20 — 스택이라 PR2 브랜치가 이 커밋들을 업고 올라갔다)** — C-106 결선 스택 PR **1/5** — 이미지 업로드 전송 계층(3 Task, 소비자 0). 저장소에 **S3 로 바이트를 보내는 경로가 아예 없다**. 아래에서 위로: `@UploadClient` 전용 OkHttp 클라이언트(1) → `PresignedUploadDataSource` PUT 전송(2) → 발급·전송·확인을 하나로 닫는 `ImageUploadRepository`(3). **클라이언트 분리는 성능 선택이 아니라 기능 전제다** — `AuthInterceptor#intercept` 가 Retrofit `Invocation` 태그로 `@NoAuth` 를 판정해 raw 요청엔 `Authorization` 이 무조건 붙고, presigned URL 에 실리면 S3 가 서명 수단 중복으로 거절한다. 기존 `@UnauthenticatedClient` 를 재사용하지 않는 이유는 그쪽이 재발급 교착 회피용 `Dispatcher` 를 들고 있어 업로드가 슬롯을 점유하면 401 폭주 시 재발급이 굶기 때문. 함정 셋을 본문에 박았다 — ① `newBuilder()` 파생은 부모 `Dispatcher` 를 물려받아 격리가 사라진다 ② 본문 로깅(`Level.BODY`)을 복사하면 원본 해상도 PNG 가 매 업로드마다 문자열로 힙에 올라간다(`HEADERS` 이하 + `asRequestBody` 스트리밍) ③ `writeTimeout` 은 바이트 간 유휴 상한이라 전송 전체가 느린 것을 못 잡아 이 표면만 `callTimeout` 을 함께 둔다. `contentType` 은 파라미터가 아니라 **Repository 가 한 번 정해 발급 요청과 PUT 헤더 양쪽에 넘긴다**(둘 다 서명 대상이고 어긋난 실패는 서버 로그에 안 남는다). S3 거절도 우리 서버를 안 거쳐 로그가 없으므로 상태 코드를 `PresignedUploadException` 에 싣는다. 지원 밖 확장자는 **발급을 부르기 전에** 끊는다(부르면 고아 `PENDING` 행과 S3 키만 남는다). 신규 테스트 **20건**. ⚠️ **`:app:assembleDebug` 는 이 PR 의 DI 안전망이 아니다** — 저장소에 `dagger.fullBindingGraphValidation` 이 없고 Dagger 는 엔트리포인트에서 도달 가능한 바인딩만 검증하는데 이 PR 은 소비자가 0 이라 한정자·바인딩 누락이 안 잡힌다. 그래서 `@UploadClient` 리텐션을 `RUNTIME` 으로 두고(이웃 `UnauthenticatedClient` 의 `BINARY` 와 의도적으로 갈린다) 생성자 애노테이션을 리플렉션으로 단언하는 것이 유일한 감지선이다. 커밋 3개, push·PR 없음. 검증은 `:domain:test`(JVM 모듈)·`:data:testDebugUnitTest`·`ktlintCheck`, 마지막 태스크만 `:app:assembleDebug`(Hilt 그래프 게이트). 범위 밖: 화면 결선(PR5)·배치 계층(PR2)·초안(PR3)·테두리 전환(PR4)·presigned 만료 판정·고아 정리·실기기(소비자가 없어 밟을 화면이 없다). 스펙: [specs](../specs/archive/2026-08-20-c106-topping-place-api.md) **실행 결과**: 브랜치는 계획이 적은 `feature/#270-c-106-topping-add-api` 가 아니라 **그 위에 새로 만든 `feature/#270-image-upload-transport`**(커밋 6개)이고 **머지·push 안 함** — 다음 PR 은 이 브랜치 위에 쌓는다. subagent-driven-development 로 3 태스크 + fix 라운드 2회 + 브랜치 최종 리뷰 + fix 웨이브 1회, 신규 테스트 **22건**(계획 예상 20건 + 2건), `:domain:test`+`:data:testDebugUnitTest` 334건 전부 통과. **계획을 뒤집은 판정 둘**: ① 업로드 클라이언트 로깅 인터셉터를 `HEADERS` 가 아니라 **통째로 제거**(리뷰가 "presigned URL 은 쿼리 스트링이 곧 자격증명이라 요청 라인만 남겨도 샌다"를 지적, 스펙 정본도 함께 정정) ② `ImageUploadRepositoryImpl` 주석에서 세그멘테이션 캐시 정리 시점을 단정하던 절 삭제. **계획 defect 3건**이 드러났다 — 테스트 픽스처의 발급 id 와 확인 id 가 같은 값이라 `confirmUpload` 를 건너뛴 구현도 통과하던 것, `put` 이 `uploadUrl` 을 받는지 아무도 단언하지 않던 것, `:app:assembleDebug` 가 DI 를 검증한다는 거짓 전제(`dagger.fullBindingGraphValidation` 미설정 + 소비자 0). 앞의 둘은 계획 검수 서브에이전트가 실행 전에 잡아 고쳤다. **PR5 선행으로 미룬 것 둘**: OQ-P-109(메인 클라이언트가 발급 응답 본문을 `Level.BODY` 로 찍는다 — 이 PR 은 소비자가 0 이라 아직 안 샌다)·OQ-P-246(S3 업로드가 코루틴 취소를 안 따라간다). 스펙: [specs](../specs/archive/2026-08-20-c106-topping-place-api.md) |
| [2026-08-18-segmentation-pipeline-hardening.md](archive/2026-08-18-segmentation-pipeline-hardening.md) | 카메라·갤러리 → 세그멘테이션 경로 보강 + `YGScaffoldV2` 이관 구현 계획. **✅ 완료·develop 머지(PR #309 `cf357937`, 2026-08-20)** — 브랜치 `refactor/segmentation-develop`(develop `750cc2dd` 위로 리베이스한 head `63ec2989`가 충돌 해소 편집 없이 그대로 머지됐다). 스펙: [specs](../specs/archive/2026-08-18-segmentation-pipeline-hardening.md). ⚠️ **본문 Global Constraints의 브랜치 전제는 낡았다** — 계획이 쓰일 당시 브랜치(`refactor/segmentation-logic`, PR #290 로컬 머지 위)는 폐기됐고 그 사실을 본문 첫 제약에 적어 뒀다. 이 계획은 재실행 대상이 아니다. 이 줄은 **2026-08-20 문서 점검이 뒤늦게 등록한 것**이다 — 스펙은 `specs/README.md` active 표에 있었는데 계획만 인덱스에서 빠져 있었다 |
| [2026-08-19-server-delta-nametag-chip-keys.md](archive/2026-08-19-server-delta-nametag-chip-keys.md) | **✅ 완료·develop 머지(PR #310 `750cc2dd`, 2026-08-20)** — 서버 `57529ec` delta 반영 계획(7 Task). **브랜치는 `feature/#300-sync-backend-api-250819`**(PR #310) — 직전 라운드 위에 얹은 브랜치이고 그 라운드가 붙인 칩 결선을 이번 delta가 무력화해서다. 순서: **시각 파싱 복구 + `MyParfaitGroupVOMapperTest` 삭제**(1, 가장 급한 버그 — 지금 그룹이 하나라도 있으면 G-001 목록이 통째로 실패한다) → 칩 JSON 키 세 자리 정정(2) → `RELEASED` → `DEFAULT`(3) → 캔버스 `groupMembers` 칩을 `CanvasMemberVO`까지(4) → **C-001 상단 칩 결선 + canvas `util/ColorChipType.kt` 신설**(5) → 소비처 없는 계약 미러링(6) → 거짓이 된 KDoc 정리 + 전체 검증(7). ⚠️ **Task 2는 TDD가 성립하지 않는다** — 이름만 바꾸는 변경이라 앱 테스트가 `@SerialName` 문자열을 검증할 수 없고(전부 DTO 객체를 직접 만들어 넣는다) 검증 수단이 컴파일과 기존 테스트뿐이다. 계획이 그 한계를 스텝에 명시한다. Task 1이 지우는 `MyParfaitGroupVOMapperTest`는 **오프셋 붙은 입력을 스스로 지어 넣어 이 버그를 초록으로 지켜 온 파일**이고, 직전 라운드가 "규약 예외라 손대지 않는다"고 park한 것을 뒤집는다. Task 5의 canvas 변환은 S-101과 글자까지 같은 복제본이라 KDoc에 **컴파일러가 잡는 것은 앱이 enum 상수를 늘릴 때의 arm 누락뿐**이라는 한계를 박는다. 커밋은 태스크마다(사용자 소유 PR 브랜치), push·PR 없음. 명령은 `:domain:test`(JVM 모듈)·`ktlintCheck`(저장소에 detekt 없음) — 직전 계획의 오기를 바로잡았다. **실행 완료**(subagent-driven, 태스크마다 구현→리뷰, 커밋 8개 `e461d0d7..4f8eb7be`; 이후 리뷰 fix로 커밋이 더 붙어 develop에는 `8d7ae46e`까지 들어갔다). 전체 빌드 통과, **뒤집힌 결정 0건**. 계획 defect 0건 — 실행 전 리뷰가 잡은 치명 1건(Task 6이 `ParfaitImageRemoteDataSourceImplTest`를 빠뜨려 `:data` 컴파일이 깨질 자리)을 미리 고쳐 둔 것이 실제로 방어됐다. **park 1건**: Task 3에서 구현자가 red(컴파일 실패) 관측을 건너뛴 것이 확인됐으나(정직하게 인정), 그 태스크의 red 는 존재하지 않는 enum 상수를 참조하는 구조라 컴파일이 보장하므로 고칠 수 없고 하중도 없다고 판정. 그 뒤 태스크 디스패치에 "red 를 실제로 실행하고 출력을 남겨라"를 명시했고 Task 4·5 는 관측했다(Task 5 의 red 가 계획 예측값 `[NametagChip1, NametagChip2]`를 정확히 냄 — 테스트가 동어반복이 아니라는 증거). 최종 리뷰(opus)는 "머지 전 필수 수정 없음", fix 웨이브 1회로 Important 2건 중 값싼 둘(널→널 커버리지 이전·생성 응답 KDoc 포맷 경고)을 닫고 나머지 하나는 OQ-P-237 로 올렸다. 스펙: [specs](../specs/archive/2026-08-19-server-delta-nametag-chip-keys.md) |
| [2026-08-18-server-delta-nametag-chip-day-boundary.md](archive/2026-08-18-server-delta-nametag-chip-day-boundary.md) | **✅ 완료·develop 머지(PR #308 `412991ea`, 2026-08-20)** — 서버 `08df1bf` delta 반영 계획(8 Task). **브랜치는 `feature/#294-group-ssot`(PR #299) 위** — 상세 조합을 걷는 자리가 SSoT 배선과 같은 파일이라 그 위에 얹는다. 순서: 하루 경계 03시(1, 독립) → `NametagChipType` + 그룹 DTO/VO/매퍼 + `ParfaitGroupRemoteDataSourceImplTest` 신설(2) → 캔버스 `placedBy` DTO 미러(3) → 디자인시스템 `Default` 2종(4) → `GroupDetailVO` 삭제·`combine` 제거(5) → S-101 칩·정원(6) → G-001 칩(7) → 전체 검증(8). Task 5 뒤 Task 6 전까지 `:feature:groups:setting:impl`이 **일시적으로 안 붙는다**(계획이 그 지점을 명시). 칩 문자열 판정(`RELEASED` 보존 · 미지 값 `null`)은 매퍼 단독 파일이 아니라 신설 DataSource 테스트로 잠근다. ⚠️ Task 4의 `YGColorChipType.Default`는 **열린 PR #298도 같은 정의로 추가**하므로 글자까지 맞춰 둔다. 커밋은 태스크마다 한다(브랜치가 사용자 소유 PR). **실행 완료**(subagent-driven, 태스크마다 구현→리뷰→fix). 계획 defect 4건이 실행 중 드러났다 — `:domain:testDebugUnitTest`·`detekt` 오기, Task 2 픽스처 목록 누락(3회 재발), Task 1이 넣게 한 동어반복 테스트. 최종 브랜치 리뷰 + **별도 독립 리뷰** 두 번을 돌려 Important 4건을 고쳤다. 스펙: [specs](../specs/archive/2026-08-18-server-delta-nametag-chip-day-boundary.md) |
| [2026-08-17-group-ssot.md](archive/2026-08-17-group-ssot.md) | **✅ 완료·develop 머지(PR #307 `8ca3329a`, 2026-08-20)** — 그룹 목록·상세 인메모리 SSoT 구현 계획(7 Task). 기준선은 `Enter` 인텐트(재진입마다 재조회)와 "캐시 있으면 목록 유지" 실패 규칙을 들여온 `feature/#288-group-list-refresh`(2026-08-17 develop 머지, PR #297). 구현 후 develop으로 리베이스하며 새로고침 실패 토스트(`ShowRefreshError`)와 합쳤다 — 판정 기준만 캐시 상태(`isNullOrEmpty()`)로 바꾸고 토스트 규칙은 develop 것을 지켰다. 순서: `GroupLocalDataSource` 신설(1) → Repository 읽기 `Flow`·갱신·write-through(2) → UseCase 구독/갱신 분리(3) → 목록(4)·설정(5)·캔버스(6) 이관 → 세션 정리(7). 2~3 구간에서는 `:feature`가 옛 API를 부르므로 **앱 빌드가 일시적으로 깨진 채 진행**되고 Task 6 Step 5에서 회복을 확인한다. 캐시 검증은 mock이 아니라 실물 `GroupLocalDataSourceImpl`을 Repository 테스트에 주입해서 한다(캐시 상태가 관찰 대상이라). **커밋하지 않는다**(TJYG-Android 규약). 실기기 미검증 5항목 명시. 스펙: [specs](../specs/archive/2026-08-17-group-ssot.md) |
| [2026-08-16-ygscaffold-v2-common-loading-error.md](archive/2026-08-16-ygscaffold-v2-common-loading-error.md) | **✅ 완료·develop 머지(PR #267 `955c4636`, 2026-08-16)** — `YGScaffoldV2` 공통 로딩·에러 토스트 구현 계획(3 Task, `core:designsystem` 안에서 끝남). 오버레이(1) → 스캐폴드+`showError`(2) → V1 `@Deprecated`(3) 순서이고, 3이 마지막인 이유는 `ReplaceWith` 문자열이 **V2의 모든 신규 파라미터가 기본값을 갖는다는 사실에 의존**해서다(Task 3 Step 3이 그 치환을 손으로 실제 컴파일해 본다). 함정 셋을 본문에 박았다 — ① `YGToastHost`의 2초 `delay` 때문에 **테스트 클럭 자동 진행이 단언 전에 토스트를 지운다**(`mainClock.autoAdvance = false` + 진입 애니메이션 300ms만 손으로 진행), ② 터치 차단에 `clickable`을 쓰면 TalkBack이 오버레이를 버튼으로 읽으므로 `pointerInput` consume, ③ `core:designsystem`에 `values/`가 없어 접근성 문자열 추가가 **모듈 최초의 `strings.xml`**이 된다. 노드 식별은 `testTag`(프로덕션 `const`), `contentDescription`은 접근성 전용으로 목적을 분리. 테스트 룰은 선례(`YGThemeSmokeTest`)를 따라 **무인자 `createComposeRule()`** — `StandardTestDispatcher`를 넘기려면 `kotlinx-coroutines-test`를 androidTest에 새로 올려야 해서 범위 밖. 계측 테스트 5건(에뮬레이터 필요, 없으면 "미실행"으로 보고). **커밋하지 않는다**(TJYG-Android 규약). 범위 밖 명시: 호출처 11곳 이관·`app-preview` 갤러리 등록(임시 컴포넌트라 디자인 확정 후)·`AppError` 문구 매핑·기존 로딩/에러 화면 정리. 계획 대비 갈린 것 둘 — 계측 테스트가 **5건이 아니라 7건**(접근성 차단 대조 2건 추가), 그리고 "범위 밖"이던 **화면 이관 3건이 같은 브랜치에 동승**했다(계약 검증용, 일괄 이관은 여전히 안 함). 스펙: [specs](../specs/archive/2026-08-16-ygscaffold-v2-common-loading-error.md) |
| [2026-08-15-user-info-ssot.md](archive/2026-08-15-user-info-ssot.md) | 유저 정보 SSoT·자동로그인 구현 계획(8 Task). 저장(1)→조율(2)→도메인 진입점(3)이 아래층이고 그 위에 세션 정리(4)·부트스트랩(5)·스플래시(6)·로그인 직후 갱신(7)·화면(8)이 얹힌다. 4를 5보다 앞에 두는 이유는 부트스트랩 실패 경로가 정리 동작을 부르기 때문. 함정 셋을 본문에 박았다 — 매퍼가 `memberId`·`nickname`을 뒤바꿔도 **타입이 달라 컴파일러가 막지 못하므로** Task 1 Step 5에서 일부러 뒤집어 RED를 실측하고, `runCatching`을 쓰면 취소가 `null`로 둔갑해 '저장분 없음'으로 보고되므로 `runSuspendCatching`을 쓰며, mock을 지우면 기본값이 사라져 화면이 `null`을 로딩으로 다뤄야 한다. Task 5는 스펙의 열린 질문(네트워크 실패 시 토큰까지 지울지)과 맞닿아 있었고, **구현 중 인증 거절(401·`MEMBER_NOT_FOUND`)일 때만 파기**로 확정됐다 — 5xx나 로컬 저장 실패까지 파기하면 서버 장애 중에 전 사용자가 로그아웃되고 200 받은 세션이 로컬 쓰기 실패로 죽는다. 매퍼 단독 테스트는 만들지 않는다(저장소 규약). 신규 테스트 약 20건 + 수동 확인 7항목. 스펙: [specs](../specs/archive/2026-08-15-user-info-ssot.md) · [ADR-0022](../../adr/0022-user-info-local-ssot.md) | **✅ 완료·develop 머지(PR #263 `2143c229`, 2026-08-16)** — 8 Task 전량 반영. **계획과 갈린 곳 4건**: `ObserveMyAccountUseCase` → `GetMyAccountFlowUseCase` 개명, 계획에 없던 `EncryptedPreferences`(`data/datastore/`) 추출로 두 저장소가 암호화·복호화·폐기를 공유(암호문 상태 dedupe는 편집 중 입력 필드가 재발급 저장에 흔들리던 경로를 막는다), `changeGlobalNickname`이 로컬 공백 시 재조회 폴백, 부트스트랩 정리를 `LogoutUseCase`로 위임. 테스트는 예상 약 20건보다 크게 늘어 저장소 전역 358 → **415건**(파일 40 → 47). **수동 확인 7항목 미수행** |
| [2026-08-15-session-token-refresh-infra.md](archive/2026-08-15-session-token-refresh-infra.md) | 세션 인프라 — 401 자동 재발급·강제 로그아웃·로그아웃 결선(9 Task). Task1 `SessionEvent`·`SessionEventBus`(`Channel(CONFLATED)`) → Task2 토큰 거절 코드 3종 → Task3 인증기 성공 경로 → Task4 실패 갈래 4종 → Task5 선점 확인·루프 가드 → Task6 `OkHttpClient` 결합 → Task7 `AuthRepository.logout` → Task8 설정 화면 결선 → Task9 앱 루트 단일 수집. **Task를 3·4·5로 쪼갠 이유**는 리뷰어가 한쪽만 반려할 수 있는 자리이기 때문 — 성공 경로 / 실패를 어떻게 분류하는가 / 동시성 방어는 서로 다른 규칙을 잠근다. `Mutex`만으로는 안 되고 선점 확인이 있어야 대기하다 깨어난 요청이 재발급을 또 쏘지 않는다는 것이 Task5의 회귀 감지선이다. 함정 둘: **네트워크 실패에 토큰을 지우면 오프라인 진입이 곧 로그아웃**이라 그 갈래를 테스트가 직접 단언하고, refresh token 부재에 `ForcedLogout`을 쏘면 로그인 화면이 자기 자신으로 튕긴다. Task6의 `:app:kspDebugKotlin`이 `Provider<AuthService>` 지연 주입이 Dagger 순환을 끊었는지 판정하는 게이트다. 동시성은 스레드 대신 순차 호출로 검증한다(스케줄러에 좌우되면 감지선이 흐려진다). 신규 테스트 12건 + 수동 확인 4항목. 스펙: [specs](../specs/archive/2026-08-15-session-token-refresh-infra.md) · [ADR-0021](../../adr/0021-token-refresh-forced-logout.md) **✅ 완료·develop 머지(PR #260 `9cfbd117`, 2026-08-15)** — 9 Task 전량 반영. **계획이 코드와 갈린 곳 3건**: ① Task 3의 `Provider<AuthService>` 지연 주입이 **채택되지 않았다** — 재발급 전용 클라이언트(`@UnauthenticatedClient`)를 따로 만들며 Dagger 순환 자체가 사라졌고, 그래서 Task 6 게이트의 의미도 바뀌었다(`NetworkModule`·`ServiceModule`에 provider 3개 추가). ② Task 8·9의 `clearBackStack()`+`goTo`가 **`replaceAll()`** 한 줄이 됐다 — 같은 라운드가 `Navigator.clearBackStack()`을 제거하고 기존 호출부 3곳(`Splash`·`TermAgree`·`Login`)까지 옮겼으며 계획에 없던 `NavigatorTest` 3케이스가 그 결정을 잠근다. ③ `isLoggingOut` 상태 + `YGActionItem(enabled)` 신설이 계획 밖에서 붙었다. 테스트는 계획 12건 → **파일 5개 26케이스**. **수동 확인 4항목은 미수행**(OQ-P-146) |
| [2026-08-15-parfait-canvas-topping-member-api-service-layer.md](archive/2026-08-15-parfait-canvas-topping-member-api-service-layer.md) | **완료·머지(PR #250, 2026-08-15)** — TJYG-Android `:data`·`:domain`에 서버 신규 **5 엔드포인트**(캔버스 오늘 조회·과거 목록 · 토핑 테두리 수정·삭제 · 회원 탈퇴) 표면 배선. **7 Task**(0 브랜치 → 1 domain 모델 7종 → 2 parfait DTO·Service·매퍼 → 3 parfait DataSource+테스트 14케이스 → 4 토핑 테두리/삭제 → 5 회원 탈퇴 → 6 `http/` 보강). **DI 줄은 늘지 않는다** — 기존 인터페이스에 함수를 더하는 첫 라운드다. Task별 커밋(브랜치 `feature/canvas-topping-member-api-260815`), push·PR은 확인 후. 핵심 배선 3가지: ① `today` 3층 중첩을 `TodayCanvasVO`로 접되 `images: null`만 `emptyList()`로 접고 `background`·`lastClosedDate`의 null은 유지 ② 테두리는 `ToppingBorder` sealed 왕복(보내기는 기존 `toPlaceRequest` 선례, **받기는 이 라운드가 처음**)이고 응답의 SOLID인데 색·두께 결손은 `None`으로 방어 ③ **두 DELETE가 `ApiCaller` 진입점을 달리 쓴다** — 탈퇴 204는 `safeApiCallNoContent`, 토핑 삭제 200+`data: null`은 `safeApiCallWithoutData`(死코드였던 진입점의 첫 소비처). 검증은 `./gradlew test ktlintCheck :app:assembleDebug`이고 실서버 호출은 0건. 스펙: [specs](../specs/archive/2026-08-15-parfait-canvas-topping-member-api-service-layer.md) |
| [2026-08-13-mvi-error-infrastructure.md](archive/2026-08-13-mvi-error-infrastructure.md) | MVI 공통 에러·이펙트 인프라(4 Task). Task1 `AppError` 3갈래 + `ApiException` 매핑(`CancellationException`은 재던짐) → Task2 `core:ui` 테스트 소스셋 신설 + 이펙트를 `Channel(BUFFERED)`로 교체 → Task3 `launch(key, onError, block)` 중복 가드 + `postError`·`error` 채널 → Task4 `CollectAppError` 컴포저블. **기존 19개 ViewModel diff 0줄이 완료 조건**이다 — `postSideEffect` 시그니처가 같아 무수정으로 동작이 개선된다. RED 신호가 명확한 계획이다: Task2 Step3에서 "구독자 없을 때 발행 → 나중에 수집"이 현재 `SharedFlow` 구현으로 반드시 실패한다. 함정 셋을 본문에 박았다 — `runTest`를 인자 없이 부르면 Main 큐가 안 비고, `invokeOnCompletion`에서 무조건 `remove(key)` 하면 같은 key 새 job 엔트리를 지워 중복 방어가 뚫리며(`=== job` 비교가 방어), `catch (Throwable)`이 `CancellationException`보다 앞서면 취소를 삼킨다. 신규 테스트 15건. 스펙: [specs](../specs/archive/2026-08-13-mvi-error-infrastructure.md) · [ADR-0020](../../adr/0020-mvi-error-effect-infrastructure.md) **✅ 완료·develop 머지(2026-08-15, PR #241 `80895eb1`)** — SDD 4 Task, 커밋 5개(`0521e0bc..fd6b913d`), fix 라운드 0(전 Task 리뷰 1회 통과). **계획이 틀린 곳 1건**: Task 1 테스트의 `HttpException` 헬퍼가 저장소 기존 `ApiCallerTest` 패턴과 달라 실행 전 preflight 에서 정정했다(커밋 `3b2c54d`). **최종 전체 리뷰(opus)가 task 리뷰 전부를 통과한 코드에서 Important 2건을 잡았다** — `launch`의 가드가 `block` 안 자식 코루틴 실패를 못 잡아 원래 예외가 `viewModelScope`로 새어 앱이 죽던 경로, `CollectAppError`의 낡은 람다 캡처. 그 뒤 사용자 판정으로 **공용 `error` 채널 자체가 철회**됐다(커밋 `68c3e9e1`) — 계획·스펙 대비 가장 큰 이탈이고 이유는 ADR-0020 번복 절 |
| [2026-08-13-a002-kakao-login-api.md](archive/2026-08-13-a002-kakao-login-api.md) | A-002 카카오 로그인 API 결선(8 Task, **위 인프라 계획 선행 필수**). Task1 nonce 생성기 → Task2 `@SerialName` 정정(**RED가 곧 실서버 실패 재현** — `MissingFieldException: Field 'newUser' is required`) → Task3 `AuthRepository`(에러 변환 + 토큰 저장) → Task4 `LoginWithKakaoUseCase`(기존 회원이면 세션 저장) → Task5 `KakaoLoginHelper` nonce·`idToken`(**단독 컴파일 불가, Task6과 한 커밋**) → Task6 ViewModel 분기·로딩·실패 8케이스 로그 → Task7 `NavKeyTermAgree` data class화 → Task8 Route 배선 + 실기기 8항목. SDK 시그니처는 실물 대조로 확정했다(`loginWithKakaoTalk(context, requestCode, nonce, channelPublicIds, serviceTerms, callback)` · `OAuthToken.getIdToken()` nullable). 함정: **nonce를 두 번 만들면** SDK와 서버 값이 갈려 401이 나는데 응답에 단서가 없고, `token.accessToken`을 쓰는 현재 코드가 바로 그 오류이며, 기존 회원만 백스택을 지운다(신규는 약관에서 뒤로가기가 로그인으로 와야 함). 실기기 항목은 **결과만 보고하고 그 자리에서 고치지 않는다** — 평문 HTTP 차단이면 즉시 중단. 신규 테스트 20건. 스펙: [specs](../specs/archive/2026-08-13-a002-kakao-login-api.md) · [api/auth.md](../../api/auth.md) **✅ 완료·develop 머지(2026-08-15, PR #241 `80895eb1`)·실기기 미검증** — SDD 8 Task(5·6 통합 디스패치), 커밋 8개(`fd6b913d..e03b784c`), fix 라운드 0. **preflight 룰링 3건**: Task 5가 단독 컴파일 불가라 6과 합침 / `java.util.Base64` 채택(저장소의 `android.util.Base64`는 JVM 테스트에서 스텁이라 nonce 생성기를 못 덮는다) / 미사용 `registrationToken`은 전방 배선. **최종 리뷰(opus) Important 3건** — 로딩 플래그 누수로 버튼 영구 비활성, UseCase의 `Result` 계약 누수, 회전 시 로그인 유실. 계획 이후 추가된 것: `ServerErrorCode.Auth`(서버 코드 문자열을 도메인으로), `KEY_KAKAO_LOGIN`을 companion 으로, `runSuspendCatching` 도입(`EncryptedTokenStore.read`가 취소를 '토큰 없음'으로 보고하던 실제 버그를 잡았다). 저장소 전역 테스트 158 → **163건** |
| [2026-08-09-setting-danger-zone-popups.md](archive/2026-08-09-setting-danger-zone-popups.md) | 설정 Danger Zone 확인 팝업 3종 구현(5 Task, 브랜치 `feature/group-and-app-setting-pop-up`): 앱 설정 ViewModel(유닛 테스트 플러그인 신규 도입 + 탈퇴 팝업 Boolean 상태 TDD) → 앱 설정 화면·Route 배선 → 그룹 설정 ViewModel(`GroupSettingDialog?` 단일 enum + 기존 테스트 파일 확장) → 그룹 설정 화면·Route 배선 → 통합 검증. **신규 DS 컴포넌트 0** — 피그마 3팝업이 전부 기존 `YGModalPopup` 인스턴스라 `core:designsystem`은 한 줄도 안 건드린다. 함정 하나가 계획 전체를 지배한다: **파괴적 액션이 왼쪽 `secondaryText`, 취소("그만두기")가 시각적으로 강조된 오른쪽 `primaryText`** — 뒤집으면 취소하려던 사용자가 탈퇴하고, 유닛 테스트로는 절대 안 잡혀 Task 5 육안 체크리스트에 즉시 실패 항목으로 박아 뒀다. 상태를 VM에 두는 이유는 확인 핸들러가 곧 API 자리이기 때문이고, 그래서 기존 stub의 TODO 주석이 클릭 핸들러에서 확인 핸들러로 그대로 이사한다. 그룹 쪽만 enum인 것은 팝업이 둘이라 Boolean 2개면 "둘 다 켜짐"이 타입에 남기 때문(`when`에 `else`를 두지 않아 팝업이 늘면 컴파일러가 짚는다). **Compose `Dialog`는 별도 window라 `@Preview`에 안 뜬다** → 프리뷰 파라미터를 늘리지 않고 새 콜백 인자만 채워 컴파일만 통과시킨다. 문구는 마침표 유무까지 피그마 그대로(나가기 본문만 마침표 없음). 검증은 `testDebugUnitTest`(신규 4 + 확장 6) + `ktlintCheck` + `:app:assembleDebug` + 육안 7항목. 팝업 폭이 피그마와 다르면 **고치지 말고 보고**(DS 미결). 커밋하지 않는다. 스펙: [specs](../specs/archive/2026-08-09-setting-danger-zone-popups.md) | **완료(2026-08-09, 미머지)** — SDD 5 Task, 커밋 4개(`08b97005`~`5b3290e8`), **fix 라운드 0**(모든 Task 리뷰 1회 클린 통과). **계획서 커밋 정책은 실행 시점에 뒤집혔다** — SDD는 Task마다 `BASE..HEAD` diff로 리뷰 패키지를 만들어서 커밋이 없으면 리뷰 게이트가 못 돈다. 사용자가 Task별 로컬 커밋을 승인했고 push는 안 했다. **계획서가 틀린 곳 0건** — 코드블록이 그대로 컴파일·통과했고 as-built가 계획과 일치한다. 판정 1건: 확인 핸들러 2개의 1줄 중복을 리뷰가 plan-mandated Important로 올렸고 사용자가 "계획 유지"로 park(공통화하면 API 연동 시 되돌려야 함). **계획이 잘 작동한 지점**은 버튼 좌우 배치를 Global Constraints와 리뷰어 calibration(“뒤바뀜은 Critical”) 양쪽에 박아둔 것 — Task 2·4 리뷰어가 각각 `YGModalPopup`의 `Row` 자식 순서와 `YGButtonType` 색을 직접 대조해 브리프 코드를 그대로 믿지 않고 검증했다. **육안 확인은 기기가 보안 잠금이라 8항목 전부 미수행**이었으나, 최종 리뷰(opus)가 그중 문구·버튼 좌우·닫기 경로 3종·문자열 일치를 코드 대조로 판정 완료했고 실기기가 필요한 것은 3건(본문 soft-wrap 실제 위치·편집 중 팝업 왕복 후 IME 복귀·아이콘 틴트 피그마 대조)으로 좁혔다. 최종 리뷰 Critical 0·Important 0·Minor 3(전부 이월). 이월 minor 중 "테스트명 3세그먼트 정본" 지적은 **전제가 틀린 것으로 판명**(기존 `GroupSettingViewModelTest`가 2·3세그먼트 혼용) **✅ 2026-08-13 develop 머지(PR #225).** 머지 시점 재대조에서 **계획서가 틀린 곳 0건 · 드리프트 0건** — 계획의 코드블록이 그대로 as-built다. 계획 이후 추가된 것은 2026-08-10 독립 리뷰가 넣은 확인 핸들러 3종의 **멱등 가드**와 교차 방어 테스트뿐(커밋 `6965d901`). 테스트 as-built: `AppSettingViewModelTest` 5개, `GroupSettingViewModelTest` 16→24개 |
| [2026-08-07-s101-group-side-menu.md](archive/2026-08-07-s101-group-side-menu.md) | S-101 그룹 사이드 메뉴 화면 구현(7 Task, 브랜치 `feature/#211-S-101-group-side-menu` — **`develop`이 아니라 `feature/#215-test-environment`(PR #219) 위**): `YGColorChipType` 드리프트 정정(11번이 3번과 완전 중복이라 12·13이 한 칸씩 밀려 있었다, 9번 글자색이 테두리색과 동일) → `YGTextFormField`에 `keyboardOptions`·`keyboardActions` 전달 경로 신설(키보드 엔터 확정용, 기본값이라 기존 호출부 4곳 무영향) → **`GroupSettingViewModel` TDD**(테스트 12개 먼저 → 실패 확인 → 구현 → 기대값 뒤집어 FAIL 재확인) → strings + 화면 로컬 컴포넌트 2종 → `GroupSettingScreen` 조회 상태 → 편집 모드 하단 `확인` 버튼(`imePadding()`) → Route·클립보드 배선 + 통합. Figma `S-102`는 별도 화면이 아니라 이 화면의 `isEditing` 상태다. 확정 트리거는 버튼·엔터 둘이고 한 Intent로 모이며, 화면 콜백은 **확정 → `clearFocus()` 순서를 강제**한다(뒤집으면 포커스 상실이 먼저 편집을 취소해 확정이 무시되는 함정). 데이터는 Mock이고 Repository·UseCase 신설 0. 신규 DS 컴포넌트·에셋·토큰 0. 검증은 `testDebugUnitTest`(12) + 컴파일·ktlint + `:app:assembleDebug` + 프리뷰 6종·실기기 11항목이고, **호출 순서·`imePadding()`·클립보드·긴 문자열 레이아웃은 유닛 테스트가 못 잡아 육안이 유일한 그물**이다. 커밋하지 않는다. 스펙: [specs](../specs/archive/2026-08-07-s101-group-side-menu.md) | **완료(2026-08-09, 미머지)** — SDD 7 Task + 추가 라운드 5회, `develop` rebase 후 커밋 20개, 테스트 16개·프리뷰 13종. **커밋 정책은 실행 시점에 뒤집혔다**(사용자가 Task별 로컬 커밋 승인 — 리뷰 범위가 Task 단위로 잘리고 되돌리기가 쉬워서). 계획이 틀렸던 곳 3건: Task 1 grep 전제(`app-preview` 사용처 누락, 구현자가 잡음)·Task 2 호출부 목록 오기·**Task 6 하단 여백 처리 2회 번복**(리뷰 지적으로 측정 로직 삽입 → Figma 근거로 제거 → 실기기 결함으로 구조 변경). 실기기에서만 드러난 결함 2건(인셋 이중 계산으로 버튼이 떠오름, 편집 중 마지막 블록이 버튼 뒤에 갇힘)은 최종 리뷰가 예측했으나 기기 없이 확정 못 해 이월했던 항목이다. 범위 밖 추가(사용자 요청): 초대 문구 복사+2초 복귀·`YGTextFieldImpl` 최소 높이 48·`model` 패키지 분리·State 3필드 VO 치환·**ADR-0016 원안 수렴**(4개 화면 동시)·컴포넌트 프리뷰 7종 **✅ 2026-08-13 develop 머지(PR #223).** 실행 기록은 그대로 유효하고 머지 코드가 실행 결과와 일치한다. 계획서 Task 1의 grep 전제 오류(`app-preview`가 `NametagChip12`를 쓰고 있었다)는 구현자 정정본 그대로 머지됐다 — 이 오류가 스펙 본문에도 옮아 있어 스펙 쪽도 함께 정정했다 |
| [2026-08-11-member-parfait-image-api-service-layer.md](archive/2026-08-11-member-parfait-image-api-service-layer.md) | member·parfait-image API Service·DataSource 배선(5 Task, 브랜치는 **PR #229 위에 새로 판다** — 배치 요청이 그 PR의 `ImageId`를 받고 DI·`http/` 파일이 겹친다. #229의 `develop` rebase가 선행): 매퍼 테스트 이관 후 삭제 → member 슬라이스 → 토핑 배치 확정(POST) → 토핑 수정(PATCH) → `http/` 요청 모음 2건 + README 정정. **Task 1을 맨 앞에 둔 이유**는 2026-08-11 개정된 "매퍼 단독 테스트 금지" 규약을 브랜치에 먼저 안착시키기 위해서다 — 뒤에 두면 신규 Task의 "매퍼 테스트 없음"이 누락처럼 읽힌다. **삭제 전 이관이 먼저**이고 실제로 비는 케이스는 넷뿐이다(`expiresIn` 초 해석·두 URL 배선은 `ImageRemoteDataSourceImplTest`가 **이미** 단언하고 있어 옮길 것이 없다 — 계획서가 그 사실을 확인하고 목록에서 뺐다). 관용구가 답하지 않는 지점 하나: 배치 요청 필드 3개가 얽혀 있어(`SOLID`면 색·두께 필수) **domain 쪽만 sealed `ToppingBorder`**로 묶고 mapper가 평면 3필드로 편다 — wire DTO는 계약 문서와 눈으로 대조돼야 해서 평면을 유지한다. `ToppingTransform`은 `Double` 넷 연속의 순서 사고를 막으려 도입했으므로 **생성은 항상 named argument**(Global Constraints에 박음). PATCH는 팀 명세가 명시한 `partial update`를 nullable 5파라미터로 미러링하고, 안 바꾸는 필드가 `explicitNulls` 기본값 때문에 `null`로 실려 나가지만 서버 `?:` 병합이라 키 부재와 동치라는 것을 DTO KDoc에 남긴다. **Task 3·4를 나눈 것은 잠그는 규칙이 다르기 때문**(sealed→평면 변환 vs 부분 수정 null 의미) — 리뷰어가 한쪽만 반려할 수 있다. Kotlin이라 RED는 컴파일 실패(`Unresolved reference`)이고, domain 타입·DTO·mapper가 테스트보다 앞서는 이유(테스트 컴파일에 필요한 선언이지 로직이 아니다)를 리뷰어용 메모로 명시했다. `http/parfait-image.http`는 **선행 요청이 셋**(로그인 → 그룹 → 이미지 confirm)이고 `parfaitId`는 조회 API가 없어 리터럴을 손으로 바꿔야 한다 — upsert가 새 행을 안 만드는 것·서버 좌표 검증 부재·PATCH의 403 비대칭처럼 **유닛 테스트로는 못 잡는 계약**을 대조용 요청으로 재현하게 짰다. 같은 Task에서 `http/README.md`의 사실 오류 2건도 고친다(판별자 키 `newUser`→`isNewUser` 정정, `AuthErrorCode` 12→14종 + enum 2개 추가). 검증은 `./gradlew test ktlintCheck :app:assembleDebug` + 매퍼 테스트 0건 grep. Task별 로컬 커밋 5개, **push·PR은 사용자 승인 게이트**. ⚠️ **런타임 검증 0은 그대로** — 평문 HTTP 차단·소비처 부재. 배치 목록 조회 API가 없어 `http/`로도 "배치가 남았는가"를 직접 조회할 수 없고 `parfaitImageId` 비교가 간접 확인이다. 스펙: [specs](../specs/archive/2026-08-11-member-parfait-image-api-service-layer.md) | **완료(2026-08-11, 미머지·미푸시)** — SDD 5 Task, 브랜치 `feature/member-parfait-image-api-260811` 커밋 7개(`40e8e5a6`~`24dd7c11`), **Task 리뷰 fix 라운드 1회**(Task 5만). 계획서 코드블록이 거의 그대로 컴파일·통과했고 as-built가 계획과 일치한다. **계획서 자체의 결함 3건**: ① Task 2의 리터럴 한 줄이 **정확히 120자**라 ktlint `standard:function-signature`가 시그니처+본문 합치기를 강제(구현자가 포맷만 재조정) ② Task 4의 RED 예고가 `Unresolved reference`인데 Step 순서상 실제로는 "Impl이 추상 멤버 미구현"이 난다(인터페이스가 테스트보다 먼저 생기므로 전자는 불가능) ③ 스펙 테스트 표가 `INVALID_BORDER`를 잠근다고 적었으나 그건 sealed 때문에 **앱에서 도달 불가능**하다 — 구현자가 `IMAGE_NOT_CONFIRMED`로 치환했고 최종 리뷰가 "구현이 스펙보다 낫다"고 판정. **실행 중 계획서 밖의 것 하나가 라운드를 구했다**: Task 1 리뷰어의 ⚠️를 파 보니 `:app` Hilt 컴파일 에러가 다른 브랜치 빌드 잔재였고(`CreateGroupUseCase` 등 두 클래스가 추적 소스에 0건), `clean` 전까지 Task 2~4의 `:app:assembleDebug` DI 게이트가 통째로 무의미했다. **최종 전체 리뷰(opus) Critical 0 · Important 1** — 그마저 코드가 아니라 `http/parfait-image.http` 주석의 자기모순("다른 도메인"이라 경고해 놓고 4줄 뒤에 "같은 도메인")이었다. fix 웨이브 1회로 7건 반영·재검수 전부 ADDRESSED·새 breakage 0. 이월 minor 5건 전부 "이월 가능"(테스트명 2세그먼트 건은 **규약 위반이 아니라 기존 관행과 일치**함이 확인됐다). 사람 결정 이월 1건: `parfait_image_id`가 `http-client.env.json`·`_reset.http` 미등록(계획이 금지했으나 형제 변수는 전부 등록돼 있다). 실서버 요청은 이번에도 0건. **✅ develop 머지(PR #230, 2026-08-12)** — 두 라운드가 `feature/sync-backend-api-260810` 한 브랜치·한 PR로 나갔다(PR #229는 별도로 머지되지 않음). 머지본 재대조 **계획과 갈린 곳 0건**. Task 5의 `http/README.md` 정정 ①이 develop 안에 자기모순을 남겼다 — README는 판별자 키를 `isNewUser`로 적는데 같은 디렉토리 `http/auth.http`는 여전히 `newUser`를 가르치고 응답 핸들러도 그 키를 읽는다(앱 DTO도 그대로). |
| [2026-08-10-image-api-service-layer.md](archive/2026-08-10-image-api-service-layer.md) | image API Service·DataSource 배선(3 Task, 브랜치 `feature/sync-backend-api-260810` — 이미 존재하고 `develop`과 동일·커밋 0): DTO·domain 타입·VO 매퍼(+매퍼 테스트 6) → Service·RemoteDataSource·DI(+DataSource 테스트 8) → `http/images.http`. **Task 경계는 "변환 규칙" vs "호출 경로"** — 리뷰어가 한쪽을 반려하며 다른 쪽을 승인할 수 있는 자리다. Kotlin이라 **RED는 실행 실패가 아니라 컴파일 실패**(`Unresolved reference`)이고, 기존 코드 특성화가 아닌 신규라 논리적 RED가 약한 매퍼는 Task 1 Step 7에서 `.seconds`를 `.milliseconds`로 뒤집어 FAIL을 확인하고 되돌린다. 매퍼 테스트는 **결정이 있는 곳만** — status 문자열 파싱(UNKNOWN 폴백·대소문자)과 초→`Duration`, 그리고 `uploadUrl`·`imageUrl`이 **둘 다 String이라 뒤바꿔도 컴파일러가 못 막는** 배선. DataSource 테스트는 `slot`으로 요청 바디를 잡아 `imageType.name` 문자열화와 `fileName` 통과를 확인하고, `confirmUpload`가 `ImageId`를 Retrofit 경계에서 raw `Long`으로 벗기는지 `coVerify(exactly = 1)`로 잠근다. **409 `IMAGE_ALREADY_CONFIRMED`를 성공으로 번역하지 않는 것도 테스트로 고정**한다(서버가 소유자를 검증하지 않아 "내가 이미 했다"와 "남이 했다"가 구분되지 않는다). suspend는 전부 `coEvery`/`coVerify` — `every`를 쓰면 `Continuation` 인자가 안 맞아 `MockKException`이 난다. 검증은 `./gradlew test ktlintCheck` + `:app:assembleDebug`(Hilt 바인딩 누락은 여기서 `[Dagger/MissingBinding]`으로 잡힌다). Task별 로컬 커밋 3개, **push·PR은 사용자 승인 게이트**. ⚠️ **런타임 검증 0은 그대로** — 평문 HTTP 차단·`YG_BASE_URL` 부재라 `@SerialName` 오타·단위 오해는 유닛 테스트가 매퍼까지만 잡고, wire 형태와 실응답의 일치는 `http/images.http`를 사람이 쏘는 것이 유일한 그물이다(쏘기 전까지 계약 일치는 미검증이며 완료 보고에 명시). 스펙: [specs](../specs/archive/2026-08-10-image-api-service-layer.md) | **완료(2026-08-10, 미머지·미푸시)** — SDD 3 Task, 커밋 4개(`abd4b99a`~`f6f76813`), **Task별 리뷰 fix 라운드 0**(3회 전부 1회 클린 통과). 계획서 코드블록이 그대로 컴파일·통과했고 as-built가 계획과 일치한다. **계획서 자체의 결함 2건을 최종 리뷰(opus)가 잡았다** — 둘 다 `http/images.http` 주석이고 둘 다 Important였다: ① "(서버 소관 미결. **5번에서 재현할 수 있다**)"인데 5번은 존재하지 않는 imageId(404) 테스트라 소유자 미검증을 재현하지 못한다 ② "Content-Type이 발급 때와 **달라야 하면** 거절한다"가 요구사항처럼 읽혀, 이 파일이 잡으라고 만들어진 함정(서버 로그에 안 남는 실패)으로 사람을 미는 문장이었다. **교훈은 사전 검수의 한계** — 실행 전 독립 검수가 코드 축(시그니처·import·ktlint·DI 앵커)을 9건 잡아냈지만 **산문 주석의 사실성은 통과시켰다**. 계약 문서와 코드를 대조하는 눈으로는 "5번"이 무엇인지 세지 않는다. fix 웨이브 1회로 4건(위 2건 + `EmptyBody` 단언 선례 정합 + 새 전역 변수 2개를 `http-client.env.json`·`_reset.http`에 미등록) 해소, 재검수 전부 ADDRESSED·새 breakage 0. **다음 라운드로 넘긴 발견 4건**은 open-questions 등록: **`AuthInterceptor`가 S3 PUT에 Bearer를 붙인다**(`@NoAuth` 판정이 Retrofit `Invocation` 태그를 읽는데 raw OkHttp 요청엔 태그가 없다 → presigned URL에 `Authorization`이 실려 S3가 거절 → **전용 클라이언트 분리는 성능이 아니라 기능 전제**), presigned URL이 debug 로그 `Level.BODY`로 노출(응답 본문이 곧 자격증명인 첫 사례), `domain/repository/image/`·`domain/usecase/image/`가 이미 기기 이미지 뜻으로 선점됨(다음 라운드가 이름 정하기 전에 결정 필요), `expiresIn`이 상대값인데 발급 시각 미기록. 파킹: KDoc 계약 사실이 6곳에 복제됨 **✅ develop 머지(PR #230, 2026-08-12)** — 두 라운드가 `feature/sync-backend-api-260810` 한 브랜치·한 PR로 나갔다(PR #229는 별도로 머지되지 않음). 머지본 재대조 **계획과 갈린 곳 0건**. File Structure 표의 `ImageVOMapperTest.kt`만 develop에 없다 — 다음 라운드 Task 1이 케이스 이관 후 삭제했고 브랜치 안에서 생겼다 사라져 머지 diff에 안 보인다. |
| [2026-08-10-ci-gradle-cache-seeding.md](archive/2026-08-10-ci-gradle-cache-seeding.md) | CI Gradle 캐시 시딩(3 Task, 브랜치 `chore/build-action-cache` — 이미 존재하고 `develop`과 동일): `gradle.properties` 빌드캐시·병렬·힙 → `gradle-cache-seed.yml` 신설 → 제출·머지 후 적중 검증. 파일 2개만 건드리고 기존 `test.yml`·`ktlint.yml`·합성 액션은 무변경(**생산자/소비자 분리**). **Task 순서가 뒤바뀌면 안 된다** — `org.gradle.caching`이 꺼진 채 시딩 job을 붙이면 태스크 출력 저장소가 비어 아무것도 안 데워진다. 검증은 코드가 아니라 **관측**이다: Task 1이 `clean` 후 재빌드로 `from cache` 0건(RED) → N건(GREEN)을 로컬에서 잡고, `clean`이 프로젝트 `build/`만 지우고 `~/.gradle/caches/build-cache-1`은 남기는 비대칭이 이 관측을 가능하게 한다. 병렬 실행이 빌드를 깨면 모듈 간 미선언 의존이 있다는 뜻이라 우회하지 말고 보고(축소 경로: `parallel`만 빼고 캐시는 유지). YAML 검증은 actionlint·PyYAML 부재로 **macOS ruby psych**를 쓰고 `on:`이 `true` 키로 파싱되는 YAML 1.1 동작은 정상이다. 태스크 경로 오타는 `--dry-run`으로 잡는다. **`cache-read-only: false`로 전환된다는 전제는 로컬에서 검증 불가** — `develop` 머지 후 seed 런 로그로만 확인되고, 틀리면 합성 액션에 입력을 뚫는 후속이 붙는다. Actions 로그 grep은 제어문자 때문에 `-a` 필수. 커밋 3종(TJYG-Android 로컬 커밋·push·PR) 전부 사용자 승인 게이트. configuration cache는 범위 밖. 스펙: [specs](../specs/archive/2026-08-10-ci-gradle-cache-seeding.md) | **완료(PR #227 develop 머지, 2026-08-10)** — SDD, 브랜치 `chore/build-action-cache` 커밋 4개(`1db48217`~`027631a3`), Task별 리뷰 **fix 라운드 0**. Task 3(push·PR·머지 후 적중 판정)까지 완료 — 머지 후 seed 런 `31402546861`이 `cache-read-only: false`로 캐시를 저장했고, 후속 PR `unit-test` 런 `31403327032`에서 `Gradle User Home cache not found`가 사라지며 `368 from cache`·소요 6m16s → 2m45s로 성공 판정 2조건을 모두 만족했다. 워크플로 as-built는 계획 Step 2 대비 `workflow_dispatch`·`concurrency`·`timeout-minutes`·`continue-on-error`가 더 붙은 스펙 확정본이다. **최종 전체 리뷰(opus)가 계획의 핵심 전제 하나를 사전 해소했다** — "머지 후에만 확인 가능"이라던 `cache-read-only` 전환이 `setup-gradle@v4` 소스에서 정적으로 검증됨(`github.ref_name != default_branch`: push/develop → `false`, PR은 `ref_name`이 `25/merge` → `true`, 관측 로그와 일치). 복원 키가 job-agnostic `\|<os>-<arch>` 프리픽스로 폴백해 `seed` → `unit-test`/`lint` 연결도 성립. **리뷰 Critical 0 · Important 3**(전부 fix 웨이브 1회로 해소): Kotlin 데몬이 `-Xmx`를 상속해 힙 상향이 4GB+4GB 예약이 되던 건, `--continue`가 종료 코드를 성공으로 만들지 않아 seed 실패가 develop을 빨갛게 만들던 건, 메타스페이스 주석 오기. **그 뒤 사용자 판단으로 `parallel`+힙 전체를 되돌렸다** — 참고 워크플로 검토 중 나온 질문에서 출발해, 병렬 유무로 측정치가 동일함(`447 from cache` 양쪽 일치)을 실측 확인하고 캐시 한 줄만 남겼다. 남은 것은 `org.gradle.caching=true` + `gradle-cache-seed.yml` 둘뿐. 계획서 자체가 틀린 곳 0건. 진행 원장은 `.superpowers/sdd/2026-08-10-ci-gradle-cache-seeding/progress.md` |
| [2026-08-06-unit-test-infrastructure.md](archive/2026-08-06-unit-test-infrastructure.md) | 유닛 테스트 기반 구조 구현(9 Task, 브랜치 `feature/#215-test-environment`): 카탈로그·`parfait-test-unit`·`core:util:jvm` 첫 테스트 → `:core:testing` 모듈 + `MainDispatcherRule` + `domain` → `DayWindow` `Clock` 주입(유일한 프로덕션 변경, 이 Task만 진짜 RED가 성립) → `data` 배선 + `VOMapper` → `PolicyRemoteDataSourceImpl`(MockK) → `AuthInterceptor`(MockWebServer + Retrofit 실제 구성, `Invocation` 태그 때문에 Request 직접 조립으로는 검증 불가) + `ApiCaller` → `parfait-test-android` + 계측 스모크 → `parfait-test-compose` + Compose 스모크 → CI. 기존 코드 특성화 테스트가 대부분이라 논리적 RED가 없어, 각 Task는 **기대값을 일부러 뒤집어 FAIL을 확인한 뒤 되돌리는 절차**로 테스트가 실제 구현을 호출함을 증명한다. 공용 Fake는 이번에 만들지 않는다 — 쓰이는 곳이 없어 미리 만들면 첫 사용자가 다시 고친다. 최종 검증에 `runBlocking`·`isReturnDefaultValues`·백틱 메서드명 grep 포함. **PR #219 리뷰 반영 3건(2026-08-09)**: CI 셋업을 composite action 2개로 공통화(+`setup-gradle` 전환으로 수동 캐시·cleanup·chmod 제거), `:core:testing`의 `api` → `implementation`(전제로 `setConfigTestAndroid()`의 `:core:testing` 의존 제거), VO 매퍼 테스트 6건 → 4건 + `PolicyVOMapperTest` 개명. 스펙: [specs](../specs/archive/2026-08-06-unit-test-infrastructure.md) | **완료(PR #219 develop 머지, 2026-08-09)** — Task 1~9 전량, 커밋 17개. Task 10(Fake 3종)은 보류 유지 |
| [2026-08-02-network-envelope-token-storage.md](archive/2026-08-02-network-envelope-token-storage.md) | `:data` 서버 계약 정합(6 Task): `ApiResponse` 필드 보강 + 성공 판정 `success`로 교체(`isSuccess`·`SUCCESS_CODE` 제거) → **`SafeApiCall` top-level → `ApiCaller` 클래스 승격** + `HttpException` 바디 envelope 파싱(파싱 실패·바디 없음은 `Http`로 폴백 — 인프라 계층 429·502는 envelope 없이 온다) + 진입점 3개(`safeApiCallNoContent` 신설, 204용) + `Business`에 `statusCode: Int?`·`errorDetail` 추가 + `TempRemoteDataSourceImpl` 주입 교체 + `SafeApiCall.kt` 삭제 → `CryptoManager`(Keystore AES/GCM, IV 매회 신규 + `IV+암호문` Base64 결합, `ENCRYPTION_PADDING_NONE`) → `TokenStore`/`EncryptedTokenStore`(**복호화 예외를 전파하지 않고 `clear()` 후 null** — 전파하면 인터셉터에서 터져 전 요청이 죽고 앱 삭제 전까지 복구 불가) → `TokenStoreTokenProvider`(`runBlocking`, 근거는 계획서·ADR에 기록) + DI 배선(`NetworkModule`은 `object`라 `@Binds` 불가 → `@Provides`가 구현체 주입받아 반환, `TokenStore`는 `LocalDataSourceModule`에 `@Binds`) + `EmptyTokenProvider` 삭제 + `:app:assembleDebug` Hilt 그래프 → ADR-0019 신설·ADR-0017 5항목 갱신·data-layer 반영. **테스트 없음**(`test`·`androidTest` 0건, Keystore는 JVM 불가) → compileDebugKotlin + ktlint + assembleDebug + **실기기 암복호화 왕복 육안**(앱 완전 종료 후 재시작, DataStore 파일에 평문 없는지 `adb shell run-as`로 확인). 미검증: 키 유실 경로. **TJYG-Android는 커밋하지 않는다**(작업 트리 변경만). 스펙: [specs](../specs/archive/2026-08-02-network-envelope-token-storage.md) | **완료(PR #190 develop 머지, 2026-08-04)** — Task 1~6 전량 반영, Task 5 Step 7(실기기 암복호화 왕복)만 `TokenStore.save()` 호출부 0건이라 미수행·이월 |
| [2026-07-22-s002-account-info.md](archive/2026-07-22-s002-account-info.md) | S-002 계정 정보 화면 구현(6 Task): ~~domain `CheckNameValidUseCase`에 `CheckEmpty` 규칙 추가~~(**#179로 선행 머지 — Task 1 skip**) → AccountInfoViewModel(MVI, 실시간 검증) → strings → AccountInfoScreen(YGTopBarDetail·YGLabel·YGTextFormField·`clearFocusOnTap()`·프리뷰) → Route 배선 → 통합 빌드/동작 확인. 테스트 없음(compile+ktlint+프리뷰 검증). 🔁 **2026-08-03 3차 애드덤 — Task 1~5 구현 완료(체크박스 미체크), as-built 4곳 상이**: Danger Zone은 S-001로 이관·State 필드명 `nickname`/`errorMessageResId`·상단바 `YGTopBarDetail`+라벨 `YGLabel`·포커스 해제 opt-in Modifier화. 계획 재실행 금지, 정본은 스펙. 스펙: [specs](../specs/archive/2026-07-22-s002-account-info.md) **✅ develop 머지(#192, 2026-08-04)** — `status: done`·archive 이동. 체크박스는 미체크로 남기고 종료(Task 6 실기기 확인 기록 없음). 산출물 계약은 스펙이 정본. |
| [2026-08-01-designsystem-bar-listdate-components.md](archive/2026-08-01-designsystem-bar-listdate-components.md) | Figma `List-Date`·`Floating Bar` 신설 + `Top Bar` Canvas 변형(4 Task): `YGListDate`(`YGDateButton` 44dp + `YGChipColorIndicator`를 `gap1`로 쌓은 44×50, 미업로드는 `Transparent`라 자리 유지) + 갤러리 `BUTTON` → `YGFloatingBar` 4변형(`BackClose`/`Close`/`Edit`/`EditTab` 공개 함수 + private `YGFloatingBarContent`, `Close`만 `Arrangement.End` — SpaceBetween에 자식 1개면 좌측으로 붙어 Figma `justify-end`와 어긋난다) + 갤러리 `BAR`(EditTab은 `remember`로 탭 전환 실검증) → `YGTopBarContent`에 `contentPadding`·`trailingContent` 2개 추가(기본값이 현재 동작과 동일 → 기존 4변형 무영향, `trailingContent`는 `weight(1f)` Row 바깥 형제) + `YGTopBarCanvas` 신설(사방 `padding3`, 우측 정렬은 안쪽 Row arrangement 대신 `titleContent` 안 `Spacer(weight(1f))`로 — arrangement를 건드리면 나머지 4변형에 영향) + 프리뷰 칩 색 정정(`CherrySolid`→`CherrySubtle`. **2026-08-01 개정** — 원안의 `YGTopBarDefault` 드리프트 제거는 #173이 그 변형을 삭제·`Empty(rightContent)`로 통합하면서 대상이 사라졌고, 칩 색·문구는 호출 화면 G-001이 이미 정본대로 쓴다) + 갤러리에 List-Member 조립 예시(`Arrangement.spacedBy((-12).dp)`, 마지막 `NametagChipPlus` `+7`) → repo 전체 빌드·ktlint·실기기 육안 대조 + 문서 갱신. 세 컴포넌트가 서로를 소비하지 않아 Task 1~3은 독립이고, 부품 5종(`YGDateButton`·`YGChipColorIndicator`·`YGCircleButton`·`YGEditTabButton`·`YGIconButton`)은 **한 줄도 고치지 않는다** — 기존 파일 수정은 Task 3의 `YGTopBar.kt`뿐. Task 3만 `:app:assembleDebug`까지 도는데 `YGTopBar*`가 feature 모듈 실사용이라 시그니처 회귀를 거기서 잡아야 하기 때문. 신규 토큰·에셋 0(필요 심볼 전량 2026-08-01 실재 확인). 테스트 없음(assembleDebug+ktlint+프리뷰/실기기). 육안 결함은 **컴포넌트 vs 갤러리 화면**을 먼저 가른다(선행 라운드 결함 4건 중 3건이 갤러리 한정). **Task 1~4 전량 실행 완료** — Task 1·3은 리뷰 1회 클린 통과, Task 2만 fix round 1회. **실행 도중 베이스가 바뀌었다**: 중단·재개 사이 `develop`이 나아가고(#173) 브랜치도 `feature/bar-listdate-component`→**`feature/sync-component`**로 옮겨졌으며 `YGTopBarDefault`가 삭제돼 Task 3이 낡았다. 교훈: 며칠에 걸치는 계획은 Task 착수마다 대상 파일 현재 상태를 다시 읽어야 한다. **계획 자체의 결함 2건을 최종 전체 리뷰가 잡았다**(Task 단위 리뷰 3회는 전부 통과): Task 3 Step 2의 `Text`→`Spacer(weight(1f))`→`memberContent()` 배치와 Task 2 Step 1의 `YGFloatingBarEdit` 중앙 `Text`가 같은 측정 버그 — 가중치 없는 `Text`가 먼저 측정돼 긴 문자열이 옆 요소를 0dp로 민다. plan-mandated 충돌이라 질의 후 `weight(1f)`+`maxLines`+`Ellipsis`로 수정. **프리뷰·실기기가 놓친 이유는 샘플 제목이 전부 4자였기 때문**이라 두 프리뷰에 긴 제목 변형을 상설했다 — 계획서에 UI 코드를 박을 때 사용자 입력 텍스트마다 긴 문자열 케이스를 함께 지정해야 한다는 것이 이 라운드의 수확. Task 2 리뷰의 중복 지적(닫기 4회·확인 2회)도 plan-mandated라 질의 후 private 컴포저블 2개 추출로 개정. 재리뷰가 import 정렬을 오판한 1건은 컨트롤러가 직접 확인해 뒤집었고 최종 리뷰가 확인(`.editorconfig`가 import-ordering 룰 off). **검증은 컨트롤러가 직접 수행** — repo 전체 빌드·ktlint 통과, 실기기(SM-A356N) 스크린샷 대조 3화면(Task 1 리포트의 gradle 출력 신뢰 불가 이월 건도 이로써 해소). **2차 라운드 Task 5~7 추가·완료**(Figma 재조회): 칩 프리셋 `CherrySubtle`→`GrayOutline` 교체·개명(소비처 6곳) → `YGTopBarEmpty` 날짜 파라미터 + `White75` 배경 + `GraphicsLayer`/`BlurEffect` 배경 블러 → 실기기 검증·문서. Task 5·6 모두 리뷰 1회 클린 통과. **블러 구현이 두 번 뒤집혔고 그 사이 오검증이 있었다** — 초안 haze → `BlurEffect` 질문으로 자체 `GraphicsLayer` 전환 → **자체 구현이 블러를 전혀 걸지 않는데 "동작 확인"으로 보고**(틴트만으로도 대비가 낮아져 흐린 것처럼 보였다) → 40dp 극단값 대조로 발각, 세 형태 시도 후 **haze로 복귀·동작 확인**([ADR-0018](../../adr/0018-backdrop-blur-haze.md)). 교훈 둘: **블러는 극단값 대조로 검증한다**(사양값 한 번으로는 미동작이 통과한다), 그리고 Figma MCP의 블러 값은 CSS 환산값이라 저작값을 인스펙터에서 확인해야 한다. **계획서 결함 1건** — Task 6 import 목록에 `androidx.compose.ui.graphics.layer.drawLayer` 누락(`GraphicsLayer`와 별개 top-level 확장이라 명시 import 필요, 본문은 2회 호출). 구현자가 발견·보충, 리뷰 확인. **검증은 컨트롤러가 직접** — 구현자가 IDE를 못 열어 육안 검증을 빠뜨렸고 리뷰가 그것을 유일한 Important로 지적했으나, 컨트롤러가 실기기(API 36)에서 이미 마친 상태였다(칩 색·날짜·블러 동작·**스크롤 후 좌표 정합 유지**). **TJYG-Android 커밋 안 함**(작업자 지시), 커밋이 없어 SDD 워크스페이스가 유일한 실행 기록. **브랜치 코드리뷰가 계획서 결함 2건을 추가로 잡았다(2026-08-01)** — Task 4 Step 4의 "`GroupListScreen`은 아직 날짜 데이터가 없다" 전제가 사실이 아니어서(ViewModel이 이미 `init`에서 계산 중) 상단바만 `"December 31"` 고정 노출로 남았고, Task 1 코드블록이 C-201 예외 조항을 옮기지 않아 `YGListDate`가 `isEnabled=false, isUploaded=true` 위반 조합을 렌더했다(Task 1 Step 3 갤러리가 실제로 그 조합을 노출). 각각 `uiState` 결선 / `isChecked = isEnabled && isUploaded`로 수정. 두 코드블록은 역사 스냅샷으로 남기고 결함 노트를 달았다. 스펙: [specs](../specs/archive/2026-08-01-designsystem-bar-listdate-components.md) **✅ develop 머지(#188, 2026-08-04)** — 미커밋 상태로 남아 있던 브랜치 `feature/sync-component`를 작업자가 커밋해 머지했고, 머지 코드와 계획 산출물의 갈림은 없다. 계획 본문 체크박스는 실행 기록을 상단 블록에 모으는 관례대로 미체크로 둔다. |
| [2026-08-03-data-api-service-layer.md](archive/2026-08-03-data-api-service-layer.md) | `:data`·`:domain` 서버 API 표면 14 엔드포인트(6 Task): domain value class 10종(토큰 3·ID 4·이름값 3)+VO(sealed `KakaoLoginVO`·`AuthSessionVO`·`PolicyVO`/`PolicyType`·group 7종, `expiresIn`은 `kotlin.time.Duration`) → `AuthService`(4) → `PolicyService`(1) → `ParfaitGroupService`(8) → `ParfaitService`(1) → `Temp*` 7파일 삭제+통합 검증. Task 2~5는 상호 무의존이고 공유 파일은 `ServiceModule`·`RemoteDataSourceModule` 둘뿐(추가만), Task 1이 전체 선행·Task 6이 마지막. **Service 함수명은 계약 미러**(`postAuthKakao`·`getParfaitGroupsByGroupId`·`deleteParfaitGroupsByGroupIdMembersMe`), 의미 이름은 DataSource가 갖는다. 함정 반영: `@SerialName("newUser")`, logout 반환 타입 `Unit`+`safeApiCallNoContent`(204라 envelope 없음), `@NoAuth` 정확히 4곳(logout 제외), `recentImageUploadedAt`은 **Asia/Seoul 벽시계** → `kotlinx.datetime.LocalDateTime.parse`. value class 경계는 domain까지 — DTO·Retrofit 시그니처는 raw. **테스트 없음**(repo 소스셋 0) → Task별 `:data:compileDebugKotlin`+ktlint, Task 6에서 `:app:assembleDebug`로 Hilt 그래프. **Task 1~6 전량 완료, Task 단위 리뷰 전부 클린 통과**(Task 2·5는 minor deferred 관찰만 — Task 2는 리포트 서술 과대, 코드 결함 아님; Task 5는 응답 DTO 파일명이 계획의 `ParfaitResponses.kt`에서 `ParfaitYearsResponse.kt`로 정당하게 이탈, ktlint `standard:filename`이 단일 선언 파일에 강제). **최종 전체 브랜치 리뷰(opus): Critical 0**(계약 대조 — 14 경로·요청 9/응답 15 DTO 전 필드·`@SerialName`·`@NoAuth` 4곳·mapper 교차대입 0건 전량 일치) **· Important 2 · Minor 3**, 사용자 확정 후 fix 웨이브 1회로 5건 전부 해소(스코프 재리뷰 ADDRESSED 확인). I1: `@NoAuth` keep 규칙이 `:data`(Android 라이브러리) `proguardFiles`엔 있었으나 `consumerProguardFiles` 미등록으로 앱 R8 실행에 전달되지 않음 — release였다면 화이트리스트 4경로에 헤더가 붙어 토큰 재발급이 막혔을 결함. `consumer-rules.pro` 이관 + `setConfigAndroidLibrary`에 `consumerProguardFiles` 등록으로 수정. I2: `kotlin.Result.map`이 `transform` 예외를 재던져 매퍼가 `safeApiCall` 가드 밖에서 실패 — `ApiCaller`에 `safeApiCall(block, transform)` 오버로드 신설, 13개 호출부(auth 3·policy 1·group 8·parfait 1) 전환, 잘못된 KDoc 2곳 정정. Minor 3건: `AuthResponses` KDoc의 `coerceInputValues` 서술 오류 정정 / `ParfaitGroupRemoteDataSourceImpl` 인라인 변환 2곳(`toGroupName`·`toGroupId`)을 mapper로 이관 / `:domain`의 `kotlinx-datetime`을 `implementation`→`api`로 승격했다가 **사용자 판단으로 되돌렸다**(`KotlinJvmConfig`·`DependencyHandler` 원복). 지적 자체가 이론적이었다 — 실제로 깨진 소비자가 0이고 `AndroidConfig`가 모든 Android 모듈에 같은 의존성을 따로 넣어준다. 반면 `setConfigKotlinJvm`은 `:domain`과 `:core:util:jvm`이 공유해 승격이 소비자 5곳까지 전이 노출을 넓혔다(재리뷰가 부작용으로 지적). `api` 지양 관례를 지키는 쪽이 이득이 크다는 판단. deferred minor 1건(`ApiCaller.safeApiCallWithoutData`가 14 엔드포인트 전량 구현으로 사실상 死코드, HEAD 기존 코드라 이번 delta 아님 — 별도 정리 이월). 검증 4종(`:domain:compileKotlin`·`:data:compileDebugKotlin`·`:domain:ktlintCheck :data:ktlintCheck`·`:app:assembleDebug`) fix 웨이브 후 전부 재실행 통과. **TJYG-Android 커밋 상태**: 라운드 종료 시점엔 미커밋이었으나, 이후 사용자가 `Temp*` 삭제(`07715737 refactor: 임시 코드 제거`)를 시작으로 브랜치 `feature/sync-api-service`에 순차 커밋했고, 이 문서 갱신 시점엔 나머지 구현분(`358bd430`~`f1d5d6a2`, 9개 커밋)까지 전량 커밋 완료된 상태다(작업 트리 클린) — SDD 원장(`.superpowers/sdd/2026-08-03-data-api-service-layer/`)은 커밋 이전 세션의 실행 기록으로 남는다. 이후 커밋에는 **이 라운드가 끝난 뒤의 추가 리팩터**도 섞여 있다: DTO·도메인 값 객체 그룹 파일 10개(`AuthResponses.kt`·`Ids.kt`·`ParfaitGroupVO.kt` 등)를 선언당 파일 39개로 재분리(ktlint `standard:filename`이 단일 선언 파일에만 걸려 그룹 파일이 그동안 이 규칙을 피해 있었다), 요청/응답 DTO 59개 프로퍼티 전체에 `@SerialName` 명시(키=프로퍼티명이어도 예외 없이). ⚠️ **런타임 미검증**으로 들어간다 — 평문 HTTP 차단·`YG_BASE_URL` 부재로 앱에서 호출 자체가 불가, `@SerialName` 누락류 결함은 컴파일·lint·런타임 어디서도 안 잡히는 채로 이 라운드를 통과한다 → [open-questions](../../synthesis/open-questions.md). 스펙: [specs/archive](../specs/archive/2026-08-03-data-api-service-layer.md). **PR #197로 develop 머지(2026-08-06)** — Task 1~6 산출물 전량 반영 확인, R8 keep 규칙 이관분도 함께 머지. |
| [2026-08-01-parfait-api-contract-docs.md](archive/2026-08-01-parfait-api-contract-docs.md) | `parfait/api/` 서버 API 계약 문서 체계 신설(6 Task, **코드 변경 0** — TJYG-Android·TEAMYG-SERVER 양쪽 무수정): 골격 4파일(`template`·`conventions`·`server-baseline`·`README`) → `auth.md`(4) → `parfait-group.md`(8) → `parfait.md`(1) → 스킬 `sync-teamyg-server-api` + 기존 `sync-tjyg-develop-baseline`에 경계 문구 → 연동(`index.md` 라우팅·`open-questions` 8건·private submodule 경로·링크/민감정보 검사·스펙·계획 아카이브). **추적 브랜치는 서버 `main`**(`origin/HEAD -> origin/main`, 기능 PR이 main으로 머지 — `develop`은 뒤처져 signup·파르페 연도 조회 2 API가 빠져 있었다. TJYG-Android는 `develop` 추적이라 두 저장소 통합 브랜치 이름이 다름). **실행 중 기준선이 `6b05b8c`→`6f5bffc`로 전진**(2026-08-02, 사용자 승인) — auth가 신규 엔드포인트 2건(`reissue`·`logout`)을 더 얻어 2→4, `AuthErrorCode` 11→12종, 화이트리스트가 `/api/v1/auth/**`에서 `kakao`·`signup`·`reissue` 개별 3경로로 좁혀지고 `logout`은 인증 대상으로 남음. 그룹 계약은 불변. 모든 서버 조회는 `git show origin/main:<path>` — 로컬 워킹트리는 `develop`이라 믿지 않는다. 계약 데이터(엔드포인트·필드·에러코드)는 계획서에 실측 전재해 구현자가 서버를 다시 훑지 않아도 되게 했고, **에러코드의 엔드포인트별 귀속·`expiresIn` 단위·`LocalDateTime` 직렬화 포맷·타임존·그룹명/닉네임/인원 유효성 규칙·파르페 조회 권한 검사 유무는 core 계층을 직독해 전부 확정**했다(회원 전역 닉네임 규칙 동일성 1건만 조사 범위 밖으로 미결 이월). 테스트 없음(문서) — 검증은 서버 소스 1:1 재대조 + 링크 resolve + 민감정보 grep, 전부 통과. 불일치 3건 + URL 규약 혼재 1건 + 도메인 문서 미결 취합 2건(파르페 경로 단수/복수, 그룹 닉네임 규칙 동일성) + 2026-08-02 최종 리뷰에서 보완한 스펙 "열린 질문" 이월 2건(`errorDetail` 미채움 의도, `GET /health` 인증 대상 의도) 합계 8건을 `open-questions.md`에 등록. private submodule(`wiki/personal-private/project-paths.md`)은 편집만 하고 커밋은 별도 승인 대기. **done**(TJYG-Android·TEAMYG-SERVER 코드 변경 0건). 스펙: [specs](../specs/archive/2026-08-01-parfait-api-contract-docs.md) |
| [2026-07-26-data-network-setup.md](archive/2026-07-26-data-network-setup.md) | `:data` 원격 네트워크 기초 구조(6 Task): network 컨벤션 플러그인(`AndroidNetworkConventionPlugin`+`NetworkConfig`+`PropertySettingManager.loadBaseUrl`, network 의존 이관) → `ApiResponse`+`safeApiCall` → `AuthInterceptor`+`TokenProvider` stub → `NetworkModule`(OkHttp·Retrofit) → Temp 예시 1세트+`RemoteDataSourceModule` → ADR-0017+data-layer 갱신. 테스트 없음(`:data:assembleDebug`+ktlint, Hilt는 `:app:assembleDebug`). 실행 중 코드리뷰로 3건이 계획과 갈렸다(DI 도메인별 분할 기각·`Json` 분리·`safeApiCall` 진입점 2개), 머지 시점 재대조에서 패키지 배치 차이가 추가로 드러났다(`service/model/{request,response}`·`data/model/{exception,qualifier}`). **develop 머지(#174, 2026-08-01)**. 스펙: [specs](../specs/archive/2026-07-26-data-network-setup.md) |
| [2026-07-31-designsystem-grouptag-topping-components.md](archive/2026-07-31-designsystem-grouptag-topping-components.md) | Figma `Grouptag-Chip`·`Topping-Group` 2종 신설(5 Task): 빌드 기반(`coil-network-okhttp`·`SizeTokens.Size96`/`Size160`) → `YGGrouptagChipType`+`YGGrouptagChip`+갤러리 `TEXT` → 에셋 42개(7종 × 6 density)+모델 3종 → `YGToppingGroup`(중심 정렬 + `.offset().rotate()`, 클리핑·`onClick` 없음, `AsyncImage` `error` 폴백)+갤러리 `CONTAINER` → 전체 빌드·ktlint·실기기. Task 단위 리뷰 5회를 다 통과한 뒤 **최종 전체 브랜치 리뷰**가 Task 경계 결함 2건(칩의 160dp 측정 제약, 비정사각 Remote 오버플로)을 잡아낸 것이 이 라운드의 수확. 계획서가 박은 샘플 URL이 404라 핵심 검증이 무력화됐던 것도 기록. **develop 머지(#186, 2026-08-01)**, 코드=설계 일치. 스펙: [specs](../specs/archive/2026-07-31-designsystem-grouptag-topping-components.md) |
| [2026-07-31-designsystem-canvas-components.md](archive/2026-07-31-designsystem-canvas-components.md) | Figma 캔버스 영역 5종 전량 신설(8 Task): 공용 컷 `Shape` → `YGStrokeButton` → `YGMenuItem` → `YGCanvasMenu`+모델 → `YGCanvasDateSelectButton` → `YGCanvasBackground`/`YGCanvas`(9:16 영역 + 토핑 `BoxScope` 슬롯 + Dim 최상단·터치 차단, 확장메뉴·캘린더만 승격) → `:app-preview` 배선 → 실기기 육안. 아래에서 위로 쌓아 각 Task가 앞 산출물만 소비. 실행 중 고친 것 2건: Expanded 총높이 붕괴(`Spacer(Size44)` 예약), Dim이 터치를 안 막던 문제(소비 전용 `pointerInput`). **develop 머지(#185, 2026-08-01)**, 코드=설계 일치. 스펙: [specs](../specs/archive/2026-07-31-designsystem-canvas-components.md) |
| [2026-07-30-designsystem-button-missing-components.md](archive/2026-07-30-designsystem-button-missing-components.md) | Figma 버튼 영역 미구현 5종 신설(8 Task): `SizeTokens` `Size18`·`Size28` → `YGCircleButton`+`Type` → `YGEditActionButton` → `YGCameraShutter` → `YGEditButton` → `YGEditTabButton` → `YGToggleButton` 삭제+갤러리 잔재 정리 → 전체 빌드·ktlint·갤러리 육안. 컴포넌트마다 프리뷰·갤러리 화면까지 같은 Task에서 끝내 Task별로 눈에 보이는 결과가 남게 배치. 갤러리 검증에서 결함 2건(`iconTint` 신설, 밑줄 폭 `IntrinsicSize.Max`). **develop 머지(#183, 2026-08-01)**. 스펙: [specs](../specs/archive/2026-07-30-designsystem-button-missing-components.md) |
| [2026-07-30-designsystem-button-component-sync.md](archive/2026-07-30-designsystem-button-component-sync.md) | Figma 버튼 영역 기존 7종 드리프트 제거(7 Task): `YGButtonColors` 테두리 + `YGButton` `border`·아이콘 `size`(공용 결함 먼저) → `YGButtonType` 값 정정 → `YGInputNumber` 각짐 → `SIZE_48` 아이콘 → `YGChipButton` 세로 패딩 + 프리셋 재명명(호출처 4파일) → `YGActionItem` 선두 아이콘 + 갤러리 → 전체 빌드·갤러리 육안. **develop 머지(#183, 2026-08-01)**. 스펙: [specs](../specs/archive/2026-07-30-designsystem-button-component-sync.md) |
| [2026-07-27-designsystem-text-component-sync.md](archive/2026-07-27-designsystem-text-component-sync.md) | Figma 텍스트 영역 컴포넌트 sync(6 Task): YGDate(배경 `Gray.White`·`modifier`를 Row로·`Arrangement.spacedBy(gap3)`·프리뷰 규약) → YGToast(가로/세로 패딩 교정 `padding6`/`padding5`·`Fail` 타입 신설 `Cherry500`·Figma 변형명 KDoc·프리뷰 규약) → YGAlert(프리뷰 규약 + `YGAlertPolicy.show`/`YGAlertItem`/`YGAlertHost` 버튼 변형 전달) → 갤러리 YGToast showcase(NavKey+화면+카탈로그+entry, 정적 4변형 + Host 트리거) → 갤러리 YGAlert showcase(정적 2변형 + 트리거 2) → **Task 5b(계획 후 추가)** 정책 파일 2개에 호스트 프리뷰 신설 → 전체 빌드·실기기 검증 + 스펙/계획 아카이브. YGLabel·YGChipButton 무변경. 테스트 없음(compile+ktlint+프리뷰+갤러리 실행 검증). **develop 머지(#181)** — Task 1~6 전량 수행, 코드=설계 일치. 스펙: [specs](../specs/archive/2026-07-27-designsystem-text-component-sync.md) |
| [2026-07-22-vendor-android-kotlin-skills.md](archive/2026-07-22-vendor-android-kotlin-skills.md) | 스킬 벤더링(11 Task): skills-vendor 스캐폴드 → vendor.py(clone/discover/full/render/update+CLI, TDD) → update-injected-skills 스킬 → **73개 전량 벤더 실행** → search.py(TDD) → skill-finder 스킬 → CLAUDE.md 워크플로 A 라우팅 → 검증·아카이브. stdlib 전용, 로직 `parfait/script/`. **done(inline 실행)**. 스펙: [specs](../specs/archive/2026-07-22-vendor-android-kotlin-skills.md) |
| [2026-07-21-feature-common-terms-module.md](archive/2026-07-21-feature-common-terms-module.md) | `:feature:common:terms:{api,impl}` 신규 이동(5 Task: api모듈+NavKey → impl모듈+7파일이동+entry+strings → app배선 → setting정리 → 빌드검증). A-003 재사용 위한 feature/common 공유 레이어([ADR-0015](../../adr/0015-feature-common-shared-layer.md)). git mv 히스토리 보존, 동작 무변경. **develop 머지(#161)**. 스펙: [specs](../specs/archive/2026-07-21-feature-common-terms-module.md) |
| [2026-07-20-s004-terms-privacy-webview.md](archive/2026-07-20-s004-terms-privacy-webview.md) | S-004 약관/개인정보 화면 분리 구현(7 Task: NotionWebView+strings → ServiceTerms/PrivacyPolicy VM → Screen 2 → Route 배선 → EntryBuilder 분리). MVI BaseViewModel, YGTopBarDetail 재사용, WebView(AndroidView) 로딩/에러 폴백, url State placeholder. **develop 머지(#161)** — 최종 위치 common:terms(모듈 분리 plan과 동일 PR). 스펙: [specs](../specs/archive/2026-07-20-s004-terms-privacy-webview.md) |
| [2026-07-21-app-preview-component-gallery.md](archive/2026-07-21-app-preview-component-gallery.md) | `:app-preview` 컴포넌트 갤러리 앱(10 Task: 카탈로그+NavKey17 → PreviewSection → Button6화면 → Input2 → Text3 → Container5 → Bar1 → EntryBuilders+DI배선 → MainScreen그룹목록+Route → 실기기검증). core:designsystem YG* 17종 실렌더 갤러리, 상태형은 remember 인터랙션, navigator.goTo/onBack. NavKey 파일당 1개(17). 기존 Navigation3+Hilt multibinding 확장. **develop 머지(#163)**. 스펙: [specs](../specs/archive/2026-07-21-app-preview-component-gallery.md) |
| [2026-07-19-app-setting-s001.md](archive/2026-07-19-app-setting-s001.md) | `:feature:app:setting` — S-001 앱 설정 화면 구현(7 Task: NavKey3 → ViewModel/State → strings+ProfileCard → Screen → Route배선 → stub Route → EntryBuilder). MVI BaseViewModel, YGListItem·YGTopBarBack 재사용, 계정/약관/개인정보 nav stub. 테스트 없음(compile+ktlint+프리뷰 검증). **develop 머지(#160)**. ProfileCard 각짐만 `RectangleShape` 대체. 스펙: [specs](../specs/archive/2026-07-19-app-setting-s001.md) |
| [2026-07-18-designsystem-preview-migration.md](archive/2026-07-18-designsystem-preview-migration.md) | 프리뷰 관용구 통일 계획(4 Task: 파일럿 YGColorChip → param 5파일 → 무param 6파일 → 검증+문서). `@YGPreview`+`PreviewBox` 전환. **develop 머지(#158)**. 스펙: [specs](../specs/archive/2026-07-18-designsystem-preview-migration.md) |
| [2026-07-14-yginvitecard.md](archive/2026-07-14-yginvitecard.md) | YGInviteCard 구현 계획(component/card, Column+라벨 Row+코드박스 Row, Active/Invalid when 분기, YGButton SmallSquare 재사용, @YGPreview). **develop 머지(#148/#136)**. 스펙: [specs](../specs/archive/2026-07-14-yginvitecard.md) |
| [2026-07-15-ygmodalpopup.md](archive/2026-07-15-ygmodalpopup.md) | YGModalPopup 구현 계획(component/modal, Dialog 래퍼 + private YGModalPopupContent 분리, Contents Column[아이콘+텍스트]+Action Row[확인 Secondary/취소 Primary weight1f], iconTint 기본 cherry-600, @YGPreview는 content 직접 렌더). **develop 머지(#151/#135)**. 스펙: [specs](../specs/archive/2026-07-15-ygmodalpopup.md) |
| [2026-07-10-ygtextfield.md](archive/2026-07-10-ygtextfield.md) | YGTextField 구현 계획(component/textfield). 구현 완료. 스펙: [specs](../specs/archive/2026-07-10-ygtextfield.md) |
| [2026-07-10-ygtextformfield.md](archive/2026-07-10-ygtextformfield.md) | YGTextFormField 구현 계획(YGTextFieldImpl 재사용 + errorDescription). 구현 완료. 스펙: [specs](../specs/archive/2026-07-10-ygtextformfield.md) |
| [2026-07-12-yghorizontaldivider.md](archive/2026-07-12-yghorizontaldivider.md) | YGHorizontalDivider 구현 계획(component/etc, Spacer 1dp 수평선). 구현 완료. 스펙: [specs](../specs/archive/2026-07-12-yghorizontaldivider.md) |
| [2026-07-12-yglistitem.md](archive/2026-07-12-yglistitem.md) | YGListItem 구현 계획(component/etc, Row 가로: 메인+옵션 sub+옵션 trailing 아이콘 Box). 구현 완료. 스펙: [specs](../specs/archive/2026-07-12-yglistitem.md) |
| [2026-07-12-ygiconbutton.md](archive/2026-07-12-ygiconbutton.md) | YGIconButton 구현 계획(component/ygiconbutton, Box+Image tint, 크기 enum, PreviewParameterProvider). 구현 완료(사후 기록). 스펙: [specs](../specs/archive/2026-07-12-ygiconbutton.md) |
| [2026-07-12-ygactionitem.md](archive/2026-07-12-ygactionitem.md) | YGActionItem 구현 계획(component/ygactionitem, Box+Text, pressed 색, Role.Button). 구현 완료(사후 기록). 스펙: [specs](../specs/archive/2026-07-12-ygactionitem.md) |
| [2026-07-12-ygtextfield-clear-iconbutton.md](archive/2026-07-12-ygtextfield-clear-iconbutton.md) | YGTextField clear→YGIconButton 교체(component/textfield, 2 Task: Impl 교체 → clearIconTint 제거). 구현 완료(코드는 별도 커밋 예정). 스펙: [specs](../specs/archive/2026-07-12-ygtextfield-clear-iconbutton.md) |
| [2026-07-12-yglistitem-trailingicon-iconbutton.md](archive/2026-07-12-yglistitem-trailingicon-iconbutton.md) | YGListItem trailing 아이콘→YGIconButton 교체(component/etc, 1 Task: 블록 치환 + trailingIconColor 제거). 구현 완료(compile·ktlint 통과, 코드는 별도 커밋 예정). 스펙: [specs](../specs/archive/2026-07-12-yglistitem-trailingicon-iconbutton.md) |
| [2026-07-13-yginputnumber.md](archive/2026-07-13-yginputnumber.md) | YGInputNumber 구현 계획(component/yginputnumber, 50×50 선택 셀). 사후 기록(develop #129 머지 완료). 스펙: [specs](../specs/archive/2026-07-13-yginputnumber.md) |
| [2026-07-12-clickableyg-throttle.md](archive/2026-07-12-clickableyg-throttle.md) | clickableYG 구현 계획(표준 clickable 위 throttle, @Composable, `core:util:android`). **develop 머지(#143)** — 코드블록은 역사 스냅샷. 스펙: [specs](../specs/archive/2026-07-12-clickableyg-throttle.md) |
| [2026-07-13-ygripple.md](archive/2026-07-13-ygripple.md) | ygDimRipple 구현 계획(커스텀 dim ripple IndicationNodeFactory, createRippleModifierNode 위임). **develop 머지(#143)**, `core:util:android clickable/`. 스펙: [specs](../specs/archive/2026-07-13-ygripple.md) |
| [2026-07-13-clickableyg-ripple-variants.md](archive/2026-07-13-clickableyg-ripple-variants.md) | clickableYG 리플 변형(Dim/Scale/Merge) + ygScaleRipple 구현 계획(@Composable, 합성 indication, `core:util:android`). **develop 머지(#143)**. 스펙: [specs](../specs/archive/2026-07-13-clickableyg-ripple-variants.md) |

## Frontmatter (필수)
모든 plan은 YAML frontmatter를 단다(형식 권위: [`template.md`](template.md)). 필드: `id`(slug) · `title` · `status`(**draft / in-progress / done / abandoned / superseded**) · `type`(work-order / handoff) · `created` · `updated` · `platforms`(=android) · `owner`(**실명 금지**) · `related_adr` · `related_spec` · `related_code`(심볼명, 라인번호 금지) · `archived_reason` · `tags`. 완료/폐기 시 `archive/`로 이동.
