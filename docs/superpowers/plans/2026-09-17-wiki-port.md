# Wiki Port Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `huihun-private-wiki`의 LLM 운영 위키 체계를 이 저장소에 단일 도메인 평탄 구조로 이식해, 콘텐츠가 비어 있어도 검사가 도는 스캐폴드를 세운다.

**Architecture:** 원본 스크립트 6개와 테스트 14개를 `wiki/script/`로 복사한 뒤 도메인 차원을 제거한다. 콘텐츠는 `wiki/pages/` 아래에 두고 graphify 산출물은 `wiki/graphify-out/`에 두어 스캔 루트와 산출물을 분리한다. 각 스크립트는 테스트를 먼저 고쳐 실패를 본 뒤 구현을 고친다.

**Tech Stack:** Python 3.14, pytest 9.1.1, graphify CLI. Gradle 빌드와는 완전히 분리된다.

**Spec:** `docs/superpowers/specs/2026-09-17-wiki-port-design.md`

## Global Constraints

- 원본 저장소 경로: `/Users/jeonheehoon/Documents/work_station/github/huihun-private-wiki`. 이 문서에서 `$SRC`로 쓴다. 원본은 **읽기 전용**이다 — 절대 수정하지 않는다.
- 작업 저장소 루트: `/Users/jeonheehoon/Documents/work_station/mashup/github/TJYG-Android`. 브랜치 `feature/ai/llm-wiki`.
- 모든 스크립트는 저장소 루트에서 `python3`으로 실행한다. 루트가 아니면 종료 코드 2로 막힌다.
- 문서 내용은 **한국어**. 코드·식별자·파일명·커밋 메시지는 **영어**. (저장소 `CLAUDE.md`)
- 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`를 붙인다.
- `sync_issues.py`, `test_sync_issues.py`, `test_issue_templates.py`, `test_lint_issue_field.py`는 가져오지 않는다.
- `.obsidian/`은 만들지 않는다. `wikilib.SKIP_DIRS`에서도 뺀다.
- CI 워크플로우(`.github/workflows/`)는 건드리지 않는다.
- 실제 콘텐츠 ingest는 이 계획의 범위 밖이다. 산출물은 빈 스캐폴드다.

**최종 배치** (Task 1이 만들고 나머지가 채운다):

```
wiki/
  CLAUDE.md  conventions.md  routing.json  log.md
  open-questions.md  routing-misses.md
  references/  templates/  script/  raw/  graphify-out/
  pages/
    purpose.md  overview.md  index.md
    sources/  concepts/  entities/  queries/  synthesis/
```

---

### Task 1: 디렉토리 골격과 ignore 파일

**Files:**
- Create: `wiki/pages/{sources,concepts,entities,queries,synthesis}/.gitkeep`
- Create: `wiki/{raw,graphify-out,references,templates,script}/.gitkeep`
- Create: `wiki/script/tests/.gitkeep`
- Create: `.claudeignore`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: 없음 (첫 태스크)
- Produces: 이후 모든 태스크가 쓰는 디렉토리 경로

- [ ] **Step 1: 디렉토리와 .gitkeep 생성**

```bash
cd /Users/jeonheehoon/Documents/work_station/mashup/github/TJYG-Android
mkdir -p wiki/pages/{sources,concepts,entities,queries,synthesis}
mkdir -p wiki/{raw,graphify-out,references,templates}
mkdir -p wiki/script/tests
for d in wiki/pages/sources wiki/pages/concepts wiki/pages/entities \
         wiki/pages/queries wiki/pages/synthesis \
         wiki/raw wiki/graphify-out wiki/references wiki/templates \
         wiki/script wiki/script/tests; do
  touch "$d/.gitkeep"
done
```

- [ ] **Step 2: `.claudeignore` 생성**

`GRAPH_REPORT.md`를 일부러 빼는 이유를 주석으로 남긴다 — `routing.json`의 `research` 의도가 그 파일을 reference로 내보내므로, 막으면 라우팅이 열라고 건넨 문서를 열 수 없게 된다.

```
# graphify 산출물 중 자주 다시 쓰이는 큰 파일만 막는다. graphify가 워크스페이스에
# 파일을 쓸 때마다 Claude Code 프롬프트 캐시가 무효화되어 다음 턴에 전체
# 재업로드가 발생한다.
wiki/graphify-out/graph.json
wiki/graphify-out/graph.html
wiki/graphify-out/cache/

# wiki/graphify-out/GRAPH_REPORT.md는 일부러 뺐다 — wiki/routing.json의 research
# 의도가 이 파일을 reference 후보로 내보낸다. 여기서 막으면 라우팅이 열라고 건넨
# 문서를 열 수 없다(route.py는 존재만 확인하고 읽을 수 있는지는 보지 않아 종료
# 코드 0으로 통과한다).
```

- [ ] **Step 3: `.gitignore`에 4줄 추가**

파일 끝에 덧붙인다. 기존 내용은 건드리지 않는다.

```
# Python 바이트코드 캐시 — 인터프리터 버전마다 달라져 공유 가치가 없다
__pycache__/
*.pyc

# graphify 로컬 비용 집계 — 머신마다 다르다
/wiki/graphify-out/cost.json
# graphify 로컬 조회 타임스탬프 — 실행한 머신에서만 의미가 있다
/wiki/graphify-out/cache/last_query_stamp
```

- [ ] **Step 4: 배치 확인**

Run: `find wiki -type f | sort`
Expected: `.gitkeep` 11개만 보인다.

- [ ] **Step 5: Commit**

```bash
git add wiki .claudeignore .gitignore
git commit -m "chore: scaffold wiki directory layout

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: `wikilib.py` — 도메인 차원 제거

도메인 개념의 뿌리다. 여기를 먼저 못 박아야 나머지 스크립트가 일관되게 따라온다.

**Files:**
- Create: `wiki/script/wikilib.py` (원본 복사 후 수정)
- Create: `wiki/script/tests/conftest.py` (원본 그대로)
- Create: `wiki/script/tests/test_wikilib.py` (원본 복사 후 수정)

**Interfaces:**
- Consumes: 없음
- Produces:
  - `Page` 데이터클래스 — `path`, `stem`, `text`, `has_fm`, `fm`, `body`, 프로퍼티 `rel`, `parent_name`. **`domain` 프로퍼티는 없다.**
  - `Finding(level, code, path, message)`
  - `load_pages(repo_root) -> list[Page]`
  - `all_markdown(repo_root) -> list[pathlib.Path]`
  - `find_links(text) -> list[str]`, `build_name_index(pages) -> dict[str, list[Page]]`
  - `nfc(s)`, `as_list(value)`, `strip_noise(text)`, `split_frontmatter(text)`, `parse_fm(text)`, `is_collected(rel)`
  - 상수 `SKIP_DIRS`, `UNCOLLECTED_DIRS`, `UNCOLLECTED_FILES`, `GENERATED_DIRS`

- [ ] **Step 1: 원본 복사**

```bash
SRC=/Users/jeonheehoon/Documents/work_station/github/huihun-private-wiki
cp "$SRC/wiki/script/wikilib.py" wiki/script/wikilib.py
cp "$SRC/wiki/script/tests/conftest.py" wiki/script/tests/conftest.py
cp "$SRC/wiki/script/tests/test_wikilib.py" wiki/script/tests/test_wikilib.py
```

- [ ] **Step 2: 실패하는 테스트를 먼저 쓴다**

`wiki/script/tests/test_wikilib.py` 끝에 아래를 덧붙인다. 원본 테스트 중 `wiki/domains/...` 경로나 `.domain`을 쓰는 케이스는 Step 4에서 고친다.

```python
def test_page_has_no_domain_attribute(make_repo):
    """도메인 차원은 제거됐다. 남아 있으면 소비자가 조용히 옛 구조를 가정한다."""
    root = make_repo({"wiki/pages/concepts/개념.md": page(sources="[]")})
    pg = wikilib.load_pages(root)[0]
    assert not hasattr(pg, "domain")


def test_all_markdown_is_scoped_to_wiki(make_repo):
    """저장소의 안드로이드 쪽 마크다운은 [[이름]] 링크 대상이 아니다."""
    root = make_repo({
        "README.md": "# root",
        "http/README.md": "# http",
        "wiki/pages/concepts/개념.md": page(sources="[]"),
    })
    rels = {p.relative_to(root).as_posix() for p in wikilib.all_markdown(root)}
    assert rels == {"wiki/pages/concepts/개념.md"}


def test_graphify_output_is_not_collected(make_repo):
    """생성물이 위키 페이지로 수집되면 파일명중복·고아 검사에 자기가 걸린다."""
    root = make_repo({
        "wiki/graphify-out/GRAPH_REPORT.md": "# report",
        "wiki/pages/concepts/개념.md": page(sources="[]"),
    })
    assert [pg.rel for pg in wikilib.load_pages(root)] == [
        "wiki/pages/concepts/개념.md"]
    assert wikilib.all_markdown(root) == [
        root / "wiki/pages/concepts/개념.md"]


def test_wiki_entry_files_are_not_collected(make_repo):
    root = make_repo({
        "wiki/CLAUDE.md": "# ops",
        "wiki/conventions.md": "# contract",
        "wiki/routing-misses.md": "# misses",
        "wiki/open-questions.md": page(),
    })
    assert [pg.rel for pg in wikilib.load_pages(root)] == ["wiki/open-questions.md"]
```

파일 상단에 `from conftest import page`와 `import wikilib`가 이미 있는지 확인하고, 없으면 추가한다.

- [ ] **Step 3: 테스트를 돌려 실패를 확인**

Run: `python3 -m pytest wiki/script/tests/test_wikilib.py -v`
Expected: `test_page_has_no_domain_attribute`, `test_all_markdown_is_scoped_to_wiki`, `test_graphify_output_is_not_collected`, `test_wiki_entry_files_are_not_collected` 4건 FAIL. 원본에서 옮겨온 `wiki/domains/` 기반 케이스도 일부 FAIL한다.

- [ ] **Step 4: `wikilib.py` 수정**

상수 블록을 아래로 교체한다. `.obsidian`이 빠지고 `GENERATED_DIRS`가 생긴다.

```python
# 링크 대상이 아니라 수집도 하지 않는 경로
UNCOLLECTED_DIRS = ("wiki/templates", "wiki/script", "wiki/references")
UNCOLLECTED_FILES = (
    "wiki/CLAUDE.md",
    "wiki/conventions.md",
    "wiki/routing-misses.md",
)
# graphify 산출물. 수집도 안 하고 파일명 유일성 검사에도 넣지 않는다 — graphify가
# 쓰는 GRAPH_REPORT.md가 위키 페이지와 이름이 겹치는 것은 위키의 문제가 아니라
# 생성물의 문제이고, 여기서 위반으로 띄우면 사람이 고칠 수 없는 경고가 남는다.
# 스캔 루트(wiki/pages/) 밖에 두는 것과 별개로 이 마개가 필요하다 — 산출물이
# wiki/ 안에 있기 때문이다.
GENERATED_DIRS = ("wiki/graphify-out",)
# vault 스캔에서 제외할 디렉토리.
# .pytest_cache·.superpowers는 추적되지 않는 작업 부산물이다. 포함하면 lint 결과가
# 로컬 작업 사본과 새 클론에서 달라진다 — 검사는 커밋된 트리만 봐야 한다.
SKIP_DIRS = {
    ".git", "node_modules", ".pytest_cache", ".superpowers", ".claude",
}
```

`Page` 데이터클래스에서 `domain` 프로퍼티를 통째로 삭제한다.

```python
    @property
    def domain(self) -> str | None:
        """wiki/domains/<도메인>/... 이면 도메인 이름, 아니면 None."""
        parts = self.path.parts
        if len(parts) >= 3 and parts[0] == "wiki" and parts[1] == "domains":
            return parts[2]
        return None
```

`is_collected`를 `GENERATED_DIRS`도 보게 고친다.

```python
def is_collected(rel: str) -> bool:
    if rel in UNCOLLECTED_FILES:
        return False
    skip = UNCOLLECTED_DIRS + GENERATED_DIRS
    return not any(rel.startswith(d + "/") for d in skip)
```

`all_markdown` 범위를 `wiki/`로 좁히고 생성물을 뺀다.

```python
def all_markdown(repo_root: pathlib.Path) -> list[pathlib.Path]:
    """위키에 속하는 모든 마크다운. 파일명 유일성 검사의 대상이다.

    범위가 저장소 전체가 아니라 wiki/인 이유: [[이름]] 링크는 위키 안에서만
    쓰이고, 이 저장소의 안드로이드 쪽 마크다운(README.md가 세 곳에 있다)은
    링크 대상이 아니다. 전체를 보면 고칠 수 없는 파일명중복이 영구히 뜬다.
    """
    out = []
    wiki_root = repo_root / "wiki"
    if not wiki_root.exists():
        return out
    for p in wiki_root.rglob("*.md"):
        rel = p.relative_to(repo_root).as_posix()
        if SKIP_DIRS & set(pathlib.PurePosixPath(rel).parts):
            continue
        if any(rel.startswith(d + "/") for d in GENERATED_DIRS):
            continue
        out.append(p)
    return sorted(out)
```

- [ ] **Step 5: 옮겨온 원본 테스트의 경로를 고친다**

`test_wikilib.py`에 남아 있는 `wiki/domains/<도메인>/<도메인>-*.md` 형태를 새 배치로 바꾼다.

| 옛 경로 | 새 경로 |
|---|---|
| `wiki/domains/a/a-purpose.md` | `wiki/pages/purpose.md` |
| `wiki/domains/a/a-index.md` | `wiki/pages/index.md` |
| `wiki/domains/a/a-overview.md` | `wiki/pages/overview.md` |
| `wiki/domains/a/concepts/X.md` | `wiki/pages/concepts/X.md` |
| `wiki/domains/a/sources/src-X.md` | `wiki/pages/sources/src-X.md` |
| `wiki/synthesis/open-questions.md` | `wiki/open-questions.md` |
| `wiki/synthesis/routing-misses.md` | `wiki/routing-misses.md` |
| `wiki/purpose.md` / `wiki/index.md` / `wiki/overview.md` | `wiki/pages/...` (같은 이름) |

`.domain`을 단언하는 케이스는 삭제한다. 그 성질은 더 이상 존재하지 않으므로 다른 단언으로 바꾸지 않는다.

- [ ] **Step 6: 테스트를 돌려 통과를 확인**

Run: `python3 -m pytest wiki/script/tests/test_wikilib.py -v`
Expected: 전부 PASS.

- [ ] **Step 7: Commit**

```bash
git add wiki/script/wikilib.py wiki/script/tests/conftest.py wiki/script/tests/test_wikilib.py
git commit -m "feat: port wikilib without the domain dimension

Scope page collection and filename uniqueness to wiki/, drop
Page.domain, and keep graphify output out of both.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: `lint.py` — 배치 규칙 재작성과 `issue` 검사 삭제

가장 큰 덩어리다. 검사 12개 중 9개를 고치고 1개를 삭제한다.

**Files:**
- Create: `wiki/script/lint.py` (원본 복사 후 수정)
- Create: `wiki/script/tests/test_lint_{cli,frontmatter,layout,links,names,raw,sensitive}.py` (7개)
- 가져오지 않음: `test_lint_issue_field.py`

**Interfaces:**
- Consumes: Task 2의 `wikilib` 전부
- Produces:
  - `run_all(repo_root, pages=None) -> list[Finding]`
  - `repo_root_ok(repo_root) -> bool`, `wrong_root_message(repo_root) -> str`
  - 개별 검사 함수: `check_unique_names(repo_root)`, `check_links(pages, index)`, `check_orphans(pages, inbound)`, `check_layout(pages)`, `check_frontmatter(pages)`, `check_provenance(pages)`, `check_raw_sync(repo_root, pages)`, `check_manifest(repo_root)`, `check_sensitive(pages, repo_root=None)`, `check_action_enum(repo_root)`
  - 상수 `CONTENT_DIRS`, `SOURCED_DIRS`, `STRUCTURE_FILES`, `STRUCTURE_PATHS`, `ORPHAN_EXEMPT_PATHS`, `ORPHAN_EXEMPT_DIRS`, `SENSITIVE`, `ACTION_VALUES`
  - **없어진 것**: `check_issue_field`, `issue_value_message`, `ISSUE_LINE`, `ISSUE_VALUE`, `STRUCTURE_SUFFIXES`, `_raw_pairs`

- [ ] **Step 1: 원본 복사 (issue 테스트 제외)**

```bash
SRC=/Users/jeonheehoon/Documents/work_station/github/huihun-private-wiki
cp "$SRC/wiki/script/lint.py" wiki/script/lint.py
for t in cli frontmatter layout links names raw sensitive; do
  cp "$SRC/wiki/script/tests/test_lint_$t.py" "wiki/script/tests/test_lint_$t.py"
done
```

- [ ] **Step 2: 배치 테스트를 새 구조로 다시 쓴다**

`wiki/script/tests/test_lint_layout.py`를 통째로 아래로 교체한다.

```python
from conftest import page

import lint
import wikilib


def _layout(root):
    return lint.check_layout(wikilib.load_pages(root))


def test_accepted_shapes_pass(make_repo):
    root = make_repo({
        "wiki/log.md": page(),
        "wiki/open-questions.md": page(),
        "wiki/pages/purpose.md": page(),
        "wiki/pages/index.md": page(),
        "wiki/pages/overview.md": page(),
        "wiki/pages/sources/src-정책.md": page(sources="[정책.md]"),
        "wiki/pages/concepts/개념.md": page(sources="[]"),
        "wiki/pages/entities/인물.md": page(sources="[]"),
        "wiki/pages/queries/질의-2026-09-17.md": page(),
        "wiki/pages/synthesis/분석.md": page(),
    })
    assert _layout(root) == []


def test_subdirectory_of_content_dir_is_violation(make_repo):
    root = make_repo({"wiki/pages/concepts/sub/깊은개념.md": page(sources="[]")})
    found = _layout(root)
    assert [f.code for f in found] == ["배치"]
    assert found[0].level == "violation"
    assert found[0].path == "wiki/pages/concepts/sub/깊은개념.md"
    assert "하위 폴더" in found[0].message


def test_loose_file_at_pages_root_is_violation(make_repo):
    root = make_repo({"wiki/pages/아무거나.md": page()})
    found = _layout(root)
    assert [f.code for f in found] == ["배치"]
    assert found[0].level == "violation"
    assert "wiki/pages/ 루트에 올 수 없는 파일" in found[0].message


def test_unknown_content_dir_is_violation(make_repo):
    root = make_repo({"wiki/pages/notes/메모.md": page()})
    found = _layout(root)
    assert [f.code for f in found] == ["배치"]
    assert "콘텐츠 디렉토리가 아님" in found[0].message


def test_sources_page_without_src_prefix_is_violation(make_repo):
    root = make_repo({"wiki/pages/sources/정책.md": page(sources="[정책.md]")})
    found = _layout(root)
    assert [f.code for f in found] == ["배치"]
    assert "src-" in found[0].message


def test_files_outside_pages_are_ignored(make_repo):
    """wiki/ 바로 아래 운영 파일은 배치 검사 대상이 아니다."""
    root = make_repo({
        "wiki/log.md": page(),
        "wiki/open-questions.md": page(),
    })
    assert _layout(root) == []
```

- [ ] **Step 3: 나머지 6개 테스트의 경로를 치환한다**

`test_lint_{cli,frontmatter,links,names,raw,sensitive}.py`에서 Task 2 Step 5의 경로 대응표를 그대로 적용한다. 더해서 `test_lint_raw.py`는 `raw/<도메인>/X.md` → `wiki/raw/X.md`, `raw/.manifest.json` → `wiki/raw/.manifest.json`으로 바꾼다.

`test_lint_cli.py`에서 `check_issue_field`를 언급하는 케이스가 있으면 삭제한다.

- [ ] **Step 4: 테스트를 돌려 실패를 확인**

Run: `python3 -m pytest wiki/script/tests/ -v -k lint`
Expected: `test_lint_layout.py` 전건 FAIL(아직 `wiki/domains` 분기를 본다), `test_lint_raw.py` FAIL, 나머지도 경로 변경분만큼 FAIL.

- [ ] **Step 5: `lint.py` 상수 블록 교체**

```python
ORPHAN_EXEMPT_PATHS = {
    "wiki/log.md",
    "wiki/open-questions.md",
    "wiki/pages/index.md",
    "wiki/pages/purpose.md",
    "wiki/pages/overview.md",
}
ORPHAN_EXEMPT_DIRS = {"queries"}
# wiki/pages/ 루트에 올 수 있는 구조 파일. 한 곳에서만 정의한다 — 네 번째 구조
# 파일이 생겼을 때 배치 검사와 frontmatter 검사가 어긋나지 않게 하기 위해서다.
STRUCTURE_FILES = ("purpose.md", "index.md", "overview.md")
```

`STRUCTURE_SUFFIXES` 정의와 그 위 주석 블록은 삭제한다.

- [ ] **Step 6: `check_orphans` 단순화**

구조 파일 3개가 전부 `ORPHAN_EXEMPT_PATHS`에 있으므로 약연결 면제 분기가 죽은 코드가 된다. 제거한다.

```python
def check_orphans(pages: list[Page], inbound: dict[str, set[str]]) -> list[Finding]:
    out = []
    for pg in pages:
        if _orphan_exempt(pg):
            continue
        deg = len(inbound.get(pg.stem, set()) - {pg.stem})
        if deg == 0:
            out.append(Finding("violation", "고아", pg.rel, "인바운드 링크 0건"))
        elif deg == 1:
            src = next(iter(inbound[pg.stem] - {pg.stem}))
            out.append(Finding("warning", "약연결", pg.rel,
                               f"인바운드 1건({src}) — 개념망 편입 검토"))
    return out
```

- [ ] **Step 7: `STRUCTURE_PATHS`와 `_needs_frontmatter` 교체**

```python
CONTENT_DIRS = {"sources", "concepts", "entities", "queries", "synthesis"}
# frontmatter에 sources 필드가 추가로 필요한 디렉토리
SOURCED_DIRS = {"sources", "concepts", "entities"}
STRUCTURE_PATHS = {f"wiki/pages/{f}" for f in STRUCTURE_FILES}


def _needs_frontmatter(pg: Page) -> bool:
    # wiki/log.md와 wiki/open-questions.md는 여기서 명시적으로 빼지 않는다.
    # STRUCTURE_PATHS에 없고 부모가 CONTENT_DIRS도 아니라 False가 된다.
    return pg.rel in STRUCTURE_PATHS or pg.parent_name in CONTENT_DIRS
```

- [ ] **Step 8: `check_provenance` 교체**

```python
def check_provenance(pages: list[Page]) -> list[Finding]:
    """concepts·entities의 sources 선언과 본문 인용이 맞아야 한다.

    두 방향을 본다.
    - 본문이 인용한 소스 요약 페이지가 frontmatter sources에도 있는가
    - frontmatter sources가 선언한 대상이 sources/에 실제로 있는가

    `sources/` 페이지의 sources는 raw 원본 파일명을 가리키며 check_raw_sync가
    그쪽을 검사한다. 여기서 중복 보고하지 않는다.
    """
    source_stems = {pg.stem for pg in pages if pg.parent_name == "sources"}
    out = []
    for pg in pages:
        if pg.parent_name not in ("concepts", "entities"):
            continue
        declared = {nfc(x).removesuffix(".md")
                    for x in wikilib.as_list(pg.fm.get("sources"))}
        cited = set(wikilib.find_links(pg.body)) & source_stems
        for miss in sorted(cited - declared):
            out.append(Finding("violation", "출처추적", pg.rel,
                               f"본문이 [[{miss}]]를 인용하나 frontmatter sources에 없음"))
        for dangling in sorted(declared - source_stems):
            out.append(Finding(
                "violation", "출처추적", pg.rel,
                f"frontmatter sources '{dangling}' 대상 없음 — "
                f"wiki/pages/sources/{dangling}.md 가 없다"))
    return out
```

- [ ] **Step 9: `check_layout` 교체**

```python
def check_layout(pages: list[Page]) -> list[Finding]:
    """wiki/pages/ 아래의 배치 규칙. 규정 밖 위치는 콘텐츠 검사를 통째로 비껴간다.

    frontmatter·raw 정합·판본 검사가 모두 부모 디렉토리 이름으로 대상을 고른다.
    `concepts/sub/x.md`나 pages/ 루트의 임의 파일은 어느 검사에도 걸리지 않으면서
    인바운드 링크 1건만으로 합법이 된다. 그 구멍을 여기서 막는다.
    """
    expected = (f"{{{','.join(sorted(STRUCTURE_FILES))}}} "
                f"또는 {{{','.join(sorted(CONTENT_DIRS))}}}/ 바로 아래")
    allowed_root = {nfc(f) for f in STRUCTURE_FILES}
    out = []
    for pg in pages:
        parts = pg.path.parts
        if parts[:2] != ("wiki", "pages"):
            continue
        tail = parts[2:]
        if len(tail) == 1:
            if nfc(tail[0]) not in allowed_root:
                out.append(Finding(
                    "violation", "배치", pg.rel,
                    f"wiki/pages/ 루트에 올 수 없는 파일 — {expected}"))
            continue
        if len(tail) > 2:
            out.append(Finding(
                "violation", "배치", pg.rel,
                f"콘텐츠 디렉토리 하위 폴더는 허용되지 않음 — {expected}"))
            continue
        if tail[0] not in CONTENT_DIRS:
            out.append(Finding(
                "violation", "배치", pg.rel,
                f"'{tail[0]}/' 은 콘텐츠 디렉토리가 아님 — {expected}"))
            continue
        if tail[0] == "sources" and not pg.stem.startswith("src-"):
            out.append(Finding(
                "violation", "배치", pg.rel,
                "sources/ 페이지는 stem이 'src-'로 시작해야 함 "
                "(원본 파일명과 겹치지 않게 하는 접두사)"))
    return out
```

- [ ] **Step 10: `_raw_pairs`·`check_raw_sync` 교체**

`_raw_pairs`를 지우고 `_raw_stems`로 바꾼다.

```python
def _raw_stems(repo_root: pathlib.Path) -> set[str]:
    """wiki/raw/**/X.md → X. 도메인 차원이 없어 stem 하나로 짝을 맞춘다."""
    raw_root = repo_root / "wiki" / "raw"
    if not raw_root.exists():
        return set()
    return {nfc(p.stem) for p in raw_root.rglob("*.md")}


def check_raw_sync(repo_root: pathlib.Path, pages: list[Page]) -> list[Finding]:
    raw = _raw_stems(repo_root)
    src = {pg.stem.removeprefix("src-")
           for pg in pages if pg.parent_name == "sources"}
    out = []
    for stem in sorted(raw - src):
        out.append(Finding(
            "violation", "raw정합", f"wiki/raw/{stem}.md",
            f"ingest 안 됨 — wiki/pages/sources/src-{stem}.md 없음"))
    for stem in sorted(src - raw):
        out.append(Finding(
            "violation", "raw정합", f"wiki/pages/sources/src-{stem}.md",
            f"대응 raw 원본 없음 — wiki/raw/ 아래에 {stem}.md 가 없다"))
    return out
```

- [ ] **Step 11: `check_manifest`·`_raw_texts`·`check_action_enum` 경로 교체**

```python
def check_manifest(repo_root: pathlib.Path) -> list[Finding]:
    raw_root = repo_root / "wiki" / "raw"
    mf = raw_root / ".manifest.json"
    rel = "wiki/raw/.manifest.json"
    if not mf.exists():
        return []
    try:
        entries = json.loads(mf.read_text(encoding="utf-8"))
    except json.JSONDecodeError as e:
        return [Finding("violation", "매니페스트", rel, f"JSON 파싱 실패: {e}")]
    actual = {p.relative_to(repo_root).as_posix() for p in raw_root.rglob("*.md")}
    recorded = set(entries)
    out = []
    for miss in sorted(actual - recorded):
        out.append(Finding("violation", "매니페스트", miss, "매니페스트에 등록 안 됨"))
    for stale in sorted(recorded - actual):
        out.append(Finding("violation", "매니페스트", stale,
                           "매니페스트에 있으나 파일 없음"))
    return out
```

`_raw_texts`의 `raw_root = repo_root / "raw"`를 `repo_root / "wiki" / "raw"`로 바꾼다.

`check_action_enum`의 두 줄을 바꾼다.

```python
    path = repo_root / "wiki" / "open-questions.md"
    if not path.exists():
        return []
    rel = "wiki/open-questions.md"
```

- [ ] **Step 12: `issue` 관련을 전부 삭제**

지울 것: `ISSUE_LINE`·`ISSUE_VALUE` 정규식과 그 위 주석, 함수 `check_issue_field`, 함수 `issue_value_message`, `run_all`의 `*check_issue_field(repo_root),` 줄.

`run_all`은 아래가 된다.

```python
def run_all(repo_root: pathlib.Path, pages: list[Page] | None = None) -> list[Finding]:
    if pages is None:
        pages = wikilib.load_pages(repo_root)
    index = wikilib.build_name_index(pages)
    link_findings, inbound = check_links(pages, index)
    # 함수 안에서 import한다 — graph_signals는 wiki/graphify-out/graph.json이라는
    # 산출물만 읽는 독립 모듈이라 항상 존재하지만, 모듈 로드를 호출 시점까지
    # 늦춰 두면 이 파일 상단의 import 목록이 "항상 필요한 것"과 "검사 하나가
    # 쓰는 것"으로 갈리지 않는다.
    import graph_signals
    return [
        *check_unique_names(repo_root),
        *link_findings,
        *check_orphans(pages, inbound),
        *check_layout(pages),
        *check_frontmatter(pages),
        *check_provenance(pages),
        *check_raw_sync(repo_root, pages),
        *check_manifest(repo_root),
        *check_sensitive(pages, repo_root),
        *check_action_enum(repo_root),
        *graph_signals.check_graph_signals(repo_root, pages),
    ]
```

`run_all`은 Task 7에서 `graph_signals.py`를 놓을 때까지 `ImportError`를 낸다. 그래서 이 태스크의 Step 13은 `run_all`을 부르지 않는 테스트만 돌린다.

- [ ] **Step 13: 개별 검사 테스트를 돌려 통과를 확인**

Run: `python3 -m pytest wiki/script/tests/ -v -k "lint and not cli"`
Expected: 전부 PASS.

`test_lint_cli.py`는 `run_all`/`main`을 타므로 Task 7 이후에 통과한다. 지금은 `ImportError: No module named 'graph_signals'`로 실패하는 것이 정상이다.

- [ ] **Step 14: Commit**

```bash
git add wiki/script/lint.py wiki/script/tests/test_lint_*.py
git commit -m "feat: port lint with flat layout rules

Rewrite check_layout for wiki/pages/, pair raw sources by stem alone,
and drop check_issue_field along with the GitHub issue projection.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: `check_status.py` — 판본 체인에서 도메인 제거

**Files:**
- Create: `wiki/script/check_status.py` (원본 복사 후 수정)
- Create: `wiki/script/tests/test_check_status.py` (원본 복사 후 수정)

**Interfaces:**
- Consumes: Task 2의 `wikilib`
- Produces:
  - `collect_sources(repo_root, pages=None) -> dict[str, dict]` — **키가 `stem` 단독**이다(옛 `도메인/stem` 아님). 값은 frontmatter에 `_rel`·`_stem`을 더한 것. `_domain`은 없다
  - `run_all(repo_root, pages=None) -> list[Finding]`
  - `check_fields`, `check_refs`, `check_chain`, `check_reverse`, `check_index_projection`
  - `repo_root_ok`, `wrong_root_message`

- [ ] **Step 1: 원본 복사**

```bash
SRC=/Users/jeonheehoon/Documents/work_station/github/huihun-private-wiki
cp "$SRC/wiki/script/check_status.py" wiki/script/check_status.py
cp "$SRC/wiki/script/tests/test_check_status.py" wiki/script/tests/test_check_status.py
```

- [ ] **Step 2: 테스트 경로를 치환하고 도메인 케이스를 지운다**

`wiki/domains/a/sources/src-X.md` → `wiki/pages/sources/src-X.md`,
`wiki/domains/a/a-index.md` → `wiki/pages/index.md`로 바꾼다.

"도메인을 가로지르는 `superseded_by`는 위반"을 검증하는 케이스가 있으면 삭제한다. 도메인이 없으므로 그 성질 자체가 사라졌다.

아래 케이스를 파일 끝에 덧붙인다.

```python
def test_sources_are_keyed_by_stem_alone(make_repo):
    """도메인 접두사가 사라졌다. 키가 옛 형태면 체인 추적이 통째로 빗나간다."""
    root = make_repo({
        "wiki/pages/sources/src-v1.md": page_src(status="current"),
    })
    sources = check_status.collect_sources(root)
    assert set(sources) == {"src-v1"}
    assert "_domain" not in sources["src-v1"]
```

`page_src` 헬퍼가 없으면 파일 상단에 추가한다.

```python
def page_src(status, superseded_by=None, supersedes=None, scope=None):
    lines = ["---", "tags: [source]", "updated: 2026-09-17",
             "sources: [원본.md]", f"status: {status}"]
    if superseded_by is not None:
        lines.append(f"superseded_by: {superseded_by}")
    if supersedes is not None:
        lines.append(f"supersedes: {supersedes}")
    if scope is not None:
        lines.append(f"scope: {scope}")
    lines += ["---", "", "본문"]
    return "\n".join(lines)
```

- [ ] **Step 3: 테스트를 돌려 실패를 확인**

Run: `python3 -m pytest wiki/script/tests/test_check_status.py -v`
Expected: 다수 FAIL. `collect_sources`가 `pg.domain`을 참조해 `AttributeError`를 낸다.

- [ ] **Step 4: 모듈 docstring에서 도메인 문장 삭제**

```python
"""판본 상태(status) 무결성 검사.

사용: python3 wiki/script/check_status.py    (저장소 루트에서)
종료 코드: 위반 0건이면 0, 있으면 1.
"""
```

- [ ] **Step 5: `collect_sources`와 `_key` 교체**

```python
def collect_sources(repo_root: pathlib.Path, pages: list | None = None) -> dict[str, dict]:
    """{stem: frontmatter}. pages를 주면 재수집하지 않는다."""
    out = {}
    for pg in (wikilib.load_pages(repo_root) if pages is None else pages):
        if pg.parent_name != "sources":
            continue
        out[pg.stem] = {**pg.fm, "_rel": pg.rel, "_stem": pg.stem}
    return out


def _key(name: str) -> str:
    return wikilib.nfc(name)
```

- [ ] **Step 6: `_key` 호출부 4곳을 고친다**

`check_refs`:

```python
                if _key(target) not in sources:
                    out.append(Finding(
                        "violation", "status", fm["_rel"],
                        f"{field} 대상 없음 → {target}"))
```

`check_chain`:

```python
            cur = _key(nxt[0])
```

`check_reverse`: 시작 줄을 `stem = fm["_stem"]`으로 바꾸고 두 `tk` 줄을 `tk = _key(target)`으로 바꾼다.

- [ ] **Step 7: `check_index_projection` 교체**

```python
def check_index_projection(repo_root: pathlib.Path,
                           sources: dict[str, dict]) -> list[Finding]:
    """wiki/pages/index.md의 표기가 frontmatter status와 맞는지. frontmatter가 정답이다."""
    idx = repo_root / "wiki" / "pages" / "index.md"
    lines = idx.read_text(encoding="utf-8").split("\n") if idx.exists() else []
    out = []
    for key, fm in sorted(sources.items()):
        stem, rel = fm["_stem"], fm["_rel"]
        line = next((l for l in lines if l.startswith(f"- [[{stem}]]")), None)
        if line is None:
            out.append(Finding("violation", "status", rel,
                               "wiki/pages/index.md에 줄 없음"))
            continue
        has_cur, has_sup = "현행 정본" in line, "🔁" in line
        st = fm.get("status")
        if st == "current" and not has_cur:
            out.append(Finding("violation", "status", rel,
                               "index 불일치 — status=current인데 '현행 정본' 표기 없음"))
        if st in ("superseded", "partial") and not has_sup:
            out.append(Finding("violation", "status", rel,
                               f"index 불일치 — status={st}인데 🔁 표기 없음"))
        if st == "superseded" and has_cur:
            out.append(Finding("violation", "status", rel,
                               "index 불일치 — 전면 폐기본인데 '현행 정본' 표기"))
        if st == "partial" and not (has_cur and has_sup):
            out.append(Finding(
                "violation", "status", rel,
                "index 불일치 — partial은 현행 범위와 🔁 대체 범위를 모두 표기해야 함"))
    return out
```

- [ ] **Step 8: 테스트를 돌려 통과를 확인**

Run: `python3 -m pytest wiki/script/tests/test_check_status.py -v`
Expected: 전부 PASS.

- [ ] **Step 9: Commit**

```bash
git add wiki/script/check_status.py wiki/script/tests/test_check_status.py
git commit -m "feat: port check_status keyed by source stem

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: `ingest_cache.py` — 매니페스트 경로 이동

**Files:**
- Create: `wiki/script/ingest_cache.py` (원본 복사 후 수정)
- Create: `wiki/script/tests/test_ingest_cache.py` (원본 복사 후 수정)

**Interfaces:**
- Consumes: 없음 (`wikilib`를 쓰지 않는다)
- Produces:
  - 상수 `MANIFEST_REL = "wiki/raw/.manifest.json"`
  - `sha256_of(path) -> str`, `load_manifest(repo_root) -> dict[str, str]`, `save_manifest(repo_root, manifest)`, `raw_markdown(repo_root) -> list[pathlib.Path]`, `pending(repo_root, manifest=None) -> list[str]`, `record(repo_root, rel)`, `prune(repo_root) -> list[str]`, `repo_root_ok(repo_root) -> bool`

- [ ] **Step 1: 원본 복사**

```bash
SRC=/Users/jeonheehoon/Documents/work_station/github/huihun-private-wiki
cp "$SRC/wiki/script/ingest_cache.py" wiki/script/ingest_cache.py
cp "$SRC/wiki/script/tests/test_ingest_cache.py" wiki/script/tests/test_ingest_cache.py
```

- [ ] **Step 2: 테스트 경로를 치환한다**

`raw/X.md` → `wiki/raw/X.md`, `raw/<도메인>/X.md` → `wiki/raw/X.md`,
`raw/.manifest.json` → `wiki/raw/.manifest.json`.

- [ ] **Step 3: 테스트를 돌려 실패를 확인**

Run: `python3 -m pytest wiki/script/tests/test_ingest_cache.py -v`
Expected: FAIL. `pending`이 옛 `raw/`를 보므로 대상 0건을 돌려준다.

- [ ] **Step 4: 상수와 두 함수를 고친다**

```python
MANIFEST_REL = "wiki/raw/.manifest.json"
```

```python
def raw_markdown(repo_root: pathlib.Path) -> list[pathlib.Path]:
    raw = repo_root / "wiki" / "raw"
    return sorted(raw.rglob("*.md")) if raw.is_dir() else []
```

```python
def repo_root_ok(repo_root: pathlib.Path) -> bool:
    """lint.py·route.py와 달리 wiki/conventions.md까지는 요구하지 않는다.

    저 둘은 검사 대상이 0건이 되는 것을 막아야 하므로 계약 파일을 함께 요구한다.
    이 스크립트는 검사하지 않고 wiki/raw/의 해시만 기록하며, 매니페스트는 위키
    트리가 채워지기 *전에* 만들어질 수 있어야 한다.
    """
    return (repo_root / "wiki").is_dir()
```

`main()`의 오류 문구도 함께 고친다.

```python
        print(f"저장소 루트에서 실행한다. 현재 위치({repo_root})에 wiki/ 가 없다.",
              file=sys.stderr)
```

- [ ] **Step 5: 테스트를 돌려 통과를 확인**

Run: `python3 -m pytest wiki/script/tests/test_ingest_cache.py -v`
Expected: 전부 PASS.

- [ ] **Step 6: Commit**

```bash
git add wiki/script/ingest_cache.py wiki/script/tests/test_ingest_cache.py
git commit -m "feat: port ingest_cache with manifest under wiki/raw

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: `route.py`와 `routing.json` — `--domain` 제거, 의도 7개

**Files:**
- Create: `wiki/script/route.py` (원본 복사 후 수정)
- Create: `wiki/routing.json` (새로 작성)
- Create: `wiki/script/tests/test_route_lookup.py`, `test_route_expand.py`, `test_routing_json.py`

**Interfaces:**
- Consumes: Task 2의 `wikilib`
- Produces:
  - `Route` 데이터클래스 — 필드 `intent`, `required`, `reference`, `notes`, `stop`, `stop_message`. **`domain` 필드는 없다**
  - `lookup(repo_root, intent, routing=None) -> Route` — 인자 3개다(옛 4개 아님)
  - `expand(repo_root, route, seeds, routing=None, pages=None) -> Route`
  - `validate_routing(repo_root, routing) -> list[Finding]`, `load_routing(repo_root) -> dict`
  - `resolve_seeds(pages, seeds) -> list[str]`, `build_graph(pages)`, `score_candidates(pages, seeds, weights)`
  - `as_dict(route) -> dict`, `render(route) -> str`, `repo_root_ok`, `main(argv=None) -> int`
  - `RoutingError`

- [ ] **Step 1: 원본 복사**

```bash
SRC=/Users/jeonheehoon/Documents/work_station/github/huihun-private-wiki
cp "$SRC/wiki/script/route.py" wiki/script/route.py
for t in route_lookup route_expand routing_json; do
  cp "$SRC/wiki/script/tests/test_$t.py" "wiki/script/tests/test_$t.py"
done
```

- [ ] **Step 2: `wiki/routing.json`을 새로 쓴다**

`domains` 블록이 없고 의도가 7개다. 경로는 전부 새 배치다.

```json
{
  "version": 1,
  "intents": {
    "ingest": {
      "required": ["wiki/conventions.md", "wiki/open-questions.md",
                   "wiki/pages/purpose.md", "wiki/pages/index.md"],
      "reference": ["wiki/references/ingest-checklist.md"]
    },
    "query": {
      "required": ["wiki/open-questions.md", "wiki/pages/index.md"],
      "reference": []
    },
    "research": {
      "required": ["wiki/conventions.md", "wiki/pages/purpose.md",
                   "wiki/pages/overview.md", "wiki/open-questions.md"],
      "reference": ["wiki/references/research.md",
                    "wiki/graphify-out/GRAPH_REPORT.md"]
    },
    "delete-source": {
      "required": ["wiki/conventions.md", "wiki/references/cascade-delete.md"],
      "reference": []
    },
    "lint": {
      "required": ["wiki/conventions.md"],
      "reference": ["wiki/references/lint-rules.md"]
    },
    "schema-change": {
      "required": ["wiki/conventions.md"],
      "reference": []
    },
    "unclear": {
      "action": "stop",
      "message": "의도를 사용자에게 확인한다"
    }
  },
  "expansion": {
    "weights": {
      "source_overlap": 4,
      "direct_link": 3,
      "shared_neighbor": 1.5,
      "same_type": 1
    },
    "min_score": 4,
    "top_k": 5,
    "hops": 1
  }
}
```

원본 `schema-change`는 `docs/llm-wiki.md`도 required로 걸었다. 그 설계 문서는 이 저장소로 옮기지 않으므로 뺀다 — `validate_routing`이 없는 경로를 위반으로 잡아 `route.py`가 종료 코드 4를 낸다.

`wiki/graphify-out/GRAPH_REPORT.md`는 첫 graphify 실행 전까지 존재하지 않는다. `validate_routing`은 `reference` 경로도 검사하므로, 이 파일이 없으면 모든 의도가 4로 막힌다. Step 4에서 자리표시 파일을 만든다.

- [ ] **Step 3: 테스트를 새 구조로 고친다**

세 파일에서 아래를 적용한다.

- `lookup(root, "ingest", None, routing)` → `lookup(root, "ingest", routing)`
- `Route(...)` 생성자에서 두 번째 위치 인자(도메인)를 뺀다
- `--domain` 인자를 넘기는 `main()` 케이스를 삭제한다
- `"add-domain"` 의도를 쓰는 케이스를 삭제한다
- `domains` 블록을 담은 fixture `routing` 딕셔너리에서 그 블록을 삭제한다
- 경로는 Task 2 Step 5 대응표대로 치환한다

`test_route_lookup.py` 끝에 아래를 덧붙인다. 상단에 `import pytest`와 `import route`가 있는지 확인하고 없으면 추가한다.

```python
def test_unknown_intent_raises(make_repo):
    root = make_repo({"wiki/conventions.md": "# contract"})
    routing = {"intents": {"lint": {"required": ["wiki/conventions.md"]}}}
    with pytest.raises(route.RoutingError) as e:
        route.lookup(root, "add-domain", routing)
    assert "알 수 없는 의도" in str(e.value)


def test_route_has_no_domain_field():
    r = route.Route(intent="lint")
    assert not hasattr(r, "domain")
    assert "domain" not in route.as_dict(r)
```

`test_routing_json.py`에는 실제 `wiki/routing.json`을 검증하는 케이스를 둔다. 파일 상단에 `import json`, `import pathlib`, `import route`가 있는지 확인하고 없으면 추가한다. 아래 `parents[3]`은 `wiki/script/tests/` 기준으로 저장소 루트를 가리킨다.

```python
def test_shipped_routing_json_has_seven_intents():
    repo_root = pathlib.Path(__file__).resolve().parents[3]
    routing = json.loads((repo_root / "wiki" / "routing.json")
                         .read_text(encoding="utf-8"))
    assert "domains" not in routing
    assert set(routing["intents"]) == {
        "ingest", "query", "research", "delete-source",
        "lint", "schema-change", "unclear"}


def test_shipped_routing_json_paths_exist():
    repo_root = pathlib.Path(__file__).resolve().parents[3]
    routing = json.loads((repo_root / "wiki" / "routing.json")
                         .read_text(encoding="utf-8"))
    assert route.validate_routing(repo_root, routing) == []
```

- [ ] **Step 4: `GRAPH_REPORT.md` 자리표시 파일을 만든다**

```bash
cat > wiki/graphify-out/GRAPH_REPORT.md <<'EOF'
# 지식 그래프 리포트

아직 graphify를 돌리지 않았다. 이 파일은 `wiki/routing.json`의 `research` 의도가
reference로 가리키는 자리라 비어 있어도 존재해야 한다 — 없으면 `route.py`의
`validate_routing`이 라우팅경로 위반으로 잡아 모든 의도가 종료 코드 4로 막힌다.

첫 추출은 `wiki/CLAUDE.md`의 "작업 후" 절을 따른다. 추출이 돌면 graphify가 이
파일을 덮어쓴다.
EOF
```

- [ ] **Step 5: 테스트를 돌려 실패를 확인**

Run: `python3 -m pytest wiki/script/tests/ -v -k "route or routing"`
Expected: FAIL. `lookup()`이 위치 인자 4개를 요구한다.

- [ ] **Step 6: `route.py`에서 도메인을 제거한다**

`RoutingError` docstring과 `Route`를 고친다.

```python
class RoutingError(Exception):
    """라우팅 입력이 잘못됐다 — 알 수 없는 의도, 또는 해석되지 않는 seed."""


@dataclass(frozen=True)
class Route:
    intent: str
    required: list[str] = field(default_factory=list)
    reference: list[str] = field(default_factory=list)
    notes: list[str] = field(default_factory=list)
    stop: bool = False
    stop_message: str | None = None
```

`validate_routing`의 `buckets` 두 줄을 한 줄로 줄인다.

```python
    buckets = [(f"intents.{n}", s) for n, s in routing.get("intents", {}).items()]
```

`lookup`을 교체한다.

```python
def lookup(repo_root: pathlib.Path, intent: str,
           routing: dict | None = None) -> Route:
    routing = routing if routing is not None else load_routing(repo_root)
    intents = routing.get("intents", {})
    if intent not in intents:
        raise RoutingError(
            f"알 수 없는 의도 '{intent}'. 허용: {', '.join(sorted(intents))}")
    spec = intents[intent]
    if spec.get("action") == "stop":
        return Route(intent=intent, stop=True,
                     stop_message=spec.get("message", "사용자에게 확인한다"))

    required = _dedup(list(spec.get("required", [])))
    reference = _dedup(list(spec.get("reference", [])))
    reference = [r for r in reference if r not in set(required)]
    notes: list[str] = []
    if len(required) > 7:
        notes.append(
            f"required {len(required)}개 — 7개 경고선을 넘었다. 라우팅이 과한지 검토하고 "
            f"wiki/routing-misses.md에 기록한다")
    return Route(intent=intent, required=required,
                 reference=reference, notes=notes)
```

`resolve_seeds`의 오류 문구 예시를 고친다.

```python
                f"--seed '{seed}' 가 수집된 페이지가 아니다. 저장소 상대 경로로 쓴다 "
                f"(예: wiki/pages/concepts/<페이지>.md). graphify 노드명은 "
                f"경로가 아니므로 그대로 넘기지 않는다")
```

`expand`의 `Route(...)` 두 곳에서 `route.domain` 인자를 뺀다.

```python
        return Route(route.intent, route.required, route.reference,
                     notes, route.stop, route.stop_message)
```

```python
    return Route(route.intent, route.required,
                 route.reference + [p for _, p in ranked],
                 notes, route.stop, route.stop_message)
```

`as_dict`와 `render`를 고친다.

```python
def as_dict(route: Route) -> dict:
    return {"intent": route.intent,
            "required": route.required, "reference": route.reference,
            "notes": route.notes, "stop": route.stop,
            "stop_message": route.stop_message}


def render(route: Route) -> str:
    if route.stop:
        return f"STOP — {route.stop_message}\n읽을 문서를 산출하지 않는다."
    lines = [f"의도: {route.intent}", "",
             f"## required ({len(route.required)}) — 작업 전 반드시 읽는다"]
    lines += [f"- {r}" for r in route.required] or ["- (없음)"]
    lines += ["", f"## reference ({len(route.reference)}) — 필요할 때만 연다"]
    lines += [f"- {r}" for r in route.reference] or ["- (없음)"]
    if route.notes:
        lines += ["", "## 참고"] + [f"- {n}" for n in route.notes]
    return "\n".join(lines)
```

`main()`에서 `ap.add_argument("--domain", default=None)` 줄을 삭제하고 호출을 고친다.

```python
        route = lookup(repo_root, args.intent, routing)
```

모듈 docstring의 사용법 줄도 고친다.

```python
사용: python3 wiki/script/route.py --intent <의도> [--seed <경로> ...]
```

- [ ] **Step 7: 테스트를 돌려 통과를 확인**

Run: `python3 -m pytest wiki/script/tests/ -v -k "route or routing"`
Expected: 전부 PASS.

- [ ] **Step 8: CLI를 직접 확인**

Run: `python3 wiki/script/route.py --intent lint; echo "exit=$?"`
Expected: `의도: lint`가 찍히고 `exit=0`. 이 시점에는 `wiki/conventions.md`와 `wiki/references/lint-rules.md`가 아직 없어 종료 코드 4가 날 수 있다 — 그 경우는 Task 8 이후에 다시 확인하며, 여기서는 `pytest`가 통과하면 넘어간다.

- [ ] **Step 9: Commit**

```bash
git add wiki/script/route.py wiki/routing.json wiki/graphify-out/GRAPH_REPORT.md wiki/script/tests/test_route_*.py wiki/script/tests/test_routing_json.py
git commit -m "feat: port route with seven intents and no domain argument

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: `graph_signals.py` — 스캔 루트와 산출물 경로 이동

**Files:**
- Create: `wiki/script/graph_signals.py` (원본 복사 후 수정)
- Create: `wiki/script/tests/test_graph_signals.py` (원본 복사 후 수정)

**Interfaces:**
- Consumes: Task 2의 `wikilib` (`Finding`, `nfc`)
- Produces:
  - `check_graph_signals(repo_root, pages) -> list[Finding]` — `lint.run_all`이 부른다
  - 상수 `SCAN_ROOT = "wiki/pages"`

- [ ] **Step 1: 원본 복사**

```bash
SRC=/Users/jeonheehoon/Documents/work_station/github/huihun-private-wiki
cp "$SRC/wiki/script/graph_signals.py" wiki/script/graph_signals.py
cp "$SRC/wiki/script/tests/test_graph_signals.py" wiki/script/tests/test_graph_signals.py
```

- [ ] **Step 2: 테스트 경로를 치환한다**

`graphify-out/` → `wiki/graphify-out/`, `wiki/domains/` → `wiki/pages/`,
`source_file` 값의 `<도메인>/concepts/X.md` → `concepts/X.md`(스캔 루트 기준 상대경로라 도메인 한 겹이 사라진다).

파일 끝에 아래를 덧붙인다.

```python
def test_missing_graph_is_silent(make_repo):
    """graph.json이 없는 저장소는 정상 상태다. 경고를 내면 첫 ingest 전까지 노이즈다."""
    root = make_repo({"wiki/pages/concepts/개념.md": page(sources="[]")})
    assert graph_signals.check_graph_signals(root, wikilib.load_pages(root)) == []
```

- [ ] **Step 3: 테스트를 돌려 실패를 확인**

Run: `python3 -m pytest wiki/script/tests/test_graph_signals.py -v`
Expected: FAIL. 모듈이 옛 `graphify-out/`을 본다.

- [ ] **Step 4: 상수와 경로 함수를 고친다**

```python
# graphify가 노드를 스캔하는 루트. graph.json의 source_file은 이 루트 기준
# 상대경로다 — 저장소 상대경로인 Page.rel과 형태가 다르므로 그대로 비교하면 안 된다.
#
# 같은 값이 wiki/graphify-out/.graphify_root에도 (절대경로로) 기록돼 있다. 두
# 사본이 어긋나면 이 모듈의 판정이 통째로 빗나가므로 check_scan_root가 매번
# 대조한다 — 상수는 그 파일이 없을 때의 fallback이다.
SCAN_ROOT = "wiki/pages"
```

```python
def _graph_path(repo_root: pathlib.Path) -> pathlib.Path:
    return repo_root / "wiki" / "graphify-out" / "graph.json"


def _manifest_path(repo_root: pathlib.Path) -> pathlib.Path:
    return repo_root / "wiki" / "graphify-out" / "manifest.json"


def _root_path(repo_root: pathlib.Path) -> pathlib.Path:
    return repo_root / "wiki" / "graphify-out" / ".graphify_root"
```

모듈 docstring의 `graphify-out/graph.json` 언급도 `wiki/graphify-out/graph.json`으로 고친다.

- [ ] **Step 5: 테스트 전체를 돌려 통과를 확인**

Run: `python3 -m pytest wiki/script/tests/ -q`
Expected: 전부 PASS. `test_lint_cli.py`도 이제 `graph_signals`를 찾아 통과한다.

- [ ] **Step 6: Commit**

```bash
git add wiki/script/graph_signals.py wiki/script/tests/test_graph_signals.py
git commit -m "feat: port graph_signals for wiki/pages scan root

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 규약과 절차 문서

**Files:**
- Create: `wiki/conventions.md`, `wiki/CLAUDE.md`
- Create: `wiki/references/{ingest-checklist,cascade-delete,lint-rules,research}.md`
- Create: `wiki/templates/{concept,entity,query,source,synthesis-analysis,synthesis-lint}.md`
- 가져오지 않음: `wiki/references/add-domain.md`, `wiki/templates/domain-{purpose,index,overview}.md`

**Interfaces:**
- Consumes: Task 3~7의 스크립트 동작(문서가 그 동작을 서술한다)
- Produces: `wiki/routing.json`이 `required`/`reference`로 가리키는 실제 파일들. 이 태스크가 끝나야 `route.py`가 모든 의도에서 종료 코드 0을 낸다

- [ ] **Step 1: 원본 복사**

```bash
SRC=/Users/jeonheehoon/Documents/work_station/github/huihun-private-wiki
cp "$SRC/wiki/conventions.md" wiki/conventions.md
cp "$SRC/wiki/CLAUDE.md" wiki/CLAUDE.md
for f in ingest-checklist cascade-delete lint-rules research; do
  cp "$SRC/wiki/references/$f.md" "wiki/references/$f.md"
done
for f in concept entity query source synthesis-analysis synthesis-lint; do
  cp "$SRC/wiki/templates/$f.md" "wiki/templates/$f.md"
done
```

- [ ] **Step 2: `wiki/conventions.md` 5곳 수정**

- **§1 근거 우선순위** — 허브와 도메인 index 2단계를 한 단계로 합친다.

  > `wiki/pages/index.md` → `concepts/`·`entities/` → `wiki/open-questions.md`(필수)
  > → 필요 시 `sources/`.

- **§2 판본 상태** — "판본 관계는 **같은 도메인 안에서만** 성립한다. 도메인을 가로지르는 참조는 위반이다." 줄을 삭제한다. `<도메인>-index.md`를 `wiki/pages/index.md`로 바꾼다.

- **§3 미결 항목** — 경로를 `wiki/open-questions.md`로 바꾼다. `issue` 필드를 설명하는 문단 3개(선택 필드 정의, `sync_issues.py` 투영, 빈 값 금지)를 통째로 삭제하고, 항목 형식 예시에서 `- **issue**: #12` 줄을 뺀다.

- **§4 파일명과 링크** — Obsidian 근거를 교체한다.

  > - 파일명은 `wiki/` 안에서 유일해야 한다. `route.py`의 seed 확장과 `lint.py`의
  >   링크 검사가 `[[이름]]`을 파일 하나로 확정할 수 있어야 하기 때문이다. 이름이
  >   겹치면 링크가 깨지지 않고 아무 파일로나 해석된다 — 조용히 틀리는 것이
  >   깨지는 것보다 나쁘다.
  >   - 예외: 런타임이 이름을 정하는 진입 파일(`wiki/CLAUDE.md`,
  >     `wiki/conventions.md`, `wiki/routing-misses.md`)은 **서로 간에만** 같은
  >     stem을 허용한다. 이들은 수집 대상이 아니라 경로로만 참조된다.
  >   - 검사 범위는 `wiki/` 안으로 한정한다. 저장소의 안드로이드 쪽 마크다운
  >     (`README.md`가 `/`·`http/`·`tools/build-cache-bench/` 세 곳에 있다)은
  >     `[[이름]]` 링크 대상이 아니다.

- **디렉토리 배치 절** — `wiki/domains/<도메인>/` 기반 서술을 Global Constraints의 최종 배치 트리로 교체한다. `wiki/graphify-out/`이 수집 대상이 아니라는 점과, 그것이 스캔 루트 `wiki/pages/` 밖에 있어야 하는 이유를 한 문단으로 적는다.

- [ ] **Step 3: `wiki/CLAUDE.md` 수정**

- 라우팅 절차에서 도메인 판정 단계를 삭제하고, `--domain` 인자 언급을 전부 뺀다.
- 의도 목록을 7개로 줄인다(`add-domain` 삭제).
- 라우터 실행 줄을 고친다.

  ```
  python3 wiki/script/route.py --intent <의도> [--seed <저장소상대경로> ...] [--json]
  ```

- "작업 후" 절을 아래로 교체한다.

  > `python3 wiki/script/lint.py`로 위반 0건을 확인한다.
  >
  > PR을 올리기 전 로컬에서 graphify를 1회 수동 실행하고 `wiki/graphify-out/`
  > 산출물까지 함께 커밋한다.
  >
  >     graphify extract ./wiki/pages
  >
  > 백엔드는 실행하는 환경에 맞춰 고른다. 셋 중 하나다 — 로컬 `ollama`
  > (`--backend ollama`), `OPENAI_BASE_URL`을 로컬 서버로 돌린 `openai` 백엔드,
  > 또는 API 키를 쓰는 원격 백엔드. 앞의 둘은 API 키가 필요 없다.
  > `graphify update`와 `--code-only`는 코드 전용이라 이 위키에는 쓰지 않는다.
  >
  > `graph.json`이 아직 없으면 `lint.py`의 그래프 신호 4종은 아예 뜨지 않는다.
  > 백엔드가 없는 환경에서도 lint는 정상 동작한다.

- graphify 조회 명령 예시의 `--graph` 경로를 `wiki/graphify-out/graph.json`으로 고친다.

- [ ] **Step 4: `wiki/references/` 4개 수정**

- `lint-rules.md` — `issue` 규칙 행을 삭제한다. `파일명중복` 행의 근거를 Step 2 §4와 같은 문장으로 바꾼다. `그래프stale` 행의 `graphify-out/` 경로를 `wiki/graphify-out/`으로, `graph_signals.SCAN_ROOT` 값을 `wiki/pages`로 고친다. `배치` 행의 `wiki/domains/` 서술을 `wiki/pages/`로 고친다. `raw정합` 행을 stem 단독 짝으로 고친다.
- `ingest-checklist.md` — 도메인 디렉토리를 고르는 단계를 삭제한다. 경로를 `wiki/pages/`·`wiki/raw/`로 고친다.
- `cascade-delete.md` — 경로만 고친다.
- `research.md` — 경로를 고치고, 말미의 `graphify-out/GRAPH_REPORT.md`를 `wiki/graphify-out/GRAPH_REPORT.md`로 바꾼다.

- [ ] **Step 5: `wiki/templates/` 확인**

6개 템플릿에 `wiki/domains/`나 `issue` 필드 언급이 있으면 고친다. `source.md`의 frontmatter에 `status`가 있는지 확인한다.

- [ ] **Step 6: 라우팅이 실제로 도는지 확인**

Run:

```bash
for i in ingest query research delete-source lint schema-change; do
  python3 wiki/script/route.py --intent "$i" >/dev/null; echo "$i=$?"
done
python3 wiki/script/route.py --intent unclear >/dev/null; echo "unclear=$?"
python3 wiki/script/route.py --intent add-domain >/dev/null 2>&1; echo "add-domain=$?"
```

Expected: 앞 6개 전부 `=0`, `unclear=3`, `add-domain=4`.

`ingest`와 `query`는 `wiki/pages/{purpose,index}.md`를 required로 요구한다. 그 파일들은 Task 9가 만든다 — 이 단계에서 `=4`가 나오면 Task 9 이후 Task 10에서 다시 확인한다.

- [ ] **Step 7: Commit**

```bash
git add wiki/conventions.md wiki/CLAUDE.md wiki/references wiki/templates
git commit -m "docs: port wiki contract and procedures for flat layout

Drop the domain layer and the GitHub issue projection from the data
contract, and rewrite the link-uniqueness rationale around route.py
seed resolution instead of Obsidian.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: 콘텐츠 골격

**Files:**
- Create: `wiki/pages/purpose.md`, `wiki/pages/overview.md`, `wiki/pages/index.md`
- Create: `wiki/open-questions.md`, `wiki/routing-misses.md`, `wiki/log.md`

**Interfaces:**
- Consumes: Task 8의 `wiki/conventions.md`(frontmatter 계약), Task 3의 `lint.py` 검사
- Produces: `lint.py`가 위반 0건을 낼 수 있는 최소 페이지 집합

- [ ] **Step 1: `wiki/pages/purpose.md` 작성**

`$SRC/wiki/domains/parfait/parfait-purpose.md`를 바탕으로 한다. 바꿀 것 셋:

1. frontmatter `tags: [purpose, parfait]` → `tags: [purpose]`, `updated: 2026-09-17`
2. 제목 `# parfait 목적` → `# 목적`, 본문의 "이 도메인" → "이 위키"
3. **`[[parfait-screen-id-scheme]]` 링크를 평문으로 푼다.** 그 페이지는 아직 없으므로 링크로 두면 `깨진링크` 위반이 뜬다

```markdown
---
tags: [purpose]
updated: 2026-09-17
---

# 목적

## 무엇을 알아내려 하는가

parfait가 **무엇을 만드는 것이고 무엇을 만들지 않기로 했는지**를 추적한다. 요구가
어디서 나왔고 범위가 언제 어떻게 조정됐는지가 이 위키의 대상이다. 결정된 범위만
남기지 않고 범위 밖으로 밀어낸 것과 그 이유도 함께 남긴다 — 같은 요구가 다시 올라올
때 매번 처음부터 판단하지 않기 위해서다.

## 핵심 질문

1. parfait는 무엇을 만드는가 — 해결하려는 문제와 산출물은 무엇인가.
2. 누구를 위한 것인가 — 사용자와 그들이 지금 겪는 불편은 무엇인가.
3. 무엇을 만들지 않기로 했는가 — 범위 밖으로 민 요구는 무엇이고 근거는 무엇인가.
4. 범위가 바뀌었다면 무엇이 바뀌게 했는가 — 어떤 사실이 들어와 판단이 뒤집혔는가.

## 범위

- 포함: parfait의 제품 요구, 사용자와 사용 맥락, 범위 결정과 그 근거, 범위가 바뀐
  시점과 계기.
- 제외: 코드 구현 세부. 함수·파일 단위 구조는 이 저장소의 소스가 이미 담고 있다.
  이 위키는 무엇을 왜 하기로 했는지만 담는다.

## 지역 규약

범위를 좁힌 결정은 근거 없이 적지 않는다. "안 한다"만 남고 이유가 사라지면, 다음에
같은 요구가 올라왔을 때 그 줄이 판단의 재료가 되지 못한다.

기능정의서 판본군을 다룰 때 아래 셋을 더 지킨다.

- 화면은 항상 `A-001` 같은 **ID로 지칭한다.** 화면명은 판본마다 바뀌지만 ID는 대체로
  유지되므로 판본 비교의 기준점이 ID다. 단 번호가 재사용된 사례가 있으므로
  (`C-305`, `C-104`~`C-106`, `A-003`) 그런 ID는 판본을 함께 적는다. 화면 ID 체계를
  다루는 개념 페이지가 생기면 여기서 그 페이지로 링크한다.
- 원본 표의 빈 칸은 "제약 없음"이 아니라 **"미정"**으로 읽는다. 이 구분을 흐리면
  공백이 결정으로 승격된다.
- **답 없이 사라진 서술을 해소로 읽지 않는다.** 판본이 올라가며 미결 표기나 결정
  서술이 지워지는 일이 반복됐다. 그런 항목은 [[open-questions]]에 계속 남긴다.
```

- [ ] **Step 2: `wiki/pages/overview.md` 작성**

```markdown
---
tags: [overview]
updated: 2026-09-17
---

# 개요

아직 통합된 원본이 없다. 첫 ingest가 끝나면 이 페이지가 위키 전체의 논지를 담는다 —
개별 페이지를 읽지 않고도 지금까지 무엇이 밝혀졌는지 알 수 있어야 한다.

페이지 목록은 [[index]]에 있고, 이 위키가 무엇을 추적하는지는 [[purpose]]에 있다.
```

- [ ] **Step 3: `wiki/pages/index.md` 작성**

`check_status.check_index_projection`이 `- [[stem]]`으로 시작하는 줄을 찾는다. 소스가 생기면 그 형식으로 줄을 추가해야 한다.

```markdown
---
tags: [index]
updated: 2026-09-17
---

# 페이지 카탈로그

이 위키의 페이지 목록이다. 판본 상태(`status`) 표기는 각 소스 페이지 frontmatter의
투영이며, 어긋나면 frontmatter가 정답이다 — 규칙은 `wiki/conventions.md` §2에 있다.

목적은 [[purpose]], 논지는 [[overview]], 미결은 [[open-questions]]에 있다.

## sources

아직 없다. 통합된 원본이 생기면 `- [[src-<이름>]] — <요약>` 형식으로 한 줄씩
추가하고, 현행 정본에는 `**현행 정본**`을, 대체된 판본에는 `🔁`를 붙인다.

## concepts

아직 없다.

## entities

아직 없다.

## queries

아직 없다.

## synthesis

아직 없다.
```

`wiki/conventions.md`를 `[[conventions]]`로 링크하지 않는다. 그 파일은 `UNCOLLECTED_FILES`라 name index에 오르지 않고, 링크하면 `깨진링크` 위반이 된다. 경로로만 참조한다. 같은 이유로 `wiki/CLAUDE.md`와 `wiki/routing-misses.md`도 링크 대상이 아니다.

- [ ] **Step 4: `wiki/open-questions.md` 작성**

```markdown
---
tags: [open-questions]
updated: 2026-09-17
---

# 미결 항목

모순·미결 질문의 단일 추적 장소다. 항목 형식과 `action` 허용값은 `wiki/conventions.md`
§3을 따른다.

**미해결 0건, 해소 0건.**

## 해소된 항목

아직 없다.

## 미해결 항목

아직 없다.
```

- [ ] **Step 5: `wiki/routing-misses.md` 작성**

```markdown
# 라우팅 실패 기록

`route.py`가 내놓은 문서로 작업이 되지 않았던 경우를 적는다. `required`가 7개를
넘었다는 경고를 받았을 때도 여기에 남긴다.

이 파일은 수집 대상이 아니다 — `[[라우팅 실패 기록]]`으로 링크할 수 없고 경로로만
참조한다.

아직 기록 없음.
```

- [ ] **Step 6: `wiki/log.md` 작성**

```markdown
# 작업 로그

append-only다. 앞 항목을 고치지 않는다.

## [2026-09-17] setup | 위키 체계 이식

- `huihun-private-wiki`의 위키 체계를 단일 도메인 평탄 구조로 옮겼다. 설계는
  `docs/superpowers/specs/2026-09-17-wiki-port-design.md`에 있다
- 도메인 차원 제거 — 콘텐츠는 `wiki/pages/` 아래, 구조 파일은 `purpose`·`overview`·
  `index` 한 벌
- 스크립트 6개 이식: `route` `lint` `check_status` `ingest_cache` `wikilib`
  `graph_signals`. `sync_issues.py`는 가져오지 않았다 — 팀 공용 저장소라 이슈를
  자동 생성하면 안 된다. 딸려서 `lint`의 `check_issue_field`와 계약의 `issue`
  필드도 뺐다
- Obsidian 제외. `[[이름]]` 링크 규약과 세 검사는 유지하되 근거를 `route.py` seed
  확장의 결정성으로 다시 썼다
- graphify 산출물은 `wiki/graphify-out/`, 스캔 루트는 `wiki/pages/`
- 콘텐츠는 아직 0건이다. 첫 ingest는 별도 작업
```

- [ ] **Step 7: lint를 돌려 확인**

Run: `python3 wiki/script/lint.py; echo "exit=$?"`
Expected: `exit=0`, 위반 0건.

경고가 뜨면 내용을 보고 판단한다. `약연결` 경고는 종료 코드에 영향이 없다. `고아` 위반이 뜨면 해당 페이지가 `ORPHAN_EXEMPT_PATHS`에 있는지 확인한다.

- [ ] **Step 8: Commit**

```bash
git add wiki/pages wiki/open-questions.md wiki/routing-misses.md wiki/log.md
git commit -m "docs: add empty wiki content scaffold

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

### Task 10: 전체 검증

**Files:**
- Modify: 검증에서 드러난 파일만

**Interfaces:**
- Consumes: Task 1~9 전부
- Produces: 스펙 §7의 검증 결과

- [ ] **Step 1: 테스트 전체**

Run: `python3 -m pytest wiki/script/tests/ -q`
Expected: 전부 PASS, 실패 0건.

- [ ] **Step 2: lint와 check_status**

Run:

```bash
python3 wiki/script/lint.py; echo "lint=$?"
python3 wiki/script/check_status.py; echo "status=$?"
```

Expected: `lint=0`, `status=0`. `check_status`는 "소스 0건 검사"를 찍는다.

- [ ] **Step 3: 라우팅 의도 전부**

Run:

```bash
for i in ingest query research delete-source lint schema-change; do
  python3 wiki/script/route.py --intent "$i" >/dev/null; echo "$i=$?"
done
python3 wiki/script/route.py --intent unclear >/dev/null; echo "unclear=$?"
python3 wiki/script/route.py --intent add-domain >/dev/null 2>&1; echo "add-domain=$?"
```

Expected: 앞 6개 `=0`, `unclear=3`, `add-domain=4`.

- [ ] **Step 4: 루트 가드**

Run:

```bash
REPO=$(pwd)
cd /tmp && python3 "$REPO/wiki/script/lint.py" >/dev/null 2>&1; echo "wrong-root=$?"
cd "$REPO"
```

Expected: `wrong-root=2`.

- [ ] **Step 5: ingest_cache**

Run: `python3 wiki/script/ingest_cache.py --list; echo "exit=$?"`
Expected: "처리 대상 0건"과 `exit=0`.

- [ ] **Step 6: Gradle 빌드가 영향받지 않는지 확인**

Run: `./gradlew --version >/dev/null && echo "gradle ok"`
Expected: `gradle ok`. 위키 추가는 Gradle 설정을 건드리지 않으므로 이 확인만으로 충분하다.

- [ ] **Step 7: 최종 상태 확인**

Run: `git status --short && find wiki -name "*.md" | sort`
Expected: working tree 클린. 마크다운은 `wiki/CLAUDE.md`, `wiki/conventions.md`, `wiki/log.md`, `wiki/open-questions.md`, `wiki/routing-misses.md`, `wiki/graphify-out/GRAPH_REPORT.md`, `references/` 4개, `templates/` 6개, `pages/` 3개 — 총 18개.

- [ ] **Step 8: 검증 결과를 로그에 남기고 Commit**

`wiki/log.md`의 2026-09-17 항목 끝에 한 줄 덧붙인다.

```markdown
- 검증: `pytest` 전건 통과, `lint.py` 위반 0건, `check_status.py` 위반 0건,
  라우팅 6개 의도 0 / `unclear` 3 / 삭제된 `add-domain` 4
```

```bash
git add wiki/log.md
git commit -m "chore: record wiki port verification results

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## 남은 일 (이 계획의 범위 밖)

- 첫 graphify 추출. 현재 머신에 `ollama`가 없고 관련 API 키 환경변수도 설정돼 있지 않다. 백엔드를 정한 뒤 `graphify extract ./wiki/pages`를 1회 돌리고 `wiki/graphify-out/` 산출물을 커밋한다. 그 시점에 `GRAPH_REPORT.md` 자리표시 파일이 실제 리포트로 덮인다.
- 첫 콘텐츠 ingest. `wiki/raw/`에 원본을 넣고 `route.py --intent ingest`로 시작한다.
- `wiki/pages/purpose.md`의 화면 ID 체계 문장을 개념 페이지 링크로 되돌리는 일 — 그 페이지가 실제로 생긴 뒤.
