---
id: ygripple
title: ygDimRipple (커스텀 dim ripple) Implementation Plan
status: done
type: work-order
created: 2026-07-13
updated: 2026-07-15
platforms: android
owner:
related_adr: ADR-0010
related_spec: ygripple
related_code: YGRipple.kt#ygDimRipple
archived_reason:
tags: [plan, parfait, designsystem]
---

# ygDimRipple (커스텀 dim ripple) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `core:designsystem`에 YG 커스텀 dim ripple을 `IndicationNodeFactory`로 제공하는 `ygDimRipple(...)`을 구현한다.

**Architecture:** `ygDimRipple(...)` 팩토리가 `YGDimRippleNodeFactory`(`IndicationNodeFactory`)를 반환하고, `create(interactionSource)`가 `DelegatingYGDimRippleNode`(`DelegatingNode`)를 만들어 `onAttach`에서 `androidx.compose.material.ripple.createRippleModifierNode(...)`에 위임한다. 색(`ColorProducer`)·alpha(`YGDimRippleAlpha` 0.15 균일)를 직접 지정. skt `idDimRipple`/`DimNodeFactory` 패턴 미러.

**Tech Stack:** Kotlin, Jetpack Compose foundation(`IndicationNodeFactory`), `androidx.compose.material.ripple`(`createRippleModifierNode`·`RippleAlpha`), ui.node(`DelegatingNode`). 자체 테마([ADR-0010](../../../adr/0010-custom-compositionlocal-theme.md)).

**Spec:** [specs/2026-07-13-ygripple.md](../../specs/archive/2026-07-13-ygripple.md)

> **구현 후 갱신** — 생성 파일 `YGRipple.kt`는 [리플 변형 플랜](2026-07-13-clickableyg-ripple-variants.md) Task 2에서 `YGDimRipple.kt`로 리네임(`ygScaleRipple`은 별도 `YGScaleRipple.kt`). 이후 (2026-07-14) **`core:designsystem` → `core:util:android` `clickable/`로 이동**, 기본색 `YGAtomicColors.Gray.Gray900` → 리터럴 `YGDimRippleColor = Color(0xFF29292C)`. 아래 코드블록의 패키지·경로·색은 옛 위치.

## Global Constraints
- 대상 repo: `TJYG-Android`. (구현 브랜치는 사용자 지정 — 현재 작업 브랜치 사용.)
- 패키지: `com.teamyg.parfait.core.designsystem.utils.clickable`(신규).
- 색은 `YGAtomicColors.*` 직접(ripple 색 토큰 미정, 과도기 — YGButton 선례).
- 검증: 유닛 TDD 인프라 없음(Compose UI). **compile(`:core:designsystem:compileReleaseKotlin`) + `:core:designsystem:ktlintMainSourceSetCheck` + 기기 press 육안**으로 대체. ripple은 정적 `@Preview`로 안 보임(press 필요) → 기기 확인.
- ktlint 엄격. 커밋 전 `ktlintMainSourceSetFormat`.

---

### Task 1: ygDimRipple 팩토리 + YGDimRippleNodeFactory + DelegatingYGDimRippleNode + YGDimRippleAlpha

**Files:**
- Create: `core/designsystem/src/main/kotlin/com/teamyg/parfait/core/designsystem/utils/clickable/YGRipple.kt`
- (조건부) Modify: `core/designsystem/build.gradle.kts` — Step 2에서 `material.ripple` 미해결 시에만.

**Interfaces:**
- Consumes: `IndicationNodeFactory`(foundation), `InteractionSource`(foundation.interaction), `createRippleModifierNode`·`RippleAlpha`(material.ripple), `DelegatingNode`·`DelegatableNode`·`CompositionLocalConsumerModifierNode`(ui.node), `ColorProducer`(ui.graphics), `YGAtomicColors`.
- Produces: public `fun ygDimRipple(bounded: Boolean = true, radius: Dp = Dp.Unspecified, color: Color = YGAtomicColors.Gray.Gray900, rippleAlpha: RippleAlpha = YGDimRippleAlpha): IndicationNodeFactory`, public `val YGDimRippleAlpha: RippleAlpha`.

- [ ] **Step 1: 파일 작성** — 신규 파일 생성.

```kotlin
package com.teamyg.parfait.core.designsystem.utils.clickable

import androidx.compose.foundation.IndicationNodeFactory
import androidx.compose.foundation.interaction.InteractionSource
import androidx.compose.material.ripple.RippleAlpha
import androidx.compose.material.ripple.createRippleModifierNode
import androidx.compose.runtime.Stable
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.node.CompositionLocalConsumerModifierNode
import androidx.compose.ui.node.DelegatableNode
import androidx.compose.ui.node.DelegatingNode
import androidx.compose.ui.unit.Dp
import com.teamyg.parfait.core.designsystem.theme.colors.YGAtomicColors

val YGDimRippleAlpha: RippleAlpha = RippleAlpha(
    pressedAlpha = 0.15f,
    focusedAlpha = 0.15f,
    draggedAlpha = 0.15f,
    hoveredAlpha = 0.15f,
)

@Stable
fun ygDimRipple(
    bounded: Boolean = true,
    radius: Dp = Dp.Unspecified,
    color: Color = YGAtomicColors.Gray.Gray900,
    rippleAlpha: RippleAlpha = YGDimRippleAlpha,
): IndicationNodeFactory = YGDimRippleNodeFactory(
    bounded = bounded,
    radius = radius,
    color = color,
    rippleAlpha = rippleAlpha,
)

internal class YGDimRippleNodeFactory(
    private val bounded: Boolean,
    private val radius: Dp,
    private val color: Color,
    private val rippleAlpha: RippleAlpha,
) : IndicationNodeFactory {
    override fun create(interactionSource: InteractionSource): DelegatableNode = DelegatingYGDimRippleNode(
        interactionSource = interactionSource,
        bounded = bounded,
        radius = radius,
        color = color,
        rippleAlpha = rippleAlpha,
    )

    override fun equals(other: Any?): Boolean {
        if (this === other) {
            return true
        }
        if (other !is YGDimRippleNodeFactory) {
            return false
        }
        if (bounded != other.bounded) {
            return false
        }
        if (radius != other.radius) {
            return false
        }
        if (color != other.color) {
            return false
        }
        return rippleAlpha == other.rippleAlpha
    }

    override fun hashCode(): Int {
        var result = bounded.hashCode()
        result = 31 * result + radius.hashCode()
        result = 31 * result + color.hashCode()
        result = 31 * result + rippleAlpha.hashCode()
        return result
    }
}

private class DelegatingYGDimRippleNode(
    private val interactionSource: InteractionSource,
    private val bounded: Boolean,
    private val radius: Dp,
    private val color: Color,
    private val rippleAlpha: RippleAlpha,
) : DelegatingNode(), CompositionLocalConsumerModifierNode {
    override fun onAttach() {
        delegate(
            createRippleModifierNode(
                interactionSource = interactionSource,
                bounded = bounded,
                radius = radius,
                color = { color },
                rippleAlpha = { rippleAlpha },
            ),
        )
    }
}
```

- [ ] **Step 2: 컴파일 검증**

Run: `./gradlew :core:designsystem:compileReleaseKotlin --offline`
Expected: `BUILD SUCCESSFUL`

`Unresolved reference: ripple`(또는 `createRippleModifierNode`/`RippleAlpha`) 발생 시 → `material-ripple` 의존이 없는 것. `core/designsystem/build.gradle.kts`의 `dependencies { }`에 아래 추가 후 재실행:

```kotlin
    implementation("androidx.compose.material:material-ripple")
```

(compose BOM으로 버전 관리되므로 버전 생략. `libs.versions.toml`에 alias가 있으면 그것을 우선 사용.) 그 외 시그니처 불일치(`createRippleModifierNode`의 `rippleAlpha` 파라미터 형태 등)는 컴파일 에러에 맞춰 동일 의미로 보정(foundation/material-ripple 1.11.0 기준).

- [ ] **Step 3: ktlint 검증**

Run: `./gradlew :core:designsystem:ktlintMainSourceSetFormat :core:designsystem:ktlintMainSourceSetCheck --offline`
Expected: `BUILD SUCCESSFUL`

- [ ] **Step 4: 기기 press 육안 확인** — 임시로 `Modifier.clickable(interactionSource = remember { MutableInteractionSource() }, indication = ygDimRipple()) { }`(또는 `clickableYG(indication = ygDimRipple(), ...)`)를 박스에 붙여 누름. 은은한(0.15) 회색 리플이 뜨는지, bounded=true라 컨텐츠 경계로 클립되는지 확인. (임시 코드는 커밋 제외.)

- [ ] **Step 5: 커밋** (사용자 승인 후)

```bash
git add core/designsystem/src/main/kotlin/com/teamyg/parfait/core/designsystem/utils/clickable/YGRipple.kt
# (Step 2에서 의존 추가했으면) git add core/designsystem/build.gradle.kts
git commit -m "feat: YG 커스텀 dim ripple ygDimRipple(IndicationNodeFactory)"
```

---

## Self-Review
- **Spec coverage**: 목표(커스텀 dim ripple IndicationNodeFactory)·API(ygDimRipple 시그니처 + 기본 Gray900/YGDimRippleAlpha)·구조(YGDimRippleNodeFactory create+equals/hashCode, DelegatingYGDimRippleNode onAttach→createRippleModifierNode)·YGDimRippleAlpha(0.15 균일)·단일 파일·과도기 색·의존성(material-ripple 조건부) — Task 1에 대응.
- **Placeholder**: 없음(파일 전량 코드). Step 2 의존 추가는 조건부 원격 조치(정확한 라인 명시).
- **Type consistency**: `YGDimRippleNodeFactory`↔`DelegatingYGDimRippleNode` 생성자 파라미터(bounded/radius/color/rippleAlpha) 동일. `create(): DelegatableNode`·`ColorProducer { color }`·`rippleAlpha = { rippleAlpha }` 모두 material-ripple `createRippleModifierNode` 시그니처와 정합(skt `DimNodeFactory` 검증됨).

## 열린 질문
- **material-ripple 의존**: material3 전이 포함 여부는 Step 2 컴파일이 판정. 미포함 시 명시 추가.
- **ripple 색 토큰**: `Gray.Gray900` 직접 참조(과도기) → 시맨틱 `effect/ripple` 토큰화 후속([open-questions](../../../synthesis/open-questions.md)).
- **정적 프리뷰 불가**: ripple은 press 시에만 보여 `@Preview` 미포함, 기기 확인 필수.
