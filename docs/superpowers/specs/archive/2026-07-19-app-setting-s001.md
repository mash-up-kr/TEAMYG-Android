---
id: app-setting-s001
title: 앱 설정 화면 (S-001 AppSetting) + 계정/약관/개인정보 nav stub
status: implemented
category: ui-spec
platforms: android
verified: 2026-08-18
related_code: NavKeyAppSetting.kt, NavKeyAccountInfo.kt, NavKeyWebView.kt, AppSettingRoute.kt#AppSettingRoute, AppSettingScreen.kt#AppSettingScreen, AppSettingViewModel.kt#AppSettingViewModel, AppSettingViewModel.kt#AppSettingState, ProfileCard.kt#ProfileCard, AccountInfoRoute.kt, TermsRoute.kt, EntryBuilder.kt, YGDangerZone.kt#YGDangerZone, YGActionItem.kt#YGActionItem, YGLabel.kt#YGLabel
related_adr: ADR-0010
related_spec: yglistitem, ygtopbar, ygdangerzone, ygdangerzone-dashed, s002-account-info, ygtext-date-label
related_architecture: design-system, navigation-flow, module-structure
supersedes:
superseded_by:
tags: [spec, parfait, feature, setting, navigation]
---

# Spec: 앱 설정 화면 (S-001 AppSetting) + nav stub

- 대상: `:feature:app:setting:api` · `:feature:app:setting:impl`
- 구현 위치: `AppSettingRoute`(`feature/app/setting/impl/.../route/`)
- 관련: [ADR-0010](../../../adr/0010-custom-compositionlocal-theme.md)(자체 테마) · [design-system](../../../architecture/design-system.md) · [navigation-flow](../../../architecture/navigation-flow.md) · 브랜치 `feature/#85-app-side-menu`(PR #160 develop 머지)
- Figma: `S-001`(node `220:2176`) — 파르페 v0.1
- 위키 화면 정의: `S-001`(앱 사이드 메뉴), 이동 타겟 `S-002`(계정 정보/앱 닉네임), `S-004`(약관·개인정보, 노션 웹뷰) — [[기능정의서-v5]]

> 상태·날짜·대상·관련은 frontmatter가 단일 출처. 본문은 설계 내용에 집중.
>
> 🔁 **2026-08-03 추가 — Danger Zone(로그아웃 / 서비스 탈퇴하기)이 이 화면 소속으로 확정.**
> 최초 구현(#160)에는 없었고 [S-002 계정 정보](2026-07-22-s002-account-info.md) 초안이 S-002에 뒀으나, Figma 실물 대조 결과
> `Danger-Zone`(node `2019:6010`)은 **`S-001`(node `220:2176`) Contents 최하단**에 있고
> `S-002`(node `220:2192`)에는 없다. → S-002에서 이관받아 아래 [Danger Zone](#danger-zone-2026-08-03-추가) 절 신설.
> 같은 정정으로 `ProfileCard`의 "내 프로필" 인라인 `Text`가 **`YGLabel`**로 교체됐다(동일 렌더 — `body.b02R` + `Gray400`).
>
> ✅ **2026-08-04 develop 머지(PR #192)** — 위 이관분이 실제 코드로 들어왔다. `AppSettingScreen`에 `YGDangerZone`
> (`YGActionItem` 2개, 좌우 `padding7`), `AppSettingIntent`에 `ClickLogout`·`ClickWithdraw`(VM 핸들러는 로그만 남기는 stub,
> `AppSettingSideEffect` 무변경 4종), `strings.xml`에 `setting_logout`·`setting_withdraw`, `ProfileCard` `YGLabel` 교체까지
> **문서 서술과 일치**. `ProfileCard`의 `RectangleShape` 직접 참조는 여전히 잔존(→ [open-questions](../../../synthesis/open-questions.md) [2026-07-20]).
>
> 🔁 **as-built 정정 (2026-08-18, develop 머지 #295·#296) — 약관 두 줄이 서버 목록을 열고, 버전이 실값이 됐다.**
> - **약관 이동이 목적지 둘에서 하나로**: `NavigateToServiceTerms`·`NavigateToPrivacyPolicy` 두
>   `data object`가 `NavigateToPolicyDetail(title, url)` 하나로 합쳐지고 Route가
>   `goTo(NavKeyWebView(title, url))`를 부른다. 인텐트는 종전대로 둘이다(`ClickServiceTerms`·
>   `ClickPrivacyPolicy`) — 누른 줄이 어느 종류인지는 화면이 알고, 그 종류로 목록에서 고른다.
> - **약관 목록을 진입 시 받아 둔다**: `GetPoliciesUseCase`가 주입되고 `AppSettingState.policies`가
>   생겼다(`policyOf(type)`). 화면의 약관 두 줄은 여전히 `strings.xml` 고정이라 **조회가 실패해도
>   줄이 사라지지 않고**, 목록은 "누를 때 열 제목과 주소"의 출처로만 쓴다. 찾는 종류가 없으면 로그만
>   남기고 아무것도 하지 않는다 — 재조회해도 같은 응답이라는 판단이고, 사용자에게는 실패와 무반응이
>   구분되지 않는다 → [open-questions](../../../synthesis/open-questions.md).
> - **버전 실값**(#295, 아래 "열린 질문" 참고)과 **`YGListItem` 줄 높이 정렬**(컴포넌트가 흡수 — 화면
>   쪽 임시 조정은 develop에 남지 않았다)이 함께 들어왔다.

## 목표
앱 사이드 메뉴(설정) 화면 S-001을 구현한다. 프로필 카드 + 리스트 4항목(계정 정보 / 서비스 이용약관 / 개인정보 처리 방침 / 버전 정보) + Danger Zone 2항목(로그아웃 / 서비스 탈퇴하기, 🔁 2026-08-03 추가). 리스트 앞 3개는 클릭 시 해당 화면으로 네비게이트, 버전 정보는 정적 표시. **이동 타겟 화면(S-002·S-004)은 이번 범위 밖 — Route stub + entry 등록까지만** 해서 `navigator.goTo()`가 런타임 크래시 없이 동작하게 한다.

## 범위
- **포함**:
  - `AppSettingScreen`(stateless UI) + `AppSettingRoute`(navigator·ViewModel 배선) + `AppSettingViewModel`/`AppSettingState`.
  - `ProfileCard` 컴포넌트 분리(`impl/component/`, login:impl 선례 `OnboardingPager`·`KakaoSignInButton`와 동일 배치).
  - 정적 UI 라벨 문자열 리소스화(`res/values/strings.xml`).
  - 이동 타겟 3종 NavKey(api) + Route stub(impl) + EntryBuilder 등록.
- **제외**:
  - 계정 정보(S-002)·서비스약관/개인정보(S-004) 화면 실제 구현 — **stub(`// TODO impl`)만**.
  - 백엔드 프로필 데이터 연동 — ViewModel placeholder emit.
  - 버전 자동 주입(`BuildConfig.VERSION_NAME`) — placeholder 문자열, 후속 TODO.
  - 리스트 항목 전체 행 클릭 — `YGListItem`(오버로드 A)는 trailing 아이콘만 클릭(Figma도 Button-Icon(44)이 탭 타겟이라 일치).

## 모듈 / 파일 구성
```
feature/app/setting/api/.../
  NavKeyAppSetting.kt        (기존)
  NavKeyAccountInfo.kt       (신규, S-002)   data object : NavKey
  NavKeyServiceTerms.kt      (신규, S-004)   data object : NavKey
  NavKeyPrivacyPolicy.kt     (신규, S-004)   data object : NavKey
feature/app/setting/impl/.../
  route/AppSettingRoute.kt        (구현: hiltViewModel + collectAsStateWithLifecycle → Screen)
  route/AppSettingScreen.kt       (신규: stateless UI)
  route/AccountInfoRoute.kt       (신규: // TODO impl)
  route/TermsRoute.kt             (신규: // TODO impl, ServiceTerms·PrivacyPolicy entry 공용)
  viewmodel/AppSettingViewModel.kt (신규: @HiltViewModel + AppSettingState(data class)·AppSettingIntent·AppSettingSideEffect 한 파일에 동거 — login LoginViewModel 선례와 동일)
  component/ProfileCard.kt        (신규: 프로필 카드 컴포넌트 + @YGPreview)
  navigation/EntryBuilder.kt      (수정: entry 3개 추가)
  res/values/strings.xml          (신규: 정적 라벨)
```
> 파일별 패키지(`viewmodel/` 위치 등)는 기존 feature 모듈 관례(예: `feature/*/impl/.../viewmodel/`)를 코드 대조 후 맞춘다.

## State / ViewModel
한 파일(`AppSettingViewModel.kt`)에 State + Intent/SideEffect + ViewModel 동거(MVI, `BaseViewModel` 상속). **내비게이션은 Intent→SideEffect 경유**(Route가 아니라 VM이 결정):
```kotlin
data class AppSettingState(
    val nickname: String = "...",       // placeholder (백엔드 TODO)
    val loginProvider: String = "Kakao",
    val version: String = "1.0v",       // placeholder (BuildConfig 주입 TODO)
) : UiState

sealed interface AppSettingIntent : UiIntent {
    data object ClickBack : AppSettingIntent
    data object ClickAccount : AppSettingIntent
    data object ClickServiceTerms : AppSettingIntent
    data object ClickPrivacyPolicy : AppSettingIntent
}

sealed interface AppSettingSideEffect : UiSideEffect {
    data object NavigateBack : AppSettingSideEffect
    data object NavigateToAccountInfo : AppSettingSideEffect
    data object NavigateToServiceTerms : AppSettingSideEffect
    data object NavigateToPrivacyPolicy : AppSettingSideEffect
}

@HiltViewModel
class AppSettingViewModel @Inject constructor() :
    BaseViewModel<AppSettingState, AppSettingIntent, AppSettingSideEffect>(
        initialState = AppSettingState(),
    ) {
    // processIntent → intent별 private handle* 함수 위임 → 각 handle*가 postSideEffect 호출
    override fun processIntent(intent: AppSettingIntent) = when (intent) {
        AppSettingIntent.ClickBack -> handleClickBack()
        AppSettingIntent.ClickAccount -> handleClickAccount()
        AppSettingIntent.ClickServiceTerms -> handleClickServiceTerms()
        AppSettingIntent.ClickPrivacyPolicy -> handleClickPrivacyPolicy()
    }

    private fun handleClickBack() = postSideEffect(AppSettingSideEffect.NavigateBack)
    // handleClickAccount/ServiceTerms/PrivacyPolicy → 각 Navigate* SideEffect postSideEffect
}
```
- 런타임 상태: 정적(백엔드 연동 전). `state: StateFlow<AppSettingState>`(BaseViewModel)로 placeholder 노출.
- 각 intent는 **private `handleClick*()` 함수로 랩핑**한 뒤 `postSideEffect(...)` 호출(when 분기가 postSideEffect를 직접 호출하지 않음).
- 동적 값(nickname/loginProvider/version)은 **State 소유(데이터)** — 문자열 리소스 대상 아님.

## UI 매핑 (Figma S-001 → 디자인시스템 심볼)
루트 `Column`:
| 요소 | 구현 |
|------|------|
| 상단바 | `YGTopBarBack(onIconClick = onClickBack)` (Figma Top Bar = back only, 타이틀 없음) |
| Contents 좌우 패딩 | `YGTheme.layout.padding.padding7` (Figma Profile-Card x=20, List-Item px-20) |
| 프로필↔리스트 간격 | `YGTheme.layout.gap.gap8` (Figma gap-8=32) |
| **ProfileCard** | 배경 `YGAtomicColors.Transparency.White75`, 보더 `YGAtomicColors.Cherry.Cherry100` 1px, `padding.padding6`(16), 내부 `gap.gap4`(12) |
| ├ 라벨 | `YGLabel(stringResource(setting_profile_label))`="내 프로필" — 🔁 2026-08-03 인라인 `Text(body.b02R, gray-400)`에서 DS 프리셋으로 교체(렌더 동일) |
| └ Profile-Item | `gap.gap2`(4): 닉네임 `typography.title.t03SB` gray-900 / provider `typography.caption.c01R` gray-500 |
| **List-Container** | `gap.gap3`(8) |
| ├ 계정 정보 | `YGListItem(text=계정정보, trailingIcon=ic_caret_right, onClickTrailingIcon=onClickAccount)` |
| ├ 서비스 이용약관 | `YGListItem(text=약관, ic_caret_right, onClickTerms)` |
| ├ 개인정보 처리 방침 | `YGListItem(text=개인정보, ic_caret_right, onClickPrivacy)` |
| └ 버전 정보 | `YGListItem(text=버전정보, subText=state.version)` — subText 오버로드, nav 없음 |
| **Danger-Zone** (🔁 2026-08-03) | `YGDangerZone(topZone, bottomZone)` — 좌우 `padding.padding7`(Figma 폭 335 = 375−40), dashed 테두리·내부 구분선은 컴포넌트 내장 |
| ├ 로그아웃 | `YGActionItem(text=setting_logout, onClick=onClickLogout)` |
| └ 서비스 탈퇴하기 | `YGActionItem(text=setting_withdraw, onClick=onClickWithdraw)` |

- `YGListItem` 기본 `textColor = gray-800`(Figma List-Item 텍스트 gray-800 일치).

## Danger Zone (2026-08-03 추가)

S-002에서 이관. **UI만 이관이고 처리는 여전히 미구현**이다.

- `AppSettingIntent`에 `ClickLogout`·`ClickWithdraw` 추가 → VM 핸들러는 **stub**(`viewModelLogger` + TODO).
  side effect·네비게이션·API 호출 없음. `AppSettingSideEffect`는 무변경(4종 그대로).
- `AppSettingRoute`가 `onClickLogout`/`onClickWithdraw` 콜백을 두 Intent에 배선.
- 문자열 키는 **`setting_logout`·`setting_withdraw`** — S-002 초안의 `account_info_logout`/`account_info_withdraw`에서 개명해 이관(모듈 내 접두 일관성).
- `YGDangerZone` 자체의 피그마 델타(좌우 gap-5·고정폭 335 미반영)는 컴포넌트 쪽 미결 → [open-questions](../../../synthesis/open-questions.md) [2026-07-22]. 이 화면은 `padding7`로 좌우 인셋을 호출부에서 보정한다.
- ProfileCard radius: Figma에 rounded 미표기 → 각짐. **실제 구현은 `RectangleShape` 직접 참조**(배경·보더 both) — 당시 `YGTheme.shapes.radius.none` 토큰이 develop 미머지라 우회. 이후 토큰이 [designsystem-radius-none-sync](2026-07-19-designsystem-radius-none-sync.md)(PR #159, 2026-07-22)로 머지됨 → ProfileCard `RectangleShape`를 `radius.none`으로 승격 권장(코드 교체 잔존, [open-questions](../../../synthesis/open-questions.md) [2026-07-20]).

## 문자열 리소스 (`res/values/strings.xml`)
| key | 값 |
|-----|-----|
| `setting_profile_label` | 내 프로필 |
| `setting_item_account` | 계정 정보 |
| `setting_item_service_terms` | 서비스 이용약관 |
| `setting_item_privacy_policy` | 개인정보 처리 방침 |
| `setting_item_version` | 버전 정보 |
| `setting_logout` | 로그아웃 (🔁 2026-08-03 추가, S-002에서 이관·개명) |
| `setting_withdraw` | 서비스 탈퇴하기 (🔁 2026-08-03 추가, S-002에서 이관·개명) |
- 정적 UI 라벨만 리소스화. `stringResource(R.string.*)`, R = `com.teamyg.parfait.feature.app.setting.impl.R`.

## 네비게이션 배선 (MVI Intent→SideEffect)
- 흐름: `AppSettingScreen` 콜백 → `viewModel.processIntent(AppSettingIntent.*)` → VM `handleClick*()` → `postSideEffect(AppSettingSideEffect.*)` → `AppSettingRoute`가 `LaunchedEffect`로 `viewModel.effect` 수집 → `navigator` 호출.
- `AppSettingRoute` 콜백 배선(Screen→Intent):
  - `onClickBack = { viewModel.processIntent(AppSettingIntent.ClickBack) }`
  - `onClickAccount = { viewModel.processIntent(AppSettingIntent.ClickAccount) }`
  - `onClickTerms = { viewModel.processIntent(AppSettingIntent.ClickServiceTerms) }`
  - `onClickPrivacy = { viewModel.processIntent(AppSettingIntent.ClickPrivacyPolicy) }`
  - `onClickLogout = { viewModel.processIntent(AppSettingIntent.ClickLogout) }` (🔁 2026-08-03)
  - `onClickWithdraw = { viewModel.processIntent(AppSettingIntent.ClickWithdraw) }` (🔁 2026-08-03)
- `AppSettingRoute` effect 수집(SideEffect→navigator):
  - `NavigateBack → navigator.onBack()`
  - `NavigateToAccountInfo → navigator.goTo(NavKeyAccountInfo)`
  - `NavigateToServiceTerms → navigator.goTo(NavKeyServiceTerms)`
  - `NavigateToPrivacyPolicy → navigator.goTo(NavKeyPrivacyPolicy)`
- `EntryBuilder`(`featureAppSettingEntryBuilder`)에 entry 추가:
  - `entry<NavKeyAppSetting>` → `AppSettingRoute` (기존). Scaffold 배경 `containerColor = YGAtomicColors.Gray.White`(S-001 화면 흰 배경).
  - `entry<NavKeyAccountInfo>` → `AccountInfoRoute` (stub)
  - `entry<NavKeyServiceTerms>` → `TermsRoute` (stub)
  - `entry<NavKeyPrivacyPolicy>` → `TermsRoute` (stub, 공용)
- `Navigator` API: `goTo(NavKey)` / `onBack()` (`core:navigation`).

## 검증
- **자동화 테스트 없음** — 이 프로젝트는 현재 테스트 코드 선례가 없다(테스트 미도입). 별도 지침 전까지 테스트 파일을 만들지 않는다.
- 검증선: `:feature:app:setting:impl` **컴파일**(`compileDebugKotlin`) + **ktlint** 통과.
- `AppSettingScreen`·`ProfileCard`: `@YGPreview`+`PreviewBox` 프리뷰로 Android Studio 육안 확인.
- nav 동작: 3개 항목 클릭 → `goTo` → stub 화면(`// TODO impl`) 진입, back 복귀(런타임 크래시 없음) 수동 확인.

## 주의 / 열린 질문
- **원자 색 직접 참조**: `Transparency.White75`·`Cherry.Cherry100` 등 시맨틱 슬롯 미경유 — #158 이후 `YGAtomicColors` public 확정, 설계 전반 과도기 패턴과 동일 → [design-system](../../../architecture/design-system.md)·[open-questions](../../../synthesis/open-questions.md).
- **각짐 `RectangleShape` 직접 참조**: ProfileCard가 `radius.none` 토큰 부재로 `RectangleShape`를 직접 씀 → [open-questions](../../../synthesis/open-questions.md) [2026-07-20] 등록. 토큰 머지 시 정리.
- **stub Route 모듈 배치**: 타겟 3화면을 `setting` 모듈에 코로케이트(별도 `:feature:app:account`/`:terms` 모듈 미생성). 타겟이 실제 구현·확장되면 모듈 분리 재검토.
- ~~**버전 주입**: 지금 placeholder 문자열. `BuildConfig.VERSION_NAME` 주입은 후속.~~
  ✅ **해소(2026-08-18, #295)** — `AppSettingState.version` 기본값이 `core:util:android`의
  `APP_VERSION_NAME`이다. `versionName`은 애플리케이션 모듈 속성이라 라이브러리 `BuildConfig`에 없어,
  그 모듈이 `:app`과 **같은 버전 카탈로그 항목**을 `buildConfigField`로 다시 심고 상수로 노출한다.
  DI로 주입하지 않는 이유는 빌드에 박힌 값이라 바뀔 일이 없어서다(브랜치 안에서 DI로 갔다가 상수로
  되돌아왔다). 그래서 이 필드는 **로딩 상태가 없는 유일한 값**이다(`nickname`·`loginProvider`와 달리
  non-null). 화면 표시의 `v` 접두는 `setting_item_version_value`(`v%1$s`) 포맷 리소스가 붙인다 —
  상태에는 숫자만 있다 → [module-structure](../../../architecture/module-structure.md).
- 구현 완료(#160 develop 머지, 2026-07-20) → `status: implemented`·`specs/archive/` 이동함.
