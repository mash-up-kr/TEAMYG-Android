---
id: ygalert
title: YGAlert 배너 알림 + 노출 정책 (YGAlertHost)
status: implemented
category: ui-spec
platforms: android
verified: 2026-09-01
related_code:
  - YGAlert.kt#YGAlert
  - YGAlertPolicy.kt#YGAlertPolicy
  - YGAlertPolicy.kt#YGAlertHost
  - YGAlertPolicy.kt#rememberYGAlertPolicy
  - YGAlertPolicy.kt#YGAlertItem
related_adr: ADR-0010
related_spec:
related_architecture: design-system
supersedes:
superseded_by:
tags: [spec, parfait, designsystem]
---

# Spec: YGAlert 배너 알림 + 노출 정책 (YGAlertHost)

> 상태·날짜·대상·관련은 frontmatter가 단일 출처. 본문은 설계에 집중.
>
> **사후 기록(post-hoc)**: 선작성 스펙 없이 develop 머지(#149, 2026-07-23)됨. as-built 역기록, 코드가 SoT.

- 대상: `core:designsystem` — `component/ygalert/`
- 관련: [ADR-0010](../../../adr/0010-custom-compositionlocal-theme.md)(자체 테마) · [design-system](../../../architecture/design-system.md)

## 목표
상단에서 내려오는 배너형 알림. **표시(YGAlert)** 와 **노출 정책(YGAlertPolicy/YGAlertHost)** 을 분리해,
화면은 정책 홀더에 `show()`만 하고 자동 소멸·스와이프 닫기·애니메이션은 Host가 담당한다.
(YGToast와 동일한 Policy/Host 패턴 — [ygtoast](2026-07-23-ygtoast.md) 참조.)

## 범위
- 포함: 배너 컴포저블, 단일 슬롯 정책 홀더, Host(자동 소멸·위로 스와이프 닫기·슬라이드 인/아웃).
- 제외: 문자열 생성·다국어 매핑(호출자 소유). 완성 문자열만 받는다.

## API / 인터페이스
```kotlin
@Composable
fun YGAlert(
    title: String,
    sub: String,
    modifier: Modifier = Modifier,
    buttonText: String? = null,
    onButtonClick: (() -> Unit)? = null,
    @DrawableRes buttonIconResource: Int = R.drawable.ic_caret_right,   // as-built(#411)
)

// as-built(#411): buttonText·onButtonClick·buttonIconResource 포함
data class YGAlertItem(val id: String, val title: String, val sub: String, …, val visible: Boolean = true)

@Stable
class YGAlertPolicy {
    var alert: YGAlertItem?           // 단일 슬롯(스택 아님)
    fun show(title: String, sub: String, buttonText: String? = null,
             onButtonClick: (() -> Unit)? = null,
             @DrawableRes buttonIconResource: Int = R.drawable.ic_caret_right)
    internal fun clearAlert()
}

@Composable fun rememberYGAlertPolicy(): YGAlertPolicy
@Composable fun YGAlertHost(policy: YGAlertPolicy, modifier: Modifier = Modifier)
```

## 레이아웃 / 토큰 매핑 (심볼명)
- 루트 `Row`(수직 중앙) — `fillMaxWidth`, 배경 `YGAtomicColors.Transparency.Black75`, 패딩 세로 `YGTheme.layout.padding.padding5` / 가로 `padding7`.
- 좌측 텍스트 `Column`(`weight(1f)`, `gap.gap2`):
  - `title`: `YGTheme.typography.body.b02SB` · `YGAtomicColors.Cherry.Cherry200`.
  - `sub`: `body.b02R` · `YGAtomicColors.Transparency.White75`.
- `buttonText != null`이면 우측에 `YGChipButton`(`YGChipButtonColorsDefaults.CherrySolid`(#183 개명), `endIconResource = buttonIconResource`, `onClick = onButtonClick ?: {}`).
  **as-built(#411 develop 머지, 2026-09-01)**: 끝 아이콘이 `ic_caret_right` 고정에서 **기본값 있는
  파라미터**가 됐다 — 배너가 "가는 곳"만이 아니라 "그 자리에서 하는 일"도 담게 되면서(초대코드 복사는
  `ic_copy`) 화살표가 항상 맞지는 않는다. `YGAlertItem`·`YGAlertPolicy.show`·`YGAlertHost`까지 같은
  파라미터가 관통한다.

## 노출 정책 (YGAlertPolicy / YGAlertHost)
- **단일 슬롯**: `alert`(nullable) 하나만 보유 — 새 `show()`가 이전 배너를 대체(스택 없음. YGToast와 대비).
  📌 **첫 프로덕션 소비처가 그 성질을 상태 갱신에 쓴다(#411, 2026-09-01)** — C-001 환영 배너의 복사
  버튼을 누르면 같은 제목·본문을 **문구만 `복사완료`로 바꿔 다시 `show`**한다. 새 `id`라 자동 소멸
  타이머도 처음부터 다시 돌고, 두 번째 배너에는 `onButtonClick`이 없어 다시 누를 수 없다.
- **자동 소멸**: `YGAlertHost`가 `LaunchedEffect(alert.id)`에서 `delay(DISMISS_DELAY)` 후 `clearAlert()`.
- **애니메이션**: `AnimatedVisibility` + `slideInVertically/slideOutVertically`(위에서 진입·이탈, `tween(ANIMATION_DURATION)`).
- **위로 스와이프 닫기**: `draggable`(Vertical) — 음수 델타만 누적, `onDragStopped`에서 오프셋이 `SWIPE_DISMISS_THRESHOLD` 미만이거나 속도가 `FLING_DISMISS_VELOCITY` 미만이면 `clearAlert()`, 아니면 오프셋 리셋.
- 상수: `DISMISS_DELAY` · `ANIMATION_DURATION` · `SWIPE_DISMISS_THRESHOLD` · `FLING_DISMISS_VELOCITY`(파일 private const, 값은 코드 SoT — 파르페 규율상 미기재).

## 파일 구성
- `component/ygalert/YGAlert.kt` — public `YGAlert` + `@Preview`.
- `component/ygalert/YGAlertPolicy.kt` — `YGAlertItem`·`YGAlertPolicy`·`rememberYGAlertPolicy`·`YGAlertHost`.

## 주의 / 열린 질문
- **원자 색 직접 참조(과도기)**: `YGAtomicColors.{Cherry,Transparency}` 직접 참조(시맨틱 미경유) — design-system 전반 과도기와 동일.
- **프리뷰 관용구 이탈**: `@Preview` + `YGCustomTheme`(공통 `@YGPreview`+`PreviewBox` 미사용). → [open-questions](../../../synthesis/open-questions.md) [2026-07-23].
