<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.TeamYg">

        <!--
            Android 16(targetSdk 36)은 smallest width 600dp 이상 기기에서 screenOrientation을
            무시한다. 이 opt-out이 그 예외를 되돌려 대화면에서도 세로 고정을 유지한다.
            TODO: targetSdk 37부터 opt-out 자체가 제거돼 무력화된다. 그 전에 대화면 대응 방침을
            정하고 이 속성을 지운다.
        -->
        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_RESTRICTED_ORIENTATION_AND_ASPECT_RATIO_OPT_OUT"
            android:value="true" />

        <!--
            단일 Activity 라 자동 수집은 화면과 무관하게 MainActivity 이름으로 찍힌다.
            그 이벤트가 같은 보고서에 섞이면 화면별 수치를 흐린다.
        -->
        <meta-data
            android:name="google_analytics_automatic_screen_reporting_enabled"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.TeamYg"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                App Links. autoVerify 는 서버가 /.well-known/assetlinks.json 을 배포해야
                실제로 검증이 붙는다 — 그전까지는 시스템이 앱과 브라우저 중 고르는 선택지로
                내려간다. URL 쿼리 파라미터 스펙은 아직 서버와 확정 전이라
                AppLinkDeepLink/AppLinkDeepLinkIntent 쪽에서 임시로 설계했다.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="parfait-app.store" />
            </intent-filter>
        </activity>

        <service
            android:name=".push.ParfaitFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:host="oauth"
                    android:scheme="${SCHEME_KAKAO_NATIVE_APP_KEY}" />
            </intent-filter>
        </activity>

    </application>

</manifest>
