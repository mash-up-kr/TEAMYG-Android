# 작업 로그

append-only다. 앞 항목을 고치지 않는다.

## [2026-09-17] setup | 위키 체계 이식

- `huihun-private-wiki`의 위키 체계를 단일 도메인 평탄 구조로 옮겼다. 설계는
  `docs/superpowers/specs/2026-09-17-wiki-port-design.md`에 있다
- 도메인 차원 제거 — 콘텐츠는 `wiki/pages/` 아래, 구조 파일은 `purpose`·`overview`·
  `index` 한 벌
- 스크립트 6개 이식: `route` `lint` `check_status` `ingest_cache` `wikilib`
  `graph_signals`. `sync_issues.py`는 가져오지 않았다 — 팀 공용 저장소라 이슈를
  자동 생성하면 안 된다. 딸려서 `lint`의 `check_issue_field`와 계약의 `issue`
  필드도 뺐다
- Obsidian 제외. `[[이름]]` 링크 규약과 세 검사는 유지하되 근거를 `route.py` seed
  확장의 결정성으로 다시 썼다
- graphify 산출물은 `wiki/graphify-out/`, 스캔 루트는 `wiki/pages/`
- 콘텐츠는 아직 0건이다. 첫 ingest는 별도 작업
- 검증: `pytest` 전건 통과, `lint.py` 위반 0건, `check_status.py` 위반 0건,
  라우팅 6개 의도 0 / `unclear` 3 / 삭제된 `add-domain` 4

## [2026-09-17] ingest | 기능정의서_MVP v2

- 원본: `wiki/raw/기능정의서-v2.md` (2026-05-30자, Notion 캡처 2장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 첫 콘텐츠 ingest다. 이전까지 위키 콘텐츠는 0건이었다
- 소스 1건: `src-기능정의서-v2` (`status: current`, 비교 대상 판본이 없어
  `supersedes`/`superseded_by` 없음)
- 엔티티 1건: `parfait`
- 개념 5건: `canvas` `group` `cutout-placement-pipeline` `screen-id-scheme`
  `mvp-scope-limits`
- synthesis는 만들지 않았다 — 소스가 하나라 가로지르는 비교 분석이 나오지 않는다
- 화면 34건 중 단발 언급 화면은 페이지로 만들지 않고 관련 개념 본문 서술로 남겼다
- `purpose.md`가 예고한 "화면 ID 체계 개념 페이지"를 만들고 거기서 링크를 걸었다
- open-questions 8건 등록(전부 `action: research`). 소스가 "정의 필요"로 남긴 정책
  16건을 주제별로 묶은 것과, 소스 자신이 밝힌 공백(`C-002` 미정의, 세부 열 공백
  4건), 판본 번호 불일치(제목 v2 / 내부 DB 제목 v3), 대상 사용자 정의 부재다
- 판본 번호 불일치는 대체 관계와 무관하다고 보고 진행했다 — 비교할 소스가 없어
  `status` 판정에는 영향이 없다

## [2026-09-17] ingest | 기능정의서_MVP v3

- 원본: `wiki/raw/기능정의서-v3.md` (2026-06-07자, Notion 캡처 2장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 판본 관계: v3가 v2를 **전면 대체**. v3가 MVP 화면 전체를 다시 정의하고 원본
  스스로 "v2 대비 주요 변경"을 밝혔으므로 `partial`이 아니라 `superseded`로 봤다
  - `src-기능정의서-v3`: `status: current`, `supersedes: [src-기능정의서-v2]`
  - `src-기능정의서-v2`: `status: superseded`, `superseded_by: [src-기능정의서-v3]`
- synthesis 1건 신설: `v2-to-v3-scope-shift`. 판본 비교가 처음 가능해진 시점이라
  [[purpose]] 핵심 질문 4에 답을 시도했다. `index.md`의 `## synthesis`에 등록하고
  `parfait`·`canvas`·`group`·`cutout-placement-pipeline`·`screen-id-scheme`·
  `mvp-scope-limits`에서 링크했다
- 개념 신설 없음. `C-301`(파르페 편집 모드)은 별도 페이지를 만들지 않고
  `cutout-placement-pipeline` 안 서술로 뒀다
- 기존 6개 페이지 전부 갱신 — v3 기준으로 다시 쓰고 v2에서 바뀐 지점만 판본을
  밝혔다
- 미결 2건 해소: `C-002` 미정의(v3가 명시적으로 제거), 판본 번호 불일치(별도 v3
  문서 실재로 v2가 진짜 2차 판본임이 확정). 둘 다 하류 반영 완료
- 미결 1건 신설: v3 **내부 모순** — 페이지 비고는 "그룹장 기능 삭제"인데 `A-005`
  시스템 동작은 "사용자 그룹장 등록"을, `S-103` 분기조건은 "그룹장 + 잔여 멤버"를
  남겼다. 어느 쪽이 옳은지 소스로 판정되지 않아 `group`에 상충 마커를 달고
  `open-questions`에 등록했다
- 8일 사이 화면 구조는 크게 움직였으나 미정 정책은 거의 그대로다. 삭제는 셋 늘고
  근거는 하나도 늘지 않았다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 기능정의서_MVP v4

- 원본: `wiki/raw/기능정의서-v4.md` (2026-06-14자, Notion 캡처 2장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 판본 관계: v4가 v3를 **전면 대체**. 사슬은 v2 ← v3 ← v4로 이어진다
  - `src-기능정의서-v4`: `status: current`, `supersedes: [src-기능정의서-v3]`
  - `src-기능정의서-v3`: `status: superseded`, `superseded_by: [src-기능정의서-v4]`
  - `src-기능정의서-v2`의 안내문을 고쳤다 — "현행 사실은 v3에서 확인한다"가 더는
    맞지 않는다
- **페이지 개명 2건.** 둘 다 앞으로 판본이 계속 들어온다는 전제에서 내린 판단이다
  - `cutout-placement-pipeline` → `topping-placement-pipeline`. v4가 캔버스 위
    객체의 공식 명칭을 "토핑"으로 정했다. 위키가 제품 용어를 따라가지 않으면
    v5 이후 계속 어긋난다. 누끼는 여전히 **처리 단계**(`C-103`)의 이름이고
    토핑은 **결과물 객체**의 이름이라는 구분을 페이지 첫 절에 명시했다
  - `v2-to-v3-scope-shift` → `spec-version-history`. 판본 쌍마다 synthesis를
    만들면 개념망이 파편화된다. 판본 이력을 한 페이지에 누적하고 판본 쌍은 절로
    나눈다
  - `wiki/log.md`는 append-only라 개명 일괄 치환에서 제외하고 원래대로 되돌렸다.
    위 두 항목의 기존 이름은 당시 사실 그대로 둔다
- 개념 신설 없음. "토핑"은 별도 페이지를 만들지 않고
  `topping-placement-pipeline` 첫 절의 용어 정의로 뒀다
- 기존 페이지 전부 갱신 — v4 기준으로 다시 쓰고 이전 판본에서 바뀐 지점만 판본을
  밝혔다
- 미결 1건 해소: `S-004` 약관 화면이 "노션 웹뷰"로 확정. 세 판본에 걸쳐 열려
  있던 정책 중 실제로 닫힌 유일한 건이다
- 미결 2건 신설
  - **답 없이 사라진 서술 4건** — 그룹장 권한 이양, 그룹 삭제 정책, 정렬 기준
    권장안("최근 활동순"), 닉네임 정책 항목 목록. 넷 다 답이 나온 적 없이
    판본이 올라가며 지워졌다. `purpose.md` 지역 규약이 정확히 이 패턴을 두고
    만들어진 것이라 한 항목으로 묶어 추적한다
  - **v4 캡처에서 `G-002` 행 누락** — 다른 화면들이 참조하므로 존재는 확실하나
    v4에서 바뀌었는지 알 수 없다. 원본 전사본은 v3 값으로 채워져 있다
- 기존 미결 갱신: 그룹장 모순은 `S-103`만 정리되고 `A-005`의 "사용자 그룹장
  등록"이 세 판본 내내 남아 절반만 풀렸다
- 이번 판본의 실질 변경은 명칭 확정(누끼 사진 → 토핑), 하루 경계 00시 → 03시,
  가입 시 그룹 내 닉네임 입력 셋이다. 하루 경계 이동은 `SY-001`·`SY-003`의 실행
  시각과 `C-102`의 갤러리 시간 창이 한 덩어리로 움직인 것이라 `canvas`에서 한
  절로 묶어 서술했다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 기능정의서_MVP v5

- 원본: `wiki/raw/기능정의서-v5.md` (2026-06-25자, Notion 캡처 3장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 판본 관계: v5가 v4를 **전면 대체**. 사슬은 v2 ← v3 ← v4 ← v5
  - `src-기능정의서-v5`: `status: current`, `supersedes: [src-기능정의서-v4]`
  - `src-기능정의서-v4`: `status: superseded`, `superseded_by: [src-기능정의서-v5]`
  - v2·v3 페이지의 "현행 사실은 …에서 확인한다" 안내문도 v5로 고쳤다
- 개념 신설 없음. 담당자 배정은 제품 범위 결정이 아니라 프로젝트 운영이라
  개념 페이지로 만들지 않고 소스 페이지에 사실로만 적었다. 서브 화면 ID 체계는
  `screen-id-scheme`에 흡수했다
- **이 판본의 사건은 화면이 아니라 열이다.** v5가 표의 열 구성을 바꾼 첫 판본이다
  - 담당자 4열 추가(디자인/iOS/AOS/Spring), **비고 열과 사용자 액션 열 삭제**
  - v4까지 "정의 필요"로 남아 있던 정책이 거의 전부 비고 열에 있었다. 열이
    사라지며 **미결 표기 12건이 한꺼번에 증발**했다. 사용자 액션 열과 함께 v4가
    추가했던 "그룹 내 닉네임 입력"도 없어졌다
  - 답이 나온 것이 하나도 없으므로 `purpose.md` 규약대로 전부 미해결로 남겼다.
    `open-questions`의 "답 없이 사라진 서술" 항목에 표로 누적한다
- 미결 2건 해소
  - 약관 동의 흐름 분리 — v5가 `A-003`을 약관 동의 화면으로 만들어 실현
  - 프로필 이미지 — "넣을지 말지" → "안넣음" 확정
- 미결 3건 신설
  - **그룹 참여/생성 선택 화면 소실** — `A-003`이 약관 동의로 교체됐는데
    `A-004`·`A-005`는 여전히 `A-003`을 진입 경로로 참조한다. v2의 `C-002`와
    반대 방향의 같은 문제다
  - **답 없이 사라진 서술** — 기존 항목을 표로 재구성하고 v5의 열 삭제 2건을
    더했다
  - **Spring 담당 전 행 미배정** — 범위 결정은 아니지만 MVP 완성 가능성에 직접
    걸려 있어 등록했다
- 기존 미결 갱신: 그룹장 모순은 `A-005`의 "사용자 그룹장 등록"이 **네 판본 내내**
  남아 여전히 미해결. `G-002` 캡처 누락은 v4에 이어 **두 판본 연속**
- ID 체계가 처음으로 깨졌다 — `C-101-confirm` `C-103-loading` `SY-001-new` 같은
  접미사 꼴이 도입돼 `접두사-세자리숫자` 한 꼴로는 더는 설명되지 않는다. 또
  `C-104`~`C-106`이 한 칸씩 밀렸다. `purpose.md`가 경고 목록에 적어 둔
  (`C-305`, `C-104`~`C-106`, `A-003`) ID가 **전부 실제로 재사용됐다**
- 원본 헤더가 `wiki/personal-private/담당자-매핑.md`를 참조하는데 이 저장소에는
  그 경로가 없다. 다른 저장소(`team-yg-pesonal-agent`) 기준 경로다. raw 파일
  자체에는 실명이 없고 역할 코드로 마스킹돼 있어 민감 검사는 통과한다
- 검증: `lint.py` 위반 0건
