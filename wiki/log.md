# 작업 로그

append-only다. 앞 항목을 고치지 않는다.

## [2026-09-17] setup | 위키 체계 이식

- `huihun-private-wiki`의 위키 체계를 단일 도메인 평탄 구조로 옮겼다. 설계는
  `docs/superpowers/specs/2026-09-17-wiki-port-design.md`에 있다
- 도메인 차원 제거 — 콘텐츠는 `wiki/pages/` 아래, 구조 파일은 `purpose`·`overview`·
  `index` 한 벌
- 스크립트 6개 이식: `route` `lint` `check_status` `ingest_cache` `wikilib`
  `graph_signals`. `sync_issues.py`는 가져오지 않았다 — 팀 공용 저장소라 이슈를
  자동 생성하면 안 된다. 딸려서 `lint`의 `check_issue_field`와 계약의 `issue`
  필드도 뺐다
- Obsidian 제외. `[[이름]]` 링크 규약과 세 검사는 유지하되 근거를 `route.py` seed
  확장의 결정성으로 다시 썼다
- graphify 산출물은 `wiki/graphify-out/`, 스캔 루트는 `wiki/pages/`
- 콘텐츠는 아직 0건이다. 첫 ingest는 별도 작업
- 검증: `pytest` 전건 통과, `lint.py` 위반 0건, `check_status.py` 위반 0건,
  라우팅 6개 의도 0 / `unclear` 3 / 삭제된 `add-domain` 4

## [2026-09-17] ingest | 기능정의서_MVP v2

- 원본: `wiki/raw/기능정의서-v2.md` (2026-05-30자, Notion 캡처 2장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 첫 콘텐츠 ingest다. 이전까지 위키 콘텐츠는 0건이었다
- 소스 1건: `src-기능정의서-v2` (`status: current`, 비교 대상 판본이 없어
  `supersedes`/`superseded_by` 없음)
- 엔티티 1건: `parfait`
- 개념 5건: `canvas` `group` `cutout-placement-pipeline` `screen-id-scheme`
  `mvp-scope-limits`
- synthesis는 만들지 않았다 — 소스가 하나라 가로지르는 비교 분석이 나오지 않는다
- 화면 34건 중 단발 언급 화면은 페이지로 만들지 않고 관련 개념 본문 서술로 남겼다
- `purpose.md`가 예고한 "화면 ID 체계 개념 페이지"를 만들고 거기서 링크를 걸었다
- open-questions 8건 등록(전부 `action: research`). 소스가 "정의 필요"로 남긴 정책
  16건을 주제별로 묶은 것과, 소스 자신이 밝힌 공백(`C-002` 미정의, 세부 열 공백
  4건), 판본 번호 불일치(제목 v2 / 내부 DB 제목 v3), 대상 사용자 정의 부재다
- 판본 번호 불일치는 대체 관계와 무관하다고 보고 진행했다 — 비교할 소스가 없어
  `status` 판정에는 영향이 없다

## [2026-09-17] ingest | 기능정의서_MVP v3

- 원본: `wiki/raw/기능정의서-v3.md` (2026-06-07자, Notion 캡처 2장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 판본 관계: v3가 v2를 **전면 대체**. v3가 MVP 화면 전체를 다시 정의하고 원본
  스스로 "v2 대비 주요 변경"을 밝혔으므로 `partial`이 아니라 `superseded`로 봤다
  - `src-기능정의서-v3`: `status: current`, `supersedes: [src-기능정의서-v2]`
  - `src-기능정의서-v2`: `status: superseded`, `superseded_by: [src-기능정의서-v3]`
- synthesis 1건 신설: `v2-to-v3-scope-shift`. 판본 비교가 처음 가능해진 시점이라
  [[purpose]] 핵심 질문 4에 답을 시도했다. `index.md`의 `## synthesis`에 등록하고
  `parfait`·`canvas`·`group`·`cutout-placement-pipeline`·`screen-id-scheme`·
  `mvp-scope-limits`에서 링크했다
- 개념 신설 없음. `C-301`(파르페 편집 모드)은 별도 페이지를 만들지 않고
  `cutout-placement-pipeline` 안 서술로 뒀다
- 기존 6개 페이지 전부 갱신 — v3 기준으로 다시 쓰고 v2에서 바뀐 지점만 판본을
  밝혔다
- 미결 2건 해소: `C-002` 미정의(v3가 명시적으로 제거), 판본 번호 불일치(별도 v3
  문서 실재로 v2가 진짜 2차 판본임이 확정). 둘 다 하류 반영 완료
- 미결 1건 신설: v3 **내부 모순** — 페이지 비고는 "그룹장 기능 삭제"인데 `A-005`
  시스템 동작은 "사용자 그룹장 등록"을, `S-103` 분기조건은 "그룹장 + 잔여 멤버"를
  남겼다. 어느 쪽이 옳은지 소스로 판정되지 않아 `group`에 상충 마커를 달고
  `open-questions`에 등록했다
- 8일 사이 화면 구조는 크게 움직였으나 미정 정책은 거의 그대로다. 삭제는 셋 늘고
  근거는 하나도 늘지 않았다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 기능정의서_MVP v4

- 원본: `wiki/raw/기능정의서-v4.md` (2026-06-14자, Notion 캡처 2장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 판본 관계: v4가 v3를 **전면 대체**. 사슬은 v2 ← v3 ← v4로 이어진다
  - `src-기능정의서-v4`: `status: current`, `supersedes: [src-기능정의서-v3]`
  - `src-기능정의서-v3`: `status: superseded`, `superseded_by: [src-기능정의서-v4]`
  - `src-기능정의서-v2`의 안내문을 고쳤다 — "현행 사실은 v3에서 확인한다"가 더는
    맞지 않는다
- **페이지 개명 2건.** 둘 다 앞으로 판본이 계속 들어온다는 전제에서 내린 판단이다
  - `cutout-placement-pipeline` → `topping-placement-pipeline`. v4가 캔버스 위
    객체의 공식 명칭을 "토핑"으로 정했다. 위키가 제품 용어를 따라가지 않으면
    v5 이후 계속 어긋난다. 누끼는 여전히 **처리 단계**(`C-103`)의 이름이고
    토핑은 **결과물 객체**의 이름이라는 구분을 페이지 첫 절에 명시했다
  - `v2-to-v3-scope-shift` → `spec-version-history`. 판본 쌍마다 synthesis를
    만들면 개념망이 파편화된다. 판본 이력을 한 페이지에 누적하고 판본 쌍은 절로
    나눈다
  - `wiki/log.md`는 append-only라 개명 일괄 치환에서 제외하고 원래대로 되돌렸다.
    위 두 항목의 기존 이름은 당시 사실 그대로 둔다
- 개념 신설 없음. "토핑"은 별도 페이지를 만들지 않고
  `topping-placement-pipeline` 첫 절의 용어 정의로 뒀다
- 기존 페이지 전부 갱신 — v4 기준으로 다시 쓰고 이전 판본에서 바뀐 지점만 판본을
  밝혔다
- 미결 1건 해소: `S-004` 약관 화면이 "노션 웹뷰"로 확정. 세 판본에 걸쳐 열려
  있던 정책 중 실제로 닫힌 유일한 건이다
- 미결 2건 신설
  - **답 없이 사라진 서술 4건** — 그룹장 권한 이양, 그룹 삭제 정책, 정렬 기준
    권장안("최근 활동순"), 닉네임 정책 항목 목록. 넷 다 답이 나온 적 없이
    판본이 올라가며 지워졌다. `purpose.md` 지역 규약이 정확히 이 패턴을 두고
    만들어진 것이라 한 항목으로 묶어 추적한다
  - **v4 캡처에서 `G-002` 행 누락** — 다른 화면들이 참조하므로 존재는 확실하나
    v4에서 바뀌었는지 알 수 없다. 원본 전사본은 v3 값으로 채워져 있다
- 기존 미결 갱신: 그룹장 모순은 `S-103`만 정리되고 `A-005`의 "사용자 그룹장
  등록"이 세 판본 내내 남아 절반만 풀렸다
- 이번 판본의 실질 변경은 명칭 확정(누끼 사진 → 토핑), 하루 경계 00시 → 03시,
  가입 시 그룹 내 닉네임 입력 셋이다. 하루 경계 이동은 `SY-001`·`SY-003`의 실행
  시각과 `C-102`의 갤러리 시간 창이 한 덩어리로 움직인 것이라 `canvas`에서 한
  절로 묶어 서술했다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 기능정의서_MVP v5

- 원본: `wiki/raw/기능정의서-v5.md` (2026-06-25자, Notion 캡처 3장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 판본 관계: v5가 v4를 **전면 대체**. 사슬은 v2 ← v3 ← v4 ← v5
  - `src-기능정의서-v5`: `status: current`, `supersedes: [src-기능정의서-v4]`
  - `src-기능정의서-v4`: `status: superseded`, `superseded_by: [src-기능정의서-v5]`
  - v2·v3 페이지의 "현행 사실은 …에서 확인한다" 안내문도 v5로 고쳤다
- 개념 신설 없음. 담당자 배정은 제품 범위 결정이 아니라 프로젝트 운영이라
  개념 페이지로 만들지 않고 소스 페이지에 사실로만 적었다. 서브 화면 ID 체계는
  `screen-id-scheme`에 흡수했다
- **이 판본의 사건은 화면이 아니라 열이다.** v5가 표의 열 구성을 바꾼 첫 판본이다
  - 담당자 4열 추가(디자인/iOS/AOS/Spring), **비고 열과 사용자 액션 열 삭제**
  - v4까지 "정의 필요"로 남아 있던 정책이 거의 전부 비고 열에 있었다. 열이
    사라지며 **미결 표기 12건이 한꺼번에 증발**했다. 사용자 액션 열과 함께 v4가
    추가했던 "그룹 내 닉네임 입력"도 없어졌다
  - 답이 나온 것이 하나도 없으므로 `purpose.md` 규약대로 전부 미해결로 남겼다.
    `open-questions`의 "답 없이 사라진 서술" 항목에 표로 누적한다
- 미결 2건 해소
  - 약관 동의 흐름 분리 — v5가 `A-003`을 약관 동의 화면으로 만들어 실현
  - 프로필 이미지 — "넣을지 말지" → "안넣음" 확정
- 미결 3건 신설
  - **그룹 참여/생성 선택 화면 소실** — `A-003`이 약관 동의로 교체됐는데
    `A-004`·`A-005`는 여전히 `A-003`을 진입 경로로 참조한다. v2의 `C-002`와
    반대 방향의 같은 문제다
  - **답 없이 사라진 서술** — 기존 항목을 표로 재구성하고 v5의 열 삭제 2건을
    더했다
  - **Spring 담당 전 행 미배정** — 범위 결정은 아니지만 MVP 완성 가능성에 직접
    걸려 있어 등록했다
- 기존 미결 갱신: 그룹장 모순은 `A-005`의 "사용자 그룹장 등록"이 **네 판본 내내**
  남아 여전히 미해결. `G-002` 캡처 누락은 v4에 이어 **두 판본 연속**
- ID 체계가 처음으로 깨졌다 — `C-101-confirm` `C-103-loading` `SY-001-new` 같은
  접미사 꼴이 도입돼 `접두사-세자리숫자` 한 꼴로는 더는 설명되지 않는다. 또
  `C-104`~`C-106`이 한 칸씩 밀렸다. `purpose.md`가 경고 목록에 적어 둔
  (`C-305`, `C-104`~`C-106`, `A-003`) ID가 **전부 실제로 재사용됐다**
- 원본 헤더가 `wiki/personal-private/담당자-매핑.md`를 참조하는데 이 저장소에는
  그 경로가 없다. 다른 저장소(`team-yg-pesonal-agent`) 기준 경로다. raw 파일
  자체에는 실명이 없고 역할 코드로 마스킹돼 있어 민감 검사는 통과한다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 무한 파르페 정책 설계 (G-001)

- 원본: `wiki/raw/G-001-무한파르페-정책설계.md` (2026-06-13자, Notion export +
  이미지 2장). 팀원 실명은 역할 코드로 마스킹된 상태로 들어왔다
- **기능정의서 판본군과 다른 계열의 첫 소스다.** 화면 전체를 한 줄씩 정의하는
  기능정의서와 달리 `G-001` 한 화면을 데이터 모델부터 z-index까지 파고든다.
  대체 관계가 없으므로 `status: current`, `supersedes` 없음
- 개념 1건 신설: `infinite-parfait`. `G-001`의 시각 구조(2-1-2-1 스태거 배치,
  크림·토핑·접시·체리·라벨, 상대 시간 표기, NEW 판정)를 담는다. 도메인 모델인
  `group`과는 층이 달라 분리했다 — `group`은 사람 묶음, `infinite-parfait`는
  그 묶음이 화면에 그려지는 방식이다
- 미결 1건 해소: **그룹 목록 정렬 기준 = 활동순.** 투표에서 9명이 활동순을
  골랐고 나머지 둘은 0표다. **근거까지 적힌 첫 결정이다** — "자주 보는 게 위로
  올라와야 편의성에 좋을 것 같은데"(`AOS-A`). 다만 "활동순으로 한다"는 확정
  문장은 없고 투표 결과만 있어 해소 메모에 단서로 남겼다
  - 이 해소로 `open-questions`의 "답 없이 사라진 서술" 표에서 정렬 권장안 한
    줄이 빠졌다. v3의 "최근 활동순 권장"이 v4에서 사라진 것은 맞지만, **답이
    다른 문서에 가 있었다**
- 미결 2건 신설
  - `G-001` 화면 요소의 세부 6건(크림 가로 길이, 체리, 이미지 뷰어, 2열·1열
    네이밍, 자동 재조회, 캐싱)
  - **두 문서 계열의 관계 미정.** 정렬 기준이 06-13에 투표로 정해졌는데 하루 뒤
    기능정의서 v4는 여전히 "정의 필요"를 달았다. 상태별 화면 표기도 각자 푼다
    (`C-102-error` 대 `G-001 Error Case`). 지금은 `G-001` 한 화면만 겹치지만
    정책 설계서가 늘면 화면마다 정본을 판정해야 한다
- `spec-version-history`에 절 둘 추가 — "두 계열이 따로 돈다", "근거는 딱 한 번
  적혔다". 후자는 형식의 차이로 보인다. 기능정의서는 표 한 줄에 결정만 담고,
  정책 설계서는 선택지를 나열하고 고른 흔적을 남긴다
- **assets 2건을 함께 커밋했다** — `G-001-크림정렬-예시.png`,
  `G-001-시뮬레이션-캡처.png`. 원본 본문이 실제로 참조하는 둘이고, 위키
  페이지에서도 상대경로로 건다. 본문이 참조하지 않는
  `G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`와 `C-001-캔버스-background-case.png`는
  `wiki/raw/assets/`에 미추적으로 남긴다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 그룹명 정책 v0.1 (A-005)

- 원본: `wiki/raw/A-005-그룹명-정책-v0.1.md` (2026-07-02자 `v0.1`, 정책 이미지
  1장 전사). 작성자 실명과 예시 속 팀원 이름은 마스킹·더미 치환된 상태로 들어왔다
- 정책 설계서 계열 두 번째 문서. 대체 관계 없음 — `status: current`
- **이 계열은 자체 번호를 쓴다**(`v0.1`). 기능정의서 v2~v5와 별개 체계라
  `spec-version-history`에 그 사실을 적었다. "v5"라고만 쓰면 어느 계열인지
  갈리지 않는다
- 개념 신설 없음. 규칙이 셋뿐이라 `group`에 "그룹명 입력 규칙" 절로 넣었다
- **닉네임 미결을 해소하지 않았다.** 규칙 본문 제목이 "닉네임 생성 정책"이지만
  적용 범위(§3)는 그룹명 하나다. 원본이 어긋남을 스스로 밝히며 "닉네임 정책과
  규칙 공유 가능성 있음"이라고만 적었다 — 가능성은 결정이 아니다
- **v5 ingest에서 놓쳤던 것을 잡았다.** 기능정의서 v4까지 `A-005` 예외처리에
  있던 **금칙어**가 v5에서 빠졌다. 이번 정책서도 금칙어를 다루지 않는다(허용
  문자만 정함). `open-questions`의 "답 없이 사라진 서술" 표에 한 줄 더했다 —
  **두 계열 어디에도 남지 않은 첫 항목이다**
- 미결 2건 신설
  - 그룹명 규칙이 닉네임에도 적용되는지
  - **입력 10자 대 목록 표시 6자** — 모순은 아니지만 입력 가능 범위의 절반가량이
    목록에서 항상 잘린다. 두 문서가 서로를 모르고 각자 정한 것인지 서술이 없다
- `spec-version-history`의 "두 계열이 따로 돈다" 절을 갱신했다. 결정이 한쪽에서
  다른 쪽으로 넘어가지 않을 뿐 아니라, **양쪽 어디에도 남지 않고 빠지는 항목이
  생긴다**는 것이 이번에 드러났다. "근거는 딱 한 번 적혔다" 절에는 정책 설계서도
  늘 근거를 적지는 않는다는 단서를 붙였다 — 그룹명 정책서는 왜 10자인지 적지
  않았다
- assets 커밋 없음. 이 원본은 이미지를 본문에서 참조하지 않는다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 캔버스 정책 v0.1 (C-001)

- 원본: `wiki/raw/C-001-캔버스-정책-v0.1.md` (2026-07-09자 `v0.1` 초안, 정책
  이미지 1장 + Background Case 목업 1장 전사). 작성자 실명은 마스킹된 상태다
- 정책 설계서 계열 세 번째 문서. 대체 관계 없음 — `status: current`
- 개념 신설 없음. `canvas`가 이미 `C-001` 중심으로 쓰여 있어 레이아웃만 떼면
  조각난다. "화면 레이아웃" 절로 넣었다. `G-001`에 `infinite-parfait`를 따로
  만든 것과 갈리는 지점인데, 그쪽은 파르페 잔이라는 독립된 시각 은유와 배치
  알고리즘이 있었고 이쪽은 반응형 규칙이라 층이 다르지 않다
- 미결 1건 부분 해소: **캔버스 초기 속성**에서 크기가 정해졌다 — 16:9를 최우선
  으로 지키고 좌우 여백이 양보한다. **기본 배경은 여전히 미정**이라 항목은
  남겼다. 레이아웃 정책서는 표시 규칙을 정하지 캔버스가 처음 생성될 때 무엇을
  담는지는 다루지 않는다
- 미결 2건 신설: 세로 모드 전용 결정의 근거, Background Blur를 항상 적용하는
  근거. 둘 다 원본이 규칙만 적고 이유를 적지 않았다
- 범위 결정 1건 추가: **태블릿·가로모드 미고려.** `mvp-scope-limits` 표에 넣었다.
  16:9를 무조건 지키는 구조라 가로모드는 나중에 붙이기 어려울 수 있다는 점을
  미결에 적어 뒀다
- `spec-version-history` 갱신
  - 정책 설계서가 셋이 됐고 **문서마다 자체 번호를 쓴다** — 그룹명 정책서와
    캔버스 정책서가 둘 다 `v0.1`이다. 같은 번호, 다른 문서다
  - **계열이 굳어지고 있다.** 6월 하순 이후 들어온 소스는 전부 정책 설계서고
    기능정의서는 v5(06-25)에서 멈췄다
  - "근거는 딱 한 번 적혔다" 절 수정 — 정책 설계서 계열이라 근거를 적는 것이
    아니다. 근거가 남은 것은 정렬 기준 한 건뿐이고 그때만 선택지를 나열하고
    투표했다. **형식이 계열이 아니라 사안마다 갈린다**
- 이 문서가 캔버스에 날짜 라벨(`May 20 (Wed)`, 영문)이 표시된다는 사실을 처음
  기록한다. 기능정의서 네 판본은 `C-001`의 표시 요소로 배경·이미지·업로더
  닉네임만 적었다
- **assets 1건을 함께 커밋했다** — `C-001-캔버스-background-case.png`. 원본 §3이
  참조하고 위키 페이지에서도 상대경로로 건다. 남은
  `G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`는 아직 어느 소스도 참조하지 않아
  미추적으로 둔다
- 이 ingest 중 `lint.py`가 매니페스트 위반 2건을 냈다. 산출물 문제가 아니라
  스크립트 버그였다 — 아래 별도 항목 참조
- 검증: `lint.py` 위반 0건, `pytest` 209건 통과

## [2026-09-17] fix | 매니페스트 경로 NFC 정규화

- 캔버스 정책 v0.1을 ingest하다 `lint.py`가 같은 파일을 두 위반으로 동시에
  보고했다 — `wiki/raw/C-001-캔버스-정책-v0.1.md`가 "매니페스트에 등록 안 됨"
  이면서 "매니페스트에 있으나 파일 없음"
- 원인: **그 파일만 파일시스템에 NFD로 저장돼 있었다.** 나머지 raw 파일은 NFC다.
  `conventions.md` §4가 "한글 파일명 비교는 항상 NFC 정규화 후 한다"고 정했는데
  두 스크립트가 그 계약을 지키지 않았다
- `check_raw_sync`는 `_raw_stems`에서 이미 `nfc()`를 거치므로 같은 상황에서
  걸리지 않았다. 그래서 증상이 매니페스트 검사에만 나타났다
- **`ingest_cache.py`의 버그가 더 나빴다.** 증분 게이트가 NFD 파일에 대해
  무력화된다
  - `record()`가 NFC 키로 쓰고 `pending()`이 NFD 키로 조회 → 기록 직후에도
    계속 처리 대상으로 뜬다 (실제로 재현했다)
  - `prune()`은 `set(manifest) - actual`이 NFC − NFD라 **멀쩡한 기록을
    지운다**. 실행하지 않고 고쳤다
- 고친 곳
  - `ingest_cache.py` — `wikilib.nfc`를 import해 매니페스트 **키**만 정규화.
    `pending` `record` `prune` 세 곳
  - `lint.check_manifest` — `actual`·`recorded` 양쪽 정규화
  - **파일을 여는 경로는 정규화하지 않는다.** 리눅스는 NFC/NFD를 다른 파일로
    보므로 실제 경로는 파일시스템이 준 그대로 써야 한다. 주석에 적어 뒀다
- 회귀 테스트 4건 추가(`test_ingest_cache.py` 3, `test_lint_raw.py` 1). 수정 전
  코드에서 4건 모두 실패하는 것을 확인했다
- 파일명을 NFC로 고치는 대신 스크립트를 고쳤다. 파일명만 바꾸면 증상은 사라지고
  다음에 들어오는 NFD 파일에서 그대로 재발한다
- 검증: `pytest` 209건 통과(이전 205 + 신규 4), `lint.py` 위반 0건,
  `ingest_cache.py --list` 처리 대상 0건

## [2026-09-17] ingest | 캘린더 컴포넌트 정의 v0.1 (C-201)

- 원본: `wiki/raw/C-201-캘린더-정책-v0.1.md` (2026-07-12자 `v0.1` 초안, 정책
  이미지 1장 전사, 작성자 `DSN-B`)
- 정책 설계서 계열 네 번째 문서. 대체 관계 없음 — `status: current`
- 개념 신설 없음. `canvas`에 "과거 캔버스 열람과 캘린더" 절로 넣었다. `G-001`에
  `infinite-parfait`를 따로 만든 것과 갈리는데, 이 문서는 컴포넌트 상태 정의라
  독립 개념으로 세울 만한 무게가 없고 캔버스 열람 흐름의 일부다. `C-001`
  레이아웃을 `canvas`에 넣은 것과 같은 판단이다
- **새 사실: `C-201`에 캘린더가 있다.** 기능정의서 네 판본은 이 화면을 "이전
  캔버스 열람"이라 부르고 사용자 액션을 "스와이프/탭"으로만 적었다. 캘린더를
  언급한 적이 없다
- 미결 2건 신설
  - **캘린더 `Today`와 03시 경계 충돌.** 하루는 03:00에 끊기는데 `Today`는 달력
    날짜다. 02:00에 앱을 열면 열려 있는 캔버스는 어제치인데 캘린더는 오늘을
    가리킨다. `Disabled`도 마찬가지로 모호하다 — 그 시각 달력상 오늘은 캔버스가
    없어 "볼 수 없는 날짜"지만 미래는 아니다. 원본에 서술이 없다
  - **스와이프·탭과 캘린더의 관계.** 공존인지 대체인지 정해지지 않았다
- 기존 미결 갱신: **과거 조회 가능 기간이 이제 UI에 걸린다.** `Disabled` 정의가
  "미래의 날짜"만 덮어서, 한도가 정해지면 한도를 넘은 과거 날짜의 버튼 상태가
  정의되지 않은 채 남는다. 지금은 한도 자체가 미정이라 구멍이 드러나지 않았을
  뿐이다
- `spec-version-history`에 절 하나 신설하고 둘 갱신
  - **신설 "결정이 쌓이면서 서로 부딪힌다"** — 따로 정해진 값들이 맞물리기
    시작했다. 03시 경계 대 캘린더 `Today`, 그룹명 10자 대 목록 칩 6자, 16:9 고정
    대 가로모드 미고려. **어느 쌍도 같은 문서 안에서 만나지 않는다.** 각각은
    자기 문서 안에서 일관되고 겹쳐 놓을 때만 문제가 보인다 — 이 위키가 할 수
    있는 일이 정확히 여기다
  - "두 계열이 따로 돈다"에 **한쪽만 아는 사실**을 더했다. `C-201`의 캘린더와
    `C-001`의 날짜 라벨은 정책 설계서에만 있다. **기능정의서만 읽으면 화면을
    잘못 그린다**
  - 계열 관찰 보강: 7월 문서 셋이 전부 `DSN-A`·`DSN-B` 작성이고, 다루는 층이
    동작에서 컴포넌트 상태로 내려가고 있다
- assets 커밋 없음. 이 원본은 이미지를 본문에서 참조하지 않는다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 무한 파르페 간격 정책 v0.1 (G-001)

- 원본: `wiki/raw/G-001-무한파르페-간격-정책-v0.1.md` (2026-07-09자 `v0.1`, 정책
  이미지 1장 전사, 작성자 `DSN-B`)
- 정책 설계서 계열 다섯 번째. **같은 화면(`G-001`)에 두 번째로 붙은 문서다.**
  대체가 아니라 짝이다 — 앞 문서가 무엇을 어디에 놓을지 정하고 이 문서가 얼마나
  띄울지 정한다. 둘 다 `status: current`
- 개념 신설 없음. `infinite-parfait`의 배치 절을 타입 이름으로 다시 쓰고 간격
  절을 더했다
- 미결 1건 해소: **2열·1열 자리의 이름** → `Type 1` / `Type 2-Left` /
  `Type 2-Right`
- **스태거의 정체를 밝혔다.** 앞 문서는 "2-1-2-1 스태거 패턴"이라 부르면서 무엇이
  어긋나는지 적지 않았다. 갭 표를 보면 같은 행인데 위쪽 갭이 `Type 2-Left` 12px,
  `Type 2-Right` 24px로 갈린다 — **오른쪽이 12px 아래에 놓인다.** 끝 처리가
  이를 뒷받침한다(마지막 좌측→접시 12px, 우측→접시 0px). 두 표가 일관된다
- 미결 3건 신설
  - **배치 순서 상충.** 앞 문서는 2열이 맨 위(예시 `[A][B]`로 시작, `index % 3`
    0·1이 2열), 이 문서는 `Type 1`이 맨 위(갭 표가 "체리 ↔ 첫번째 `Type 1`"로
    시작). 같은 그리드를 둘 다 "2-1-2-1"이라 부르면서 순서를 반대로 적는다.
    **같은 화면의 후속 문서끼리 어긋난 첫 사례다** — 앞선 상충들은 서로 다른
    화면·시점의 결정이 맞물린 것이었다
  - 마지막이 `Type 1`로 끝날 때 접시까지의 갭. 끝 처리 표가 2열로 끝나는 경우만
    정의하는데, 앞 문서는 1열로 끝나는 쪽이 더 자연스럽다고 했다
  - **아직 위키에 없는 원본을 참조한다** — `G-001-토핑-정책-v0.1.md`.
    회전 오버플로우 처리가 거기 있다고 하나 `wiki/raw/`에 없다
- 부분 해소: 프리뷰 이미지 **크기**가 180×180으로 정해졌다. 앞 문서는 "디자인이
  원하는 고정 사이즈"라고만 적었다. 뷰어 정책은 그대로 미결
- `infinite-parfait`의 인덱스 표에 주석을 달았다. 원본 §8.2가 `index % 3` 0과 1을
  둘 다 "2열 중 좌측"이라 적었는데 명백한 오타로 보고 우측으로 읽었다. 앞선
  ingest에서 조용히 고쳤던 것을 이번에 명시했다
- `spec-version-history` 갱신
  - "결정이 쌓이면서 서로 부딪힌다" 표에 배치 순서 상충 추가. 앞의 셋과 성격이
    다르다는 점을 본문에 적었다
  - "두 계열이 따로 돈다"에 **한 화면에 문서가 여러 개 붙기 시작했다**는 관찰
    추가. 화면 단위로 정본을 판정하는 것으로는 부족하고 **같은 화면 안에서도
    어느 문서가 어느 측면의 정본인지 갈라야 한다**
- assets 커밋 없음. 이 원본은 이미지를 본문에서 참조하지 않는다.
  `G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`는 이름상 아직 안 들어온 토핑
  정책서와 짝일 가능성이 있어 계속 미추적으로 둔다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 토핑 정책 v0.1 (G-001)

- 원본: `wiki/raw/G-001-토핑-정책-v0.1.md` (2026-07-09자 `v0.1`, 정책 이미지 1장
  전사, 작성자 `DSN-B`)
- 정책 설계서 계열 여섯 번째. **`G-001`에만 세 번째 문서다** — 배치(06-13),
  간격(07-09), 토핑(07-09). 셋 다 `status: current`이고 서로 대체하지 않는다
- 개념 신설 없음. `infinite-parfait`에 "토핑 컴포넌트" 절을 더했다
- 미결 1건 해소: **간격 정책서가 참조하던 문서가 들어왔다.** 오버플로우는 자르지
  않는다(`Clip Contents OFF`)
- **근거가 달린 두 번째 결정이다.** 오버플로우를 자르지 않는 이유가 적혀 있다 —
  삐져나온 픽셀이 인접 토핑을 밀지 않으므로 다 노출해 자연스러운 분위기를
  살린다. 원본이 정답과 오답을 나란히 적고 "이건 아님!"이라고 못박았다
  - `spec-version-history`의 "근거는 딱 한 번 적혔다" 절을 "두 번 적혔다"로
    고치고 두 근거의 성격 차이를 적었다. 앞은 선택지를 놓고 투표한 흔적이고
    뒤는 오답을 함께 밝힌 것이다. **구현자가 "왜 이렇게 안 하지"라고 물을 법한
    자리에만 근거가 붙는다**
- 배치 순서 상충이 **2:1이 됐다.** 토핑 정책서의 반복 다이어그램도 `[ 1 ]`로
  시작한다. 07-09자 두 문서가 같은 작성자에게서 나와 독립적으로 같은 순서를
  적는다. 그래도 해소로 넘기지 않았다 — 앞 문서의 `index % 3` 표가 철회된 적이
  없고 **"2-1-2-1"이라는 이름 자체가 2가 먼저임을 가리킨다**. `purpose.md`
  규약대로 임의로 한쪽을 정본으로 정하지 않는다
- 미결 2건 신설
  - **사용자가 준 토핑 각도와 목록의 고정 회전.** `C-305`에서 사용자가 각도를
    수정하는데 목록의 토핑은 타입별 고정 회전(-6°/-4°/8°)을 받는다. 더해지는지
    덮는지 없다. 대표 토핑이 캔버스에서와 다른 각도로 보일 수 있다
  - **참조 사슬이 위키 밖으로 이어진다.** `Grouptag-Chip`의 Timestamp 텍스트
    컬러가 "프로필 이미지 및 토스트 닉네임 텍스트 컬러 규칙"을 따른다는데 그
    문서가 없다. 게다가 **프로필 이미지는 v5에서 "안넣음"으로 확정됐다** —
    지워진 기능의 이름이 규칙 이름에 남아 있다
- z-index 보완: 앞 문서가 "토핑 / 라벨"을 같은 4로 묶었는데 이 문서가 그 안의
  순서를 가른다 — `Img` > `Grouptag-Chip`. 어긋나지 않고 보완한다
- assets 커밋 없음. 이 원본도 이미지를 본문에서 참조하지 않는다.
  `G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`는 이름이 `v0.2`라 이 `v0.1`
  문서와 짝이 아니다. 계속 미추적으로 둔다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 앱 닉네임 정책 v0.1 (S-002)

- 원본: `wiki/raw/S-002-앱닉네임-정책-v0.1.md` (2026-07-02자 `v0.1`, 정책 이미지
  1장 전사, 작성자 `DSN-B`)
- 정책 설계서 계열 일곱 번째. 대체 관계 없음 — `status: current`
- **개념 1건 신설: `account`.** 지금까지 계정 관련 서술(소셜 로그인, 계정 정보,
  탈퇴, 로그아웃)이 `group`에 끼어 있었는데 앱 닉네임은 그룹 소관이 아니다.
  `A-002`·`S-002`·`S-003`·`S-005`에 걸쳐 반복 등장하므로 별도 개념으로 세웠다.
  `parfait`의 핵심 단위를 둘에서 셋으로 고쳤다
- **새 사실: 닉네임이 두 층이다.** 계정에 하나뿐인 앱 닉네임(`S-002`)과 가입한
  그룹마다 따로인 그룹 내 닉네임(`S-102`). 입력 규칙은 같고 상한은 15자.
  기능정의서는 두 닉네임을 각각 적기만 했지 관계를 다룬 적이 없다
- 미결 1건 해소: **닉네임 정책과 그룹명 규칙의 관계.** 구조는 같고 상한만 다르다
  (닉네임 15자, 그룹명 10자)
- **앞선 ingest에서 남긴 의문이 풀렸다.** `A-005` 그룹명 정책서의 통과 예시
  `열다섯글자열다섯글자`가 이름과 달리 10자였던 것을 그때는 "규칙은 10자로
  일관된다"고만 적었다. 이 문서의 예시는 `열다섯글자` 세 토막으로 정확히 15자다
  — **그룹명 정책서는 이 문서의 사본이다.** 상한을 줄이며 예시 토막을 하나
  지웠는데 토막의 이름과 문서 제목("닉네임 생성 정책")은 갱신하지 않았다. 그
  문서가 "닉네임 정책과 규칙 공유 가능성 있음"이라고 적은 것도 이것으로 설명된다
  - `src-A-005-그룹명-정책-v0.1` 페이지의 "제목과 적용 범위가 어긋난다" 절을
    "이 문서는 닉네임 정책서의 사본이다"로 다시 썼다
- `spec-version-history`에 절 신설: **"문서가 복제된다".** 복제 자체는 합리적이다
  — 입력 유효성 규칙은 상한만 다르고 구조가 같다. 문제는 사본이 원본의 흔적을
  지우지 않아 **같은 규칙이 두 군데서 따로 갱신될 수 있다**는 점이다. 한쪽 상한이
  바뀌면 다른 쪽은 모른다
- 미결 3건 신설
  - **닉네임 중복 허용 여부와 금칙어.** 이 문서도 다루지 않는다. v3까지 항목
    목록에 있다가 사라진 둘이다. 그룹명은 기능정의서가 "중복은 허용"이라고
    못박았는데 닉네임에는 그런 서술이 없다
  - **앱 닉네임과 그룹 내 닉네임의 관계.** 독립인지 기본값인지 없다. 초기 랜덤
    부여는 10자 이내인데 수정 상한은 15자로 어긋나고 이유가 없다
  - **`S-102-닉네임-정책-v0.1.md`가 없다.** 참조 사슬이 위키 밖으로 이어지는 세
    번째 사례다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 프로필 이미지 및 토스트 닉네임 컬러 규칙 v0.1

- 원본: `wiki/raw/S-101-프로필-닉네임-컬러-규칙-v0.1.md` (2026-07-02자 `v0.1`,
  정책 이미지 1장 전사, 작성자 `DSN-B`)
- 정책 설계서 계열 여덟 번째. 대체 관계 없음 — `status: current`
- 개념 신설 없음. 프로필 이미지는 `account`에, 토스트 알림은 `canvas`에 절로
  넣었다
- **이 문서는 화면이 아니라 컴포넌트가 범위다.** 앞선 일곱 건은 전부 헤더에
  "적용 화면"을 적었는데 이 문서에는 없고 "적용 범위"가 `Nametag-Chip`과
  `Toast > Username`이다. 파일명에 `S-101`이 붙어 있지만 내용이 그 화면에 매이지
  않는다. 화면별 문서에서 **컴포넌트별 문서로 한 단계 더 갈라졌다**
- **정면 상충 1건 신설: 프로필 이미지.** v5(06-25)가 `A-002`에서 "프로필 이미지
  안넣음"으로 확정했는데 이 문서(07-02)는 프로필 이미지를 정의한다 — 닉네임 첫
  글자 원형 칩, 6컬러 × 2타입 = 12종, 계정당 1종 랜덤 배정
  - 양립하는 읽기가 있다. v5가 **사용자가 올리는 사진**을 말하고 이 문서가
    **자동 생성 이니셜 칩**을 말한다면 어긋나지 않는다. 그러나 어느 문서도
    그렇게 적지 않아 `purpose.md` 규약대로 상충으로 남겼다
  - 이니셜 칩이 없으면 토스트 닉네임 컬러 매핑의 근거가 사라진다 — 매핑이
    `Nametag-Chip` 타입에 종속돼 있기 때문이다
- **새 사실 1건: 토스트 알림이 있다.** "공용 캔버스에서 액션 발생 시 동접 중인
  그룹원에게 토스트 알림이 뜬다." 기능정의서 네 판본 어디에도 없다. 어떤 액션이
  알림을 띄우는지, "동접 중"을 어떻게 판정하는지도 없다
- 미결 1건 갱신(부분 해소): **참조 사슬.** 토핑 정책서가 가리키던 컬러 규칙서가
  들어왔으나 **적용 범위에 `Grouptag-Chip`이 없다.** 원본이 후속 `v0.2`에서
  `Grouptag-Chip`과 `Nametag-Chip` 타입 고정 정책이 추가된다고 예고한다 — 토핑
  정책서(07-09)가 `v0.1`(07-02)이 아니라 **아직 오지 않은 `v0.2`를 가리킨
  셈이다**
- **정책 설계서 계열에서 자체 판본 사슬이 처음 예고됐다.** 지금까지 이 계열
  문서는 전부 `v0.1` 하나씩이었다. `v0.2`가 오면 이 문서가 `superseded`가 된다
- 디저트 은유가 팔레트까지 내려왔다 — 컬러 이름이 연핑크·진핑크·**체리**·그레이·
  **멜론**·**푸딩**이고 디자인 토큰이 `Cherry-200`·`Melon`·`Pudding`이다.
  `parfait`에 적었다
- `spec-version-history`의 "한쪽만 아는 사실" 문단을 표로 바꾸고 다섯 건을
  정리했다. 마지막 줄(프로필 칩)은 한쪽만 아는 데서 그치지 않고 정면으로
  어긋난다는 점을 덧붙였다
- 작업 중 lint가 `parfait.md`의 frontmatter `sources` 누락을 잡았다. 연관 섹션에
  링크를 더하면서 frontmatter를 빠뜨린 실수다. 앞선 ingest의 `account.md`와 같은
  실수라 다음부터는 링크 추가와 frontmatter 갱신을 한 번에 한다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건
