# 작업 로그

append-only다. 앞 항목을 고치지 않는다.

## [2026-09-17] setup | 위키 체계 이식

- `huihun-private-wiki`의 위키 체계를 단일 도메인 평탄 구조로 옮겼다. 설계는
  `docs/superpowers/specs/2026-09-17-wiki-port-design.md`에 있다
- 도메인 차원 제거 — 콘텐츠는 `wiki/pages/` 아래, 구조 파일은 `purpose`·`overview`·
  `index` 한 벌
- 스크립트 6개 이식: `route` `lint` `check_status` `ingest_cache` `wikilib`
  `graph_signals`. `sync_issues.py`는 가져오지 않았다 — 팀 공용 저장소라 이슈를
  자동 생성하면 안 된다. 딸려서 `lint`의 `check_issue_field`와 계약의 `issue`
  필드도 뺐다
- Obsidian 제외. `[[이름]]` 링크 규약과 세 검사는 유지하되 근거를 `route.py` seed
  확장의 결정성으로 다시 썼다
- graphify 산출물은 `wiki/graphify-out/`, 스캔 루트는 `wiki/pages/`
- 콘텐츠는 아직 0건이다. 첫 ingest는 별도 작업
- 검증: `pytest` 전건 통과, `lint.py` 위반 0건, `check_status.py` 위반 0건,
  라우팅 6개 의도 0 / `unclear` 3 / 삭제된 `add-domain` 4

## [2026-09-17] ingest | 기능정의서_MVP v2

- 원본: `wiki/raw/기능정의서-v2.md` (2026-05-30자, Notion 캡처 2장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 첫 콘텐츠 ingest다. 이전까지 위키 콘텐츠는 0건이었다
- 소스 1건: `src-기능정의서-v2` (`status: current`, 비교 대상 판본이 없어
  `supersedes`/`superseded_by` 없음)
- 엔티티 1건: `parfait`
- 개념 5건: `canvas` `group` `cutout-placement-pipeline` `screen-id-scheme`
  `mvp-scope-limits`
- synthesis는 만들지 않았다 — 소스가 하나라 가로지르는 비교 분석이 나오지 않는다
- 화면 34건 중 단발 언급 화면은 페이지로 만들지 않고 관련 개념 본문 서술로 남겼다
- `purpose.md`가 예고한 "화면 ID 체계 개념 페이지"를 만들고 거기서 링크를 걸었다
- open-questions 8건 등록(전부 `action: research`). 소스가 "정의 필요"로 남긴 정책
  16건을 주제별로 묶은 것과, 소스 자신이 밝힌 공백(`C-002` 미정의, 세부 열 공백
  4건), 판본 번호 불일치(제목 v2 / 내부 DB 제목 v3), 대상 사용자 정의 부재다
- 판본 번호 불일치는 대체 관계와 무관하다고 보고 진행했다 — 비교할 소스가 없어
  `status` 판정에는 영향이 없다

## [2026-09-17] ingest | 기능정의서_MVP v3

- 원본: `wiki/raw/기능정의서-v3.md` (2026-06-07자, Notion 캡처 2장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 판본 관계: v3가 v2를 **전면 대체**. v3가 MVP 화면 전체를 다시 정의하고 원본
  스스로 "v2 대비 주요 변경"을 밝혔으므로 `partial`이 아니라 `superseded`로 봤다
  - `src-기능정의서-v3`: `status: current`, `supersedes: [src-기능정의서-v2]`
  - `src-기능정의서-v2`: `status: superseded`, `superseded_by: [src-기능정의서-v3]`
- synthesis 1건 신설: `v2-to-v3-scope-shift`. 판본 비교가 처음 가능해진 시점이라
  [[purpose]] 핵심 질문 4에 답을 시도했다. `index.md`의 `## synthesis`에 등록하고
  `parfait`·`canvas`·`group`·`cutout-placement-pipeline`·`screen-id-scheme`·
  `mvp-scope-limits`에서 링크했다
- 개념 신설 없음. `C-301`(파르페 편집 모드)은 별도 페이지를 만들지 않고
  `cutout-placement-pipeline` 안 서술로 뒀다
- 기존 6개 페이지 전부 갱신 — v3 기준으로 다시 쓰고 v2에서 바뀐 지점만 판본을
  밝혔다
- 미결 2건 해소: `C-002` 미정의(v3가 명시적으로 제거), 판본 번호 불일치(별도 v3
  문서 실재로 v2가 진짜 2차 판본임이 확정). 둘 다 하류 반영 완료
- 미결 1건 신설: v3 **내부 모순** — 페이지 비고는 "그룹장 기능 삭제"인데 `A-005`
  시스템 동작은 "사용자 그룹장 등록"을, `S-103` 분기조건은 "그룹장 + 잔여 멤버"를
  남겼다. 어느 쪽이 옳은지 소스로 판정되지 않아 `group`에 상충 마커를 달고
  `open-questions`에 등록했다
- 8일 사이 화면 구조는 크게 움직였으나 미정 정책은 거의 그대로다. 삭제는 셋 늘고
  근거는 하나도 늘지 않았다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 기능정의서_MVP v4

- 원본: `wiki/raw/기능정의서-v4.md` (2026-06-14자, Notion 캡처 2장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 판본 관계: v4가 v3를 **전면 대체**. 사슬은 v2 ← v3 ← v4로 이어진다
  - `src-기능정의서-v4`: `status: current`, `supersedes: [src-기능정의서-v3]`
  - `src-기능정의서-v3`: `status: superseded`, `superseded_by: [src-기능정의서-v4]`
  - `src-기능정의서-v2`의 안내문을 고쳤다 — "현행 사실은 v3에서 확인한다"가 더는
    맞지 않는다
- **페이지 개명 2건.** 둘 다 앞으로 판본이 계속 들어온다는 전제에서 내린 판단이다
  - `cutout-placement-pipeline` → `topping-placement-pipeline`. v4가 캔버스 위
    객체의 공식 명칭을 "토핑"으로 정했다. 위키가 제품 용어를 따라가지 않으면
    v5 이후 계속 어긋난다. 누끼는 여전히 **처리 단계**(`C-103`)의 이름이고
    토핑은 **결과물 객체**의 이름이라는 구분을 페이지 첫 절에 명시했다
  - `v2-to-v3-scope-shift` → `spec-version-history`. 판본 쌍마다 synthesis를
    만들면 개념망이 파편화된다. 판본 이력을 한 페이지에 누적하고 판본 쌍은 절로
    나눈다
  - `wiki/log.md`는 append-only라 개명 일괄 치환에서 제외하고 원래대로 되돌렸다.
    위 두 항목의 기존 이름은 당시 사실 그대로 둔다
- 개념 신설 없음. "토핑"은 별도 페이지를 만들지 않고
  `topping-placement-pipeline` 첫 절의 용어 정의로 뒀다
- 기존 페이지 전부 갱신 — v4 기준으로 다시 쓰고 이전 판본에서 바뀐 지점만 판본을
  밝혔다
- 미결 1건 해소: `S-004` 약관 화면이 "노션 웹뷰"로 확정. 세 판본에 걸쳐 열려
  있던 정책 중 실제로 닫힌 유일한 건이다
- 미결 2건 신설
  - **답 없이 사라진 서술 4건** — 그룹장 권한 이양, 그룹 삭제 정책, 정렬 기준
    권장안("최근 활동순"), 닉네임 정책 항목 목록. 넷 다 답이 나온 적 없이
    판본이 올라가며 지워졌다. `purpose.md` 지역 규약이 정확히 이 패턴을 두고
    만들어진 것이라 한 항목으로 묶어 추적한다
  - **v4 캡처에서 `G-002` 행 누락** — 다른 화면들이 참조하므로 존재는 확실하나
    v4에서 바뀌었는지 알 수 없다. 원본 전사본은 v3 값으로 채워져 있다
- 기존 미결 갱신: 그룹장 모순은 `S-103`만 정리되고 `A-005`의 "사용자 그룹장
  등록"이 세 판본 내내 남아 절반만 풀렸다
- 이번 판본의 실질 변경은 명칭 확정(누끼 사진 → 토핑), 하루 경계 00시 → 03시,
  가입 시 그룹 내 닉네임 입력 셋이다. 하루 경계 이동은 `SY-001`·`SY-003`의 실행
  시각과 `C-102`의 갤러리 시간 창이 한 덩어리로 움직인 것이라 `canvas`에서 한
  절로 묶어 서술했다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 기능정의서_MVP v5

- 원본: `wiki/raw/기능정의서-v5.md` (2026-06-25자, Notion 캡처 3장 전사, 원본
  페이지는 게스트 권한이라 접근 불가)
- 판본 관계: v5가 v4를 **전면 대체**. 사슬은 v2 ← v3 ← v4 ← v5
  - `src-기능정의서-v5`: `status: current`, `supersedes: [src-기능정의서-v4]`
  - `src-기능정의서-v4`: `status: superseded`, `superseded_by: [src-기능정의서-v5]`
  - v2·v3 페이지의 "현행 사실은 …에서 확인한다" 안내문도 v5로 고쳤다
- 개념 신설 없음. 담당자 배정은 제품 범위 결정이 아니라 프로젝트 운영이라
  개념 페이지로 만들지 않고 소스 페이지에 사실로만 적었다. 서브 화면 ID 체계는
  `screen-id-scheme`에 흡수했다
- **이 판본의 사건은 화면이 아니라 열이다.** v5가 표의 열 구성을 바꾼 첫 판본이다
  - 담당자 4열 추가(디자인/iOS/AOS/Spring), **비고 열과 사용자 액션 열 삭제**
  - v4까지 "정의 필요"로 남아 있던 정책이 거의 전부 비고 열에 있었다. 열이
    사라지며 **미결 표기 12건이 한꺼번에 증발**했다. 사용자 액션 열과 함께 v4가
    추가했던 "그룹 내 닉네임 입력"도 없어졌다
  - 답이 나온 것이 하나도 없으므로 `purpose.md` 규약대로 전부 미해결로 남겼다.
    `open-questions`의 "답 없이 사라진 서술" 항목에 표로 누적한다
- 미결 2건 해소
  - 약관 동의 흐름 분리 — v5가 `A-003`을 약관 동의 화면으로 만들어 실현
  - 프로필 이미지 — "넣을지 말지" → "안넣음" 확정
- 미결 3건 신설
  - **그룹 참여/생성 선택 화면 소실** — `A-003`이 약관 동의로 교체됐는데
    `A-004`·`A-005`는 여전히 `A-003`을 진입 경로로 참조한다. v2의 `C-002`와
    반대 방향의 같은 문제다
  - **답 없이 사라진 서술** — 기존 항목을 표로 재구성하고 v5의 열 삭제 2건을
    더했다
  - **Spring 담당 전 행 미배정** — 범위 결정은 아니지만 MVP 완성 가능성에 직접
    걸려 있어 등록했다
- 기존 미결 갱신: 그룹장 모순은 `A-005`의 "사용자 그룹장 등록"이 **네 판본 내내**
  남아 여전히 미해결. `G-002` 캡처 누락은 v4에 이어 **두 판본 연속**
- ID 체계가 처음으로 깨졌다 — `C-101-confirm` `C-103-loading` `SY-001-new` 같은
  접미사 꼴이 도입돼 `접두사-세자리숫자` 한 꼴로는 더는 설명되지 않는다. 또
  `C-104`~`C-106`이 한 칸씩 밀렸다. `purpose.md`가 경고 목록에 적어 둔
  (`C-305`, `C-104`~`C-106`, `A-003`) ID가 **전부 실제로 재사용됐다**
- 원본 헤더가 `wiki/personal-private/담당자-매핑.md`를 참조하는데 이 저장소에는
  그 경로가 없다. 다른 저장소(`team-yg-pesonal-agent`) 기준 경로다. raw 파일
  자체에는 실명이 없고 역할 코드로 마스킹돼 있어 민감 검사는 통과한다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 무한 파르페 정책 설계 (G-001)

- 원본: `wiki/raw/G-001-무한파르페-정책설계.md` (2026-06-13자, Notion export +
  이미지 2장). 팀원 실명은 역할 코드로 마스킹된 상태로 들어왔다
- **기능정의서 판본군과 다른 계열의 첫 소스다.** 화면 전체를 한 줄씩 정의하는
  기능정의서와 달리 `G-001` 한 화면을 데이터 모델부터 z-index까지 파고든다.
  대체 관계가 없으므로 `status: current`, `supersedes` 없음
- 개념 1건 신설: `infinite-parfait`. `G-001`의 시각 구조(2-1-2-1 스태거 배치,
  크림·토핑·접시·체리·라벨, 상대 시간 표기, NEW 판정)를 담는다. 도메인 모델인
  `group`과는 층이 달라 분리했다 — `group`은 사람 묶음, `infinite-parfait`는
  그 묶음이 화면에 그려지는 방식이다
- 미결 1건 해소: **그룹 목록 정렬 기준 = 활동순.** 투표에서 9명이 활동순을
  골랐고 나머지 둘은 0표다. **근거까지 적힌 첫 결정이다** — "자주 보는 게 위로
  올라와야 편의성에 좋을 것 같은데"(`AOS-A`). 다만 "활동순으로 한다"는 확정
  문장은 없고 투표 결과만 있어 해소 메모에 단서로 남겼다
  - 이 해소로 `open-questions`의 "답 없이 사라진 서술" 표에서 정렬 권장안 한
    줄이 빠졌다. v3의 "최근 활동순 권장"이 v4에서 사라진 것은 맞지만, **답이
    다른 문서에 가 있었다**
- 미결 2건 신설
  - `G-001` 화면 요소의 세부 6건(크림 가로 길이, 체리, 이미지 뷰어, 2열·1열
    네이밍, 자동 재조회, 캐싱)
  - **두 문서 계열의 관계 미정.** 정렬 기준이 06-13에 투표로 정해졌는데 하루 뒤
    기능정의서 v4는 여전히 "정의 필요"를 달았다. 상태별 화면 표기도 각자 푼다
    (`C-102-error` 대 `G-001 Error Case`). 지금은 `G-001` 한 화면만 겹치지만
    정책 설계서가 늘면 화면마다 정본을 판정해야 한다
- `spec-version-history`에 절 둘 추가 — "두 계열이 따로 돈다", "근거는 딱 한 번
  적혔다". 후자는 형식의 차이로 보인다. 기능정의서는 표 한 줄에 결정만 담고,
  정책 설계서는 선택지를 나열하고 고른 흔적을 남긴다
- **assets 2건을 함께 커밋했다** — `G-001-크림정렬-예시.png`,
  `G-001-시뮬레이션-캡처.png`. 원본 본문이 실제로 참조하는 둘이고, 위키
  페이지에서도 상대경로로 건다. 본문이 참조하지 않는
  `G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`와 `C-001-캔버스-background-case.png`는
  `wiki/raw/assets/`에 미추적으로 남긴다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 그룹명 정책 v0.1 (A-005)

- 원본: `wiki/raw/A-005-그룹명-정책-v0.1.md` (2026-07-02자 `v0.1`, 정책 이미지
  1장 전사). 작성자 실명과 예시 속 팀원 이름은 마스킹·더미 치환된 상태로 들어왔다
- 정책 설계서 계열 두 번째 문서. 대체 관계 없음 — `status: current`
- **이 계열은 자체 번호를 쓴다**(`v0.1`). 기능정의서 v2~v5와 별개 체계라
  `spec-version-history`에 그 사실을 적었다. "v5"라고만 쓰면 어느 계열인지
  갈리지 않는다
- 개념 신설 없음. 규칙이 셋뿐이라 `group`에 "그룹명 입력 규칙" 절로 넣었다
- **닉네임 미결을 해소하지 않았다.** 규칙 본문 제목이 "닉네임 생성 정책"이지만
  적용 범위(§3)는 그룹명 하나다. 원본이 어긋남을 스스로 밝히며 "닉네임 정책과
  규칙 공유 가능성 있음"이라고만 적었다 — 가능성은 결정이 아니다
- **v5 ingest에서 놓쳤던 것을 잡았다.** 기능정의서 v4까지 `A-005` 예외처리에
  있던 **금칙어**가 v5에서 빠졌다. 이번 정책서도 금칙어를 다루지 않는다(허용
  문자만 정함). `open-questions`의 "답 없이 사라진 서술" 표에 한 줄 더했다 —
  **두 계열 어디에도 남지 않은 첫 항목이다**
- 미결 2건 신설
  - 그룹명 규칙이 닉네임에도 적용되는지
  - **입력 10자 대 목록 표시 6자** — 모순은 아니지만 입력 가능 범위의 절반가량이
    목록에서 항상 잘린다. 두 문서가 서로를 모르고 각자 정한 것인지 서술이 없다
- `spec-version-history`의 "두 계열이 따로 돈다" 절을 갱신했다. 결정이 한쪽에서
  다른 쪽으로 넘어가지 않을 뿐 아니라, **양쪽 어디에도 남지 않고 빠지는 항목이
  생긴다**는 것이 이번에 드러났다. "근거는 딱 한 번 적혔다" 절에는 정책 설계서도
  늘 근거를 적지는 않는다는 단서를 붙였다 — 그룹명 정책서는 왜 10자인지 적지
  않았다
- assets 커밋 없음. 이 원본은 이미지를 본문에서 참조하지 않는다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 캔버스 정책 v0.1 (C-001)

- 원본: `wiki/raw/C-001-캔버스-정책-v0.1.md` (2026-07-09자 `v0.1` 초안, 정책
  이미지 1장 + Background Case 목업 1장 전사). 작성자 실명은 마스킹된 상태다
- 정책 설계서 계열 세 번째 문서. 대체 관계 없음 — `status: current`
- 개념 신설 없음. `canvas`가 이미 `C-001` 중심으로 쓰여 있어 레이아웃만 떼면
  조각난다. "화면 레이아웃" 절로 넣었다. `G-001`에 `infinite-parfait`를 따로
  만든 것과 갈리는 지점인데, 그쪽은 파르페 잔이라는 독립된 시각 은유와 배치
  알고리즘이 있었고 이쪽은 반응형 규칙이라 층이 다르지 않다
- 미결 1건 부분 해소: **캔버스 초기 속성**에서 크기가 정해졌다 — 16:9를 최우선
  으로 지키고 좌우 여백이 양보한다. **기본 배경은 여전히 미정**이라 항목은
  남겼다. 레이아웃 정책서는 표시 규칙을 정하지 캔버스가 처음 생성될 때 무엇을
  담는지는 다루지 않는다
- 미결 2건 신설: 세로 모드 전용 결정의 근거, Background Blur를 항상 적용하는
  근거. 둘 다 원본이 규칙만 적고 이유를 적지 않았다
- 범위 결정 1건 추가: **태블릿·가로모드 미고려.** `mvp-scope-limits` 표에 넣었다.
  16:9를 무조건 지키는 구조라 가로모드는 나중에 붙이기 어려울 수 있다는 점을
  미결에 적어 뒀다
- `spec-version-history` 갱신
  - 정책 설계서가 셋이 됐고 **문서마다 자체 번호를 쓴다** — 그룹명 정책서와
    캔버스 정책서가 둘 다 `v0.1`이다. 같은 번호, 다른 문서다
  - **계열이 굳어지고 있다.** 6월 하순 이후 들어온 소스는 전부 정책 설계서고
    기능정의서는 v5(06-25)에서 멈췄다
  - "근거는 딱 한 번 적혔다" 절 수정 — 정책 설계서 계열이라 근거를 적는 것이
    아니다. 근거가 남은 것은 정렬 기준 한 건뿐이고 그때만 선택지를 나열하고
    투표했다. **형식이 계열이 아니라 사안마다 갈린다**
- 이 문서가 캔버스에 날짜 라벨(`May 20 (Wed)`, 영문)이 표시된다는 사실을 처음
  기록한다. 기능정의서 네 판본은 `C-001`의 표시 요소로 배경·이미지·업로더
  닉네임만 적었다
- **assets 1건을 함께 커밋했다** — `C-001-캔버스-background-case.png`. 원본 §3이
  참조하고 위키 페이지에서도 상대경로로 건다. 남은
  `G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`는 아직 어느 소스도 참조하지 않아
  미추적으로 둔다
- 이 ingest 중 `lint.py`가 매니페스트 위반 2건을 냈다. 산출물 문제가 아니라
  스크립트 버그였다 — 아래 별도 항목 참조
- 검증: `lint.py` 위반 0건, `pytest` 209건 통과

## [2026-09-17] fix | 매니페스트 경로 NFC 정규화

- 캔버스 정책 v0.1을 ingest하다 `lint.py`가 같은 파일을 두 위반으로 동시에
  보고했다 — `wiki/raw/C-001-캔버스-정책-v0.1.md`가 "매니페스트에 등록 안 됨"
  이면서 "매니페스트에 있으나 파일 없음"
- 원인: **그 파일만 파일시스템에 NFD로 저장돼 있었다.** 나머지 raw 파일은 NFC다.
  `conventions.md` §4가 "한글 파일명 비교는 항상 NFC 정규화 후 한다"고 정했는데
  두 스크립트가 그 계약을 지키지 않았다
- `check_raw_sync`는 `_raw_stems`에서 이미 `nfc()`를 거치므로 같은 상황에서
  걸리지 않았다. 그래서 증상이 매니페스트 검사에만 나타났다
- **`ingest_cache.py`의 버그가 더 나빴다.** 증분 게이트가 NFD 파일에 대해
  무력화된다
  - `record()`가 NFC 키로 쓰고 `pending()`이 NFD 키로 조회 → 기록 직후에도
    계속 처리 대상으로 뜬다 (실제로 재현했다)
  - `prune()`은 `set(manifest) - actual`이 NFC − NFD라 **멀쩡한 기록을
    지운다**. 실행하지 않고 고쳤다
- 고친 곳
  - `ingest_cache.py` — `wikilib.nfc`를 import해 매니페스트 **키**만 정규화.
    `pending` `record` `prune` 세 곳
  - `lint.check_manifest` — `actual`·`recorded` 양쪽 정규화
  - **파일을 여는 경로는 정규화하지 않는다.** 리눅스는 NFC/NFD를 다른 파일로
    보므로 실제 경로는 파일시스템이 준 그대로 써야 한다. 주석에 적어 뒀다
- 회귀 테스트 4건 추가(`test_ingest_cache.py` 3, `test_lint_raw.py` 1). 수정 전
  코드에서 4건 모두 실패하는 것을 확인했다
- 파일명을 NFC로 고치는 대신 스크립트를 고쳤다. 파일명만 바꾸면 증상은 사라지고
  다음에 들어오는 NFD 파일에서 그대로 재발한다
- 검증: `pytest` 209건 통과(이전 205 + 신규 4), `lint.py` 위반 0건,
  `ingest_cache.py --list` 처리 대상 0건

## [2026-09-17] ingest | 캘린더 컴포넌트 정의 v0.1 (C-201)

- 원본: `wiki/raw/C-201-캘린더-정책-v0.1.md` (2026-07-12자 `v0.1` 초안, 정책
  이미지 1장 전사, 작성자 `DSN-B`)
- 정책 설계서 계열 네 번째 문서. 대체 관계 없음 — `status: current`
- 개념 신설 없음. `canvas`에 "과거 캔버스 열람과 캘린더" 절로 넣었다. `G-001`에
  `infinite-parfait`를 따로 만든 것과 갈리는데, 이 문서는 컴포넌트 상태 정의라
  독립 개념으로 세울 만한 무게가 없고 캔버스 열람 흐름의 일부다. `C-001`
  레이아웃을 `canvas`에 넣은 것과 같은 판단이다
- **새 사실: `C-201`에 캘린더가 있다.** 기능정의서 네 판본은 이 화면을 "이전
  캔버스 열람"이라 부르고 사용자 액션을 "스와이프/탭"으로만 적었다. 캘린더를
  언급한 적이 없다
- 미결 2건 신설
  - **캘린더 `Today`와 03시 경계 충돌.** 하루는 03:00에 끊기는데 `Today`는 달력
    날짜다. 02:00에 앱을 열면 열려 있는 캔버스는 어제치인데 캘린더는 오늘을
    가리킨다. `Disabled`도 마찬가지로 모호하다 — 그 시각 달력상 오늘은 캔버스가
    없어 "볼 수 없는 날짜"지만 미래는 아니다. 원본에 서술이 없다
  - **스와이프·탭과 캘린더의 관계.** 공존인지 대체인지 정해지지 않았다
- 기존 미결 갱신: **과거 조회 가능 기간이 이제 UI에 걸린다.** `Disabled` 정의가
  "미래의 날짜"만 덮어서, 한도가 정해지면 한도를 넘은 과거 날짜의 버튼 상태가
  정의되지 않은 채 남는다. 지금은 한도 자체가 미정이라 구멍이 드러나지 않았을
  뿐이다
- `spec-version-history`에 절 하나 신설하고 둘 갱신
  - **신설 "결정이 쌓이면서 서로 부딪힌다"** — 따로 정해진 값들이 맞물리기
    시작했다. 03시 경계 대 캘린더 `Today`, 그룹명 10자 대 목록 칩 6자, 16:9 고정
    대 가로모드 미고려. **어느 쌍도 같은 문서 안에서 만나지 않는다.** 각각은
    자기 문서 안에서 일관되고 겹쳐 놓을 때만 문제가 보인다 — 이 위키가 할 수
    있는 일이 정확히 여기다
  - "두 계열이 따로 돈다"에 **한쪽만 아는 사실**을 더했다. `C-201`의 캘린더와
    `C-001`의 날짜 라벨은 정책 설계서에만 있다. **기능정의서만 읽으면 화면을
    잘못 그린다**
  - 계열 관찰 보강: 7월 문서 셋이 전부 `DSN-A`·`DSN-B` 작성이고, 다루는 층이
    동작에서 컴포넌트 상태로 내려가고 있다
- assets 커밋 없음. 이 원본은 이미지를 본문에서 참조하지 않는다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 무한 파르페 간격 정책 v0.1 (G-001)

- 원본: `wiki/raw/G-001-무한파르페-간격-정책-v0.1.md` (2026-07-09자 `v0.1`, 정책
  이미지 1장 전사, 작성자 `DSN-B`)
- 정책 설계서 계열 다섯 번째. **같은 화면(`G-001`)에 두 번째로 붙은 문서다.**
  대체가 아니라 짝이다 — 앞 문서가 무엇을 어디에 놓을지 정하고 이 문서가 얼마나
  띄울지 정한다. 둘 다 `status: current`
- 개념 신설 없음. `infinite-parfait`의 배치 절을 타입 이름으로 다시 쓰고 간격
  절을 더했다
- 미결 1건 해소: **2열·1열 자리의 이름** → `Type 1` / `Type 2-Left` /
  `Type 2-Right`
- **스태거의 정체를 밝혔다.** 앞 문서는 "2-1-2-1 스태거 패턴"이라 부르면서 무엇이
  어긋나는지 적지 않았다. 갭 표를 보면 같은 행인데 위쪽 갭이 `Type 2-Left` 12px,
  `Type 2-Right` 24px로 갈린다 — **오른쪽이 12px 아래에 놓인다.** 끝 처리가
  이를 뒷받침한다(마지막 좌측→접시 12px, 우측→접시 0px). 두 표가 일관된다
- 미결 3건 신설
  - **배치 순서 상충.** 앞 문서는 2열이 맨 위(예시 `[A][B]`로 시작, `index % 3`
    0·1이 2열), 이 문서는 `Type 1`이 맨 위(갭 표가 "체리 ↔ 첫번째 `Type 1`"로
    시작). 같은 그리드를 둘 다 "2-1-2-1"이라 부르면서 순서를 반대로 적는다.
    **같은 화면의 후속 문서끼리 어긋난 첫 사례다** — 앞선 상충들은 서로 다른
    화면·시점의 결정이 맞물린 것이었다
  - 마지막이 `Type 1`로 끝날 때 접시까지의 갭. 끝 처리 표가 2열로 끝나는 경우만
    정의하는데, 앞 문서는 1열로 끝나는 쪽이 더 자연스럽다고 했다
  - **아직 위키에 없는 원본을 참조한다** — `G-001-토핑-정책-v0.1.md`.
    회전 오버플로우 처리가 거기 있다고 하나 `wiki/raw/`에 없다
- 부분 해소: 프리뷰 이미지 **크기**가 180×180으로 정해졌다. 앞 문서는 "디자인이
  원하는 고정 사이즈"라고만 적었다. 뷰어 정책은 그대로 미결
- `infinite-parfait`의 인덱스 표에 주석을 달았다. 원본 §8.2가 `index % 3` 0과 1을
  둘 다 "2열 중 좌측"이라 적었는데 명백한 오타로 보고 우측으로 읽었다. 앞선
  ingest에서 조용히 고쳤던 것을 이번에 명시했다
- `spec-version-history` 갱신
  - "결정이 쌓이면서 서로 부딪힌다" 표에 배치 순서 상충 추가. 앞의 셋과 성격이
    다르다는 점을 본문에 적었다
  - "두 계열이 따로 돈다"에 **한 화면에 문서가 여러 개 붙기 시작했다**는 관찰
    추가. 화면 단위로 정본을 판정하는 것으로는 부족하고 **같은 화면 안에서도
    어느 문서가 어느 측면의 정본인지 갈라야 한다**
- assets 커밋 없음. 이 원본은 이미지를 본문에서 참조하지 않는다.
  `G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`는 이름상 아직 안 들어온 토핑
  정책서와 짝일 가능성이 있어 계속 미추적으로 둔다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 토핑 정책 v0.1 (G-001)

- 원본: `wiki/raw/G-001-토핑-정책-v0.1.md` (2026-07-09자 `v0.1`, 정책 이미지 1장
  전사, 작성자 `DSN-B`)
- 정책 설계서 계열 여섯 번째. **`G-001`에만 세 번째 문서다** — 배치(06-13),
  간격(07-09), 토핑(07-09). 셋 다 `status: current`이고 서로 대체하지 않는다
- 개념 신설 없음. `infinite-parfait`에 "토핑 컴포넌트" 절을 더했다
- 미결 1건 해소: **간격 정책서가 참조하던 문서가 들어왔다.** 오버플로우는 자르지
  않는다(`Clip Contents OFF`)
- **근거가 달린 두 번째 결정이다.** 오버플로우를 자르지 않는 이유가 적혀 있다 —
  삐져나온 픽셀이 인접 토핑을 밀지 않으므로 다 노출해 자연스러운 분위기를
  살린다. 원본이 정답과 오답을 나란히 적고 "이건 아님!"이라고 못박았다
  - `spec-version-history`의 "근거는 딱 한 번 적혔다" 절을 "두 번 적혔다"로
    고치고 두 근거의 성격 차이를 적었다. 앞은 선택지를 놓고 투표한 흔적이고
    뒤는 오답을 함께 밝힌 것이다. **구현자가 "왜 이렇게 안 하지"라고 물을 법한
    자리에만 근거가 붙는다**
- 배치 순서 상충이 **2:1이 됐다.** 토핑 정책서의 반복 다이어그램도 `[ 1 ]`로
  시작한다. 07-09자 두 문서가 같은 작성자에게서 나와 독립적으로 같은 순서를
  적는다. 그래도 해소로 넘기지 않았다 — 앞 문서의 `index % 3` 표가 철회된 적이
  없고 **"2-1-2-1"이라는 이름 자체가 2가 먼저임을 가리킨다**. `purpose.md`
  규약대로 임의로 한쪽을 정본으로 정하지 않는다
- 미결 2건 신설
  - **사용자가 준 토핑 각도와 목록의 고정 회전.** `C-305`에서 사용자가 각도를
    수정하는데 목록의 토핑은 타입별 고정 회전(-6°/-4°/8°)을 받는다. 더해지는지
    덮는지 없다. 대표 토핑이 캔버스에서와 다른 각도로 보일 수 있다
  - **참조 사슬이 위키 밖으로 이어진다.** `Grouptag-Chip`의 Timestamp 텍스트
    컬러가 "프로필 이미지 및 토스트 닉네임 텍스트 컬러 규칙"을 따른다는데 그
    문서가 없다. 게다가 **프로필 이미지는 v5에서 "안넣음"으로 확정됐다** —
    지워진 기능의 이름이 규칙 이름에 남아 있다
- z-index 보완: 앞 문서가 "토핑 / 라벨"을 같은 4로 묶었는데 이 문서가 그 안의
  순서를 가른다 — `Img` > `Grouptag-Chip`. 어긋나지 않고 보완한다
- assets 커밋 없음. 이 원본도 이미지를 본문에서 참조하지 않는다.
  `G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`는 이름이 `v0.2`라 이 `v0.1`
  문서와 짝이 아니다. 계속 미추적으로 둔다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 앱 닉네임 정책 v0.1 (S-002)

- 원본: `wiki/raw/S-002-앱닉네임-정책-v0.1.md` (2026-07-02자 `v0.1`, 정책 이미지
  1장 전사, 작성자 `DSN-B`)
- 정책 설계서 계열 일곱 번째. 대체 관계 없음 — `status: current`
- **개념 1건 신설: `account`.** 지금까지 계정 관련 서술(소셜 로그인, 계정 정보,
  탈퇴, 로그아웃)이 `group`에 끼어 있었는데 앱 닉네임은 그룹 소관이 아니다.
  `A-002`·`S-002`·`S-003`·`S-005`에 걸쳐 반복 등장하므로 별도 개념으로 세웠다.
  `parfait`의 핵심 단위를 둘에서 셋으로 고쳤다
- **새 사실: 닉네임이 두 층이다.** 계정에 하나뿐인 앱 닉네임(`S-002`)과 가입한
  그룹마다 따로인 그룹 내 닉네임(`S-102`). 입력 규칙은 같고 상한은 15자.
  기능정의서는 두 닉네임을 각각 적기만 했지 관계를 다룬 적이 없다
- 미결 1건 해소: **닉네임 정책과 그룹명 규칙의 관계.** 구조는 같고 상한만 다르다
  (닉네임 15자, 그룹명 10자)
- **앞선 ingest에서 남긴 의문이 풀렸다.** `A-005` 그룹명 정책서의 통과 예시
  `열다섯글자열다섯글자`가 이름과 달리 10자였던 것을 그때는 "규칙은 10자로
  일관된다"고만 적었다. 이 문서의 예시는 `열다섯글자` 세 토막으로 정확히 15자다
  — **그룹명 정책서는 이 문서의 사본이다.** 상한을 줄이며 예시 토막을 하나
  지웠는데 토막의 이름과 문서 제목("닉네임 생성 정책")은 갱신하지 않았다. 그
  문서가 "닉네임 정책과 규칙 공유 가능성 있음"이라고 적은 것도 이것으로 설명된다
  - `src-A-005-그룹명-정책-v0.1` 페이지의 "제목과 적용 범위가 어긋난다" 절을
    "이 문서는 닉네임 정책서의 사본이다"로 다시 썼다
- `spec-version-history`에 절 신설: **"문서가 복제된다".** 복제 자체는 합리적이다
  — 입력 유효성 규칙은 상한만 다르고 구조가 같다. 문제는 사본이 원본의 흔적을
  지우지 않아 **같은 규칙이 두 군데서 따로 갱신될 수 있다**는 점이다. 한쪽 상한이
  바뀌면 다른 쪽은 모른다
- 미결 3건 신설
  - **닉네임 중복 허용 여부와 금칙어.** 이 문서도 다루지 않는다. v3까지 항목
    목록에 있다가 사라진 둘이다. 그룹명은 기능정의서가 "중복은 허용"이라고
    못박았는데 닉네임에는 그런 서술이 없다
  - **앱 닉네임과 그룹 내 닉네임의 관계.** 독립인지 기본값인지 없다. 초기 랜덤
    부여는 10자 이내인데 수정 상한은 15자로 어긋나고 이유가 없다
  - **`S-102-닉네임-정책-v0.1.md`가 없다.** 참조 사슬이 위키 밖으로 이어지는 세
    번째 사례다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 프로필 이미지 및 토스트 닉네임 컬러 규칙 v0.1

- 원본: `wiki/raw/S-101-프로필-닉네임-컬러-규칙-v0.1.md` (2026-07-02자 `v0.1`,
  정책 이미지 1장 전사, 작성자 `DSN-B`)
- 정책 설계서 계열 여덟 번째. 대체 관계 없음 — `status: current`
- 개념 신설 없음. 프로필 이미지는 `account`에, 토스트 알림은 `canvas`에 절로
  넣었다
- **이 문서는 화면이 아니라 컴포넌트가 범위다.** 앞선 일곱 건은 전부 헤더에
  "적용 화면"을 적었는데 이 문서에는 없고 "적용 범위"가 `Nametag-Chip`과
  `Toast > Username`이다. 파일명에 `S-101`이 붙어 있지만 내용이 그 화면에 매이지
  않는다. 화면별 문서에서 **컴포넌트별 문서로 한 단계 더 갈라졌다**
- **정면 상충 1건 신설: 프로필 이미지.** v5(06-25)가 `A-002`에서 "프로필 이미지
  안넣음"으로 확정했는데 이 문서(07-02)는 프로필 이미지를 정의한다 — 닉네임 첫
  글자 원형 칩, 6컬러 × 2타입 = 12종, 계정당 1종 랜덤 배정
  - 양립하는 읽기가 있다. v5가 **사용자가 올리는 사진**을 말하고 이 문서가
    **자동 생성 이니셜 칩**을 말한다면 어긋나지 않는다. 그러나 어느 문서도
    그렇게 적지 않아 `purpose.md` 규약대로 상충으로 남겼다
  - 이니셜 칩이 없으면 토스트 닉네임 컬러 매핑의 근거가 사라진다 — 매핑이
    `Nametag-Chip` 타입에 종속돼 있기 때문이다
- **새 사실 1건: 토스트 알림이 있다.** "공용 캔버스에서 액션 발생 시 동접 중인
  그룹원에게 토스트 알림이 뜬다." 기능정의서 네 판본 어디에도 없다. 어떤 액션이
  알림을 띄우는지, "동접 중"을 어떻게 판정하는지도 없다
- 미결 1건 갱신(부분 해소): **참조 사슬.** 토핑 정책서가 가리키던 컬러 규칙서가
  들어왔으나 **적용 범위에 `Grouptag-Chip`이 없다.** 원본이 후속 `v0.2`에서
  `Grouptag-Chip`과 `Nametag-Chip` 타입 고정 정책이 추가된다고 예고한다 — 토핑
  정책서(07-09)가 `v0.1`(07-02)이 아니라 **아직 오지 않은 `v0.2`를 가리킨
  셈이다**
- **정책 설계서 계열에서 자체 판본 사슬이 처음 예고됐다.** 지금까지 이 계열
  문서는 전부 `v0.1` 하나씩이었다. `v0.2`가 오면 이 문서가 `superseded`가 된다
- 디저트 은유가 팔레트까지 내려왔다 — 컬러 이름이 연핑크·진핑크·**체리**·그레이·
  **멜론**·**푸딩**이고 디자인 토큰이 `Cherry-200`·`Melon`·`Pudding`이다.
  `parfait`에 적었다
- `spec-version-history`의 "한쪽만 아는 사실" 문단을 표로 바꾸고 다섯 건을
  정리했다. 마지막 줄(프로필 칩)은 한쪽만 아는 데서 그치지 않고 정면으로
  어긋난다는 점을 덧붙였다
- 작업 중 lint가 `parfait.md`의 frontmatter `sources` 누락을 잡았다. 연관 섹션에
  링크를 더하면서 frontmatter를 빠뜨린 실수다. 앞선 ingest의 `account.md`와 같은
  실수라 다음부터는 링크 추가와 frontmatter 갱신을 한 번에 한다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 프로필 이미지 및 토스트 닉네임 컬러 규칙 v0.2

- 원본: `wiki/raw/S-101-프로필-닉네임-컬러-규칙-v0.2.md` (2026-07-07자 `v0.2`,
  정책 이미지 1장 전사, 작성자 `DSN-B`)
- 판본 관계: **v0.2가 v0.1을 전면 대체.** v0.1 내용을 전부 담고 `Grouptag-Chip`과
  타입 고정 정책을 더한다
  - `src-S-101-프로필-닉네임-컬러-규칙-v0.2`: `status: current`, `supersedes`
  - `src-S-101-프로필-닉네임-컬러-규칙-v0.1`: `status: superseded`,
    `superseded_by`
- **정책 설계서 계열에서 판본이 올라간 첫 문서다.** 다른 문서는 전부 `v0.1`
  하나씩이다
- 개념 신설 없음. `account`에 타입 고정 절, `infinite-parfait`에 `Grouptag-Chip`
  Timestamp 컬러를 더했다
- 미결 1건 해소: **`Grouptag-Chip` Timestamp 컬러.** `v0.2` §2가 명시적으로
  포함한다. 토핑 정책서(07-09)가 가리킨 것은 `v0.1`(07-02)이 아니라
  `v0.2`(07-07)였다 — **참조 사슬이 끝까지 풀렸다**
- **직전 ingest에서 한 예상이 빗나갔다.** `v0.2`에 "타입 고정 정책"이 들어간다는
  예고를 보고 "지금의 계정당 랜덤 배정이 바뀔 수 있다"고 적었는데, 실제로는
  **랜덤 배정을 그대로 두고 그 위에 불변 규칙을 얹는다.** 계정 생성 시 1회
  배정하고 이후 앱 재접속·그룹 변경·닉네임 변경에도 바꾸지 않는다는 뜻이었다
- 미결 1건 신설: **그룹 안에서 칩 타입이 겹치는 것.** 칩이 12종인데 그룹 최대
  인원도 12명이다. 배정 단위가 계정이라 그룹을 고려하지 않고, 그룹 내 유일성을
  보장하는 서술이 없다. 12명이 12종을 무작위로 뽑아 전원 다를 확률은 0.006%에
  못 미친다 — 색으로 사람을 구분하려는 의도라면 거의 항상 겹친다. `Grouptag-Chip`
  Timestamp도 같은 매핑을 쓰므로 그룹 목록과 토스트 양쪽에서 드러난다
- 프로필 이미지 상충은 **강화됐다.** v5의 "안넣음"에 맞서 `v0.1`에 이어 `v0.2`가
  다시 프로필 이미지를 정의하고, 타입이 계정에 고정된다는 규칙까지 더했다 —
  철회된 기능이라면 붙이지 않았을 규칙이다. 미결 항목에 그 점을 적었다
- `spec-version-history`에 관찰 추가: **판본을 올리는 방식이 계열마다 다르다.**
  기능정의서는 판본이 올라갈 때마다 서술이 사라졌지만(비고 열 삭제, 미결 표기
  증발) 컬러 규칙 `v0.2`는 `v0.1` 내용을 전부 유지한 채 더하기만 했다
- assets 커밋 없음. `G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`는 이름에
  `v0.2`가 있으나 이 컬러 규칙이 아니라 `G-001` 토핑 템플릿 그래픽시트다. 어느
  소스도 본문에서 참조하지 않아 계속 미추적으로 둔다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 닉네임 정책 v0.1 (S-102)

- 원본: `wiki/raw/S-102-닉네임-정책-v0.1.md` (2026-07-02자 `v0.1`, 정책 이미지
  1장 전사, 작성자 `DSN-B`)
- **본문이 `S-002` 앱닉네임 정책서와 글자 하나까지 같다.** `# 1. 생성 규칙` 이후
  전체를 diff했고 차이가 0이다. 헤더(적용 화면·텍스트)만 다르다
- 판본 관계: **대체 아님.** 날짜·작성자·버전이 모두 같고, **§3 적용 범위가 두
  문서 모두 "앱 닉네임 + 그룹 내 닉네임"**이라 서로를 완전히 포함한다. 어느 쪽도
  대체·보완이 아니므로 둘 다 `status: current`로 뒀다
- 개념 신설 없음. 규칙은 이미 `account`에 정리돼 있다. **소스 페이지에 규칙을 세
  번째로 옮겨 적지 않고** 차이와 중복 사실만 적었다 — `conventions.md` §1이
  컴파일 결과를 버리고 원본 조각에서 재유도하지 말라고 못박는다
- 미결 1건 해소: **`S-102` 정책서 도착.** "규칙 내용 동일"이 사실이었다. 덤으로
  이 문서가 그룹명 정책서와의 관계를 직접 못박는다 — "규칙 구조 동일, 글자수
  상한만 다름 — 그룹명 10자 / 닉네임 15자". **예시 문자열 길이로 추론했던 사본
  관계가 원본으로 확인됐다**
- 미결 1건 신설: **같은 규칙이 세 문서에 흩어져 있다.** 2026-07-02 하루에 나온
  입력 유효성 문서 셋이 같은 규칙의 변주다 — 닉네임 둘은 본문이 완전히 같고,
  그룹명은 그 사본에서 상한만 바꿨다. 상한이나 허용 문자가 바뀌면 **세 문서를
  모두 고쳐야 하는데 그것을 강제하는 장치가 없다.** 닉네임 문서 둘은 구별할
  근거조차 없다
- `spec-version-history`의 "문서가 복제된다" 절을 표로 다시 썼다. 복제가 두
  종류라는 것이 드러났다 — **완전 복제**(닉네임 둘)와 **값 하나를 바꾼
  사본**(그룹명). 화면 단위로 문서를 여는 관행이 같은 규칙을 화면 수만큼
  복제하게 만든 것으로 보이나 원본의 서술은 아니다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 토핑 편집자 확인 규칙 v0.1 (C-202)

- 원본: `wiki/raw/C-202-토핑-편집자-확인-규칙-v0.1.md` (2026-07-14자 `v0.1`,
  작성자 `DSN-B`). **지금까지 들어온 소스 중 가장 최신이다**
- 정책 설계서 계열 열 번째. 대체 관계 없음 — `status: current`
- 개념 신설 없음. Spotlight는 캔버스 상호작용이라 `canvas`에 "토핑을 탭하면"
  절로 넣었다
- **새 진입 경로: 본인 토핑을 탭하면 `C-305` 편집으로 직행한다.** 위키는 지금까지
  `C-301`(파르페 편집 모드)를 거치는 경로만 알고 있었다. `canvas`와
  `topping-placement-pipeline` 양쪽을 고쳤다
- **탭이 누구 것이냐로 갈린다** — 타인 토핑이면 Spotlight(선택 토핑만 강조 +
  `Black-50` Dim + 작성자 Toast 1회), 본인 토핑이면 편집
- **답 없이 사라진 서술 2건 추가.** 기능정의서 `C-202`가 적었던 "객체 탭 **또는
  자동 노출**"과 "활성화 시 **닉네임 변경**"이 이 정책서에 없다. 탭만 다룬다.
  철회인지 범위 밖인지 적혀 있지 않아 `purpose.md` 규약대로 해소로 읽지 않았다
- 미결 3건 신설
  - **"Toast 공통 규칙" 문서 없음.** 위키 밖으로 이어지는 참조의 네 번째
    사례다. Toast가 두 자리에서 쓰이는데(작성자 확인, 동접 알림) 공통 규칙이
    없으면 노출 시간·중첩 처리를 확인할 수 없다
  - **Spotlight 간 직접 전환이 협상으로 남아 있다.** 원본이 "지원하지 않는다"고
    적고 디자인 주석으로 "안거쳐도되게개발해줄거면댓글주세요 (환영)"을 달았다
  - **상대시간 문구 조립이 어긋나 보인다.** `infinite-parfait`의 표는 값 자체가
    "N분 전"인데 Toast 템플릿은 `{상대시간} 전에`이고 예시가 "59분 전에"다. 같은
    표를 쓰면 "59분 전 전에"가 된다
- `spec-version-history`에 절 신설: **"결정이 협상으로 남기도 한다".** 지금까지
  본 것은 근거가 적힌 결정(정렬 기준, 오버플로우)과 근거 없는 결정이었는데,
  이것은 **구현 비용에 따라 뒤집히기로 미리 합의된 결정**이라는 세 번째 형태다.
  확정으로 읽지 않는다
- Toast 문구 `{닉네임} 님이 {상대시간} 전에 쌓았어요`의 **"쌓았어요"**가 파르페
  은유를 잇는다 — 토핑은 올리는 것이 아니라 쌓는 것이다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | Toast 공통 정책

- 원본: `wiki/raw/Toast-공통-정책.md`
- **날짜·버전·작성자가 전부 없다.** 헤더가 "컴포넌트: Toast (공통)" 한 줄이다.
  다른 정책 설계서 열 건은 예외 없이 셋을 적었고 대부분 개정이력 표까지 뒀다.
  `status: current`로 뒀으나 **판본 비교의 기준점이 없다** — 미결로 등록했다
- **개념 1건 신설: `toast`.** Toast가 `canvas`·`group`·`account`를 가로지르고
  참조하는 문서가 셋이라 공통 페이지를 세웠다. 공통 정책 자체(노출·닫기·스택)는
  어느 도메인 개념에도 속하지 않는다
- 미결 1건 해소: **Toast 공통 규칙.** 위에서 내려와 나타나고, 터치로 위로
  올리거나 2초가 지나면 사라지며, 여러 개가 겹치면 나중 것이 위에 쌓인다
- 미결 1건 부분 해소: **알림을 띄우는 액션.** 스택 정책의 예시에서 토핑 추가와
  배경 추가·변경 둘이 확인됐다. 그룹원 입장·퇴장 등은 여전히 없어 미해결로 뒀다
  — `conventions.md` §3이 `해소됨`이 아닌 값을 전부 미결로 보므로 미해결 섹션에
  배치했다(처음에 해소 섹션에 넣었다가 고쳤다)
- **새 기능 둘이 문구 목록에서 나왔다**
  - `초대 코드를 복사했어요` → **초대 코드 복사 기능이 있다.** 기능정의서 네
    판본은 발급과 입력만 적었고 복사를 언급한 적이 없다. 어느 화면에 있는지도
    모른다
  - `내 토핑만 편집할 수 있어요` → 뜨는 경로가 불명. `C-202`는 타인 토핑을 탭하면
    Spotlight로 간다고 정했지 경고를 띄운다고 하지 않았다
- 미결 3건 신설: 위 둘과 날짜·버전 부재
- **근거가 세 번째로 적혔다.** 스택 정책에 원본 주석이 붙었다 — "인스타 접속
  중에 디엠 여러개 오면 발생하는 인터렉션을 생각하긴 햇습다". 앞의 둘은 이유를
  설명했는데(정렬 기준, 오버플로우) 이것은 **본뜬 레퍼런스를 밝힌 것**이다. 왜
  옳은지가 아니라 어디서 가져왔는지를 적었다. `spec-version-history`의 근거 표에
  더하고 성격 차이를 적었다
- 원본에 자기 비하와 소통 요청도 남았다 — "설명 또 쓰레기같이 했는데 이해
  안가시면 언급 해주세요...". `C-202`의 "댓글주세요 (환영)"과 같은 결이라
  `spec-version-history`의 "결정이 협상으로 남기도 한다" 절에 **문서 안에 협업
  대화가 남는다**는 관찰로 묶었다. 정제된 스펙이 아니라 작업 중인 메모를 받고
  있다는 뜻이고, 그래서 이런 주석을 지우지 않고 옮긴다 — 결정의 무게를 재는 데
  필요하다
- 상대시간 미결 갱신: Toast를 다루는 두 문서가 `{상대시간}` = "59분" 형식으로
  **서로 일관**한다. `infinite-parfait`의 표(값이 "N분 전")와만 어긋난다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 무한 파르페 토핑 배치·간격 정책 v0.2 (G-001)

- 원본: `wiki/raw/G-001-무한파르페-간격-정책-v0.2.md` (2026-07-22 스크린샷
  확인일, `v0.2`, **작성자 미표기**). 출처에 Figma 파일(파르페 `v0.1` 디자인,
  node `1569-7151`)이 처음 등장한다
- 판본 관계: **v0.2가 간격 정책 v0.1을 전면 대체.** 원본이 "이전 버전"으로 그것만
  명시한다
- **배치 패턴이 통째로 재설계됐다.** 2열+1열 조합(`2-1-2-1` 스태거)이 사라지고
  **행당 1개를 좌/우 번갈아** 놓는 지그재그가 됐다. 인덱스 규칙도 `index % 3` →
  `index % 2`, 타입도 3종 → 6종(`Left-1~3`/`Right-1~3`)
- 미결 2건 해소 — 다만 **답이 나온 것이 아니라 폐기됐다**
  - **배치 순서 상충.** 세 문서가 2열이 먼저냐 1열이 먼저냐로 갈렸는데 v0.2가
    패턴 자체를 없앴다. 2열이라는 것이 없다
  - **마지막이 `Type 1`로 끝날 때의 갭.** `Type` 체계가 사라져 물음이 소멸
  - `spec-version-history`에 절 신설: **"다투던 것이 폐기로 끝나기도 한다".**
    상충을 붙들고 있었기에 해소와 폐기를 구분해 적을 수 있었다 — 답이 나온
    것으로 기록했다면 틀린 기록이 됐다
- **상충 1건 신설: 토핑 규격.** 프레임 180×180 → 160×160, `Img` 144×144 → 96×96.
  타입 체계와 상수도 전부 다르다. **원본이 토핑 정책서를 "이전 버전"이 아니라
  "연관 문서"로만 들어 대체를 선언하지 않았다** — `purpose.md` 규약대로 임의로
  판본 관계를 만들지 않고 양쪽 다 `current`로 두고 상충 마커를 달았다
- 미결 4건 더 신설
  - 6타입 중 `1`·`2`·`3`을 고르는 규칙이 없다. 인덱스는 좌우만 가른다.
    `Left-3`와 `Right-3`는 상수가 완전히 같아 복사 흔적일 수 있다
  - **택일 미정 표기 둘** — `[ Left / Right ]`(시작 사이드),
    `[ 프레임 중앙 하단 / Img 중앙 하단 ]`(Chip 기준점). `[ A / B ]`를 고른 것이
    아니라 고르지 않은 것으로 읽었다. `N = 1`·`N = 2` 저개수 배치도 항목만 있고
    내용이 비어 있다
  - 그룹명 잘림 기준이 **6자에서 80px로** 바뀌었다. 대체인지 병행인지 없다
  - **`S-101` 컬러 규칙 `v0.3`이 없다.** 토핑 정책서가 컬러 규칙을 가리켰을 때도
    필요한 내용이 당시 없던 `v0.2`에 있었다 — **같은 자리에서 같은 일이 한 판본
    뒤로 되풀이된다**
- `infinite-parfait`의 "토핑 컴포넌트"와 배치 절을 다시 썼다. 규격은 두 문서가
  어긋나므로 표로 나란히 두고 v0.2 기준으로 서술했다
- `spec-version-history` 갱신: 메타데이터가 빠지는 문서가 늘고 있다는 관찰을
  더했다. Toast 공통 정책은 날짜·버전·작성자가 전부 없고, 이 문서는 작성자
  미표기에 날짜가 "스크린샷 확인일"이다. **판본 비교의 기준점이 약해지고 있다**
- assets 커밋 없음. `G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`는 이름의 `v0.2`가
  이 문서와 맞지만 본문이 참조하지 않는다. 규칙대로 미추적으로 둔다
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 프로필 이미지 및 토스트 닉네임 컬러 규칙 v0.3

- 원본: `wiki/raw/S-101-프로필-닉네임-컬러-규칙-v0.3.md` (2026-07-17자 `v0.3`,
  작성자 `DSN-B`)
- 판본 관계: **v0.3이 v0.2를 전면 대체.** 사슬은 `v0.1` ← `v0.2` ← `v0.3`
- **규칙 내용은 바뀌지 않았다.** `v0.2`와 프로필 이미지 절·타입 고정·컬러 매핑
  표가 글자 하나까지 같다(diff 0으로 확인). 절 순서만 다르다
- **바뀐 것은 적용 범위다.** `Grouptag-Chip`이 `v0.2`에서 들어왔다가 `v0.3`에서
  **별도 문서로 이관**됐다 —
  `S-101-프로필-이미지-및-그룹칩-내-타임스탬프-컬러-규칙-v0.1.md`. 범위가
  `v0.1`과 같은 둘로 회귀했다
- 미결 1건 해소: **`v0.3` 도착.** 다만 **문서는 들어왔으나 가리킨 내용은 거기
  없었다** — 간격 정책 `v0.2`(07-22)가 `Grouptag-Chip` 컬러를 두고 `v0.3`을
  지목했는데, `v0.3`은 07-17자로 **그때 이미 그 내용을 빼낸 뒤였다.** 5일 뒤
  문서가 이미 빠진 내용을 가리킨 셈이다
- 미결 1건 신설: **`Grouptag-Chip` Timestamp가 무엇을 따르는지 알 수 없다.**
  `v0.3`이 "별개 매핑"이라 못박았고 이관처 문서가 없다. `infinite-parfait`의
  그룹 목록 Timestamp 컬러 서술이 근거를 잃어 그 사실을 본문에 적었다
- **이관과 소실을 구분해 기록했다.** `spec-version-history`의 판본 진화 관찰을
  고쳤다 — 앞서 "컬러 규칙 `v0.2`는 더하기만 했다"고 적었는데 `v0.3`은 덜어냈다.
  다만 **덜어낸 것이 어디로 갔는지를 적었다.** 기능정의서의 비고 열은 행선지가
  없어 소실이 됐고, `Grouptag-Chip`은 이관처가 적혀 있어 추적이 이어진다. 같은
  "빠짐"이라도 행선지를 적으면 위키가 따라갈 수 있다
- `spec-version-history`에 **"참조가 계속 빗나간다"** 표를 더했다. 같은 자리에서
  두 번이다 — 토핑 정책서(07-09)가 가리킨 내용은 당시 없던 `v0.2`에 있었고,
  간격 정책서(07-22)가 가리킨 내용은 `v0.3`이 아니라 그 이관처에 있다. **문서가
  갈라질 때 그것을 참조하던 쪽이 따라오지 못한다**
- 프로필 이미지 상충은 **세 판본째** 재확인됐다. `v0.3`이 범위를 정리하면서도
  프로필 이미지와 타입 고정 규칙은 그대로 뒀다 — 철회된 기능이라면 쓰지 않았을
  규칙이다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 프로필 이미지 및 그룹칩 내 타임스탬프 컬러 규칙 v0.1

- 원본: `wiki/raw/S-101-프로필-이미지-및-그룹칩-내-타임스탬프-컬러-규칙-v0.1.md`
  (2026-07-17자 `v0.1`, 작성자 `DSN-B`)
- 판본 관계: **대체 없음.** `v0.3`이 예고한 이관처이지만 원본이 "이전 버전"으로
  선언하지 않았고 파일명 계열도 다르다. 임의로 사슬을 만들지 않고 `current`로 둔다.
  **`v0.3`과 같은 날(07-17) 한 쌍으로 갈라진 문서다**
- 미결 1건 해소: **`Grouptag-Chip` Timestamp가 무엇을 따르는지.**
  **"별개 매핑"이 "다른 근거"가 아니라 "같은 근거, 다른 표"였다** — 읽는 대상은
  그대로 `Nametag-Chip` 타입이고 Cherry 계열만 한 단계씩 어긋난다(Timestamp
  100/200/300, 토스트 닉네임 200/300/400). 그레이·멜론·푸딩은 같다.
  `infinite-parfait`의 라벨 절이 근거를 되찾아 표까지 실었다
- **원본이 스스로 상충을 막은 첫 사례다.** 두 표가 겹쳐질 때 상충으로 읽힐 것을
  알고 "서로 다른 UI 요소용 별개 매핑이므로 상충이 아니다"라고 문서 안에 해명을
  남겼다. `spec-version-history`에 절을 신설했다 — 지금까지 §4 표의 여덟 쌍은
  전부 "같은 문서 안에서 만나지 않는" 쪽이었다
- **참조 빗나감의 종착점을 확인했다.** `spec-version-history`에
  **"참조가 문서 분화를 따라오지 못한다"** 절을 신설했다. 두 번 다 **한 칸
  옆**이었다 — 토핑 정책서(07-09)는 판본 한 칸(`v0.2`), 간격 정책서(07-22)는
  문서 한 칸(이관처). **내용이 사라진 적은 없다.** 이관은 소실이 아니다
- **이전 ingest 로그와 실물이 어긋난 것을 고쳤다.** `v0.3` ingest 로그가
  "`spec-version-history`에 '참조가 계속 빗나간다' 표를 더했다"고 적었으나 그
  절이 파일에 없었다. 이번에 채웠다
- **stale 서술 2건 정리.** `src-G-001-무한파르페-간격-정책-v0.2`가 여전히
  "이 위키에는 `v0.2`까지 들어와 있다"와 미결 "`v0.3`이 위키에 없다"를 달고
  있었다 — `v0.3` ingest 때 하류에서 빠진 것이다. `index.md`의 `infinite-parfait`
  한 줄 요약도 폐기된 "2-1-2-1 배치"로 남아 있어 "지그재그"로 고쳤다
- 미결 1건 신설: **캔버스 변화 목록이 두 벌이다.** 이 문서가 Timestamp 컬러의
  주인을 고르며 "변화"를 넷으로 열거한다(토핑 추가, **토핑 편집**, 배경 추가,
  배경 변경). Toast를 띄우는 액션 목록은 셋이라 **토핑 편집 한 줄이 어긋나고,
  양쪽 다 토핑 삭제를 다루지 않는다.** `G-001`의 정렬·상대 시간·NEW 판정이 전부
  "마지막 활동"에 걸려 있는데 그 활동의 정의가 컬러 규칙서에만 있다
- 프로필 이미지 상충은 **네 문서째** 재확인됐다. 이 문서 §1이 `v0.3` §1의
  사본인데 **사본임을 밝히고 원본 파일명을 가리킨다** — 07-02의 입력 유효성
  문서 셋이 서로를 가리키지 않아 정본이 갈리지 않던 것과 다른 처리다
- 새 개념·엔티티 페이지 없음. `Grouptag-Chip`은 `infinite-parfait`의 구성 요소로
  이미 서술돼 있어 페이지를 세우지 않았다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건

## [2026-09-17] ingest | 무한 파르페 정책 설계 v0.2

- 원본: `wiki/raw/G-001-무한파르페-정책설계-v0.2.md` (**날짜·작성자 없음.** 원본
  첫 줄이 `# v0.2` 하나다)
- 판본 관계: **`src-G-001-무한파르페-정책설계`(06-13)를 전면 대체.** 문서명·적용
  화면·목차 뼈대가 같고 열 개 절을 같은 순서로 다시 적었다
- **흩어졌던 것이 다시 합쳐졌다.** `G-001`은 06-13 정책서 하나였다가 07-09에 간격·
  토핑으로 갈라졌고, 이 판본이 배치·좌표·규격·라벨을 한 문서에 다시 담았다.
  `spec-version-history`에 **"갈라졌던 문서가 다시 합쳐지기도 한다"** 절을 신설했다
  — 분화가 참조를 빗나가게 했고 재통합이 미결을 닫았다
- 미결 **3건 해소, 3건 신설, 2건 부분 해소.** 미해결 36 → 35, 해소 15 → 18
  - **6타입 중 1/2/3 선택 규칙** — 변형 시드가 **그룹 ID에 고정**된다. 목록이
    당겨져 side가 뒤집혀도 변형 번호는 따라가지 않는다
  - **택일 미정 표기 둘** — 시작 사이드는 **Left 고정**으로 골랐고, `Chip` 배치
    기준점은 **항목째 사라졌다.** 저개수 `N = 1`·`N = 2`는 y +12로 채워졌다
  - **그룹명 잘림 기준** — 80px 확정. **"구 6자/7자 문자수 기준 폐기"**를 원본이
    직접 적어 대체냐 병행이냐를 끝냈다
- **좌표가 처음으로 숫자가 됐다.** 간격 `v0.2`는 "프레임 기준으로 잰다"는 측정
  규칙만 두고 값을 적지 않았다. 이 문서가 인셋 28, 첫 토핑 y 280/268, 같은 side
  세로 갭 -12, 템플릿 고정 갭 셋을 정했다
- 미결 신설: **y가 프레임 상단인지 중심인지 없다.** 원본이 `Right`를 "인접한 두
  `Left`의 가운데 높이"라 적었는데 268·416의 가운데 342가 아니라 **354**다. 이후도
  일정하게 12씩 어긋난다 — 기준점을 모르면 어느 계산이 맞는지 가릴 수 없다
- 미결 신설: **날짜가 없어 간격 정책서와의 선후를 확정할 수 없다.** 내용이 간격
  `v0.2`의 빈자리를 채우므로 뒤로 읽었으나 원본만으로는 확정되지 않는다.
  `Toast-공통-정책`에 이어 **날짜 없는 두 번째 소스**다
- **오버플로우 정책이 뒤집혔다.** 토핑 정책서가 근거까지 달아 정한 "프레임 밖
  픽셀을 자르지 않는다"를 **"구 clip OFF/삐짐 노출 정책 삭제"**로 폐기했다.
  규격이 180/144에서 160/96으로 줄어 `Img`가 프레임을 넘지 않게 된 결과다 —
  **근거가 틀렸다고 적힌 것이 아니라 전제가 사라졌다.** `spec-version-history`의
  "근거는 두 번 적혔다" 표에서 셋 중 하나가 취소선이 됐다
- **규격 상충은 값으로는 끝났고 문서 관계로는 남았다.** 160×160이 세 번째 문서에서
  확인됐지만 **어느 문서도 토핑 정책서를 대체한다고 선언하지 않는다** — 간격
  `v0.2`는 "연관 문서"로만 들고 이 문서는 연관 문서 블록 자체가 없다. `purpose`
  규약대로 임의로 판본 관계를 만들지 않고 `current`로 뒀다
- **폐기를 적는 방식이 한 문서 안에서 갈린다.** 그룹명 문자수 기준·오버플로우·
  이미지 뷰어는 "폐기"·"삭제 확정"이라 적고 지웠는데, `Chip` 배치 기준점과 크림
  가로 길이는 말없이 빠졌다. **명시적 폐기가 소리 없는 삭제보다 많아진 첫
  문서**이지만 둘이 공존한다. `spec-version-history`에 절을 신설하고 "답 없이
  사라진 서술" 표에 둘을 더했다
- **참조가 세 번째로 빗나갔다.** Timestamp 컬러를 두고 다시 "프로필 이미지 및
  토스트 닉네임 텍스트 컬러 규칙"을 가리킨다 — `Grouptag-Chip`은 07-17에 거기서
  이관됐다. **고쳐지지 않은 참조가 다음 문서로 복사되고 있다**
- 그 밖에 정해진 것: 자동 재조회 **확정**, 이미지 뷰어 정책 **삭제 확정**, 크림
  개수(0~3 → 3 고정, 4부터 1:1), `G-001-Empty` 온보딩 툴팁, 자체 로딩 그래픽,
  지그재그 재배정을 **"버그 아니라 정상 동작"**으로 명시, 정렬 **"활동순 확정"**
  문장 신설
- **stale 서술 2건 정리.** `parfait`가 정책 설계서를 "지금은 한 건"이라 했고
  (실제 열한 건), `group`이 목록 칩을 "6자만 노출"이라 했다(80px로 바뀐 지 오래)
- `screen-id-scheme`에 관찰 추가: 이 문서가 **한 문서 안에서 두 표기를 섞는다** —
  케이스는 `G-001 Empty Case`(띄어쓰기), 툴팁은 `G-001-Empty`(붙임표)
- 새 개념·엔티티 페이지 없음. `infinite-parfait`을 크게 고쳐 썼다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 기능정의서 v6

- 원본: `wiki/raw/기능정의서-v6.md` (2026-07-23자 6차 판본, 캡처 5장)
- 판본 관계: **v5를 전면 대체.** 사슬은 v2 ← v3 ← v4 ← v5 ← v6
- **멈춰 있던 계열이 28일 만에 움직였다.** v5(06-25) 뒤로 7월 소스는 전부 정책
  설계서였고, v6는 간격 정책서(07-22) 다음 날에 놓인다
- **줄 단위로 맞춰 보니 실제 변경은 다섯이다.** 나머지 행은 오탈자 수준이다.
  `G-002` 삭제, `A-005`의 그룹장 등록 제거, `C-301` 개명, `SY-001-new`의 UX 3안,
  담당 열 개편
- 미결 **2건 해소, 3건 신설.** 미해결 35 → 36, 해소 18 → 20
  - **그룹장 모순** — `A-005`의 "사용자 그룹장 등록"이 지워졌다. v3 페이지 비고가
    "그룹장 기능 삭제"를 선언한 것이 06-07이니 **선언이 표를 따라잡는 데 네 판본,
    47일**이 걸렸다. `spec-version-history`가 "선언이 표를 완전히 따라오지 못하는
    패턴"이라 든 대표 사례가 닫혔다. **상충을 붙들고 있었기에 닫히는 순간을 적을
    수 있었다** — 중간에 한쪽을 정본으로 단정했다면 근거가 남지 않았다
  - **`G-002` 캡처 공백** — 화면이 실제로 삭제됐다. **삭제와 재배선이 같은
    판본에서 함께 이뤄진 첫 사례다.** `C-002`(참조되는 화면이 정의된 적 없음),
    `A-003`(정의가 바뀌었는데 참조가 그대로), 컬러 규칙 참조 세 번(한 칸씩
    빗나감)과 대비된다. 다만 **캡처 공백 상태로 사라져 v4·v5의 변화는 끝내 알 수
    없다**
- 미결 신설: **`G-002`가 맡던 권한 검증의 행선지가 없다.** "그룹 권한 유효 여부"와
  "강퇴/탈퇴 그룹 접근 차단"을 v6의 어느 행도 받지 않는다. **화면은 정리하고 그
  화면이 하던 일은 답 없이 지웠다**
- 미결 신설: **"에딧 모드 삭제" 페이지 비고가 표와 어긋난다.** `C-301`~`C-306`이
  여섯 행 모두 남아 있고, 표에서 실제로 없어진 것은 `G-002`다. 같은 판본에서
  `C-301` 이름이 바뀐 것과 나란히 놓이지만 원본이 그렇게 적지 않아 짐작으로
  채우지 않았다
- 미결 신설: **"어제의 파르페" UX 3안이 UT 대기로 미확정이다**
- **기능정의서가 처음으로 판단 기준을 적었다.** `SY-001-new`의 기타가 16:9 한
  줄에서 UX 3안으로 늘었고 **"UT 설문 결과 + 완성 파르페 갤러리 저장 비율 종합"**이
  판단 기준으로 붙었다. 다섯 판본 중 처음이다. 다만 **이미 고른 것의 근거가 아니라
  아직 고르지 않은 것을 고르는 방법**이라 성격이 다르다 — 그렇게 적었다
- **새 기능이 딸려 왔다.** 마감분을 다시 노출한다는 서술이 v5까지 없었다. 과거
  캔버스는 `C-201`로 사용자가 직접 찾아가는 것이었다
- **디저트 은유가 처음으로 물러섰다.** `C-301`이 "파르페 편집 모드"에서 "캔버스
  편집 모드"로 돌아갔다. v3이 붙인 이름이 세 판본을 버티다 되돌려졌다. "토핑"은
  그대로다 — 바뀐 것은 이름 하나이고 이유는 없다
- **7월에 정해진 것이 하나도 안 넘어왔다.** 지그재그 배치와 좌표, 그룹명 80px,
  프로필 칩 12종, Toast 규칙 — 어느 것도 v6 표에 없다. `G-001` 행은 v5와 글자
  하나 다르지 않다. **판본 간격이 7일이든 28일이든 두 계열은 서로를 읽지 않는다**
- **열 개편이 두 판본 연속이다.** v5의 역할코드 4열이 인물·진행상황 2열이 됐고
  값이 "iOS 작업 중 / 디자인 검수" 따위다 — 표가 명세에서 작업 추적으로 기운다.
  **그 열이 가변 항목이라 raw에 담기지 않아** v5가 드러낸 "Spring 열 전 행 비어
  있음"을 확인할 수 없다
- `A-003` 겸직 미결은 그대로 두되 **단서 셋을 더했다** — `G-001`의 다음 단계에
  `A-003`이 있고, `A-002` 분기조건이 "진입 경로 선택으로 이동"이며, `A-003`의
  시스템 동작이 "분기 화면 노출"이다. **약관 동의와 진입 경로 선택을 겸하는
  것으로 읽히지만** 원본이 말하지 않아 정하지 않았다
- **stale 1건 정리.** `spec-version-history` §6이 정책 설계서를 "지금 두 건"이라
  했는데 표에는 열넷이 있었다
- 새 개념·엔티티 페이지 없음. `mvp-scope-limits`에 v6 결정 둘과 정책설계 `v0.2`
  결정 둘을 더했다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 캔버스 정책 v0.2

- 원본: `wiki/raw/C-001-캔버스-정책-v0.2.md` (2026-07-22자 `v0.2`, 작성자 `DSN-A`)
- 판본 관계: **`v0.1`(07-09)을 대체.** 원본 개정이력이 두 판본을 나란히 적었다 —
  "v0.2 | 2026-07-22 | Dot grid 추가 및 캔버스 정책 변경"
- 바뀐 것 셋 — 탭바 제거, 16:9 대상 축소, 배경 처리 교체
- **떠 있던 컨트롤이 캔버스에 붙었다.** `v0.1`은 탑바·탭바가 플로팅으로 화면
  상·하단에 고정되고 캔버스가 그 사이에 떠 있었다. `v0.2`는 **Canvas =
  Canvas-Area + Canvas-Menu**이고 Bottom-Bar가 Canvas-Area 하단에 gap 없이 붙으며
  **별도 탭바가 없다**
- **16:9가 캔버스 전체에서 `Canvas-Area`로 좁아졌다.** `Canvas-Menu`는 비율 계산에서
  빠지고 고정 높이로 더해진다. gap 기준도 "탑바–탭바 사이"에서 "Top Bar ↔ Canvas,
  Canvas ↔ 화면 최하단"으로 바뀌었다 — 탭바가 없어진 귀결이다.
  `canvas`가 "16:9를 무조건 지킨다"고 적어 온 것을 정밀화했다
- **신규: 좌상단 컷 도형과 `Button-Date-Select`.** 캔버스 좌상단이 비스듬히 잘린
  장식 도형이고 날짜 라벨이 그 위에 얹힌다. 라벨 뒷부분의 반투명 영역까지 캔버스
  영역이다. `v0.1`의 "크기 고정"이 **높이만 고정, 너비는 가변**으로 바뀌었다
- **신규: 저장하면 컷도 라벨도 없다.** 내보낼 때 둘 다 반영되지 않고 **완전한
  사각형으로 저장**된다. 기능정의서는 마감 저장을 "완료 캔버스 이미지 추출"로만
  적었다 — **화면에 보이는 것과 저장되는 것이 다르다는 사실을 정책서가 처음
  밝혔다**
- **신규: Background-Dot-Grid.** 2px `Gray-100`, 간격 20px, 기준점 화면 좌상단
  (0,0). **스케일되지 않고 개수가 늘어난다** — 캔버스 내부 요소가 비례 스케일되는
  것과 반대다. 화면 전체 뒤에 깔리되 캔버스에서는 배경색이 덮어 가린다
- 미결 신설: **Background Blur의 지위가 불명해졌다.** `v0.1` §2가 "배경이 없어도
  항상" 깔도록 정하고 목업 3장까지 딸렸던 규칙이 `v0.2` 원문에 없다. **폐기인지
  병존인지 적히지 않았다** — 개정 요지가 "Dot grid **추가**"이고, 둘은 층이 달라
  (Blur는 캔버스 배경 위, Dot Grid는 화면 전체 뒤) 겹쳐도 모순이 아니다.
  **답 없이 사라진 서술로 읽고 정하지 않았다** — 정책설계 `v0.2`의 `Chip` 배치
  기준점과 같은 처리다. 원본을 옮긴 사람도 "ingest 시 확인 필요"를 적어 뒀다
- 미결 신설: **`Canvas-Menu`와 `Bottom-Bar`가 같은 것인지 없다.** 원본이 두 이름을
  섞어 쓰는데 별개라면 높이 계산식에서 하나가 빠진다
- 미결 신설: **정책 설계서의 날짜가 작성 시점인지 캡처 시점인지 흔들린다.** 이
  문서는 07-22자인데 **07-23자 기능정의서 v6를 참조한다**(스크린샷 확인일 07-25).
  간격 `v0.2`는 날짜 칸이 아예 "스크린샷 확인일"이고 정책설계 `v0.2`는 날짜가
  없다. **07-22에만 정책서가 둘이라 선후를 세울 수 없다**
- **참조는 한쪽으로만 흐른다는 것을 처음 적었다.** `v0.1`은 참고로
  `raw/기능정의서-v5.md`를 들었고 `v0.2`는 그것을 **`v6`로 올렸다** — 정책 설계서가
  기능정의서 판본을 따라가고 있다. 반대 방향은 여전히 없다. `spec-version-history`에
  절 둘을 신설했다
- 새 개념·엔티티 페이지 없음. `canvas`의 "화면 레이아웃" 절을 다시 썼다
- assets 커밋 없음 — `v0.1`의 Background Case 목업은 그대로 두고 `v0.1` 페이지에
  남겼다
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 카메라 화면 정책 v0.1

- 원본: `wiki/raw/C-101-카메라-화면-정책-v0.1.md` (2026-07-22자 `v0.1`, 작성자
  `DSN-A`)
- 판본 관계: **대체 없음.** 신규 화면 정책서
- **서브 화면을 적용 범위로 삼은 첫 정책서다.** 적용 화면이 "`C-101`(촬영)·
  `C-101-confirm`(확인) 공통 적용"이다. 접미사 ID는 v5가 들여온 표기인데 정책
  설계서가 그것을 참조 대상이 아니라 **범위**로 쓴 것은 처음이다.
  `screen-id-scheme`에 적었다
- **블러 스펙이 `C-001` `v0.1`의 Background Blur와 세 값이 전부 같다** —
  `Transparency/Black-25` / Uniform / `4`. 같은 작성자가 같은 날 쓴 두 문서다
- **Blur 미결에 단서가 됐지만 닫지는 못했다.** 값이 버려진 것은 아니라는 뜻이되,
  `C-101`의 블러는 **대상도 목적도 다르다** — 뷰파인더 프레임 **바깥**을 흐려
  선명 영역을 가르는 것이고 `C-001`의 것은 **배경 전체**를 항상 덮는 것이었다.
  **공유되는 것은 정책이 아니라 시각 토큰이다.** 미결에 출처로 더하고 그렇게 적었다
- 미결 신설: **캔버스와 카메라가 반대 원칙을 쓴다.** `C-001`은 16:9를 1순위로 두고
  좌우 패딩을 양보하는데, `C-101`은 **여백을 고정하고 "비율 고정 없음"**이라 적어
  기기별 화면 비율을 그대로 반영한다. 같은 작성자의 같은 날 문서인데 우선순위가
  뒤집혔고 이유는 어느 쪽에도 없다. **촬영 프레임과 16:9 배치 프레임이 다를 때
  무엇이 잘리는지도 없다**
- 여백도 다르다 — `C-001`은 상하가 **최소 12px로 같은 값**, `C-101`은 상 8·하 10으로
  **다르고 고정**이다. 좌우 20px만 같다. 왜 8과 10인지 없다
- 미결 신설: **세로 모드 전용이 문서에 명시되지 않았다.** 원본 범위 줄이 "세로 모드
  전용(별도 명시 없음, 여백 픽셀 고정 기반)"으로, **전사자가 추론한 것**이다.
  캔버스 정책서는 본문에 직접 적었는데 같은 날 같은 작성자의 이 문서는 밝히지
  않았다. 추론을 소스 서술로 올리지 않고 `mvp-scope-limits`에 더하지 않았다
- **날짜 미결이 강해졌다.** 이 문서도 07-22자에 캡처 07-25, 참고는 07-23자
  `기능정의서-v6`다 — 캔버스 `v0.2`와 **똑같은 모양**이다. **07-22를 주장하는
  정책서가 이제 셋이고 선후를 세울 수 없다**
- 뷰파인더 서술: 화면 전체가 실시간 프리뷰이고 **프레임 안쪽만 선명**, 바깥은 같은
  피드에 블러. 별도 배경 이미지가 아니라는 것을 원본이 못박았다. 경계는 **코너
  마커**가 안내한다
- **stale 1건 정리.** `topping-placement-pipeline`이 "현행 정본은 `기능정의서-v5`"
  라고 남아 있었다 — v6 ingest 때 놓쳤다. 이 파이프라인에 관한 한 v5와 v6가 같아
  내용은 그대로 두고 판본 표기만 고쳤다
- 새 개념·엔티티 페이지 없음. `C-101`은 파이프라인의 한 단계라 페이지를 세우지
  않고 `topping-placement-pipeline`에 레이아웃 절을 더했다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 누끼 이미지 정책 v0.1 (C-103-Selected)

- 원본: `wiki/raw/C-103-Selected-누끼-이미지-정책-v0.1.md` (2026-07-23자 `v0.1`,
  작성자 `DSN-A`)
- 판본 관계: **대체 없음.** 신규 정책서
- **범위가 화면 하나가 아니라 구간이다** — `C-103-Selected`에서 만든 것을
  `C-103`~`C-105`에 **같은 스케일로 전달**한다. 파이프라인의 연속된 단계를 하나로
  묶어 다루는 첫 정책서다
- **객체만 잘라내지 않는다.** 최소 바운딩 박스에 **상하좌우 +20% 안전 여백**을
  붙인 사각형을 만들고, 사진 가장자리에 걸려 여백을 낼 데이터가 없으면 **투명
  픽셀(Alpha 0%)로 강제 확장**해 크기를 같은 규칙으로 유지한다. **투명 영역이 누끼
  실패의 잔재가 아니라 의도된 여백일 수 있다**는 것을 처음 알려 준다
- **근거가 적힌 네 번째 결정이다** — "이후 편집(영역 채우기, 테두리 추가)을
  원활하게 하기 위해". `spec-version-history`의 근거 절을 다시 썼다.
  **넷의 성격이 다 다르고 이것이 새 종류다** — 투표 흔적, 정답·오답 병기, 본뜬
  레퍼런스에 이어 **뒤 단계의 필요**다. 앞의 셋은 자기 화면 안에서 닫히는
  이유였는데 이것은 파이프라인을 앞뒤로 놓고 본 흔적이다
- 미결 신설: **"캔버스"가 두 층에서 다른 것을 가리킨다.** 이 문서의 캔버스는 누끼
  이미지 한 장의 **데이터 영역**(토핑마다 하나, 바운딩 박스 + 20%)이고,
  `canvas`는 **그룹 하루치 공유 편집 영역**(그룹마다 하루 하나, 16:9)이다. 원본이
  구분을 밝히지 않았다. `canvas`와 `topping-placement-pipeline`에 낱말 주의를 달았다
- 미결 신설: **20% 여백이 `C-106` 배치까지 가는지 없다.** 적용 범위가
  `C-103`~`C-105`라 `C-106`이 빠져 있다. 붙은 채로 놓이면 토핑이 실제 그림보다
  넓은 자리를 차지하고 탭 판정도 커지며, 벗긴다면 테두리 그린 자리가 잘릴 수 있다
- 미결 신설: **"영역 채우기"가 어느 화면인지 없다.** 기능정의서 여섯 판본
  어디에도 없는 낱말이다. `C-104`(수동 누끼 편집)로 읽히지만 적힌 곳이 없다 —
  근거의 절반이 정체불명이다
- **여백 값이 또 다르다.** 좌우 20px는 `C-001`·`C-101`·이 문서가 다 같은데 상하는
  매번 다르고 **성격까지 갈린다** — 캔버스는 **최소값**(12/12), 카메라는
  **고정값**(8/10), 여기는 **UI 높이에 더하는 값**(+10/+16)이다
- **화면 ID 표기가 갈렸다.** 이 문서는 `C-103-Selected`로 쓰고 기능정의서는
  `C-103-select`다. 정책 설계서가 접미사 ID를 쓰기 시작한 것이 `C-101` 정책서인데
  **두 번째 사용에서 벌써 철자가 달라졌다.** `screen-id-scheme`에 적었다
- **날짜 미결에 반례가 생겼다.** 이 문서는 07-23자이고 참조하는 `기능정의서-v6`도
  07-23자라 어긋나지 않는다. **어긋나는 것은 07-22자 셋뿐**이라 그날의 날짜 표기만
  기준이 달랐을 수 있다고 적었다
- 새 개념·엔티티 페이지 없음. `topping-placement-pipeline`에 절을 더했다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 이미지 렌더링 정책 v0.1 (공용)

- 원본: `wiki/raw/C-103-이미지-렌더링-정책-v0.1.md` (2026-07-09자 `v0.1`, 작성자
  `DSN-A`)
- 판본 관계: **대체 없음.** 신규 정책서
- **두 번째 공용 정책서다.** 원본이 "화면 번호에 종속되지 않음"을 못박고 적용
  대상을 성격으로 든다 — 촬영 확인, 불러온 이미지 확인, 누끼 결과.
  `Toast-공통-정책`에 이은 둘째인데 **그쪽은 날짜·버전·작성자가 전부 없었고 이
  문서는 셋 다 있다.** `screen-id-scheme`에 절을 신설했다 — **ID 체계 밖에서 도는
  규칙이 생긴다는 뜻**이고, 화면이 재번호되거나 삭제돼도 따라 움직이지 않는다
- 비율에 따라 갈린다. 어느 변이 **먼저** 최대에 닿는지가 기준이다
  - **Case A**(가로형·정방형) — 원본 단독 노출, 컨테이너도 테두리도 없음, 너비 100%
  - **Case B**(세로형) — **흰색 컨테이너**로 좌우를 채우고 외곽선에 `Gray-500` 1px.
    이미지는 그 안 좌우 중앙
  - **세로 사진만 액자에 넣는 셈**이고 근거는 없다. 테두리를 이미지가 아니라
    컨테이너에 두는 것이 요지다
- 미결 신설: **Case B가 누끼 결과에도 걸리는지 없다.** 원본이 적용 대상에 "누끼
  결과"를 넣었는데 누끼는 **투명 PNG**이고 캔버스가 바운딩 박스 + 20%라 세로형일
  수 있다. 걸리면 **투명 영역 뒤에 흰 바탕이 깔린다.**
  `C-103-Selected` 정책은 Aspect Fit과 정중앙만 말하고 컨테이너를 언급하지 않는다.
  흰 바탕이 깔리면 누끼 검수에 좋고 안 깔리면 최종 모습에 가까워 **어느 쪽이든 말이
  된다** — 정하지 않았다
- **중복이 또 나왔다.** 이 문서 §1의 최대 렌더링 영역 두 줄이
  `C-103-Selected`(07-23) §2.1에 **낱말만 바꿔 그대로 있다**("상단바 영역"→"상단
  UI"). **공용 정책이 2주 앞서므로 원조는 이쪽인데 뒤 문서가 이것을 가리키지
  않는다.** 07-02 입력 유효성 문서 셋과 같은 모양이라 그 미결에 더했다 —
  사본임을 밝히고 원본 파일명을 적은 `S-101-...-타임스탬프` 문서와 대비된다
- **날짜 미결에 새 형태가 생겼다.** 07-09자인데 **캡처가 07-25**다. 같은 07-09자인
  캔버스 `v0.1`·간격 `v0.1`·토핑 `v0.1` 셋은 전부 **07-13** 캡처다. **같은 날
  쓰인 문서가 두 무리로 나뉘어 캡처됐다** — 그 사이 고쳐졌는지 늦게 발견됐는지
  알 수 없다
- 미결 신설: **비교 목업이 `wiki/raw/assets/`에 없다.** 같은 계열의 다른 목업
  셋은 들어와 있다. 본문이 내용을 글로 옮겨 적어 사실은 남았으나 **Case A와
  Case B를 대조할 그림이 없다**
- 새 개념·엔티티 페이지 없음. `topping-placement-pipeline`에 절을 더했다
- assets 커밋 없음 (원본 목업 미확보)
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 누끼 편집 정책 v0.1 (C-104)

- 원본: `wiki/raw/C-104-누끼-편집-정책-v0.1.md` (2026-07-09자 `v0.1`, 작성자
  `DSN-A`)
- 판본 관계: **대체 없음.** 신규 정책서
- **이 화면이 무엇을 하는 곳인지 처음 밝히는 문서다.** `C-104`는 기능정의서 v5에서
  신설됐는데 **시스템 동작 열이 v5·v6 모두 비어 있었다** — 표는 이름과 분기조건만
  갖고 있었다
- **누끼 편집은 지우는 것이 아니라 되살리는 것이다.** 자동 누끼가 지운 배경 픽셀이
  사라지지 않고 **불투명도 50%**로 남아 있고, 브러시가 지나간 자리만 마스킹이
  풀려 **100%로 복구**된다. **빼는 편집이 아니라 더하는 편집이다**
- 미결 1건 해소: **"영역 채우기"가 어느 화면인지.** `C-104`의 브러시 작업이다.
  `C-103-Selected` 정책(07-23)이 20% 안전 여백의 근거로 든 낱말이고 기능정의서
  여섯 판본 어디에도 없었다. **근거를 낸 문서보다 근거를 설명하는 문서가 2주
  앞선다**(07-09 대 07-23). **그래서 20%가 왜 필요한지도 분명해졌다** — 되살릴
  배경이 캔버스 안에 남아 있어야 브러시가 닿는다
- 미결 신설: **더 지우는 브러시가 있는지 없다.** 원본은 복구만 말한다. 기능정의서는
  이 화면을 "수동 누끼 편집"이라 불러 양방향으로 읽히는데 정책서는 한 방향만 정한다
- 미결 신설: **테두리 정책이 `C-104` 문서에 있다.** 적용 화면은 `C-104`인데 §5가
  테두리(`2px ~ 50px`)다. 테두리 편집은 `C-105` 소관이고 **그 화면 전용 정책서는
  아직 없다** — 나중에 나오면 정본이 갈릴 자리다. 브러시도 `2px ~ 50px`로
  **범위가 같은데** 우연인지 같은 슬라이더인지 없다
- **20% 여백 미결에 계산이 더해졌다.** 테두리가 최대 50px인데 **20%는 비례값이고
  50px은 고정값**이다. 바운딩 박스가 100px이면 여백이 20px뿐이라 담지 못한다.
  두 문서가 같은 작성자에게서 나왔는데 그 관계를 적지 않았다
- 제스처: 2핑거 확대 허용, **Scale 1.0 미만 축소 원천 차단**, Pan clamping.
  **초기 렌더링이 하한이다** — 처음부터 다 보이는 상태로 시작해 키우기만 한다.
  확대하면 **초기 이미지 너비와 무관하게 `Device Width − 40px` 전체**가 브러시 작업
  공간이 된다
- **여백이 또 다른 값이다.** 상하 **최소 10px**로 같은 값이고 성격은 최소값이다 —
  캔버스 정책과 같은 꼴인데 숫자만 12에서 10으로 다르다. 좌우 20px는 이제 **다섯
  문서**가 같고 상하는 매번 다르다. **평소(비확대) 좌우 여백은 적히지 않았다**
- **날짜 미결이 굳어졌다.** 07-09자인데 캡처가 07-25다 — 렌더링 정책과 똑같은
  모양이다. **07-09 문서가 캡처 시점으로 두 무리**가 됐다: 07-13에 셋(캔버스
  `v0.1`·간격 `v0.1`·토핑 `v0.1`), 07-25에 둘
- `Button-Container`·`Edit-Area` — 이 문서가 처음 쓰는 컴포넌트 이름이다
- 새 개념·엔티티 페이지 없음. `topping-placement-pipeline`에 절을 더하고 표의
  `C-104` 줄을 다시 썼다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 토핑 배치 정책 v0.1 (C-106)

- 원본: `wiki/raw/C-106-토핑-배치-정책-v0.1.md` (2026-07-23자 `v0.1`, 작성자
  `DSN-A`)
- 판본 관계: **대체 없음.** 신규 정책서
- **파이프라인 마지막 화면에 정책서가 붙었다.** `C-101`~`C-106` 여섯 단계 중
  다섯에 정책서가 생겼다
- **상충 1건 신설: 캔버스 이탈.** 기능정의서는 `C-106`·`C-305` 예외처리를
  **"캔버스 영역 이탈 시 자동 보정"**으로 **v2부터 v6까지 다섯 판본 내내 같은
  문장**으로 적었는데, 이 문서는 **"강제로 막지 않음"**이다. 넘어간 픽셀은
  **잘려서 안 보인다**(Clipping Mask). **정면으로 반대다** — 한쪽은 안으로
  되돌리고 한쪽은 나가도 둔다
- **대체로 처리하지 않았다.** 정책서가 더 늦고(07-23 대 06-25) 화면 하나를 깊이
  다루지만 **기능정의서를 대체한다고 선언하지 않았다** — 참고 문서로 들 뿐이다.
  `purpose` 규약대로 임의로 판본 관계를 만들지 않았다. **`C-305`도 같은 "자동
  보정" 문장을 갖는데 이 문서의 범위는 `C-106`뿐이라 함께 바뀌는지 없다**
- **근거가 적힌 다섯 번째 결정이고 또 새 종류다.** 짧은 쪽이 48px 미만이면 그 면을
  48px에 맞추고 전체를 키우는데, 근거가 **"모바일 최소 터치 권장 규격"**이다 —
  **제품 밖의 표준을 끌어온 첫 사례다.** 앞의 넷은 전부 이 제품 안에서 나온
  이유였다. `spec-version-history`의 근거 절을 다시 썼다
- **가로로 아주 긴 토핑에서 두 규칙이 갈리고 48px가 이긴다** — 40%를 적용했을 때
  짧은 쪽이 48px에 못 미치면 전체가 커지므로 긴 쪽이 40%를 넘게 된다
- 미결 신설: **초기 스케일 40%의 근거가 없다.** 같은 문서의 48px에는 근거가 달렸다
  — **한 문서 안에서 한 값에만 붙었다.** 근거가 "구현자가 왜 이렇게 안 하지라고
  물을 법한 자리에만 붙는다"는 관찰과 맞는다
- **20% 여백 미결이 강해졌다.** `C-106` 자기 정책서가 나왔는데도 답이 없다 —
  "가져온 토핑 이미지의 가로/세로"라고만 하고 여백 포함인지 벗긴 것인지 말하지
  않는다. **그 문서의 두 규칙이 다 갈린다**: 여백 포함이면 보이는 그림은 40%보다
  작고, **투명한 부분까지 터치 영역으로 세는** 셈이 된다 — **근거가 터치인데 투명
  픽셀은 터치 대상이 아니다**
- Clipping 방향을 `infinite-parfait`과 대조했다. `G-001` 목록은 삐짐을 **자르지
  않는** 정책이었다가 폐기됐고, 캔버스는 **자른다.** 지금은 두 화면이 같은 방향이다
- 날짜는 어긋나지 않는다 — 07-23자에 07-23자 `v6` 참조로 `C-103-Selected`와 같은
  무리다. 캡처는 여전히 07-25
- 새 개념·엔티티 페이지 없음. `canvas`에 이탈 절을 신설하고
  `topping-placement-pipeline`에 절 셋을 더했다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 무한 파르페 간격 정책 v0.3

- 원본: `wiki/raw/G-001-무한파르페-간격-정책-v0.3.md` (작성자 `DSN-B`)
- 판본 관계: **`v0.2`(07-22)를 대체.** 원본이 "이전 버전"으로 명시하고 개정이력에
  세 판본을 순서대로 적었다
- **한 문서가 날짜를 셋 갖는다** — 표지 기재일 **26.07.20**, 개정이력 **07-27**,
  스크린샷 확인일 **07-27**. **표지 날짜가 `v0.2`(07-22)보다 앞서** 판본 순서를
  뒤집는데, 원본이 `v0.2`를 이전 버전으로 들었으므로 **표지 기재일이 틀린 것으로
  읽었다.** 날짜 문제가 이제 문서 사이가 아니라 **문서 안**에서도 벌어진다
- **상충 1건 신설: 인셋 28 대 4.** 정책설계 `v0.2`는 좌/우 인셋 **28**, 이 문서는
  **4**다. **나머지 좌표는 글자 하나까지 같다.** `v0.3` 개정이력이 "**그룹 토핑
  인셋 정책 변경**"을 적었고 간격 계열에는 인셋 값이 없었으므로 **바뀐 대상은
  정책설계 `v0.2`의 28로 읽힌다.** 그러나 **문서 단위 대체를 선언하지 않아**
  정책설계 `v0.2`는 여전히 `current`에 28을 갖는다 — 임의로 판본 관계를 만들지
  않았다. **토핑 프레임이 160인데 24px 차이다**
- **미결 1건 신설: 닫힌 결정이 전파되지 않는다.** 정책설계 `v0.2`가 시작 사이드를
  `Left` 고정으로 확정하고 `Chip` 배치 기준점 항목을 들어냈는데, 더 늦은 이 문서에
  **둘 다 `[ A / B ]` 미정 표기 그대로** 있다
  - **확정이 취소된 것으로 읽지 않았다.** 개정이력이 든 변경 넷에 그 둘이 없고
    `v0.2`의 해당 절이 글자 그대로 남아 있다 — **손대지 않은 절을 이어받은 것**이다
  - **되돌아간 것과 닿지 않은 것은 다르다.** 되돌아갔다면 왜 뒤집었는지를 물어야
    하고, 닿지 않았다면 두 문서를 맞추기만 하면 된다
  - 같은 일이 **한 문서 안에서도** 있다 — §1.3은 `N=1`·`N=2`가 항목만 비어 있고
    §2.4가 채운다. **옛 절과 새 절이 함께 남았다.** "삐져나가는 픽셀은 측정 대상이
    아님"도 그대로인데 정책설계 `v0.2`는 삐짐 자체를 없앴다 — **없는 것을 재는
    규칙이 남아 있다**
  - `spec-version-history`에 절을 신설했다. **"선언이 표를 따라오지 못한다"의
    문서 판이다** — 그때는 한 문서 안에서 선언과 표가 어긋났고 여기서는 같은
    화면의 두 문서가 어긋난다
- **정렬에 타이브레이크가 생겼다** — 활동순이 밀리초까지 같으면 **그룹 생성일시
  최신순**. 정렬 규칙이 처음으로 동률을 다룬다
- 원본에 **`ㅈㅉ`** 오타가 그대로 남아 있다. Toast 공통 정책·`C-202` 편집자 확인
  규칙에 이어 **작업 중 메모가 정제되지 않고 들어오는 이 계열의 특징**이다
- **폐기 선언이 위키가 놓친 것을 알려 줬다.** `N = 0` 안내를 온보딩 툴팁
  `G-001-Empty`가 맡고 **구 "크림 상단 42% 지점 빈 상태 안내 문구"를 폐기**한다고
  적는데, **그 42% 위치가 이 위키에 기록돼 있지 않았다** — 정책설계 `v0.1` 원본에
  있었는데 소스 페이지가 문구만 옮기고 위치를 빠뜨렸다. 이번에 채웠다
- **참조가 네 번째로 빗나갔다.** Timestamp 컬러가 또 `S-101-...-v0.3`을 가리킨다.
  `v0.2`가 잘못 가리킨 자리를 `v0.3`이 그대로 옮겨 왔다 — **고쳐지지 않은 참조가
  판본을 넘어 복사된다**
- 새 개념·엔티티 페이지 없음. `infinite-parfait`의 좌표·배치 절을 고쳤다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 무한 파르페 정책 설계 v0.3

- 원본: `wiki/raw/G-001-무한파르페-정책설계-v0.3.md` (**날짜·작성자 없음** — `v0.2`와
  같다)
- 판본 관계: **`v0.2`를 전면 대체.** 문서명과 목차 뼈대가 같고 제목에 `v0.3`을 달았다
- **한 소스에서 받은 가장 많은 종결이다.** 미결 **3건 해소, 1건 거의 해소, 2건
  신설.** 미해결 50, 해소 24
- **앞서 해소한 것 하나가 뒤집혔다.** `v0.2`의 "**변형 시드는 그룹 ID 고정이라
  유지**"를 `v0.3`이 **이름까지 들어 폐기**하고 **랜덤 부여**로 확정했다. 이 위키는
  그 서술을 근거로 "6타입 중 1·2·3 선택"을 해소로 적었는데 **그 답이 틀렸다.**
  물음은 닫혔고 답만 바뀐 것이라 **해소를 취소하지 않고 답을 고쳐 적었다**
  - 정렬이 활동 최신순이라 **새로고침으로 순서가 바뀌면 타입도 바뀐다**
  - **재추첨 시점은 목록 조회 응답 1회.** 리렌더·셀 재사용에서는 금지
  - **근거가 적혀 있다** — "**회전각 튐 방지**". **여섯 번째 근거 있는 결정이고
    또 새 종류다** — 값이 왜 옳은지가 아니라 **잘못 만들면 무엇이 깨지는지**를
    적는다
- 미결 해소: **인셋 28 대 4 → 4.** 더 중요한 것은 **무엇에 대한 인셋인지가 처음
  적혔다**는 점이다 — 기준이 **전체 파르페 템플릿**(체리~접시 포함 고정 이미지
  영역, 가로 **324.3**)이고 **화면 기준이 아니다.** `Left`↔`Right` 가로 관계
  3.7(겹침)은 **파생값**이고, **토핑·칩이 크림 폭을 넘는 것은 정상 동작**이다 —
  경계가 크림이 아니라 템플릿이다
- 미결 해소: **`Right` 좌표가 서술과 12 어긋나던 것.** `v0.3`이 **"실측 기준
  `Left y + 86`"**이라 직접 적고 표현도 "가운데"에서 "사이"로 바꿨다. **원본이
  어긋남을 스스로 정정했다** — 위키가 계산으로 짚어 둔 것을 다음 판본이 수치로 닫았다
- 미결 해소: **크림 가로 길이 1안 대 2안.** **고정 이미지 에셋 + 중앙 정렬**로
  확정하고 1안을 폐기로 명시했다. 화면 폭에 비례해 늘어나지 않고 넓은 기기에서는
  좌우 여백만 는다. **석 달 걸렸고 중간에 한 번 사라졌다** — `v0.2`에서 항목째
  빠져 "답 없이 사라진 서술"로 기록했던 것이다. **붙들고 있었기에 닫히는 것을 볼
  수 있었다**
- 거의 해소: **`G-001` 화면 요소 세부.** 그룹 목록 **캐싱 안 함(매번 재조회) —
  "종결"**까지 닫혀 **체리 `TBD`만 남았다**
- **새 상태가 둘 늘었다** — "특정 토핑 조회 실패"(그룹 칩은 정상 노출), "특정 토핑
  내 이미지 없음". 뒤엣것에 **빈 토핑 템플릿 6종**이 붙는데 **전부 디저트다**(딸기
  / 키위 / 오레오 / 딸기과자 / 파랑젤리 / 노랑젤리). **기능정의서 여섯 판본
  어디에도 없다.** `C-301`이 "파르페 편집 모드"에서 물러선 것과 같은 시기에
  **은유가 빈 상태 그래픽까지 내려왔다** — `parfait`에 적었다
- **저개수 기준이 넓어졌다.** `N = 3`이 +12 무리에 들어왔고 기준이 "여백"에서
  **"스크롤 발생 여부"**로 다시 적혔다
- 미결 신설: **접시 갭의 부호가 -16에서 16으로 바뀌었다.** 같은 표의 다른 둘
  (-65, -48)은 그대로 음수다. 이 계열에서 음수 갭은 겹침을 뜻해 왔다 — 겹침이
  여백이 된 것인지 부호가 정정된 것인지 없다
- 미결 신설: **가리키는 문서 둘이 `wiki/raw/`에 없다** — `G-001-Empty-툴팁-정책-v0_1`,
  `G-001-토핑-정책-v0_2`. 셋째(`간격-정책-v0_2`)는 **한 칸 뒤를 가리킨다**(간격은
  `v0.3`이 현행)
- **전파 실패가 양방향으로 확인됐다.** 간격 `v0.3`이 더한 **정렬 타이브레이크**가
  이 문서에 없다. 앞서는 정책설계가 확정한 시작 사이드가 간격 계열에 닿지
  않았다 — **두 계열이 서로의 갱신을 받지 않는다.** `spec-version-history` 절을
  고쳤다
- **참조가 다섯 번째로 빗나갔다.** Timestamp 컬러가 또 옛 문서명을 가리킨다
- 데이터 모델에 **노출 열**이 붙었다 — 그룹 ID만 미노출
- 새 개념·엔티티 페이지 없음. `infinite-parfait`을 크게 고쳐 썼다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | G-001-Empty 툴팁 노출 조건 정책 v0.1

- 원본: `wiki/raw/G-001-Empty-툴팁-노출-조건-정책-v0.1.md` (26.07.22자 `v0.1`,
  작성자 `DSN-B`)
- 판본 관계: **대체 없음.** 신규 정책서
- **정책설계 `v0.3`이 가리킨 문서가 들어왔다.** 다만 **파일명이 다르다** — 가리킨
  이름은 `G-001-Empty-툴팁-정책-v0_1.md`인데 실제는
  `G-001-Empty-툴팁-노출-조건-정책-v0.1.md`다. "노출-조건"이 더 있고 판본 표기도
  `v0_1`과 `v0.1`로 다르다. **이 계열의 참조가 자주 빗나갔는데 이번에는 문서는
  맞고 이름만 어긋났다**
- **짧다.** 노출 조건 한 줄, 닫기 조건 한 줄, 예시 한 줄이 전부다
  - **0건이면 항상 노출.** 최초 1회 플래그를 쓰지 않는다
  - **툴팁 외 영역 클릭**으로 닫는다
  - 원본 예시가 동작을 못박는다 — 닫고 앱을 나갔다 재진입하면 **다시 뜬다.**
    그룹을 만들거나 합류하기 전까지 **영구히 끄는 장치가 없다**
- 미결 신설: **"최초 1회 플래그" 안이 어느 문서에도 없다.** 원본이 "버리고"라
  적었으니 그런 안이 있었다는 뜻인데 **이 위키 어느 소스에도 나오지 않는다.**
  간격 `v0.3`이 "크림 상단 42% 지점"을 폐기하며 위키가 빠뜨린 값을 알려 준 것과
  같은 모양이지만, **이번에는 원본을 찾을 수 없다** — 위키 밖에서 오간 결정일 수
  있다. **툴팁을 영구히 끄는 장치가 의도적으로 빠졌다는 뜻이고 근거는 없다**
- 미결 신설(기존 항목에서 분리): **툴팁 문구가 여전히 없다.** **툴팁 전용 문서가
  들어왔는데도 노출·닫기 조건만 다룬다.** 06-13 정책설계의
  `"첫 그룹을 만들어 / 파르페를 쌓아보세요"`가 이 툴팁으로 옮겨 간 것인지, 위치만
  폐기되고 문구는 살아 있는지, 새로 쓸 것인지 **세 문서가 같은 자리를 다루면서
  아무도 적지 않았다**
- **툴팁에도 번호 체계가 생겼다.** 제목이 "툴팁 노출 조건 — **1. G-001-Empty**"로
  **툴팁이 여럿일 것을 전제**한다. 나머지가 무엇인지는 없다. **화면 ID와 다른 축의
  식별자가 하나 더 생긴 셈**이라 `screen-id-scheme`에 적었다
- **날짜 미결이 또 늘었다.** 표기가 **`26.07.22`**로 다른 문서의 `2026-07-22` 꼴과
  다르다 — 같은 작성자가 쓴 간격 `v0.3`의 표지 기재일도 `26.07.20`이었다. **출처
  줄과 참고 줄이 아예 없어** 캡처 시점을 알 수 없다. **07-22를 주장하는 정책서가
  넷이 됐다**
- 새 개념·엔티티 페이지 없음. `infinite-parfait`의 `N = 0` 항목에 조건을 채웠다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건


## [2026-09-17] ingest | 그룹 토핑 템플릿 정책 v0.1

- `wiki/raw/G-001-그룹-토핑-템플릿-정책-v0.1.md` → `src-G-001-그룹-토핑-템플릿-정책-v0.1`.
  **신규다.** 대체 관계 없고 `status: current`
- **`v0.3`이 표 한 칸에 적어 둔 상태 둘을 전용 문서가 받아 갔다** — 특정 토핑
  조회 실패, 특정 토핑 내 이미지 없음. **다만 `v0.3`은 이 문서를 예고하지
  않았다.** 툴팁 문서는 파일명까지 들어 가리켰던 것과 다르다
- **조회 실패 그래픽의 모양과 개수가 처음 적혔다** — **물음표 모양 과자, 1종.**
  그룹 칩 정상 노출은 `v0.3`과 같다. **에러 표현까지 디저트 은유가 내려왔다**고
  `parfait`에 적었다
- **빈 토핑 템플릿의 부여 규칙이 처음 적혔다** — 6종 중 1종 **랜덤 최초 부여**,
  **첫 토핑 등록 전까지 고정**, **새로고침·재접속·타 그룹 갱신에도 불변**
- **같은 화면의 두 랜덤이 반대로 움직인다.** 변형 타입은 목록 조회 1회마다
  재추첨하고 근거("회전각 튐 방지")가 있는데, 템플릿은 고정이고 근거가 없다.
  **모순이 아니라 지속성 요구가 정반대인 규칙 둘**이라 상충 마커를 달지 않고
  대비 표로 적었다
- 미결 신설: **부여된 템플릿이 어디에 저장되는지 없다.** 그룹 목록을 캐싱하지
  않고 매번 재조회하므로 서버가 들고 있거나 그룹 ID에서 결정론적으로 뽑아야
  하는데 어느 쪽인지 없다. **Spring 담당은 아직 배정되지 않았다**
- 미결 신설: **토핑이 전부 삭제돼 다시 빈 그룹이 됐을 때**를 원본이 다루지
  않는다. "첫 토핑이 등록되기 전까지"의 뒤가 없다
- 미결 신설: **번호가 붙은 `2.` 문서가 무엇인지 없다.** 제목이 "그룹 토핑 템플릿
  — **1. Topping-Group**"으로 툴팁 문서의 "— 1. G-001-Empty"와 같은 꼴인데
  **툴팁이 아니다.** 툴팁 전용 번호가 아니라 **컴포넌트 단위 정책 문서의
  일련번호**였다. 둘 다 `1.`이라 계열마다 따로 센다. **앞 ingest가
  `src-G-001-Empty-툴팁-노출-조건-정책-v0.1`의 `## 미결`에 적고도
  `open-questions.md`에 등록하지 않은 항목이라 이번에 함께 등록했다**
- 기존 미결 갱신: 날짜 항목에 **출처 H** 추가. 표기가 `26.07.25`로 **두 번째
  축약 표기**이고 둘 다 `DSN-B`가 썼다. **07-25는 그동안 캡처일이던 날짜인데
  작성일로 07-25를 주장하는 문서는 이것이 처음이다.** 출처·참고 줄 없음
- 어휘 충돌 하나를 원본이 스스로 피했다 — `v0.3`은 템플릿 6종을 "Type 총 6개"라
  적어 토핑 변형 6타입과 같은 말을 쓰는데, 이 문서는 **"템플릿 종류"**라 적는다
- 새 개념·엔티티 페이지 없음. `Topping-Group`은 컴포넌트 이름이라
  `screen-id-scheme`과 `infinite-parfait` 본문 서술로 남겼다
- **하류에서 고친 것 셋** — 앞 ingest들이 숫자를 올리지 않고 남긴 것들이다.
  `infinite-parfait`의 "현행 정본은 `v0.2`"(같은 페이지 표는 `v0.3`이라 적고
  있었다), `parfait`의 "정책 설계서 열 건 / `G-001` 다섯 건 / 현행 정본 `v0.2`",
  `overview`의 "스무 건 / `G-001` 일곱 건", `spec-version-history`의 "열한
  건(대체된 것까지 열넷)". 전부 실제 값으로 다시 셌다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 그룹 토핑 템플릿 정책 v0.2

- `wiki/raw/G-001-그룹-토핑-템플릿-정책-v0.2.md` → `src-G-001-그룹-토핑-템플릿-정책-v0.2`.
  **`v0.1`을 대체**한다(`supersedes` / `superseded_by` 양방향 기재). `v0.1`은
  `status: superseded`로 내렸다
- assets 1건 함께 커밋: `wiki/raw/assets/G-001-그룹-토핑-템플릿-v0.2-그래픽시트.png`.
  소스 페이지에서 `../../raw/assets/`로 참조
- **하루 만에 6종이 바뀌었다.** 딸기 / 키위 / 오레오 / 딸기과자 / 파랑젤리 /
  노랑젤리 → **별 둘 / 음표 둘 / 소용돌이 둘**, 전부 **회색 계열 반투명
  아웃라인**. 조회 실패 그래픽에서는 **"과자"라는 단어만 빠졌다**
- **에셋명이 `Template01`~`Template06`과 `Template-E…`(Error)다.** 별·음표·
  소용돌이가 각각 무늬나 굵기만 다른 둘씩이라 **6종이 여섯 가지가 아니라 세
  가지의 변주**다
- **폐기 선언이 없다.** 본문의 "템플릿 종류"가 목록에서 "총 6종, 하단 참고"로
  바뀌었을 뿐이다. 이 계열이 구 clip OFF 정책·문자수 기준·그룹 ID 시드를 전부
  이름 들어 지웠던 것과 다르다
- 미결 신설: **템플릿 6종의 정체가 두 문서에서 어긋난다.** 정책설계 `v0.3`은
  여전히 디저트 6종을 적고 손대지 않았다. 어느 쪽도 대체를 선언하지 않아 **같은
  화면의 두 문서가 다른 6종을 든 채 나란히 `current`다.** `infinite-parfait`와
  소스 페이지에 상충 마커를 달았다
- 미결 신설: **개정이력이 실제 변경과 어긋난다.** "빈 캔버스 템플릿 및 에러
  템플릿 이미지 추가"라고만 적혔는데 실제로는 교체다. **개정이력만 읽으면
  아무것도 안 바뀐 것으로 읽힌다** — 이 위키가 간격 `v0.3`의 표지 날짜를 틀린
  것으로 읽을 때 쓴 근거가 개정이력이었다. 딸린 어휘 흔들림도 적었다("빈 캔버스
  템플릿" 대 "토핑 템플릿")
- 미결 신설: **`C-001`의 요소가 `G-001` 목업에 나타난다** — `December 31 (Wed)`
  날짜 라벨과 격자 배경. 둘 다 `Button-Date-Select`·`Background-Dot-Grid`로
  `C-001`에 속한 것이다. 목업이 공용 프레임을 쓴 것인지 실제인지 본문에 없다
- 기존 미결 갱신 둘:
  - **그룹 참여/생성 화면**에 출처 D 추가 — 목업 상단바의 **`+ 그룹 추가하기`
    버튼**. 그룹 생성 진입이 `G-001`에 있다는 첫 시각 증거다
  - **전파 안 되는 결정**에 `Chip` 기준점 관련 관찰 추가. 목업에서 칩은 각 토핑
    **아래**에 붙는데 위키가 적어 온 "우측 하단"과 같은지 **치수도 주석도 없어
    판정 불가**다. **그림을 스펙으로 읽지 않기로 하고 별도 미결로 올리지
    않았다**
- **규칙 하나가 처음 명시됐다** — "템플릿 그래픽도 일반 토핑과 동일하게 지그재그
  배치·그룹 칩 노출 규칙을 따른다." 빈 그룹에도 그룹 칩이 뜬다
- **은유의 두 번째 후퇴로 기록했다.** `C-301` 개명이 **이름**의 후퇴였다면 이쪽은
  **그래픽**의 후퇴다. `parfait`·`overview`·`spec-version-history`에 반영
- 목업에서만 보이는 것은 소스 페이지에 **"본문 서술이 아니라 관찰"**로 묶어
  적었다 — 토핑 7개 지그재그, `index 0`이 `Left`, 칩 Timestamp 컬러 넷
- 날짜 미결 갱신: `26.07.26`으로 **축약 표기가 사흘 만에 셋**이 됐고 셋 다
  `DSN-B`가 썼으며 **셋 다 출처·참고 줄이 없다**
- 새 개념·엔티티 페이지 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건

## [2026-09-17] ingest | 앱 닉네임 생성 정책 v0.1

- `wiki/raw/S-002-앱-닉네임-생성-정책-v0.1.md` → `src-S-002-앱-닉네임-생성-정책-v0.1`.
  `status: current`
- **판본 관계를 대체로 적지 않았다.** 같은 화면의
  [[src-S-002-앱닉네임-정책-v0.1]](07-02)과 **둘 다 `v0.1`**이고 새 문서에는
  날짜·버전·작성자·개정이력이 **아예 없다.** 내용으로는 새 문서가 옛 문서를
  완전히 포함하지만 **어느 쪽도 대체를 선언하지 않아 둘 다 `current`로 뒀다** —
  `src-G-001-토핑-정책-v0.1`을 같은 이유로 남긴 것과 같은 처리다. 판본 관계는
  미결로 등록했다
- **선후는 문서 안 근거로 세웠다.** §1.1이 `G-001` 토핑 회전의 재추첨 규정을
  인용하는데 그 규정은 07-22 이후 문서의 것이므로 **새 문서가 최소 3주 뒤다**
- **초기 닉네임 자동 생성이 처음 들어왔다** — 계정 생성 시 1회, `형용사/동사`
  100개 × `파르페 재료` 30개에서 하나씩 뽑아 공백 없이 붙인다. **3000가지**,
  생성 즉시 DB 저장, 재계산 없음. 풀에 중복 항목은 없음(스크립트로 확인)
- 미결 신설: **조합 하나가 기능정의서의 "10자 이내"를 넘는다.** 3000개 중 정확히
  하나 — **`바스락거리는슈가파우더`**(6자 + 5자 = 11자). 나머지 2999개는 10자
  이하, 최단은 `단딸기` 3자. **3000분의 1이라 테스트로 안 잡힌다.** `account`에
  상충 마커를 달았다
- 미결 신설: **생성 주체 미정(`[K2]`).** 원본이 **"어뷰징·치우침 방지 관점에서는
  서버 생성 권장"**이라고 근거까지 적었다 — **고르기 전에 판단 기준을 남긴 첫
  사례**라 `spec-version-history` §7.0으로 적었다. **`[K2]`는 식별자 축의
  셋째다**(화면 ID / 문서 일련번호 / 미결 마커). `K1`·`K3`은 어디에도 없다
- 미결 신설: **앱 닉네임 정책서 둘의 판본 관계**(위)
- 기존 미결 갱신 넷:
  - **앱/그룹 닉네임 관계** — **부분 해소.** §4가 적용 범위를 장 번호로 못박아
    **그룹 내 닉네임 글자수가 1~15자로 확정**됐고 **자동 생성이 앱 닉네임 전용**
    이라는 것도 분명해졌다. 남은 것은 **그룹 가입 시 기본값** — 오히려 선명해졌다
  - **중복·금칙어** — 출처 C·D 추가. 새 문서도 안 다룬다("중복 허용"을 조건절로
    한 번 스칠 뿐). `기능정의서-v6`의 `S-102`가 **"글자수/금칙어/그룹 내 중복
    정책 적용"**이라 적지만 그 정책이 무엇인지는 없다
  - **같은 규칙이 세 문서에** → **네 문서에.** 07-02 셋의 복제가 3주 뒤에 또
    일어났고 원본을 가리키지도 않는다
  - **Toast 메타데이터 없음** → 제목을 **"날짜·버전·작성자가 아예 없는 문서가
    둘이다"**로 고치고 출처 B 추가
- **디저트 어휘가 한 풀을 공유한다.** 재료 30개 안에 프로필 칩 컬러 이름
  **체리·멜론·푸딩**(바닐라·초콜렛도), 폐기된 빈 토핑 템플릿 6종 중
  **딸기·키위·오레오**가 있다. **은유가 사람의 이름까지 내려왔다** — 빈 토핑
  템플릿에서 물러난 어휘가 닉네임에서는 그대로 쓰인다. `parfait`·`account`·
  `overview`에 반영
- **형용사 풀도 반복 표현의 짝으로 짜였다**(`몽글한`/`몽글몽글한` 등, `녹-` 계열만
  여섯). **빈 토핑 템플릿 6종이 세 형태의 변주 둘씩이었던 것과 같은 짜임**이다
- **정책서가 다른 화면의 규칙을 인용해 자기 규칙을 구별한 첫 사례**로 적었다
  (`spec-version-history` §7.2). 다만 **인용이 정확하지 않다** — 원 규정은
  "재추첨 시점은 목록 조회 응답 1회"이고 **`Math.random()`이라는 함수 이름은 어느
  소스에도 없다.** 구현 어휘로 옮겨 적은 것이다
- 새 개념·엔티티 페이지 없음. 자동 생성은 `account`의 하위 절로 넣었다
- assets 커밋 없음
- 검증: `lint.py` 위반 0건, `check_status.py` 위반 0건
