---
tags: [누끼, 캔버스, 플로우, mvp]
sources: [src-기능정의서-v2]
updated: 2026-09-17
---

# 누끼 배치 파이프라인

## 정의

사진 한 장이 [[canvas]] 위의 객체가 되기까지 거치는 고정 순서다. 중간 단계를
건너뛰는 경로가 없다 — 사진은 반드시 누끼를 거쳐 투명 PNG가 된 뒤에야 캔버스에
올라간다.

## 단계

| 순서 | 화면 | 하는 일 |
|---|---|---|
| 1 | `C-101` 촬영 / `C-102` 갤러리 | 이미지 한 장을 가져온다. 갤러리는 한 장씩 고른다 |
| 2 | `C-103` 누끼 따기 | 피사체를 마스킹해 **투명 PNG**를 만든다. 필요하면 영역을 손본다 |
| 3 | `C-104` 수동 편집 | 테두리 두께와 색을 조정한다 |
| 4 | `C-105` 캔버스에 배치 | 드래그와 크기 조정으로 위치를 정한다. 좌표는 `x`, `y`, `z` |
| 5 | `C-106` 배치 확정 | 서버에 저장하고 그룹원에게 브로드캐스트한다. 스티커 아이디와 사용자 아이디를 돌려받는다 |

확정하면 캔버스 메인(`C-001`)으로 돌아온다.

## 진입점

1단계의 두 화면은 진입 경로로 `C-002`를 가리킨다. 그런데 소스의 표에 `C-002`
행이 없다 — 캔버스 편집 진입 허브나 FAB 메뉴로 추정될 뿐이다. 배경 변경 경로의
촬영·갤러리 화면(`C-302`, `C-303`)도 같은 `C-002`를 참조한다. 자세한 것은 아래
미결에 있다.

## 예외

- 카메라·갤러리 **권한을 거부**하면 설정 이동을 안내한다. 촬영을 취소하면
  `C-002`로 돌아간다.
- **자동 누끼가 실패**하면 재시도하거나 원본을 그대로 쓰는 선택지를 준다. 처리
  시간이 초과되면 안내한다.
- 편집 중 **메모리가 부족**하면 자동으로 다운스케일한다.
- 배치가 **캔버스 영역을 벗어나면** 자동 보정한다.
- 저장이 실패하면 재시도하고, 동시 편집이 충돌하면 last-write-wins를 적용한다.

## 사후 조작

올린 사진은 삭제(`C-107`)하거나 위치를 고칠 수 있다(`C-108`). 위치 수정은 **자기
사진에만** 허용되고, 고친 뒤 배치 확정을 거쳐야 반영된다. 소스에서 `C-107`의 세부
열은 비어 있다.

편집 항목을 크기와 테두리 둘로 묶은 이유는 [[mvp-scope-limits]]에 있다. 화면 ID
표기 규칙은 [[screen-id-scheme]]을 따른다.

## 미결

- `C-002`가 정의되지 않았다. 이 파이프라인의 진입점이 비어 있다는 뜻이다.
- 누끼를 온디바이스로 할지 서버로 할지 정해지지 않았다.
- 최대 파일 크기와 지원 포맷이 없다.
- z-order와 회전 허용 범위가 없다.
- 동시 편집 충돌 처리 정책이 확정되지 않았다(last-write-wins는 잠정).

전부 [[open-questions]]에 있다.

## 연관

- [[src-기능정의서-v2]]
- [[canvas]]
- [[parfait]]
- [[screen-id-scheme]]
